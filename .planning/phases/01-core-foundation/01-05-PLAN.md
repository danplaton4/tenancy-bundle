---
phase: 01-core-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - src/EventListener/TenantContextOrchestrator.php
  - tests/Unit/EventListener/TenantContextOrchestratorTest.php
  - tests/Integration/ContainerCompilationTest.php
  - tests/Integration/ListenerPriorityTest.php
  - tests/Integration/AutoconfigurationTest.php
autonomous: true
requirements: [CORE-05, CORE-01, CORE-02, CORE-03]
must_haves:
  truths:
    - "TenantContextOrchestrator listens on kernel.request at priority 20"
    - "TenantContextOrchestrator listens on kernel.terminate to clear context"
    - "TenantContextOrchestrator::PRIORITY is a public constant with value 20"
    - "onKernelRequest guards with isMainRequest() — sub-requests are ignored"
    - "onKernelTerminate dispatches TenantContextCleared, clears bootstrapperChain, and clears TenantContext"
    - "Container compiles without ServiceCircularReferenceException"
    - "The kernel.request listener is registered at priority 20 in the compiled container"
    - "A class implementing TenantBootstrapperInterface is auto-tagged and appears in BootstrapperChain after container compilation"
  artifacts:
    - path: "src/EventListener/TenantContextOrchestrator.php"
      provides: "HTTP lifecycle tenant context management"
      contains: "class TenantContextOrchestrator"
    - path: "tests/Unit/EventListener/TenantContextOrchestratorTest.php"
      provides: "Unit tests for orchestrator"
      contains: "class TenantContextOrchestratorTest"
    - path: "tests/Integration/ContainerCompilationTest.php"
      provides: "Smoke test for DI container"
      contains: "class ContainerCompilationTest"
    - path: "tests/Integration/ListenerPriorityTest.php"
      provides: "Verifies kernel.request priority 20"
      contains: "class ListenerPriorityTest"
    - path: "tests/Integration/AutoconfigurationTest.php"
      provides: "Verifies registerForAutoconfiguration auto-tags TenantBootstrapperInterface"
      contains: "class AutoconfigurationTest"
  key_links:
    - from: "src/EventListener/TenantContextOrchestrator.php"
      to: "src/Context/TenantContext.php"
      via: "constructor injection"
      pattern: "private readonly TenantContext \\$tenantContext"
    - from: "src/EventListener/TenantContextOrchestrator.php"
      to: "src/Bootstrapper/BootstrapperChain.php"
      via: "constructor injection"
      pattern: "private readonly BootstrapperChain \\$bootstrapperChain"
    - from: "src/EventListener/TenantContextOrchestrator.php"
      to: "src/Event/TenantContextCleared.php"
      via: "dispatches on terminate"
      pattern: "dispatch.*TenantContextCleared"
    - from: "src/TenancyBundle.php"
      to: "src/Bootstrapper/TenantBootstrapperInterface.php"
      via: "registerForAutoconfiguration auto-tags implementations"
      pattern: "registerForAutoconfiguration.*TenantBootstrapperInterface"
---

<objective>
Create the TenantContextOrchestrator kernel event listener and integration tests that validate the entire Phase 1 wiring, including autoconfiguration of TenantBootstrapperInterface implementations.

Purpose: The orchestrator is the entry point for tenant lifecycle management in HTTP requests. It bridges Symfony's kernel events to the tenant context system. Integration tests confirm the full DI container compiles, the listener is wired at the correct priority, and autoconfiguration works end-to-end.
Output: One listener class, one unit test file, and three integration test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-VALIDATION.md

<interfaces>
<!-- From Plan 02 -->
From src/Context/TenantContext.php:
```php
namespace Tenancy\Bundle\Context;
final class TenantContext {
    public function setTenant(TenantInterface $tenant): void;
    public function getTenant(): ?TenantInterface;
    public function hasTenant(): bool;
    public function clear(): void;
}
```

From src/Bootstrapper/BootstrapperChain.php:
```php
namespace Tenancy\Bundle\Bootstrapper;
final class BootstrapperChain {
    public function __construct(private readonly EventDispatcherInterface $eventDispatcher);
    public function addBootstrapper(TenantBootstrapperInterface $bootstrapper): void;
    public function boot(TenantInterface $tenant): void;
    public function clear(): void;
}
```

From src/Bootstrapper/TenantBootstrapperInterface.php:
```php
namespace Tenancy\Bundle\Bootstrapper;
interface TenantBootstrapperInterface {
    public function boot(TenantInterface $tenant): void;
    public function clear(): void;
}
```

<!-- From Plan 03 -->
From src/Event/TenantContextCleared.php:
```php
namespace Tenancy\Bundle\Event;
final class TenantContextCleared {}
```

<!-- From Plan 01 -->
From config/services.php:
- TenantContextOrchestrator service args: service('tenancy.context'), service('tenancy.bootstrapper_chain'), service('event_dispatcher')
- autoconfigure: true (enables #[AsEventListener] discovery)

From src/TenancyBundle.php:
- registerForAutoconfiguration(Bootstrapper\TenantBootstrapperInterface::class)->addTag('tenancy.bootstrapper')
- BootstrapperChainPass collects tenancy.bootstrapper tags into BootstrapperChain via addMethodCall('addBootstrapper', [...])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TenantContextOrchestrator with kernel event listeners</name>
  <files>src/EventListener/TenantContextOrchestrator.php</files>
  <read_first>
    - .planning/phases/01-core-foundation/01-RESEARCH.md (Pattern 4: Kernel Event Listener with AsEventListener — exact attribute syntax, priority constant, isMainRequest guard)
    - .planning/phases/01-core-foundation/01-CONTEXT.md (Kernel event wiring: priority 20, kernel.terminate for clear, PRIORITY constant)
    - config/services.php (verify constructor args match: tenancy.context, tenancy.bootstrapper_chain, event_dispatcher)
    - src/Context/TenantContext.php (API: setTenant, getTenant, hasTenant, clear)
    - src/Bootstrapper/BootstrapperChain.php (API: boot, clear)
    - src/Event/TenantContextCleared.php (signal-only event to dispatch)
  </read_first>
  <action>
Create `src/EventListener/TenantContextOrchestrator.php`:
- `namespace Tenancy\Bundle\EventListener;`
- Imports:
  - `Symfony\Component\EventDispatcher\Attribute\AsEventListener`
  - `Symfony\Component\HttpKernel\Event\RequestEvent`
  - `Symfony\Component\HttpKernel\Event\TerminateEvent`
  - `Symfony\Component\HttpKernel\KernelEvents`
  - `Symfony\Contracts\EventDispatcher\EventDispatcherInterface`
  - `Tenancy\Bundle\Bootstrapper\BootstrapperChain`
  - `Tenancy\Bundle\Context\TenantContext`
  - `Tenancy\Bundle\Event\TenantContextCleared`

Class-level attributes:
- `#[AsEventListener(event: KernelEvents::REQUEST, method: 'onKernelRequest', priority: TenantContextOrchestrator::PRIORITY)]`
- `#[AsEventListener(event: KernelEvents::TERMINATE, method: 'onKernelTerminate')]`

`final class TenantContextOrchestrator`:
- `/** Priority 20: after Router (32), before Security firewall (8). */`
- `public const PRIORITY = 20;`
- Constructor: `public function __construct(private readonly TenantContext $tenantContext, private readonly BootstrapperChain $bootstrapperChain, private readonly EventDispatcherInterface $eventDispatcher)`

`public function onKernelRequest(RequestEvent $event): void`:
- First line: `if (!$event->isMainRequest()) { return; }`
- Phase 1 skeleton: no resolver logic yet (Phase 2 adds resolution).
- Comment: `// Phase 2 will inject ResolverChain here to resolve tenant from request.`
- Comment: `// Once resolved: $this->tenantContext->setTenant($tenant); $this->bootstrapperChain->boot($tenant); dispatch TenantResolved event.`
- For now, the method is a guarded no-op — it only returns early on sub-requests.

`public function onKernelTerminate(TerminateEvent $event): void`:
- `if (!$this->tenantContext->hasTenant()) { return; }`
- `$this->bootstrapperChain->clear();`
- `$this->tenantContext->clear();`
- `$this->eventDispatcher->dispatch(new TenantContextCleared());`
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && php -l src/EventListener/TenantContextOrchestrator.php</automated>
  </verify>
  <acceptance_criteria>
    - src/EventListener/TenantContextOrchestrator.php contains `final class TenantContextOrchestrator`
    - src/EventListener/TenantContextOrchestrator.php contains `public const PRIORITY = 20;`
    - src/EventListener/TenantContextOrchestrator.php contains `#[AsEventListener(event: KernelEvents::REQUEST, method: 'onKernelRequest', priority: TenantContextOrchestrator::PRIORITY)]`
    - src/EventListener/TenantContextOrchestrator.php contains `#[AsEventListener(event: KernelEvents::TERMINATE, method: 'onKernelTerminate')]`
    - src/EventListener/TenantContextOrchestrator.php contains `if (!$event->isMainRequest())`
    - src/EventListener/TenantContextOrchestrator.php contains `$this->bootstrapperChain->clear();`
    - src/EventListener/TenantContextOrchestrator.php contains `$this->tenantContext->clear();`
    - src/EventListener/TenantContextOrchestrator.php contains `$this->eventDispatcher->dispatch(new TenantContextCleared())`
    - File passes `php -l` syntax check
  </acceptance_criteria>
  <done>TenantContextOrchestrator has PRIORITY=20, kernel.request with isMainRequest guard, kernel.terminate with full teardown chain</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for TenantContextOrchestrator</name>
  <files>tests/Unit/EventListener/TenantContextOrchestratorTest.php</files>
  <read_first>
    - src/EventListener/TenantContextOrchestrator.php (the class under test)
    - src/Context/TenantContext.php (dependency to mock/use)
    - src/Bootstrapper/BootstrapperChain.php (dependency to mock)
    - src/Event/TenantContextCleared.php (event dispatched on terminate)
  </read_first>
  <action>
Create `tests/Unit/EventListener/TenantContextOrchestratorTest.php`:
- `namespace Tenancy\Bundle\Tests\Unit\EventListener;`
- `use PHPUnit\Framework\TestCase;`
- Setup: create a real TenantContext (zero deps, no mock needed), a mock BootstrapperChain (or use real one with mock EventDispatcher), a mock EventDispatcherInterface
- Construct TenantContextOrchestrator with these

Tests:
- `testPriorityConstantIs20`: `$this->assertSame(20, TenantContextOrchestrator::PRIORITY);`
- `testOnKernelRequestIgnoresSubRequests`: create a RequestEvent with `isMainRequest()` returning false (use HttpKernelInterface::SUB_REQUEST). Assert no exception, no side effects.
- `testOnKernelRequestIsNoOpInPhase1ForMainRequest`: create a RequestEvent with isMainRequest() true (HttpKernelInterface::MAIN_REQUEST). Call onKernelRequest(). Assert tenantContext->hasTenant() is still false (no resolver yet).
- `testOnKernelTerminateDoesNothingWithNoActiveTenant`: create a TerminateEvent. Call onKernelTerminate(). Assert bootstrapperChain->clear() was NOT called. Assert eventDispatcher->dispatch() was NOT called.
- `testOnKernelTerminateClearsContextWhenTenantActive`: set a tenant on tenantContext via setTenant(), then call onKernelTerminate(). Assert:
  - bootstrapperChain->clear() was called once
  - tenantContext->hasTenant() is false after the call
  - eventDispatcher->dispatch() was called once with a TenantContextCleared instance
- `testTeardownOrderIsBootstrappersThenContextThenEvent`: use a mock EventDispatcher that records call order. Verify: bootstrapperChain->clear() before tenantContext->clear() before dispatch(TenantContextCleared).
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/EventListener/TenantContextOrchestratorTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/Unit/EventListener/TenantContextOrchestratorTest.php contains `class TenantContextOrchestratorTest extends TestCase`
    - tests/Unit/EventListener/TenantContextOrchestratorTest.php contains `testPriorityConstantIs20`
    - tests/Unit/EventListener/TenantContextOrchestratorTest.php contains `testOnKernelRequestIgnoresSubRequests`
    - tests/Unit/EventListener/TenantContextOrchestratorTest.php contains `testOnKernelTerminateClearsContextWhenTenantActive`
    - tests/Unit/EventListener/TenantContextOrchestratorTest.php contains `testTeardownOrderIsBootstrappersThenContextThenEvent`
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>6 unit tests pass covering: PRIORITY constant, sub-request guard, Phase 1 no-op, no-tenant guard, full teardown, and teardown ordering</done>
</task>

<task type="auto">
  <name>Task 3: Create integration tests for container compilation and listener priority</name>
  <files>tests/Integration/ContainerCompilationTest.php, tests/Integration/ListenerPriorityTest.php</files>
  <read_first>
    - src/TenancyBundle.php (bundle under test — build, loadExtension, configure, prependExtension)
    - config/services.php (service definitions)
    - src/DependencyInjection/Compiler/BootstrapperChainPass.php (compiler pass under test)
    - src/EventListener/TenantContextOrchestrator.php (listener under test for priority)
    - composer.json (verify symfony/framework-bundle is in require-dev for test kernel)
  </read_first>
  <action>
Create `tests/Integration/ContainerCompilationTest.php`:
- `namespace Tenancy\Bundle\Tests\Integration;`
- `use PHPUnit\Framework\TestCase;`
- `use Symfony\Component\DependencyInjection\ContainerBuilder;`
- `use Symfony\Component\DependencyInjection\Loader\Configurator\ContainerConfigurator;`

Test `testContainerCompilesWithoutCircularReferences`:
- Create a ContainerBuilder
- Register a minimal set of synthetic services: `event_dispatcher` (EventDispatcherInterface)
- Register TenancyBundle services manually (load config/services.php) or use a minimal test kernel
- Compile the container
- Assert no `ServiceCircularReferenceException` thrown
- Assert `tenancy.context` service exists in container
- Assert `tenancy.bootstrapper_chain` service exists in container

Test `testTenantContextServiceHasNoConstructorArguments`:
- After compilation, get the `tenancy.context` Definition
- Assert `getArguments()` returns empty array

Test `testBootstrapperChainPassIsRegistered`:
- Create a test ContainerBuilder through TenancyBundle::build()
- Assert BootstrapperChainPass is among the registered compiler passes (use getCompilerPassConfig() or inspect pass behavior)

Alternative approach if direct ContainerBuilder testing is complex:
- Use `Symfony\Component\HttpKernel\Kernel` subclass as a minimal test kernel that registers only TenancyBundle + FrameworkBundle
- Boot the kernel and get the container
- Assert services exist

Create `tests/Integration/ListenerPriorityTest.php`:
- `namespace Tenancy\Bundle\Tests\Integration;`
- `use PHPUnit\Framework\TestCase;`

Test `testOrchestratorRegisteredAtPriority20OnKernelRequest`:
- Boot a test kernel with TenancyBundle
- Get the event_dispatcher service
- Use reflection or `getListeners(KernelEvents::REQUEST)` to find TenantContextOrchestrator::onKernelRequest
- Verify it is registered (presence check)
- Alternatively: verify `TenantContextOrchestrator::PRIORITY === 20` (unit-level) AND verify the listener is registered in the container's event dispatcher

Test `testOrchestratorRegisteredOnKernelTerminate`:
- Same approach, check `KernelEvents::TERMINATE` listeners include onKernelTerminate

Note: Integration tests may need a minimal test kernel. Create a `tests/Integration/TestKernel.php` helper if needed:
- Extends `Symfony\Component\HttpKernel\Kernel`
- Registers `FrameworkBundle` and `TenancyBundle`
- Uses a temporary cache/log directory
- Minimal framework config (secret, test: true)
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Integration/ContainerCompilationTest.php tests/Integration/ListenerPriorityTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/Integration/ContainerCompilationTest.php contains `testContainerCompilesWithoutCircularReferences`
    - tests/Integration/ContainerCompilationTest.php contains `tenancy.context`
    - tests/Integration/ListenerPriorityTest.php contains `testOrchestratorRegisteredAtPriority20OnKernelRequest`
    - tests/Integration/ListenerPriorityTest.php contains `KernelEvents::REQUEST` or `kernel.request`
    - All integration tests pass with exit code 0
  </acceptance_criteria>
  <done>Integration tests confirm: container compiles without circular deps, tenancy.context has no constructor args, orchestrator is wired at priority 20 for kernel.request and kernel.terminate</done>
</task>

<task type="auto">
  <name>Task 4: Create AutoconfigurationTest for TenantBootstrapperInterface auto-tagging</name>
  <files>tests/Integration/AutoconfigurationTest.php</files>
  <read_first>
    - src/TenancyBundle.php (loadExtension calls registerForAutoconfiguration for TenantBootstrapperInterface)
    - src/DependencyInjection/Compiler/BootstrapperChainPass.php (collects tenancy.bootstrapper tagged services into BootstrapperChain)
    - src/Bootstrapper/TenantBootstrapperInterface.php (the interface that triggers auto-tagging)
    - src/Bootstrapper/BootstrapperChain.php (target service that receives addBootstrapper calls)
    - tests/Integration/ContainerCompilationTest.php (reuse TestKernel infrastructure from Task 3)
  </read_first>
  <action>
Create `tests/Integration/AutoconfigurationTest.php`:
- `namespace Tenancy\Bundle\Tests\Integration;`
- `use PHPUnit\Framework\TestCase;`

This test proves CORE-03 end-to-end: that a class implementing TenantBootstrapperInterface is automatically discovered via registerForAutoconfiguration(), tagged with `tenancy.bootstrapper`, collected by BootstrapperChainPass, and injected into BootstrapperChain — without any manual DI configuration.

Create a dummy bootstrapper class (can be an anonymous class or a simple inner/test class):
```php
// In the test file or a test fixtures directory
class DummyBootstrapper implements TenantBootstrapperInterface {
    public function boot(TenantInterface $tenant): void {}
    public function clear(): void {}
}
```

Test `testBootstrapperInterfaceImplementationIsAutoTagged`:
- Boot the test kernel (reuse TestKernel from Task 3)
- Register the DummyBootstrapper as a service in the test kernel's container configuration (via a CompilerPass or by overriding configureContainer in the test kernel)
- Compile the container
- Get the `tenancy.bootstrapper_chain` (BootstrapperChain) service definition
- Assert it has a method call to `addBootstrapper` with a reference to DummyBootstrapper
- This confirms: registerForAutoconfiguration -> tag -> BootstrapperChainPass -> addMethodCall chain works

Test `testMultipleBootstrappersAreAllCollected`:
- Register two dummy bootstrapper implementations
- Compile the container
- Assert BootstrapperChain definition has two `addBootstrapper` method calls

Alternative approach if using the compiled container directly:
- Get the compiled BootstrapperChain service from the container
- Use reflection to access the private `$bootstrappers` array property
- Assert it contains instances of the dummy bootstrappers

Use the same TestKernel helper created in Task 3. If the TestKernel needs modification to register test services, extend it or create a dedicated AutoconfigTestKernel that adds DummyBootstrapper to the container.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Integration/AutoconfigurationTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/Integration/AutoconfigurationTest.php contains `class AutoconfigurationTest`
    - tests/Integration/AutoconfigurationTest.php contains `TenantBootstrapperInterface`
    - tests/Integration/AutoconfigurationTest.php contains `testBootstrapperInterfaceImplementationIsAutoTagged`
    - tests/Integration/AutoconfigurationTest.php contains `addBootstrapper` or `$bootstrappers`
    - All AutoconfigurationTest tests pass with exit code 0
  </acceptance_criteria>
  <done>AutoconfigurationTest proves that implementing TenantBootstrapperInterface is sufficient for auto-discovery and injection into BootstrapperChain via the compiled container</done>
</task>

</tasks>

<verification>
- `php -l src/EventListener/TenantContextOrchestrator.php` exits 0
- `vendor/bin/phpunit tests/Unit/EventListener/TenantContextOrchestratorTest.php --stop-on-failure` exits 0
- `vendor/bin/phpunit tests/Integration/ --stop-on-failure` exits 0
- `vendor/bin/phpunit --stop-on-failure` exits 0 (full suite — all Plans 01-05)
</verification>

<success_criteria>
- TenantContextOrchestrator has PRIORITY = 20 constant
- kernel.request listener guards with isMainRequest()
- kernel.terminate listener clears bootstrappers, context, and dispatches TenantContextCleared
- Container compiles without ServiceCircularReferenceException
- tenancy.context service has zero constructor arguments
- Event listener is wired at priority 20 in compiled container
- A TenantBootstrapperInterface implementation is auto-tagged and injected into BootstrapperChain after container compilation
- Full test suite passes (all unit + integration tests from Phase 1)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-05-SUMMARY.md`
</output>
