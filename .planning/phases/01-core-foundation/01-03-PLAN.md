---
phase: 01-core-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/Event/TenantResolved.php
  - src/Event/TenantBootstrapped.php
  - src/Event/TenantContextCleared.php
  - tests/Unit/Event/TenantResolvedTest.php
  - tests/Unit/Event/TenantBootstrappedTest.php
  - tests/Unit/Event/TenantContextClearedTest.php
  - tests/Unit/Bootstrapper/BootstrapperChainTest.php
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - "TenantResolved carries tenant (TenantInterface), request (?Request), and resolvedBy (string FQCN)"
    - "TenantBootstrapped carries tenant (TenantInterface) and bootstrappers (string[] of FQCNs)"
    - "TenantContextCleared is a signal-only event with no payload"
    - "All three events are final classes with readonly constructor properties"
    - "No event extends a base class or implements stopPropagation"
    - "BootstrapperChain.boot() dispatches TenantBootstrapped with correct tenant and FQCN list"
  artifacts:
    - path: "src/Event/TenantResolved.php"
      provides: "Event dispatched after tenant identification"
      contains: "final class TenantResolved"
    - path: "src/Event/TenantBootstrapped.php"
      provides: "Event dispatched after all bootstrappers run"
      contains: "final class TenantBootstrapped"
    - path: "src/Event/TenantContextCleared.php"
      provides: "Signal event dispatched on context teardown"
      contains: "final class TenantContextCleared"
  key_links:
    - from: "src/Event/TenantResolved.php"
      to: "src/TenantInterface.php"
      via: "carries tenant property"
      pattern: "public readonly TenantInterface \\$tenant"
    - from: "src/Event/TenantBootstrapped.php"
      to: "src/TenantInterface.php"
      via: "carries tenant property"
      pattern: "public readonly TenantInterface \\$tenant"
    - from: "src/Bootstrapper/BootstrapperChain.php"
      to: "src/Event/TenantBootstrapped.php"
      via: "dispatch in boot()"
      pattern: "dispatch.*TenantBootstrapped"
---

<objective>
Create the three lifecycle event classes: TenantResolved, TenantBootstrapped, and TenantContextCleared. Then add the TenantBootstrapped-dependent tests to BootstrapperChainTest that were deferred from Plan 01-02.

Purpose: These events form the public extension API of the bundle. Any listener can observe tenant lifecycle stages without coupling to internal services. CORE-02 requires all three events to be dispatched at the correct lifecycle stages.
Output: Three event classes, three unit test files, and two additional BootstrapperChainTest methods.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 02 — events reference TenantInterface -->
From src/TenantInterface.php:
```php
namespace Tenancy\Bundle;
interface TenantInterface {
    public function getSlug(): string;
    public function getDomain(): ?string;
    /** @return array<string, mixed> */
    public function getConnectionConfig(): array;
    public function getName(): string;
    public function isActive(): bool;
}
```

From src/Bootstrapper/BootstrapperChain.php:
- boot() dispatches `new TenantBootstrapped($tenant, $fqcns)` — this plan creates that class
- clear() iterates bootstrappers in reverse
- Constructor takes EventDispatcherInterface
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TenantResolved, TenantBootstrapped, and TenantContextCleared event classes</name>
  <files>src/Event/TenantResolved.php, src/Event/TenantBootstrapped.php, src/Event/TenantContextCleared.php</files>
  <read_first>
    - .planning/phases/01-core-foundation/01-CONTEXT.md (Event design section: exact payloads per event, PSR-14 plain objects, no base class)
    - .planning/phases/01-core-foundation/01-RESEARCH.md (Pattern 3: PSR-14 Events as Readonly PHP Objects)
    - src/TenantInterface.php (the interface events carry)
  </read_first>
  <action>
Create `src/Event/TenantResolved.php`:
- `namespace Tenancy\Bundle\Event;`
- `use Symfony\Component\HttpFoundation\Request;`
- `use Tenancy\Bundle\TenantInterface;`
- `final class TenantResolved`
- Constructor with readonly promoted properties:
  - `public readonly TenantInterface $tenant` — the resolved tenant entity
  - `public readonly ?Request $request` — the original Symfony request (nullable for console context)
  - `public readonly string $resolvedBy` — FQCN of the resolver that matched

Create `src/Event/TenantBootstrapped.php`:
- `namespace Tenancy\Bundle\Event;`
- `use Tenancy\Bundle\TenantInterface;`
- `final class TenantBootstrapped`
- Constructor with readonly promoted properties:
  - `public readonly TenantInterface $tenant` — the tenant that was bootstrapped
  - `/** @param string[] $bootstrappers */ public readonly array $bootstrappers` — FQCNs of bootstrappers that ran, in order

Create `src/Event/TenantContextCleared.php`:
- `namespace Tenancy\Bundle\Event;`
- `final class TenantContextCleared`
- No constructor, no properties — signal-only event per user decision. Do NOT add previousTenant (deferred to v1.1).
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && php -l src/Event/TenantResolved.php && php -l src/Event/TenantBootstrapped.php && php -l src/Event/TenantContextCleared.php</automated>
  </verify>
  <acceptance_criteria>
    - src/Event/TenantResolved.php contains `final class TenantResolved`
    - src/Event/TenantResolved.php contains `public readonly TenantInterface $tenant`
    - src/Event/TenantResolved.php contains `public readonly ?Request $request`
    - src/Event/TenantResolved.php contains `public readonly string $resolvedBy`
    - src/Event/TenantResolved.php does NOT contain `extends` (no base class)
    - src/Event/TenantBootstrapped.php contains `final class TenantBootstrapped`
    - src/Event/TenantBootstrapped.php contains `public readonly TenantInterface $tenant`
    - src/Event/TenantBootstrapped.php contains `public readonly array $bootstrappers`
    - src/Event/TenantContextCleared.php contains `final class TenantContextCleared`
    - src/Event/TenantContextCleared.php does NOT contain `__construct` (signal-only, no payload)
    - All three files pass `php -l` syntax check
  </acceptance_criteria>
  <done>Three event classes created: TenantResolved with tenant/request/resolvedBy, TenantBootstrapped with tenant/bootstrappers, TenantContextCleared signal-only</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for all three event classes</name>
  <files>tests/Unit/Event/TenantResolvedTest.php, tests/Unit/Event/TenantBootstrappedTest.php, tests/Unit/Event/TenantContextClearedTest.php</files>
  <read_first>
    - src/Event/TenantResolved.php (the class under test)
    - src/Event/TenantBootstrapped.php (the class under test)
    - src/Event/TenantContextCleared.php (the class under test)
    - src/TenantInterface.php (mock target for tenant parameter)
  </read_first>
  <action>
Create `tests/Unit/Event/TenantResolvedTest.php`:
- `namespace Tenancy\Bundle\Tests\Unit\Event;`
- `use PHPUnit\Framework\TestCase;`
- Test `testCarriesTenantRequestAndResolvedBy`: construct with mock TenantInterface, a Request instance, and a string FQCN. Assert all three readonly properties return correct values.
- Test `testRequestCanBeNull`: construct with null request. Assert `$event->request` is null.
- Test `testPropertiesAreReadonly`: use ReflectionProperty to assert all three properties have `isReadOnly() === true`.

Create `tests/Unit/Event/TenantBootstrappedTest.php`:
- `namespace Tenancy\Bundle\Tests\Unit\Event;`
- `use PHPUnit\Framework\TestCase;`
- Test `testCarriesTenantAndBootstrapperFqcns`: construct with mock TenantInterface and `['App\\Boot1', 'App\\Boot2']`. Assert `$event->tenant` and `$event->bootstrappers === ['App\\Boot1', 'App\\Boot2']`.
- Test `testEmptyBootstrappersArray`: construct with empty array. Assert `$event->bootstrappers === []`.

Create `tests/Unit/Event/TenantContextClearedTest.php`:
- `namespace Tenancy\Bundle\Tests\Unit\Event;`
- `use PHPUnit\Framework\TestCase;`
- Test `testCanBeInstantiated`: `$event = new TenantContextCleared(); $this->assertInstanceOf(TenantContextCleared::class, $event);`
- Test `testHasNoPublicProperties`: use ReflectionClass to assert zero public properties.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/Event/ --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/Unit/Event/TenantResolvedTest.php contains `testCarriesTenantRequestAndResolvedBy`
    - tests/Unit/Event/TenantResolvedTest.php contains `testRequestCanBeNull`
    - tests/Unit/Event/TenantBootstrappedTest.php contains `testCarriesTenantAndBootstrapperFqcns`
    - tests/Unit/Event/TenantBootstrappedTest.php contains `testEmptyBootstrappersArray`
    - tests/Unit/Event/TenantContextClearedTest.php contains `testCanBeInstantiated`
    - tests/Unit/Event/TenantContextClearedTest.php contains `testHasNoPublicProperties`
    - All event tests pass with exit code 0
  </acceptance_criteria>
  <done>7 event unit tests pass covering all three lifecycle events, payload correctness, nullability, and readonly enforcement</done>
</task>

<task type="auto">
  <name>Task 3: Add TenantBootstrapped-dependent tests to BootstrapperChainTest</name>
  <files>tests/Unit/Bootstrapper/BootstrapperChainTest.php</files>
  <read_first>
    - tests/Unit/Bootstrapper/BootstrapperChainTest.php (existing test file from Plan 01-02 with testBootCallsAllBootstrappersInOrder and testClearCallsBootstrappersInReverseOrder)
    - src/Bootstrapper/BootstrapperChain.php (the class under test — boot() dispatches TenantBootstrapped)
    - src/Event/TenantBootstrapped.php (the event class now available from Task 1)
  </read_first>
  <action>
Add two new test methods to the existing `tests/Unit/Bootstrapper/BootstrapperChainTest.php` file (created in Plan 01-02):

- Test `testBootDispatchesTenantBootstrappedEvent`: mock EventDispatcherInterface, add two mock TenantBootstrapperInterface instances to chain, call boot(). Assert `dispatch()` was called once with a `TenantBootstrapped` instance. Assert the event's `$tenant` property is the mock tenant. Assert the event's `$bootstrappers` array contains the FQCNs of the two mock bootstrapper classes.

- Test `testBootWithNoBootstrappersStillDispatchesEvent`: create chain with no bootstrappers added, call boot(). Assert EventDispatcher::dispatch() was called once with a `TenantBootstrapped` instance whose `$bootstrappers` array is empty.

These tests were deferred from Plan 01-02 because they depend on the TenantBootstrapped class which was created in Task 1 of this plan.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/Bootstrapper/BootstrapperChainTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/Unit/Bootstrapper/BootstrapperChainTest.php contains `testBootDispatchesTenantBootstrappedEvent`
    - tests/Unit/Bootstrapper/BootstrapperChainTest.php contains `testBootWithNoBootstrappersStillDispatchesEvent`
    - tests/Unit/Bootstrapper/BootstrapperChainTest.php contains `TenantBootstrapped`
    - All 4 BootstrapperChainTest tests pass (2 from Plan 01-02 + 2 new)
  </acceptance_criteria>
  <done>BootstrapperChainTest now has 4 passing tests: boot order, clear reverse order, TenantBootstrapped event dispatch with FQCNs, and empty-chain event dispatch</done>
</task>

</tasks>

<verification>
- `php -l src/Event/TenantResolved.php` exits 0
- `php -l src/Event/TenantBootstrapped.php` exits 0
- `php -l src/Event/TenantContextCleared.php` exits 0
- `vendor/bin/phpunit tests/Unit/Event/ --stop-on-failure` exits 0
- `vendor/bin/phpunit tests/Unit/Bootstrapper/BootstrapperChainTest.php --stop-on-failure` exits 0
- `vendor/bin/phpunit tests/Unit/ --stop-on-failure` exits 0 (all unit tests from Plans 01-03)
</verification>

<success_criteria>
- TenantResolved is a final class with readonly tenant, request, resolvedBy
- TenantBootstrapped is a final class with readonly tenant, bootstrappers
- TenantContextCleared is a final signal-only class with no payload
- No event extends a base class
- BootstrapperChain (from Plan 02) can now construct TenantBootstrapped without fatal error
- BootstrapperChainTest has 4 passing tests (2 original + 2 event-dependent)
- 7 event unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-03-SUMMARY.md`
</output>
