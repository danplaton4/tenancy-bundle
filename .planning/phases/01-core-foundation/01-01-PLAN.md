---
phase: 01-core-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - phpunit.xml.dist
  - tests/bootstrap.php
  - src/TenancyBundle.php
  - src/DependencyInjection/Compiler/BootstrapperChainPass.php
  - config/services.php
autonomous: true
requirements: [CORE-03]
must_haves:
  truths:
    - "composer install succeeds with all Symfony 6.4/7.x and Doctrine ORM 3.3+ deps"
    - "phpunit.xml.dist defines unit and integration test suites pointing to correct directories"
    - "TenancyBundle extends AbstractBundle with configure(), loadExtension(), build(), and prependExtension() methods"
    - "BootstrapperChainPass collects tenancy.bootstrapper tagged services and injects them into BootstrapperChain"
    - "registerForAutoconfiguration auto-tags TenantBootstrapperInterface implementations"
  artifacts:
    - path: "composer.json"
      provides: "Package definition with all runtime and dev dependencies"
      contains: "danplaton4/tenancy-bundle"
    - path: "phpunit.xml.dist"
      provides: "PHPUnit 11 configuration"
      contains: "testsuite"
    - path: "src/TenancyBundle.php"
      provides: "Bundle entry point"
      contains: "class TenancyBundle extends AbstractBundle"
    - path: "src/DependencyInjection/Compiler/BootstrapperChainPass.php"
      provides: "Compiler pass for bootstrapper auto-discovery"
      contains: "class BootstrapperChainPass implements CompilerPassInterface"
    - path: "config/services.php"
      provides: "Bundle service definitions"
      contains: "tenancy.context"
  key_links:
    - from: "src/TenancyBundle.php"
      to: "src/DependencyInjection/Compiler/BootstrapperChainPass.php"
      via: "build() registers compiler pass"
      pattern: "addCompilerPass.*BootstrapperChainPass"
    - from: "src/TenancyBundle.php"
      to: "config/services.php"
      via: "loadExtension imports services"
      pattern: "import.*services"
---

<objective>
Create the bundle skeleton: composer.json, PHPUnit config, TenancyBundle (AbstractBundle), BootstrapperChainPass compiler pass, and service definitions.

Purpose: Establishes the project structure and DI infrastructure that all subsequent plans build on. Without the bundle class and compiler pass, no services can be registered or auto-tagged.
Output: A working Symfony bundle skeleton that can be installed via Composer with PHPUnit test infrastructure ready.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create composer.json and PHPUnit test infrastructure</name>
  <files>composer.json, phpunit.xml.dist, tests/bootstrap.php</files>
  <read_first>
    - .planning/phases/01-core-foundation/01-RESEARCH.md (Standard Stack section for exact dependency versions)
    - .planning/phases/01-core-foundation/01-CONTEXT.md (Package identity: danplaton4/tenancy-bundle, namespace Tenancy\Bundle)
  </read_first>
  <action>
Create `composer.json` with:
- `"name": "danplaton4/tenancy-bundle"`
- `"type": "symfony-bundle"`
- `"autoload.psr-4": { "Tenancy\\Bundle\\": "src/" }`
- `"autoload-dev.psr-4": { "Tenancy\\Bundle\\Tests\\": "tests/" }`
- `"require"`: `"php": "^8.2"`, `"symfony/http-kernel": "^6.4||^7.0"`, `"symfony/dependency-injection": "^6.4||^7.0"`, `"symfony/event-dispatcher": "^6.4||^7.0"`, `"symfony/http-foundation": "^6.4||^7.0"`, `"symfony/config": "^6.4||^7.0"`
- `"suggest"`: `"doctrine/orm": "Required for Tenant entity (^3.3)"`, `"doctrine/dbal": "Required for database drivers (^4.4)"`, `"doctrine/doctrine-bundle": "Required for Doctrine integration (^2.13)"`
- `"require-dev"`: `"phpunit/phpunit": "^11.0"`, `"symfony/phpunit-bridge": "^6.4||^7.0"`, `"doctrine/orm": "^3.3"`, `"doctrine/dbal": "^4.4"`, `"doctrine/doctrine-bundle": "^2.13"`, `"symfony/framework-bundle": "^6.4||^7.0"`
- `"extra.symfony.bundles": { "Tenancy\\Bundle\\TenancyBundle": ["all": true] }`
- `"minimum-stability": "stable"`
- `"license": "MIT"`

Create `phpunit.xml.dist` with PHPUnit 11 schema:
- `bootstrap="tests/bootstrap.php"`
- `<testsuites>`: `<testsuite name="unit"><directory>tests/Unit</directory></testsuite>` and `<testsuite name="integration"><directory>tests/Integration</directory></testsuite>`
- `<source><include><directory suffix=".php">src</directory></include></source>`

Create `tests/bootstrap.php`:
- `require dirname(__DIR__).'/vendor/autoload.php';`

Run `composer install` to generate vendor/ and autoloader.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && cat composer.json | php -r "echo json_decode(file_get_contents('php://stdin'))->name;" && echo "" && test -f phpunit.xml.dist && echo "phpunit.xml.dist exists" && test -f tests/bootstrap.php && echo "bootstrap exists" && test -d vendor && echo "vendor exists"</automated>
  </verify>
  <acceptance_criteria>
    - composer.json contains `"name": "danplaton4/tenancy-bundle"`
    - composer.json contains `"Tenancy\\Bundle\\": "src/"` in autoload.psr-4
    - composer.json contains `"phpunit/phpunit"` in require-dev
    - composer.json contains `"doctrine/orm"` in suggest
    - phpunit.xml.dist contains `<testsuite name="unit">`
    - phpunit.xml.dist contains `<testsuite name="integration">`
    - phpunit.xml.dist contains `bootstrap="tests/bootstrap.php"`
    - tests/bootstrap.php contains `vendor/autoload.php`
    - `composer install` exits 0 and vendor/ directory exists
  </acceptance_criteria>
  <done>Composer installs cleanly, PHPUnit config exists with unit and integration suites, autoloader resolves Tenancy\Bundle namespace</done>
</task>

<task type="auto">
  <name>Task 2: Create TenancyBundle, BootstrapperChainPass, and services.php</name>
  <files>src/TenancyBundle.php, src/DependencyInjection/Compiler/BootstrapperChainPass.php, config/services.php</files>
  <read_first>
    - .planning/phases/01-core-foundation/01-RESEARCH.md (Pattern 1: AbstractBundle, Pattern 2: Compiler Pass with PriorityTaggedServiceTrait)
    - .planning/phases/01-core-foundation/01-CONTEXT.md (Bundle config shape: tenancy.driver, strict_mode, landlord_connection, tenant_entity_class, cache_prefix_separator)
    - composer.json (verify autoload namespace is correct)
  </read_first>
  <action>
Create `src/TenancyBundle.php`:
- `namespace Tenancy\Bundle;`
- `class TenancyBundle extends AbstractBundle` (from `Symfony\Component\HttpKernel\Bundle\AbstractBundle`)
- `configure(DefinitionConfigurator $definition): void` — define tree with:
  - `scalarNode('driver')->defaultValue('database_per_tenant')->end()`
  - `booleanNode('strict_mode')->defaultTrue()->end()`
  - `scalarNode('landlord_connection')->defaultValue('default')->end()`
  - `scalarNode('tenant_entity_class')->defaultValue('Tenancy\\Bundle\\Entity\\Tenant')->end()`
  - `scalarNode('cache_prefix_separator')->defaultValue(':')->end()`
- `loadExtension(array $config, ContainerConfigurator $container, ContainerBuilder $builder): void`:
  - `$container->import('../config/services.php');`
  - `$builder->registerForAutoconfiguration(Bootstrapper\TenantBootstrapperInterface::class)->addTag('tenancy.bootstrapper');`
  - Set container parameters: `tenancy.driver`, `tenancy.strict_mode`, `tenancy.landlord_connection`, `tenancy.tenant_entity_class`
- `build(ContainerBuilder $container): void`:
  - `parent::build($container);`
  - `$container->addCompilerPass(new DependencyInjection\Compiler\BootstrapperChainPass());`
- `prependExtension(ContainerConfigurator $container, ContainerBuilder $builder): void`:
  - `$builder->prependExtensionConfig('doctrine', ['orm' => ['mappings' => ['TenancyBundle' => ['is_bundle' => false, 'type' => 'attribute', 'dir' => __DIR__.'/Entity', 'prefix' => 'Tenancy\\Bundle\\Entity', 'alias' => 'TenancyBundle']]]])`;

Create `src/DependencyInjection/Compiler/BootstrapperChainPass.php`:
- `namespace Tenancy\Bundle\DependencyInjection\Compiler;`
- `final class BootstrapperChainPass implements CompilerPassInterface`
- `use PriorityTaggedServiceTrait;`
- `process(ContainerBuilder $container): void`:
  - Guard: `if (!$container->has(Bootstrapper\BootstrapperChain::class)) { return; }`
  - `$definition = $container->findDefinition(Bootstrapper\BootstrapperChain::class);`
  - `$bootstrappers = $this->findAndSortTaggedServices('tenancy.bootstrapper', $container);`
  - `foreach ($bootstrappers as $bootstrapper) { $definition->addMethodCall('addBootstrapper', [$bootstrapper]); }`

Create `config/services.php`:
- Return a closure receiving `ContainerConfigurator`
- Register `tenancy.context` service pointing to `Tenancy\Bundle\Context\TenantContext::class`, mark public
- Alias `Tenancy\Bundle\Context\TenantContext` to `tenancy.context`
- Register `tenancy.bootstrapper_chain` pointing to `Tenancy\Bundle\Bootstrapper\BootstrapperChain::class`
- Alias `Tenancy\Bundle\Bootstrapper\BootstrapperChain` to `tenancy.bootstrapper_chain`
- Register `Tenancy\Bundle\EventListener\TenantContextOrchestrator::class` with autoconfigure true and arguments: `service('tenancy.context')`, `service('tenancy.bootstrapper_chain')`, `service('event_dispatcher')`

Note: Some referenced classes (TenantContext, BootstrapperChain, TenantContextOrchestrator) don't exist yet — they are created in Plans 02, 03, and 05. This is expected: the DI config is the contract definition; implementations follow.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && php -l src/TenancyBundle.php && php -l src/DependencyInjection/Compiler/BootstrapperChainPass.php && php -l config/services.php</automated>
  </verify>
  <acceptance_criteria>
    - src/TenancyBundle.php contains `class TenancyBundle extends AbstractBundle`
    - src/TenancyBundle.php contains `function configure(DefinitionConfigurator $definition)`
    - src/TenancyBundle.php contains `function loadExtension(array $config, ContainerConfigurator $container, ContainerBuilder $builder)`
    - src/TenancyBundle.php contains `function build(ContainerBuilder $container)`
    - src/TenancyBundle.php contains `function prependExtension(ContainerConfigurator $container, ContainerBuilder $builder)`
    - src/TenancyBundle.php contains `registerForAutoconfiguration(Bootstrapper\TenantBootstrapperInterface::class)`
    - src/TenancyBundle.php contains `addCompilerPass(new DependencyInjection\Compiler\BootstrapperChainPass())`
    - src/TenancyBundle.php contains `prependExtensionConfig('doctrine'`
    - src/TenancyBundle.php contains `booleanNode('strict_mode')->defaultTrue()`
    - src/DependencyInjection/Compiler/BootstrapperChainPass.php contains `class BootstrapperChainPass implements CompilerPassInterface`
    - src/DependencyInjection/Compiler/BootstrapperChainPass.php contains `use PriorityTaggedServiceTrait`
    - src/DependencyInjection/Compiler/BootstrapperChainPass.php contains `findAndSortTaggedServices('tenancy.bootstrapper'`
    - config/services.php contains `tenancy.context`
    - config/services.php contains `TenantContextOrchestrator::class`
    - All three files pass `php -l` syntax check
  </acceptance_criteria>
  <done>TenancyBundle extends AbstractBundle with all four methods, BootstrapperChainPass collects tagged services, services.php defines all Phase 1 service IDs</done>
</task>

<task type="auto">
  <name>Task 3: Create unit test for BootstrapperChainPass</name>
  <files>tests/Unit/DependencyInjection/Compiler/BootstrapperChainPassTest.php</files>
  <read_first>
    - src/DependencyInjection/Compiler/BootstrapperChainPass.php (the class under test)
    - .planning/phases/01-core-foundation/01-VALIDATION.md (Wave 0 requirements for this test)
  </read_first>
  <action>
Create `tests/Unit/DependencyInjection/Compiler/BootstrapperChainPassTest.php`:
- `namespace Tenancy\Bundle\Tests\Unit\DependencyInjection\Compiler;`
- `use PHPUnit\Framework\TestCase;`
- Test: `testProcessDoesNothingWhenChainServiceMissing` — create a ContainerBuilder, run process(), assert no exception
- Test: `testProcessCollectsTaggedServicesInPriorityOrder` — create ContainerBuilder, register BootstrapperChain::class definition, register two dummy services tagged `tenancy.bootstrapper` with priorities 10 and 20, run process(), assert `addMethodCall('addBootstrapper', ...)` was called twice on the chain definition, with the higher-priority service first
- Test: `testProcessHandlesNoTaggedServices` — register BootstrapperChain::class but no tagged services, run process(), assert no method calls added

Use `Symfony\Component\DependencyInjection\ContainerBuilder` and `Symfony\Component\DependencyInjection\Definition` directly (no mocks needed — ContainerBuilder is designed for this in tests).
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/DependencyInjection/Compiler/BootstrapperChainPassTest.php --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/Unit/DependencyInjection/Compiler/BootstrapperChainPassTest.php contains `class BootstrapperChainPassTest extends TestCase`
    - tests/Unit/DependencyInjection/Compiler/BootstrapperChainPassTest.php contains `testProcessCollectsTaggedServicesInPriorityOrder`
    - tests/Unit/DependencyInjection/Compiler/BootstrapperChainPassTest.php contains `addTag('tenancy.bootstrapper'`
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>BootstrapperChainPass has 3 passing unit tests covering: missing chain service, priority ordering, and empty tag set</done>
</task>

</tasks>

<verification>
- `php -l src/TenancyBundle.php` exits 0
- `php -l src/DependencyInjection/Compiler/BootstrapperChainPass.php` exits 0
- `php -l config/services.php` exits 0
- `composer install` exits 0
- `vendor/bin/phpunit tests/Unit/DependencyInjection/Compiler/ --stop-on-failure` exits 0
</verification>

<success_criteria>
- composer.json defines danplaton4/tenancy-bundle with correct deps
- PHPUnit 11 config exists with unit + integration suites
- TenancyBundle extends AbstractBundle with configure/loadExtension/build/prependExtension
- BootstrapperChainPass collects tenancy.bootstrapper tags via PriorityTaggedServiceTrait
- services.php registers tenancy.context, bootstrapper_chain, and orchestrator
- BootstrapperChainPass unit tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-01-SUMMARY.md`
</output>
