---
phase: 22-docs-refresh
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/examples/saas-subdomain.md
  - docs/examples/api-header.md
  - docs/examples/saas-demo.md
  - docs/user-guide/examples/saas-subdomain.md
  - docs/user-guide/examples/api-header.md
  - docs/user-guide/shared-db.md
  - docs/user-guide/index.md
  - docs/user-guide/database-per-tenant.md
autonomous: true
requirements:
  - DOC-19
tags:
  - docs
  - demo
  - examples-reorg

must_haves:
  truths:
    - "File `docs/examples/saas-subdomain.md` exists (moved from `docs/user-guide/examples/saas-subdomain.md`) (D-07)"
    - "File `docs/examples/api-header.md` exists (moved from `docs/user-guide/examples/api-header.md`) (D-07)"
    - "File `docs/examples/saas-demo.md` exists as a NEW thin walkthrough page (D-06)"
    - "Old paths `docs/user-guide/examples/saas-subdomain.md` and `docs/user-guide/examples/api-header.md` no longer exist (D-07)"
    - "The 4 cross-references in shared-db.md L178, user-guide/index.md L27+L28, database-per-tenant.md L260 are updated to the new `../examples/` paths (RESEARCH §Cross-link Map)"
    - "docs/examples/saas-demo.md is THIN — contains intro + 'what this demo proves' + a one-line install + a small ASCII teaser + a link to canonical `examples/saas/README.md` (D-08)"
    - "docs/examples/saas-demo.md does NOT duplicate the 250-line walkthrough from examples/saas/README.md (D-08)"
  artifacts:
    - path: "docs/examples/saas-subdomain.md"
      provides: "Moved-from-user-guide example (D-07)"
    - path: "docs/examples/api-header.md"
      provides: "Moved-from-user-guide example (D-07)"
    - path: "docs/examples/saas-demo.md"
      provides: "Thin walkthrough page pointing at canonical demo README (D-06/D-08)"
      contains: "examples/saas/README.md"
  key_links:
    - from: "docs/examples/saas-demo.md"
      to: "examples/saas/README.md (canonical Phase 21 demo doc)"
      via: "markdown link"
      pattern: "examples/saas/README\\.md"
    - from: "docs/user-guide/shared-db.md L178"
      to: "docs/examples/api-header.md (after move)"
      via: "markdown link"
      pattern: "\\.\\./examples/api-header\\.md"
    - from: "docs/user-guide/index.md L27-28"
      to: "docs/examples/saas-subdomain.md, docs/examples/api-header.md (after move)"
      via: "markdown links"
      pattern: "\\.\\./examples/"
    - from: "docs/user-guide/database-per-tenant.md L260"
      to: "docs/examples/saas-subdomain.md (after move)"
      via: "markdown link"
      pattern: "\\.\\./examples/saas-subdomain\\.md"
---

<objective>
The Examples section gets a structural reorganization (D-07): two existing example pages move from `docs/user-guide/examples/` up to a new top-level `docs/examples/` directory, and a new thin walkthrough page `docs/examples/saas-demo.md` is created (D-06/D-08). All four cross-references that target the old paths must be updated atomically in the same plan to avoid breaking `mkdocs build --strict` (RESEARCH Landmines #1).

Purpose: Implements D-06, D-07, D-08. Sets the stage for Plan 06's mkdocs nav reorganization (D-16).

Output: Files moved, cross-references updated, new thin walkthrough page exists.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-docs-refresh/22-CONTEXT.md
@.planning/phases/22-docs-refresh/22-RESEARCH.md
@.planning/phases/21-demo-app/21-CONTEXT.md

@docs/user-guide/examples/saas-subdomain.md
@docs/user-guide/examples/api-header.md
@docs/user-guide/shared-db.md
@docs/user-guide/index.md
@docs/user-guide/database-per-tenant.md
@examples/saas/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move existing examples to docs/examples/ (D-07)</name>
  <files>
    docs/examples/saas-subdomain.md,
    docs/examples/api-header.md,
    docs/user-guide/examples/saas-subdomain.md,
    docs/user-guide/examples/api-header.md
  </files>
  <read_first>
    - docs/user-guide/examples/saas-subdomain.md (full file — note current path and any internal cross-references)
    - docs/user-guide/examples/api-header.md (full file — note current path and any internal cross-references)
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"mkdocs Nav Diff" (file moves required list)
    - .planning/phases/22-docs-refresh/22-CONTEXT.md §decisions D-07
  </read_first>
  <action>
    Use `git mv` to move the two example files from `docs/user-guide/examples/` to `docs/examples/`. After the move, the empty directory `docs/user-guide/examples/` should be removed (git mv handles this if no other files remain there).

    Commands (canonical):

    ```bash
    mkdir -p docs/examples
    git mv docs/user-guide/examples/saas-subdomain.md docs/examples/saas-subdomain.md
    git mv docs/user-guide/examples/api-header.md docs/examples/api-header.md
    rmdir docs/user-guide/examples 2>/dev/null || true
    ```

    **DO NOT** edit the content of either file in this task — they move byte-for-byte. Content changes (if any are needed for internal cross-references INSIDE these files pointing to other docs) are at Claude's discretion if such references exist; verify with `grep -nE '\.md[)#]' docs/examples/saas-subdomain.md docs/examples/api-header.md` after the move. If internal links inside these moved files point to siblings via `./other-file.md` (now still siblings since they moved together), no change is needed. If they point to `../something.md` (going up to `docs/user-guide/`), those paths now break — update to `../user-guide/something.md`. Per RESEARCH §Cross-link Map, no internal links INSIDE these two files cross to other directories — they're self-contained.

    **Verification step inside this task** (executor MUST run after the move):

    ```bash
    test ! -f docs/user-guide/examples/saas-subdomain.md && \
      test ! -f docs/user-guide/examples/api-header.md && \
      test -f docs/examples/saas-subdomain.md && \
      test -f docs/examples/api-header.md && \
      echo "moves OK"
    ```

    If `docs/user-guide/examples/` directory still exists and is empty, remove it (`rmdir docs/user-guide/examples 2>/dev/null`). If it's not empty (an unexpected file is there), abort and report.
  </action>
  <verify>
    <automated>test -f docs/examples/saas-subdomain.md &amp;&amp; test -f docs/examples/api-header.md &amp;&amp; test ! -f docs/user-guide/examples/saas-subdomain.md &amp;&amp; test ! -f docs/user-guide/examples/api-header.md &amp;&amp; test ! -d docs/user-guide/examples &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/examples/saas-subdomain.md` exists
    - `docs/examples/api-header.md` exists
    - `docs/user-guide/examples/saas-subdomain.md` does NOT exist
    - `docs/user-guide/examples/api-header.md` does NOT exist
    - `docs/user-guide/examples/` directory does NOT exist (or is empty and removed)
    - `git status` shows the moves as renames (not delete+add)
    - File content is byte-identical to pre-move state (no internal edits introduced)
  </acceptance_criteria>
  <done>Both example files moved from `docs/user-guide/examples/` to `docs/examples/`; old directory cleaned up; git treats moves as renames.</done>
</task>

<task type="auto">
  <name>Task 2: Update 4 cross-references to new examples/ paths (RESEARCH §Cross-link Map)</name>
  <files>
    docs/user-guide/shared-db.md,
    docs/user-guide/index.md,
    docs/user-guide/database-per-tenant.md
  </files>
  <read_first>
    - docs/user-guide/shared-db.md L170-190 (verify L178 currently contains `[Examples: API Header](examples/api-header.md)`)
    - docs/user-guide/index.md L20-30 (verify L27-28 contain links to `examples/saas-subdomain.md` and `examples/api-header.md`)
    - docs/user-guide/database-per-tenant.md L255-265 (verify L260 contains `[Examples: SaaS Subdomain](examples/saas-subdomain.md)`)
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"Cross-link Map" (the canonical 4-line table)
  </read_first>
  <action>
    Per RESEARCH §"Cross-link Map", update exactly four markdown links:

    | File | Current link | New link |
    |------|--------------|----------|
    | `docs/user-guide/shared-db.md` L178 | `[Examples: API Header](examples/api-header.md)` | `[Examples: API Header](../examples/api-header.md)` |
    | `docs/user-guide/index.md` L27 | `[SaaS Subdomain](examples/saas-subdomain.md)` | `[SaaS Subdomain](../examples/saas-subdomain.md)` |
    | `docs/user-guide/index.md` L28 | `[API Header](examples/api-header.md)` | `[API Header](../examples/api-header.md)` |
    | `docs/user-guide/database-per-tenant.md` L260 | `[Examples: SaaS Subdomain](examples/saas-subdomain.md)` | `[Examples: SaaS Subdomain](../examples/saas-subdomain.md)` |

    Use exact-string replacement; do not introduce other edits. The link-target part changes from `examples/` to `../examples/` because the files moved up one directory level.

    **After all four edits, run the verification command from RESEARCH:**

    ```bash
    grep -rn "user-guide/examples\|(examples/saas-subdomain.md)\|(examples/api-header.md)" docs/
    ```

    Expected: ZERO matches. If any matches surface, additional cross-references exist that RESEARCH missed — report and fix.

    **Optional secondary check (Landmines #10):** if `docs/user-guide/index.md` has additional listed pages in a Features list (RESEARCH §Landmines #10 calls out lines 14-23), they MAY need new entries for the soon-to-be-added pages from Plan 03 (mailer-bootstrapper) and the existing-but-unwired pages (origin-header-resolver, profiler-tab). This is Plan 06's nav reorg work; do NOT add nav entries here. The link UPDATE is the only edit this task makes.
  </action>
  <verify>
    <automated>! grep -rnE '\(examples/saas-subdomain\.md\)|\(examples/api-header\.md\)|user-guide/examples/' docs/ &amp;&amp; grep -c '../examples/api-header.md' docs/user-guide/shared-db.md docs/user-guide/index.md 2&gt;/dev/null | awk -F: '{s+=$2} END{exit (s&gt;=2)?0:1}' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '\(examples/saas-subdomain\.md\)|\(examples/api-header\.md\)' docs/` returns ZERO matches (old paths removed)
    - `grep -rnE 'user-guide/examples/' docs/` returns ZERO matches
    - `grep -c '\.\./examples/saas-subdomain\.md' docs/user-guide/index.md docs/user-guide/database-per-tenant.md` returns `≥ 2`
    - `grep -c '\.\./examples/api-header\.md' docs/user-guide/shared-db.md docs/user-guide/index.md` returns `≥ 2`
    - No other prose edits leaked into these three files (verify diff size: each file should show exactly 1-2 line modifications)
  </acceptance_criteria>
  <done>All four cross-references updated to `../examples/` paths; grep verification returns zero matches for old paths.</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/examples/saas-demo.md — thin walkthrough (D-06/D-08)</name>
  <files>docs/examples/saas-demo.md</files>
  <read_first>
    - examples/saas/README.md (full file, 181 lines — the canonical demo walkthrough; the new page must POINT AT this, not duplicate it)
    - .planning/phases/22-docs-refresh/22-CONTEXT.md §decisions D-06 (location: NEW top-level dir) and D-08 (thin page contents)
    - .planning/phases/21-demo-app/21-CONTEXT.md (demo scope + three-step fallback ladder context)
  </read_first>
  <action>
    Create a new file at `docs/examples/saas-demo.md`. This is a THIN page (D-08) — the canonical demo doc is `examples/saas/README.md` and must NOT be duplicated.

    **Required structural elements** (Claude has discretion on exact prose; non-negotiable inclusions below):

    1. **H1: `# SaaS Demo (runnable)`** — or similar. Title should communicate "this is a runnable demo, not a tutorial."

    2. **Intro paragraph** (2-3 sentences): What this demo is — a Symfony 7.4 skeleton in `examples/saas/` that boots two tenants (`tenant1.tenancy.localhost`, `tenant2.tenancy.localhost`) on Caddy + FrankenPHP + MariaDB via a single `docker compose up`. Mention that `bin/smoke.sh` exercises it from CI on every push.

    3. **H2: `## What this demo proves`** — bullet list (~4-6 bullets) summarizing what a reader sees by running it:
       - Two tenant subdomains serve isolated content out of the box
       - The Web Debug Toolbar shows the resolved tenant per request (cross-link to `../user-guide/profiler-tab.md`)
       - Per-tenant SMTP via Mailpit (or similar — verify against current demo) demonstrates the Mailer bootstrapper (cross-link to `../user-guide/mailer-bootstrapper.md`)
       - The `bin/smoke.sh` script gates CI — failure blocks merge

    4. **H2: `## Quick start`** — a one-line shell snippet:
       ```bash
       git clone https://github.com/danplaton4/tenancy-bundle && cd tenancy-bundle/examples/saas && docker compose up
       ```
       Plus a one-sentence pointer for the three-step fallback ladder (when `*.tenancy.localhost` isn't resolving — curl with `Host:` header → `/etc/hosts` → browser-native).

    5. **H2: `## Small ASCII teaser`** — a tiny ASCII block (~5-8 lines) showing what the user sees in a browser tab. Suggestion: a stylized two-column "Tenant 1" / "Tenant 2" comparison showing they're serving different content. Keep this small — it's a teaser, not a tutorial.

    6. **H2: `## Full walkthrough`** — one paragraph + a prominent link to the canonical file:
       - Use the link `[examples/saas/README.md](../../examples/saas/README.md)` (relative from `docs/examples/` → up two levels to repo root → `examples/saas/README.md`)
       - State that the canonical doc covers the two-minute boot, three-step fallback ladder, Profiler walkthrough, Mailpit walkthrough.

    7. **Optional H2: `## See also`** — cross-links to `../user-guide/installation.md`, `../user-guide/profiler-tab.md`, `../user-guide/mailer-bootstrapper.md`.

    **Length target**: 40-80 lines. If it grows beyond 100, the page is no longer thin — trim back.

    **Constraint check**: The page MUST NOT include the 3-step fallback ladder in full, MUST NOT include the Mailpit walkthrough in full, MUST NOT include the smoke.sh contents — those all live in `examples/saas/README.md`. This page POINTS at them with a one-line reference each.

    **mkdocs strict-build implication**: All internal links (`../user-guide/profiler-tab.md`, `../user-guide/mailer-bootstrapper.md`, `../user-guide/installation.md`) must resolve. profiler-tab.md and installation.md exist today; mailer-bootstrapper.md is created by Plan 03 (same Wave 2). Since all Wave 2 plans land before any CI build, the strict-build check passes at Plan 06's final wave.

    **Link to canonical demo README**: the path `../../examples/saas/README.md` is a relative path from `docs/examples/saas-demo.md` → up two levels → `examples/saas/README.md`. mkdocs supports linking to repo files outside `docs/` only if they have a proper anchor; cleanest pattern is to link to the file from a sentence like "see the [demo README](../../examples/saas/README.md) for the full walkthrough." Verify mkdocs strict mode tolerates this by reading `.github/workflows/docs.yml` — RESEARCH §Landmines #1 confirms strict mode runs and warns about broken NAV references; cross-tree file links typically just render as HTML anchors that 404 on the published site but are valid in repo browsers. Acceptable trade-off per D-08.

    Alternative if strict-build complains: use the GitHub URL `https://github.com/danplaton4/tenancy-bundle/blob/master/examples/saas/README.md`. Claude discretion on which path to use; the relative path is preferred for repo-browsers, the GitHub URL works for docs-site users.
  </action>
  <verify>
    <automated>test -f docs/examples/saas-demo.md &amp;&amp; grep -qE '^# SaaS Demo' docs/examples/saas-demo.md &amp;&amp; grep -q 'examples/saas/README.md' docs/examples/saas-demo.md &amp;&amp; grep -q 'docker compose up' docs/examples/saas-demo.md &amp;&amp; test "$(wc -l &lt; docs/examples/saas-demo.md)" -ge 30 &amp;&amp; test "$(wc -l &lt; docs/examples/saas-demo.md)" -le 100 &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/examples/saas-demo.md` exists
    - First line starts with `# SaaS Demo` (case-insensitive on the descriptive part)
    - File contains a link to `examples/saas/README.md` (relative or absolute URL)
    - File contains `docker compose up`
    - File length is between 30 and 100 lines (thin per D-08)
    - File does NOT contain the literal phrase `bin/smoke.sh` walkthrough body — only a one-line reference (verify with `grep -c 'bin/smoke.sh' docs/examples/saas-demo.md` returns 0 or 1, NOT 5+)
    - File does NOT duplicate large blocks from `examples/saas/README.md` (cosine-similar check is manual; the wc -l upper bound of 100 enforces brevity)
  </acceptance_criteria>
  <done>New thin walkthrough page exists at `docs/examples/saas-demo.md` (~30-100 lines), links to canonical `examples/saas/README.md`, and does not duplicate the canonical walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mkdocs build → CI gate | A broken cross-link triggers `mkdocs build --strict` failure on master push. |
| Docs reader → demo onboarding | Misleading "thin" page that duplicates content creates drift maintenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Tampering (drift) | Files move but cross-references in 4 other docs files are not updated → broken links → CI fail | mitigate | Tasks 1 and 2 land in the same plan (same commit) — the moves and the cross-ref updates are atomic. RESEARCH §Cross-link Map enumerates all 4 references. |
| T-22-09 | Tampering | New thin page duplicates content from canonical README → drift over time | mitigate | D-08 caps the page at "thin"; acceptance criteria enforce wc -l upper bound and ensure key sections (smoke.sh body, fallback ladder body) are NOT duplicated. |

**Severity overall: LOW.** Documentation reorganization; no code paths touched.
</threat_model>

<verification>
After all three tasks:
1. `git status` shows two renames (saas-subdomain.md, api-header.md) + one new file (saas-demo.md) + three modified files (shared-db.md, user-guide/index.md, database-per-tenant.md)
2. `grep -rnE '\(examples/saas-subdomain\.md\)|\(examples/api-header\.md\)|user-guide/examples/' docs/` returns ZERO matches
3. `find docs/examples/ -name '*.md'` lists exactly 3 files: saas-subdomain.md, api-header.md, saas-demo.md
4. `find docs/user-guide/examples/ -name '*.md' 2>/dev/null` returns nothing
5. Plan 06 will run `mkdocs build --strict` and that's the integration smoke — it MUST pass after Plan 06's nav reorg picks up these moves
</verification>

<success_criteria>
SC3 (`docs/examples/saas-demo.md` walks through the Phase 21 demo end-to-end) is satisfied by Task 3 — the page is thin and walks the user from "what is this" to the canonical README. D-06, D-07, D-08 all closed.

Plan is complete when:
- Two example files moved (Task 1)
- Four cross-references updated (Task 2)
- New saas-demo.md created (Task 3)
- All grep gates pass
</success_criteria>

<output>
Create `.planning/phases/22-docs-refresh/22-04-SUMMARY.md` when done. Include:
- Confirmation that git treats the two moves as renames (not delete+add)
- The 4 cross-references updated, with line numbers
- Final line count of saas-demo.md
- Note any non-D-07 cross-references found during the grep verification (RESEARCH §Cross-link Map may have missed some)
</output>
</content>
</invoke>