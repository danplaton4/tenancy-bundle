---
phase: 22-docs-refresh
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user-guide/getting-started.md
  - docs/user-guide/configuration.md
  - docs/user-guide/resolvers.md
  - docs/user-guide/cli-commands.md
autonomous: true
requirements:
  - DOC-19
tags:
  - docs
  - yellow-page-refresh

must_haves:
  truths:
    - "docs/user-guide/getting-started.md has three new subsections teasing Origin / Profiler / Mailer with cross-links to the dedicated pages (D-17)"
    - "docs/user-guide/getting-started.md preserves its current install-and-config flow at L25-218 (D-17 'do NOT rewrite the rest')"
    - "docs/user-guide/configuration.md has a new H3 `### tenancy.origin.allow_list` entry mirroring the syntax from `origin-header-resolver.md` (D-18)"
    - "docs/user-guide/configuration.md resolvers table now lists 5 aliases (origin row added with priority 25) (D-18)"
    - "docs/user-guide/configuration.md has a new H3 `### Per-tenant mailer config` subsection documenting `mailerDsn`, `mailerFrom`, `mailerReplyTo`, and `TenantMailerConfigTrait` (D-18)"
    - "docs/user-guide/resolvers.md L2 intro says 'five resolvers' (not 'four') (D-19, Research Open Q4)"
    - "docs/user-guide/resolvers.md Resolver Priority Table contains a new row for OriginHeaderResolver at priority 25 (D-19)"
    - "docs/user-guide/resolvers.md has a new `## OriginHeaderResolver` H2 section between the HostResolver and HeaderResolver sections, with cross-link to `origin-header-resolver.md#trust-model` (D-19)"
    - "docs/user-guide/cli-commands.md has a new top-level `## tenancy:install` H2 section as the headline command (D-20)"
    - "docs/user-guide/cli-commands.md `tenancy:init` is demoted to a sub-section under tenancy:install OR remains H2 with a clear cross-reference (D-20 — implementation may choose)"
    - "docs/user-guide/cli-commands.md `tenancy:migrate` and `tenancy:run` sections are NOT touched (D-20)"
    - "Anchor slug `tenancy-install` for the new H2 exists for cross-links from installation.md (Plan 01) and getting-started.md (this plan)"
  artifacts:
    - path: "docs/user-guide/getting-started.md"
      provides: "Getting Started with v0.3 feature teasers"
      contains: "origin-header-resolver.md"
    - path: "docs/user-guide/configuration.md"
      provides: "Configuration reference with origin.allow_list + per-tenant mailer notes"
      contains: "tenancy.origin.allow_list"
    - path: "docs/user-guide/resolvers.md"
      provides: "Resolvers guide with OriginHeaderResolver as 5th built-in"
      contains: "five resolvers"
    - path: "docs/user-guide/cli-commands.md"
      provides: "CLI commands page with tenancy:install at the top"
      contains: "## tenancy:install"
  key_links:
    - from: "docs/user-guide/getting-started.md"
      to: "docs/user-guide/origin-header-resolver.md, profiler-tab.md, mailer-bootstrapper.md"
      via: "markdown links in teaser subsections"
      pattern: "(origin-header-resolver|profiler-tab|mailer-bootstrapper)\\.md"
    - from: "docs/user-guide/configuration.md"
      to: "docs/user-guide/mailer-bootstrapper.md (Plan 03)"
      via: "cross-link in mailer config subsection"
      pattern: "mailer-bootstrapper\\.md"
    - from: "docs/user-guide/resolvers.md"
      to: "docs/user-guide/origin-header-resolver.md#trust-model (existing anchor verified)"
      via: "cross-link"
      pattern: "origin-header-resolver\\.md#trust-model"
---

<objective>
Four existing User Guide pages that predate v0.3's surface get surgical additions (not rewrites) so v0.3.3 docs are complete. Per CONTEXT D-17/D-18/D-19/D-20:

- `getting-started.md` — three short teaser subsections pointing at the new pages
- `configuration.md` — new `### tenancy.origin.allow_list` H3 + 5th resolver row in the resolvers table + new `### Per-tenant mailer config` H3
- `resolvers.md` — intro count 4→5, new priority-table row, new `## OriginHeaderResolver` H2
- `cli-commands.md` — promote `tenancy:install` to the top; do NOT touch `tenancy:migrate` / `tenancy:run`

Purpose: Implements D-17/D-18/D-19/D-20 + Research Open Q4 (resolver count framing matches README's 5). Anchors `cli-commands.md#tenancy-install` and `configuration.md` mailer subsection that Plan 01 and Plan 03 reference.

Output: Four files surgically augmented; no rewrites; install/config flow preserved.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-docs-refresh/22-CONTEXT.md
@.planning/phases/22-docs-refresh/22-RESEARCH.md

@docs/user-guide/getting-started.md
@docs/user-guide/configuration.md
@docs/user-guide/resolvers.md
@docs/user-guide/cli-commands.md
@docs/user-guide/origin-header-resolver.md
@README.md
@src/Mailer/TenantMailerConfigTrait.php
@src/DependencyInjection/Compiler/MailerTransportContractPass.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three v0.3 teaser subsections to docs/user-guide/getting-started.md (D-17)</name>
  <files>docs/user-guide/getting-started.md</files>
  <read_first>
    - docs/user-guide/getting-started.md L220-264 (the `## What Happens on Every Request` section L220+ and `## Next Steps` section at L258 — confirms insertion point per RESEARCH §"File Shape Survey #10")
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"File Shape Survey #10" (insertion point details)
    - .planning/phases/22-docs-refresh/22-CONTEXT.md §decisions D-17 (3 subsections required)
  </read_first>
  <action>
    Add a new H2 section titled `## Beyond the basics` (Claude discretion on the exact heading; alternatives like `## v0.3 features` or `## What else can you do?` are acceptable but `## Beyond the basics` reads cleanly).

    **Insertion point** (per RESEARCH §"File Shape Survey #10"): between line 254 (the closing ``` ``` of the kernel-flow code block in the `## What Happens on Every Request` section) and line 256 (the `---` separator before `## Next Steps`).

    The new H2 contains three short subsections, each ~2 paragraphs:

    1. **`### Resolving tenants from SPA Origin headers`** — 2-paragraph teaser. First paragraph: what OriginHeaderResolver does (browser-locked cross-origin tenant identification, sits in the resolver chain at priority 25 above HeaderResolver/20 and below HostResolver/30). Second paragraph: why it exists (SPAs running cross-origin to the tenant API can't reliably send custom headers due to CORS preflight; the browser-protected `Origin` header is the fallback). End with: `Full guide → [Origin Header Resolver](origin-header-resolver.md)`.

    2. **`### Inspecting the active tenant in dev`** — 1-paragraph teaser. The Symfony Profiler ships a "Tenancy" panel showing the active tenant slug, label, driver, connection, resolved-by FQCN, and bootstrappers run. Renders cleanly in three states (resolved / no-tenant / error). Dev-only — compile-out of the prod container is verified by CI. End with: `Full guide → [Profiler Tab](profiler-tab.md)`.

    3. **`### Per-tenant mailer config`** — 2-paragraph teaser. First paragraph: each tenant can have its own SMTP DSN, From, and Reply-To via the `mailerDsn` / `mailerFrom` / `mailerReplyTo` columns on the Tenant entity, or the `TenantMailerConfigTrait` shortcut. Second paragraph: the `X-Transport` strategy ensures correct dispatch under both synchronous and Messenger-routed async Mailer. Critical operational note: a message dequeued for a deleted tenant must throw (not silently drop) — `MailerTransportContractPass` enforces this contract at container compile time. End with: `Full guide → [Mailer Bootstrapper](mailer-bootstrapper.md)`.

    **Preservation rules per D-17:** Do NOT rewrite the existing install-and-config flow at L25-218. Do NOT touch the `## Prerequisites` section, the `## Choose Your Driver` section, `## Path A: Database-per-Tenant`, `## Path B: Shared-DB`, or `## What Happens on Every Request` body. Only ADD the new H2 between the request-flow section's end and the existing `## Next Steps` section.

    **mkdocs strict-build implication:** Cross-links to `origin-header-resolver.md` (exists), `profiler-tab.md` (exists), `mailer-bootstrapper.md` (created by Plan 03 in same wave) — all three resolve at CI run time after the full phase commit lands.
  </action>
  <verify>
    <automated>grep -q '^## Beyond the basics\|^## v0.3 features\|^## What else can you do' docs/user-guide/getting-started.md &amp;&amp; grep -q 'origin-header-resolver.md' docs/user-guide/getting-started.md &amp;&amp; grep -q 'profiler-tab.md' docs/user-guide/getting-started.md &amp;&amp; grep -q 'mailer-bootstrapper.md' docs/user-guide/getting-started.md &amp;&amp; grep -q '^## Next Steps' docs/user-guide/getting-started.md &amp;&amp; grep -q '^## What Happens on Every Request' docs/user-guide/getting-started.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains a new H2 (`## Beyond the basics` or discretionary equivalent)
    - File contains all three cross-link references: `origin-header-resolver.md`, `profiler-tab.md`, `mailer-bootstrapper.md` (each appears `≥ 1` time)
    - Existing H2 sections preserved: `## Prerequisites`, `## Choose Your Driver`, `## Path A`, `## Path B`, `## What Happens on Every Request`, `## Next Steps` all still present (`grep -c '^## ' docs/user-guide/getting-started.md` returns `≥ 7`)
    - The three new H3 subsections are present under the new H2 (`grep -c '^### ' docs/user-guide/getting-started.md` returns ≥ 3 more than before; baseline can be eyeballed from initial read)
    - File length grew by ~30-60 lines (3 subsections × ~10-20 lines each)
  </acceptance_criteria>
  <done>Getting Started has a new "Beyond the basics" H2 with three teaser subsections linking to the dedicated v0.3 feature pages. Existing install-and-config flow untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add origin.allow_list + per-tenant mailer config to docs/user-guide/configuration.md (D-18)</name>
  <files>docs/user-guide/configuration.md</files>
  <read_first>
    - docs/user-guide/configuration.md L100-130 (verifies the resolvers table at L102-110 and the `### tenancy.host.app_domain` block ending at L127 — insertion point per RESEARCH §"File Shape Survey #11")
    - docs/user-guide/origin-header-resolver.md L19-35 (canonical `tenancy.origin.allow_list` YAML shape to mirror exactly — drift between the two pages would create confusion)
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"File Shape Survey #11" (the 3 D-18 changes mapped to line ranges)
    - .planning/phases/22-docs-refresh/22-CONTEXT.md §decisions D-18
    - src/Mailer/TenantMailerConfigTrait.php (full file — confirms `mailerDsn`, `mailerFrom`, `mailerReplyTo` column names exactly)
  </read_first>
  <action>
    Three surgical edits to `docs/user-guide/configuration.md`:

    **Edit 1: Insert new `### tenancy.origin.allow_list` H3** between line 127 (end of `### tenancy.host.app_domain` block) and line 129 (`## Validation Rules` H2). This keeps all H3 config-key entries grouped together under `## Config Keys`.

    Content of the new H3:
    - 1-2 paragraph intro: what `origin.allow_list` does (list of allow-listed origin URLs, optionally with one left-most wildcard label per entry).
    - A fenced YAML code block showing the canonical configuration syntax — **mirror exactly** the YAML from `docs/user-guide/origin-header-resolver.md` L19-35. Do not invent new syntax. If origin-header-resolver.md uses a mixed map-form + wildcard-shorthand, mirror that.
    - A 1-line note: cross-link to `origin-header-resolver.md` for the full Trust Model explanation.

    **Edit 2: Add a 5th row to the resolvers table at L102-110.**

    Current table lists 4 aliases (host/header/query_param/console). Add a new row for `origin` with priority `25`. The row's column values must align with the existing column headers (read the table to confirm column shapes — RESEARCH didn't capture every column).

    **Edit 3: Add a new H3 `### Per-tenant mailer config`** in `## Config Keys`. Placement: immediately after the new `### tenancy.origin.allow_list` H3 from Edit 1. (Both new H3s sit between `### tenancy.host.app_domain` and `## Validation Rules`.)

    Content of this H3:
    - 1-paragraph intro: each tenant can carry its own SMTP DSN, From, and Reply-To via three nullable Tenant entity columns: `mailerDsn`, `mailerFrom`, `mailerReplyTo`.
    - Mention `TenantMailerConfigTrait` as the shortcut — `use TenantMailerConfigTrait;` in a custom Tenant entity supplies all three.
    - Mention `tenancy.mailer.async` config key (accepts `'auto' | 'true' | 'false'`, per `MailerTransportContractPass.php` L36-37 reference — verify by reading the file).
    - 1-sentence cross-link: `For the full X-Transport strategy and async failure-mode warning, see the [Mailer Bootstrapper guide](mailer-bootstrapper.md).`
    - Optionally include a tiny Doctrine-attribute snippet showing the three columns. Mark Doctrine as optional in a code comment (matches `installation.md` convention).

    **Preservation rules per D-18:** Do NOT touch any of the existing H3 config-key entries (`### tenancy.driver`, `### tenancy.strict_mode`, etc.). Do NOT touch `## Validation Rules`, `## Full Example`, `## Minimal Examples`.

    **YAML mirror check:** after editing, manually diff your new `### tenancy.origin.allow_list` YAML against the source in `origin-header-resolver.md` — they should be byte-identical (modulo the surrounding prose). Drift between the two pages = bug.
  </action>
  <verify>
    <automated>grep -q '^### tenancy\.origin\.allow_list' docs/user-guide/configuration.md &amp;&amp; grep -q '^### Per-tenant mailer config\|^### tenancy\.mailer' docs/user-guide/configuration.md &amp;&amp; grep -qE '\borigin\b.*\b25\b|\b25\b.*\borigin\b' docs/user-guide/configuration.md &amp;&amp; grep -q 'mailerDsn' docs/user-guide/configuration.md &amp;&amp; grep -q 'TenantMailerConfigTrait' docs/user-guide/configuration.md &amp;&amp; grep -q 'mailer-bootstrapper.md' docs/user-guide/configuration.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - New H3 `### tenancy.origin.allow_list` exists
    - New H3 for per-tenant mailer config exists (either `### Per-tenant mailer config` or `### tenancy.mailer` — Claude discretion on H3 title)
    - Resolvers table mentions both `origin` and `25` on the same line (priority value next to alias)
    - File contains `mailerDsn`, `TenantMailerConfigTrait`, and a link to `mailer-bootstrapper.md`
    - YAML snippet for `origin.allow_list` is byte-identical (modulo surrounding markdown) to the source in `origin-header-resolver.md` — manual diff confirms
    - Existing H2 structure preserved: `## Config Keys`, `## Validation Rules`, `## Full Example`, `## Minimal Examples` all present
    - File length grew by ~30-50 lines
  </acceptance_criteria>
  <done>Configuration reference has the 3 D-18 additions: origin allow-list H3, 5th resolver row, per-tenant mailer config H3. YAML mirrors origin-header-resolver.md to avoid drift.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/user-guide/resolvers.md — 4→5 resolvers + new OriginHeaderResolver section (D-19)</name>
  <files>docs/user-guide/resolvers.md</files>
  <read_first>
    - docs/user-guide/resolvers.md L1-30 (verifies L2 intro "four resolvers" and the Resolver Priority Table at L11-22)
    - docs/user-guide/resolvers.md L60-70 (verifies the boundary between `## HostResolver` and `## HeaderResolver` — insertion point per RESEARCH §"File Shape Survey #12")
    - docs/user-guide/origin-header-resolver.md (full file, 151 lines — knows the Trust Model anchor `#trust-model` at L52)
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"File Shape Survey #12" and §"Open Questions #4"
    - README.md L100-110 (verify the "5 built-in resolvers" framing per Research Open Q4)
    - .planning/phases/22-docs-refresh/22-CONTEXT.md §decisions D-19
  </read_first>
  <action>
    Three surgical edits per RESEARCH §"File Shape Survey #12":

    **Edit 1: Intro count update (L2 area).** Currently L2 reads (approximately): "The bundle ships with **four resolvers** and supports unlimited custom resolvers." Change `**four resolvers**` → `**five resolvers**`.

    Per Research Open Q4: the README counts 5 (including Origin). ConsoleResolver participates separately (operates independently from the HTTP chain — see L24 in this file). The cleanest framing matches the README: "five built-in HTTP resolvers" (Host, Origin, Header, QueryParam, Console). If the existing L2 phrasing differs slightly, just bump the count word — do not rewrite the sentence.

    **Edit 2: Resolver Priority Table at L14-22 — insert OriginHeaderResolver row.**

    Insert between the existing `HostResolver` row (currently L17, priority 30) and the `HeaderResolver` row (currently L18, priority 20). New row content (per RESEARCH):

    `| OriginHeaderResolver | 25 | Header: Origin (browser-locked) | tenancy.origin.allow_list |`

    Verify the column layout matches the existing table headers — if the actual table has 4 columns with different names, adapt the cell content but keep the priority value at 25 and the resolver name correct.

    **Edit 3: New `## OriginHeaderResolver` H2 section** between line 64 (end of `## HostResolver`'s "When app_domain is null" subsection) and line 66 (start of `## HeaderResolver`).

    Content (per CONTEXT D-19):
    - 1-paragraph intro: priority 25, sits between HostResolver and HeaderResolver in the chain, reads the `Origin` HTTP header.
    - 1-paragraph trust model summary (do NOT duplicate the full Trust Model content from `origin-header-resolver.md`): the browser locks the `Origin` header for cross-origin XHR — it's spoofable from non-browser clients (curl, server-to-server), so use it as a SECONDARY signal in trust-sensitive scenarios.
    - 1-sentence allow-list requirement: every origin must be allow-listed in `tenancy.origin.allow_list`; container compilation fails for empty allow-lists or mid-string wildcards (via `OriginHeaderResolverConfigPass`).
    - Cross-link: `[Full Trust Model →](origin-header-resolver.md#trust-model)`. The anchor `#trust-model` is verified to exist at L52 of `origin-header-resolver.md`.

    **Preservation rules per D-19:** "Do NOT duplicate the full Trust Model content from the dedicated page." Keep this section short (~15-25 lines). Do NOT touch `## HostResolver`, `## HeaderResolver`, `## QueryParamResolver`, `## ConsoleResolver`, or any other section.
  </action>
  <verify>
    <automated>grep -q 'five resolvers\|five built-in' docs/user-guide/resolvers.md &amp;&amp; ! grep -q '\bfour resolvers\b' docs/user-guide/resolvers.md &amp;&amp; grep -q '^## OriginHeaderResolver' docs/user-guide/resolvers.md &amp;&amp; grep -qE '\| OriginHeaderResolver \| 25 \|' docs/user-guide/resolvers.md &amp;&amp; grep -q 'origin-header-resolver.md#trust-model' docs/user-guide/resolvers.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains `five resolvers` (or `five built-in`) and does NOT contain `four resolvers` anywhere
    - File has a new `## OriginHeaderResolver` H2 between HostResolver and HeaderResolver (verify with `awk '/^## (HostResolver|OriginHeaderResolver|HeaderResolver)/' docs/user-guide/resolvers.md` outputs the three in that order)
    - Resolver Priority Table row for OriginHeaderResolver with priority 25 (`grep -E '\| OriginHeaderResolver \| 25 \|' docs/user-guide/resolvers.md` matches)
    - Cross-link to `origin-header-resolver.md#trust-model` present
    - Existing sections preserved: `grep -c '^## ' docs/user-guide/resolvers.md` returns `≥ 8` (was 9 → 10 with the new H2; depends on file's exact structure)
    - The new OriginHeaderResolver section is short (~15-25 lines) — does NOT contain the full Trust Model content
  </acceptance_criteria>
  <done>Resolvers page lists 5 resolvers, has a new priority-table row at 25, and has a new H2 section for OriginHeaderResolver that summarizes (not duplicates) the Trust Model.</done>
</task>

<task type="auto">
  <name>Task 4: Promote tenancy:install in docs/user-guide/cli-commands.md (D-20)</name>
  <files>docs/user-guide/cli-commands.md</files>
  <read_first>
    - docs/user-guide/cli-commands.md (full file, 191 lines — verifies the H2 structure: `## tenancy:init` (L7), `## tenancy:migrate` (L74), `## tenancy:run` (L135), `## See Also` (L185))
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"File Shape Survey #13"
    - .planning/phases/22-docs-refresh/22-CONTEXT.md §decisions D-20
    - .planning/phases/18-tenancy-install/18-CONTEXT.md (Phase 18 install command behavior — informs the new section's prose)
  </read_first>
  <action>
    Two main edits to `docs/user-guide/cli-commands.md`:

    **Edit 1: Update intro at L3-5.** Currently the intro mentions "three console commands: `tenancy:init` for scaffolding configuration, `tenancy:migrate` for ... and `tenancy:run` ...". Rewrite to mention four commands (or use "the one-shot setup command plus three subcommands" framing per RESEARCH suggestion). The headline command in the new framing is `tenancy:install`.

    **Edit 2: Insert new `## tenancy:install` H2 section** between L5 (end of intro) and L7 (current `## tenancy:init` H2).

    The H2 heading MUST be `## tenancy:install` (exact text, no trailing punctuation, no parens) — this generates the mkdocs anchor `tenancy-install` that Plan 01 (installation.md) and Plan 03 (mailer-bootstrapper.md) link to.

    Content of the new section:
    - 1-paragraph intro: what `tenancy:install` does (one-command setup — auto-registers `TenancyBundle::class` in `config/bundles.php` via AST detection + invokes `tenancy:init` programmatically + prints next-step guidance).
    - **`### Usage`** subsection with a fenced bash code block:
      ```bash
      bin/console tenancy:install
      bin/console tenancy:install --dry-run
      bin/console tenancy:install --force
      ```
    - **`### Flags`** subsection (or inline if shorter):
      - `--dry-run`: prints the proposed mutation without writing
      - `--force`: forwards `--force` to the delegated `tenancy:init` call (overwrites existing `config/packages/tenancy.yaml`)
    - **`### Behavior on non-standard bundles.php`** subsection: per DEC-INST-02 (which is reversed for the *nikic dep* but the install command's refusal-to-mutate behavior is unchanged), if the command detects a non-standard shape (DDD `registerBundles()` override, env-conditional loading), it prints a manual snippet and exits 0 — never produces an invalid `bundles.php`. Reference the `.bak` + atomic write + `php -l` safety mechanism briefly.
    - **`### Idempotency`** subsection: re-running on an already-installed project exits 0 with an informational message; no file mutation.
    - Optional: cross-link to `installation.md` for the install-flow context.

    **Edit 3 (per D-20 — demote tenancy:init):** Currently `## tenancy:init` is at L7 as a peer H2. Two options:
    - **Option A (lighter touch):** keep `tenancy:init` as a peer H2 (`## tenancy:init`) — just leave it where it is. The intro at L3-5 frames `tenancy:install` as the headline, and the new H2 sits before it. `tenancy:init` has standalone uses (regenerating tenancy.yaml without re-registering the bundle) per CONTEXT D-20, so peer-H2 is defensible.
    - **Option B (literal D-20):** demote `## tenancy:init` to `### tenancy:init` and nest it under a "Manual config scaffold" H3 (or sub-section) inside the new `## tenancy:install` H2. This is what D-20 literally describes.

    **Claude discretion** between Option A and Option B. Option A is simpler and preserves anchor links; Option B is more literal. If the file structure is straightforward, lean toward Option A and document the choice in the SUMMARY.

    **Preservation rules per D-20:** Do NOT touch `## tenancy:migrate` (L74-133) or `## tenancy:run` (L135-183). The `## See Also` section at L185 stays.

    **mkdocs strict-build implication:** The H2 `## tenancy:install` generates the canonical mkdocs Material slug `tenancy-install` (the colon is stripped and replaced with a hyphen). Plan 01 (installation.md Task 2) and Plan 03 (mailer-bootstrapper.md See-also block) both link to `cli-commands.md#tenancy-install` — those references resolve only if this H2 emits exactly that slug. Plan 06 will run `mkdocs build --strict` against the full nav after Wave 1 lands, which catches any mismatch immediately. Do NOT introduce an `<a name="tenancyinstall"></a>` fallback — the canonical slug is the contract; any deviation should be flagged at strict-build time, not papered over.
  </action>
  <verify>
    <automated>grep -q '^## tenancy:install' docs/user-guide/cli-commands.md &amp;&amp; grep -q '^## tenancy:migrate' docs/user-guide/cli-commands.md &amp;&amp; grep -q '^## tenancy:run' docs/user-guide/cli-commands.md &amp;&amp; grep -q 'dry-run\|--dry-run' docs/user-guide/cli-commands.md &amp;&amp; grep -q -- '--force' docs/user-guide/cli-commands.md &amp;&amp; grep -qE 'four commands|one-shot setup' docs/user-guide/cli-commands.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - New `## tenancy:install` H2 exists (verify with `grep -c '^## tenancy:install$' docs/user-guide/cli-commands.md` returns `1`)
    - The new H2 comes BEFORE `## tenancy:init` (or `## tenancy:init` is demoted to an H3 under it — verify with `awk '/^## /' docs/user-guide/cli-commands.md` order)
    - `tenancy:migrate` and `tenancy:run` sections are NOT modified (`grep -c '^## tenancy:migrate$' docs/user-guide/cli-commands.md` returns `1` AND `grep -c '^## tenancy:run$' docs/user-guide/cli-commands.md` returns `1`)
    - File mentions `--dry-run` and `--force` flags
    - Intro mentions "four commands" or "one-shot setup" framing
    - File length grew by ~40-80 lines (the new section)
  </acceptance_criteria>
  <done>CLI commands page has `tenancy:install` as the headline section with flag documentation. Migrate and Run sections are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs reader → configuration practice | Inaccurate YAML in configuration.md leads to misconfigured production deployments. |
| Cross-page links → mkdocs strict-build | Broken anchor slugs fail CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Tampering (drift) | `tenancy.origin.allow_list` YAML in configuration.md differs from origin-header-resolver.md → users get inconsistent guidance | mitigate | Task 2 mandates byte-identical mirror; acceptance criteria require manual diff verification. |
| T-22-11 | Tampering | Anchor slug `tenancy-install` mismatch between linkers (Plan 01, Plan 03) and the H2 in cli-commands.md | mitigate | Standard mkdocs Material slug rules apply. Plan 06's `mkdocs build --strict` smoke run catches any mismatch before commit. |
| T-22-12 | Information disclosure | Mailer config section could include real-looking DSN strings with credentials | mitigate | DSN examples use sanitized placeholders (e.g., `smtp://user:****@smtp.example.com:587`) — no real secrets. |

**Severity overall: LOW.** Surgical doc edits; no code paths touched.
</threat_model>

<verification>
After all four tasks:
1. All four files modified; no other files touched
2. All required grep patterns pass per task
3. Cross-link integrity: `grep -rE 'origin-header-resolver\.md|profiler-tab\.md|mailer-bootstrapper\.md' docs/user-guide/{getting-started,configuration,resolvers,cli-commands}.md` should show at least one match per file
4. Plan 06 will run `mkdocs build --strict` and catch any anchor slug mismatches
</verification>

<success_criteria>
SC2 (new pages exist with the right content) — yellow page refresh complements the new pages by ensuring v0.3 features are discoverable from the established User Guide entry points. D-17, D-18, D-19, D-20 all closed. Research Open Q4 closed.

Plan is complete when:
- getting-started.md has the three teaser subsections
- configuration.md has origin allow-list H3 + 5th resolver row + per-tenant mailer H3
- resolvers.md is updated to 5 resolvers + new H2 + table row
- cli-commands.md has tenancy:install at the top; migrate/run untouched
- All grep gates pass
</success_criteria>

<output>
Create `.planning/phases/22-docs-refresh/22-05-SUMMARY.md` when done. Include:
- Lines added per file (rough count)
- Confirmation of YAML mirror integrity (configuration.md vs origin-header-resolver.md)
- Choice made on Option A vs Option B for cli-commands.md tenancy:init demotion (D-20)
- Confirmation that `tenancy:migrate` and `tenancy:run` sections are byte-identical to pre-edit state
- Any unexpected cross-references discovered (none expected)
</output>
