---
phase: 22-docs-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - UPGRADE.md
autonomous: true
requirements:
  - DOC-19
tags:
  - docs
  - upgrade-guide

must_haves:
  truths:
    - "UPGRADE.md has a new `## 0.3.2 to 0.3.3` H2 section at the top of the file, between `# Upgrade Guide` and the existing `## 0.3.1 to 0.3.2` section (D-13)"
    - "The new section explains that `nikic/php-parser` is now in `require` (previously in `suggest`) and that DEC-INST-02 from Phase 18 is reversed (D-13)"
    - "The new section explicitly states 'no application code or schema changes are required' for the 0.3.2→0.3.3 upgrade (RESEARCH Landmines #8)"
    - "The new section includes a one-liner telling existing users they may remove `composer require --dev nikic/php-parser` from their dev-deps if they ran it previously (Research Open Q3)"
    - "The existing `## 0.2 to 0.3` Mailer BC break section (currently L73-173) is preserved in content (SC5 — verify wording is final)"
    - "Line 173 parenthetical `(coming in the v0.3 docs refresh)` is updated to a present-tense reference to `docs/user-guide/mailer-bootstrapper.md` (RESEARCH Landmines #7) — discretionary polish per CONTEXT D-13 verify-wording"
  artifacts:
    - path: "UPGRADE.md"
      provides: "Upgrade guide with 0.3.2→0.3.3 section at top + verified 0.2→0.3 Mailer wording"
      contains: "## 0.3.2 to 0.3.3"
  key_links:
    - from: "UPGRADE.md §0.3.2 to 0.3.3"
      to: "Phase 18 DEC-INST-02 reversal rationale (CONTEXT D-13)"
      via: "prose explanation"
      pattern: "DEC-INST-02"
    - from: "UPGRADE.md §0.2 to 0.3"
      to: "docs/user-guide/mailer-bootstrapper.md (created by Plan 03)"
      via: "internal link"
      pattern: "mailer-bootstrapper.md"
---

<objective>
Add the `## 0.3.2 to 0.3.3` upgrade section at the top of UPGRADE.md documenting the nikic/php-parser require/suggest reversal, AND verify the existing `## 0.2 to 0.3` Mailer BC break wording is final (per SC5).

Purpose: Implements D-13. Closes SC5. Lets users upgrading from v0.3.2 understand the (zero-action-required) dependency-tree change.

Output: UPGRADE.md with a new top section + polished cross-link in the existing 0.2→0.3 section.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-docs-refresh/22-CONTEXT.md
@.planning/phases/22-docs-refresh/22-RESEARCH.md

@UPGRADE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert new `## 0.3.2 to 0.3.3` section at top of UPGRADE.md</name>
  <files>UPGRADE.md</files>
  <read_first>
    - UPGRADE.md L1-75 (header + existing 0.3.1→0.3.2 section + start of 0.2→0.3 — confirms insertion point at L3)
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"UPGRADE.md Insertion Point" and §"File Shape Survey #6"
    - .planning/phases/22-docs-refresh/22-CONTEXT.md §decisions D-13
  </read_first>
  <action>
    Insert the new `## 0.3.2 to 0.3.3` section between the blank line at UPGRADE.md L2 and the existing `## 0.3.1 to 0.3.2` heading at L3. The pattern in this file is reverse-chronological (newest first), so the new section goes at the TOP.

    **Required content of the new section** (Claude has discretion on exact prose; the following bullets are non-negotiable inclusions):

    1. H2 heading: `## 0.3.2 to 0.3.3` (exact text, no trailing punctuation)
    2. One-sentence intro stating this release moves `nikic/php-parser` from `composer.json#suggest` to `composer.json#require` and that no application code or schema changes are required.
    3. A short `### What changed` (or similar) subsection explaining:
       - Previously (Phase 18, DEC-INST-02): nikic was a soft suggest; the README told users to install it themselves before running `tenancy:install`.
       - Now (Phase 22, v0.3.3): nikic is a hard production dependency. The first `tenancy:install` invocation works without users having to know about nikic.
       - DEC-INST-02 from Phase 18 is reversed by user feedback during Phase 22 discussion (per CONTEXT D-13). Production deploys gain ~50KB of AST parser code that's idle at runtime; trade-off accepted for one-command install UX.
    4. A short `### Action required` (or similar) subsection stating explicitly: **No action is required.** Run `composer update danplaton4/tenancy-bundle` and the new dependency tree resolves automatically. No application code changes, no Doctrine migrations, no config edits.
    5. **One-liner for users who previously installed nikic into their own dev deps** (Research Open Q3): "If you previously ran `composer require --dev nikic/php-parser` to make `tenancy:install` work, you can now remove it from your project's `require-dev` — Composer will resolve it as a transitive dependency of `danplaton4/tenancy-bundle`."
    6. Two blank lines separator before the existing `## 0.3.1 to 0.3.2` heading (matches the file's pattern between sections — verified at L70-72 between `## 0.3.1 to 0.3.2` and `## 0.2 to 0.3`).

    **Do NOT** touch the existing sections (`## 0.3.1 to 0.3.2`, `## 0.2 to 0.3`, `## 0.1 to 0.2`, `## Upgrading to 0.1`) in this task — Task 2 handles the small `## 0.2 to 0.3` wording polish.

    **Do NOT** add any code-block snippets that reference `composer require nikic/php-parser` as something the user must run — the whole point is they don't have to.
  </action>
  <verify>
    <automated>head -10 UPGRADE.md | grep -q '^## 0.3.2 to 0.3.3$' &amp;&amp; grep -q 'DEC-INST-02' UPGRADE.md &amp;&amp; grep -q 'no application code or schema changes' UPGRADE.md &amp;&amp; grep -q 'composer require --dev nikic/php-parser' UPGRADE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The string `## 0.3.2 to 0.3.3` appears in UPGRADE.md (`grep -c '^## 0.3.2 to 0.3.3$' UPGRADE.md` returns `1`)
    - This new section comes BEFORE `## 0.3.1 to 0.3.2` in source order (verified by `grep -nE '^## 0' UPGRADE.md | head -5` showing 0.3.3 first, then 0.3.2, then 0.3, then 0.2)
    - The new section mentions `DEC-INST-02` by name (referencing Phase 18 reversal)
    - The new section explicitly contains the phrase "no application code or schema changes" (or equivalent — `grep -q 'no application code or schema changes' UPGRADE.md` returns 0)
    - The new section includes the dev-deps removal one-liner (`grep -c 'composer require --dev nikic/php-parser' UPGRADE.md` returns `≥ 1`)
    - File still parses as a valid markdown structure (no orphan blank lines, no missing heading content)
  </acceptance_criteria>
  <done>UPGRADE.md has the new `## 0.3.2 to 0.3.3` section at the top with the four required content elements (intro, what-changed with DEC-INST-02 reference, action-required (no action), dev-deps removal note).</done>
</task>

<task type="auto">
  <name>Task 2: Polish `## 0.2 to 0.3` Mailer cross-link (SC5 verify wording is final)</name>
  <files>UPGRADE.md</files>
  <read_first>
    - UPGRADE.md L73-175 (the full existing `## 0.2 to 0.3` section; confirms current wording of the BC break, migration paths A/B, raw SQL snippet, and the stale `(coming in the v0.3 docs refresh)` parenthetical at L173)
    - .planning/phases/22-docs-refresh/22-RESEARCH.md §"File Shape Survey #6" — confirms line 173 has the parenthetical needing update
  </read_first>
  <action>
    The existing `## 0.2 to 0.3` Mailer BC break section is final in content (verified by RESEARCH — lines 73-173 cover the 3 new abstract methods on `TenantInterface`, Migration path A (trait), Migration path B (manual), raw SQL ALTER snippet). The only polish needed is at L173.

    Update the parenthetical at L173 that currently reads `(coming in the v0.3 docs refresh)` (or similar — verify the actual current text after reading the file). Replace with a present-tense reference to the new page: `(see [docs/user-guide/mailer-bootstrapper.md](docs/user-guide/mailer-bootstrapper.md))` or just `(see [Mailer Bootstrapper guide](docs/user-guide/mailer-bootstrapper.md))` — Claude discretion on phrasing.

    The target file `docs/user-guide/mailer-bootstrapper.md` is created by Plan 03 in this same phase. UPGRADE.md is NOT scanned by `scripts/docs-lint.sh` (verified RESEARCH §4 lines 12-14 of docs-lint.sh) so cross-link rules don't apply here — but the link should still resolve when humans navigate the repo on GitHub.

    Do NOT change any other prose in the 0.2→0.3 section. The migration paths, SQL snippet, and trait-import recipe stay as they are.
  </action>
  <verify>
    <automated>! grep -q 'coming in the v0.3 docs refresh' UPGRADE.md &amp;&amp; grep -q 'mailer-bootstrapper.md' UPGRADE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'coming in the v0.3 docs refresh' UPGRADE.md` returns `0` (stale parenthetical replaced)
    - `grep -c 'mailer-bootstrapper.md' UPGRADE.md` returns `≥ 1` (link to new page present)
    - The `## 0.2 to 0.3` section heading still exists
    - Migration paths A and B prose is preserved (`grep -c 'TenantMailerConfigTrait' UPGRADE.md` returns `≥ 1`, `grep -c 'ALTER TABLE' UPGRADE.md` returns `≥ 1`)
  </acceptance_criteria>
  <done>Stale "coming in the v0.3 docs refresh" parenthetical is replaced with a present-tense link to `docs/user-guide/mailer-bootstrapper.md`. The rest of the 0.2→0.3 section is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upgrade-guide reader → upgrade procedure | Stale upgrade instructions cause production breakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information disclosure / repudiation | Misleading upgrade instructions could cause users to skip a needed step | mitigate | Section explicitly states "no application code or schema changes are required" — the upgrade is dependency-only. Action-required subsection makes this unmissable. |
| T-22-04 | Tampering | Stale `(coming in the v0.3 docs refresh)` parenthetical → dead link feel | mitigate | Task 2 replaces with present-tense link to the page being created in Plan 03 of this phase. |

**Severity overall: LOW.** Documentation-only changes; no code paths touched.
</threat_model>

<verification>
After both tasks:
1. `head -5 UPGRADE.md` shows: H1 heading → blank → `## 0.3.2 to 0.3.3` heading
2. `grep -nE '^## 0' UPGRADE.md` shows version sections in order: 0.3.3 → 0.3.2 → 0.3 → 0.2 → (Upgrading to 0.1)
3. The new section contains DEC-INST-02 + dev-deps removal note + "no application code or schema changes" phrase
4. The old "coming in the v0.3 docs refresh" parenthetical is gone
5. The 0.2→0.3 section still references TenantMailerConfigTrait and the ALTER TABLE migration
</verification>

<success_criteria>
SC5 (UPGRADE.md 0.2→0.3 explains the only BC break — Mailer trait migration) is verified intact; D-13 (new 0.3.2→0.3.3 section) is implemented. Research Open Q3 (dev-deps removal note) is closed.

Plan is complete when:
- The new `## 0.3.2 to 0.3.3` section is at the top of UPGRADE.md with all required content elements
- The stale parenthetical in the 0.2→0.3 section is replaced with a present-tense link
- All grep gates pass
</success_criteria>

<output>
Create `.planning/phases/22-docs-refresh/22-02-SUMMARY.md` when done. Include:
- Line ranges added (~L3 onward for the new section)
- Line edited in the 0.2→0.3 section (L173)
- Confirmation of source-order of version headings
</output>
</content>
</invoke>