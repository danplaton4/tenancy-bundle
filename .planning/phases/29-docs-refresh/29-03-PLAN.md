---
phase: 29-docs-refresh
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - scripts/docs-lint.sh
autonomous: true
requirements: [DOC-20]
must_haves:
  truths:
    - "scripts/docs-lint.sh fails CI when any docs/ file references 'shared entit(y/ies)' but does NOT contain BOTH 'landlord-side master' AND 'tenant-side read-only copy' in that same file"
    - "The new check is scoped to docs/ only — UPGRADE.md and CHANGELOG.md (not under docs/) are exempt"
    - "bash scripts/docs-lint.sh exits 0 on the full committed tree (every docs/ file that mentions shared entities already carries both phrases, per Plans 01 + 02)"
    - "The 'docs-lint: OK' summary line still prints last, unconditionally after all checks"
  artifacts:
    - path: "scripts/docs-lint.sh"
      provides: "D-04 per-file shared-entity disambiguation check (DOC-20 / D-04)"
      contains: "tenant-side read-only copy"
  key_links:
    - from: "scripts/docs-lint.sh"
      to: "docs/"
      via: "find docs/ -name '*.md' -print0 per-file loop"
      pattern: "find docs/ -name"
---

<objective>
Extend `scripts/docs-lint.sh` with the D-04 per-file shared-entity disambiguation check: any
`docs/` file that mentions "shared entit(y/ies)" must contain BOTH canonical phrases
("landlord-side master" AND "tenant-side read-only copy") in that same file, or the script fails CI.

Purpose: DOC-20 / D-04 — the load-bearing CI gate that keeps shared-entity terminology unambiguous.
This is the LAST deliverable wired because it fails on any non-compliant docs/ file; Plans 01 and 02
make the whole docs/ tree compliant first, so `bash scripts/docs-lint.sh` exits 0 the moment this lands.

Output: one shell-script edit. No new files.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-docs-refresh/29-CONTEXT.md
@.planning/phases/29-docs-refresh/29-RESEARCH.md
@.planning/phases/29-docs-refresh/29-PATTERNS.md
@.planning/phases/29-docs-refresh/29-VALIDATION.md

<sequencing>
DEPENDS ON 29-01 AND 29-02 (Wave 2). Those plans make every docs/ file that mentions shared
entities contain both canonical phrases. A pre-flight scan found FOUR such files BEFORE Plan 01/02:
docs/user-guide/shared-db.md, docs/architecture/sql-filter.md, docs/roadmap.md (reconciled in 29-01),
and docs/user-guide/filesystem-bootstrapper.md (becomes a match via 29-02's See-also cross-link).
Plus the two NEW pages (shared-entities.md, phpstan-extension.md) which carry both phrases by design.
This check MUST NOT be committed before those files are compliant, or CI goes red on pre-existing
content. The depends_on ordering guarantees this. Before adding the block, the executor MUST run the
dry-run loop (Task 1 read_first) to confirm zero violations exist on the current tree.
</sequencing>

<existing_script_facts>
From 29-PATTERNS.md + 29-RESEARCH.md (VERIFIED against live scripts/docs-lint.sh):
- The flat `check()` helper (lines 22-33) does a single `grep -rnE` across all targets. It CANNOT
  do per-file AND-logic. Do NOT use it for D-04.
- The structural twin is the `BUNDLES_VIOLATIONS` awk block (lines 73-89): compute a violations
  string, leave it empty when clean, then print + set `EXIT=1` only if non-empty.
- The `docs-lint: OK ...` summary line (line 91-92) must remain the LAST thing before `exit $EXIT`.
  The D-04 block is inserted AFTER the BUNDLES_VIOLATIONS block and BEFORE that OK line.
- `set -euo pipefail` is active — the loop must be safe under `-e` (the per-file greps inside an
  `if` are fine; the `find ... -print0` process substitution is fine).
- Scoping: `find docs/ -name '*.md' -print0` automatically excludes UPGRADE.md and CHANGELOG.md
  (they are not under docs/), satisfying the D-05 exemption.
</existing_script_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the D-04 per-file shared-entity disambiguation block to docs-lint.sh</name>
  <files>scripts/docs-lint.sh</files>
  <read_first>
    - scripts/docs-lint.sh (the file — read in full: the check() helper, the BUNDLES_VIOLATIONS awk block lines 73-89, and the OK-summary line 91-92 the new block must precede)
    - .planning/phases/29-docs-refresh/29-PATTERNS.md (docs-lint.sh section: the exact D-04 block to append after line 89 / before line 91, with the find-print0 + while-read loop, the per-file grep-then-both-phrases logic, EXIT=1 inline, and the violation-printing tail)
    - .planning/phases/29-docs-refresh/29-RESEARCH.md (docs-lint.sh verified state + D-04 new-check design rationale: case-insensitive pattern, per-file AND-logic, docs/-only scoping, why check() cannot be used)
  </read_first>
  <action>
    Pre-flight (REQUIRED before editing): run the dry-run loop to confirm the current tree is already
    compliant (Plans 01 + 02 should have made it so):
    `for f in $(grep -rliE 'shared entit(y|ies)' docs/); do grep -q 'landlord-side master' "$f" && grep -q 'tenant-side read-only copy' "$f" || echo "VIOLATION: $f"; done`
    It MUST print no VIOLATION lines. If it does, STOP — a prerequisite file from Plan 01/02 is missing
    a canonical phrase; fix that file (it is owned by the upstream plan) before wiring the gate, otherwise
    this commit breaks CI.

    Insert the D-04 block in scripts/docs-lint.sh AFTER the closing `fi` of the BUNDLES_VIOLATIONS
    block (line ~89) and BEFORE the `if [[ $EXIT -eq 0 ]]` OK-summary block (line ~91). Mirror the
    BUNDLES_VIOLATIONS structural pattern:
    - A leading comment explaining D-04: fail when a docs/ file references "shared entit(y/ies)" without
      BOTH "landlord-side master" AND "tenant-side read-only copy"; scoped to docs/ only (UPGRADE.md /
      CHANGELOG.md exempt because not under docs/); note per-file AND-logic requires a loop (check() cannot).
    - Initialize `SHARED_ENTITY_VIOLATIONS=""`.
    - `while IFS= read -r -d $'\0' f; do` ... `done < <(find docs/ -name '*.md' -print0)`.
    - Inside: `if grep -qiE 'shared entit(y|ies)' "$f"; then if ! grep -q 'landlord-side master' "$f" || ! grep -q 'tenant-side read-only copy' "$f"; then` append `"${f}\n"` to SHARED_ENTITY_VIOLATIONS and set `EXIT=1`.
    - After the loop: `if [ -n "$SHARED_ENTITY_VIOLATIONS" ]; then` echo a blank line + an ERROR line naming
      both required phrases + `printf "%b" "$SHARED_ENTITY_VIOLATIONS"`.
    - Use the literal grep patterns exactly: the trigger is case-insensitive (`grep -qiE 'shared entit(y|ies)'`),
      the two disambiguator greps are case-sensitive fixed-ish (`grep -q 'landlord-side master'` and
      `grep -q 'tenant-side read-only copy'`).

    Do NOT modify the existing checks, the check() helper, the TARGETS array, the BUNDLES_VIOLATIONS block,
    or the OK-summary line. Keep `set -euo pipefail` intact. The OK-summary line stays last before `exit $EXIT`.

    Grep-gate hygiene: the D-04 trigger uses `grep -qi` (quiet, exit-status only) on per-file content, NOT
    a `grep -c ... == 0` count on an unfiltered file, so the gate is not self-invalidated by the script's own
    header comment (the script is not under docs/ and is never scanned).
  </action>
  <verify>
    <automated>bash scripts/docs-lint.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/docs-lint.sh` exits 0 on the committed tree (prints the "docs-lint: OK ..." summary line).
    - `grep -c "find docs/ -name" scripts/docs-lint.sh` >= 1 (the per-file loop is present).
    - `grep -c "landlord-side master" scripts/docs-lint.sh` >= 1 AND `grep -c "tenant-side read-only copy" scripts/docs-lint.sh` >= 1 (both disambiguator greps wired).
    - `grep -c "shared entit" scripts/docs-lint.sh` >= 1 (trigger pattern present).
    - NEGATIVE / mechanism proof (run in a throwaway temp dir, NOT committed): create `docs-tmp/bad.md` containing only the text "this is a shared entity" with NO disambiguators, point a copy of the loop at it, and confirm it reports a violation and would set EXIT=1. Equivalent inline check: `printf 'a shared entity here\n' > /tmp/dlbad.md && if grep -qiE 'shared entit(y|ies)' /tmp/dlbad.md && { ! grep -q 'landlord-side master' /tmp/dlbad.md || ! grep -q 'tenant-side read-only copy' /tmp/dlbad.md; }; then echo MECHANISM-OK; fi && rm -f /tmp/dlbad.md` prints MECHANISM-OK.
    - `set -euo pipefail` is still the first executable line; the OK-summary `if [[ $EXIT -eq 0 ]]` block is still the last block before `exit $EXIT`.
  </acceptance_criteria>
  <done>The D-04 check is wired, fails on non-compliant docs/ files, exempts UPGRADE.md/CHANGELOG.md, and exits 0 on the current compliant tree.</done>
</task>

<task type="auto">
  <name>Task 2: Full-tree validation — docs-lint green + mkdocs build --strict</name>
  <files>scripts/docs-lint.sh</files>
  <read_first>
    - .planning/phases/29-docs-refresh/29-VALIDATION.md (full-suite command: `bash scripts/docs-lint.sh && mkdocs build --strict`; Per-Task Verification Map; mkdocs may need `pip install mkdocs-material` locally — CI runs it regardless)
    - mkdocs.yml (confirm both new pages are registered by Plan 01 so the strict build resolves all nav references)
  </read_first>
  <action>
    Verification-only task (no further source edits unless a failure surfaces). Run the full docs gate
    end to end: `bash scripts/docs-lint.sh` (must exit 0) AND `mkdocs build --strict` (must exit 0, no
    WARNING for the two new pages or broken internal links). If `mkdocs` is not installed locally, attempt
    `pip install mkdocs-material` first; if installation is not possible in this environment, record that
    the strict build is deferred to CI (`.github/workflows/ci.yml`) in the SUMMARY and note it as a
    manual-verification carryover — do NOT fabricate a pass. If `mkdocs build --strict` surfaces a broken
    nav reference or missing-page WARNING, the fault is an upstream Plan-01/03 artifact (nav typo or missing
    file); fix the minimal cause (coordinate with the owning plan's file) and re-run.
  </action>
  <verify>
    <automated>bash scripts/docs-lint.sh && { command -v mkdocs >/dev/null 2>&1 && mkdocs build --strict || echo "MKDOCS-NOT-LOCAL: strict build deferred to CI (record in SUMMARY)"; }</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/docs-lint.sh` exits 0.
    - IF mkdocs is available locally: `mkdocs build --strict` exits 0 and emits no WARNING referencing user-guide/shared-entities.md or user-guide/phpstan-extension.md and no "doc file ... not found in nav" / broken-link warning.
    - IF mkdocs is NOT available locally: the SUMMARY explicitly records that `mkdocs build --strict` is deferred to CI, with the exact command CI runs, and the two new pages are confirmed present + registered in mkdocs.yml (grep-verified) as a proxy.
  </acceptance_criteria>
  <done>Full docs gate is green locally (or mkdocs strict build is explicitly documented as a CI-deferred manual carryover with the nav-registration proxy verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ content → CI gate | The D-04 check is the only executable artifact in this phase. A regex/loop that hangs or mis-scopes would let ambiguous shared-entity docs ship (false-negative) — a low-severity-but-real terminology-integrity threat per the phase security note. |
| script header comment → its own gate | The script is never scanned by its own checks (not under docs/), so header prose cannot self-invalidate the gate; the trigger uses `grep -qi` exit-status, not a count gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-DOS | Denial of Service (lint hang) | docs-lint.sh D-04 loop | mitigate | Bounded `find docs/ -name '*.md' -print0` iteration with per-file `grep -q` (no backtracking-prone regex; alternation `(y|ies)` is linear). No unbounded loops. Verified by `bash scripts/docs-lint.sh` completing in ~seconds (29-VALIDATION.md: ~5s). |
| T-29-06-TAMPER | Tampering (false-negative gate) | docs-lint.sh D-04 scoping | mitigate | Per-file AND-logic + case-insensitive trigger; mechanism proven by the NEGATIVE test in Task 1 acceptance (a file saying "shared entity" without both phrases is reported). docs/-only scoping deliberately exempts UPGRADE.md/CHANGELOG.md per D-05. |
| T-29-07-INFO | Information Disclosure (trust-model clarity) | shared-entity terminology discipline | mitigate | The check enforces the landlord-side-master / tenant-side-read-only-copy distinction so docs cannot understate the cross-tenant write-protection invariant. |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | The only install is the optional local `pip install mkdocs-material` for the strict build (Task 2); it is a build-time docs tool, not a runtime dependency, and CI provides it regardless. 29-RESEARCH.md Package Legitimacy Audit: phase ships NO new runtime packages. |

No high-severity threats. Surface documented honestly per the phase security note (the only executable artifact is this CI shell script).
</threat_model>

<verification>
- `bash scripts/docs-lint.sh` exits 0 (full tree compliant after Plans 01 + 02).
- NEGATIVE mechanism proof: a synthetic docs file saying "shared entity" without both phrases triggers a violation (Task 1 acceptance).
- `bash scripts/docs-lint.sh && mkdocs build --strict` is the phase-gate command (29-VALIDATION.md); mkdocs strict build green locally or documented as CI-deferred.
- The "docs-lint: OK" summary line still prints last.
</verification>

<success_criteria>
- D-04 check wired, mirrors the BUNDLES_VIOLATIONS structural pattern, scoped to docs/ only.
- `bash scripts/docs-lint.sh` exits 0 on the committed tree; the check provably fails on a non-compliant file.
- `mkdocs build --strict` green (or explicitly CI-deferred with nav-registration proxy verified).
- No existing check or the OK-summary line regresses.
</success_criteria>

<output>
Create `.planning/phases/29-docs-refresh/29-03-SUMMARY.md` when done.
</output>
