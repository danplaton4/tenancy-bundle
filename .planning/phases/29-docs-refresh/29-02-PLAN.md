---
phase: 29-docs-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user-guide/filesystem-bootstrapper.md
  - docs/user-guide/cli-commands.md
  - UPGRADE.md
autonomous: true
requirements: [DOC-20]
must_haves:
  truths:
    - "filesystem-bootstrapper.md no longer describes the services? key as functional — it is annotated as a reserved no-op in v0.4"
    - "filesystem-bootstrapper.md See-also cross-links to both new pages (shared-entities.md, phpstan-extension.md)"
    - "cli-commands.md mentions tenancy:shared:resync with a cross-link to the comprehensive shared-entities.md docs"
    - "UPGRADE.md 0.3 to 0.4 section is expanded with Shared Entities + PHPStan subsections, the Phase-29 placeholder note is removed, and an explicit no-breaking-changes statement is present"
  artifacts:
    - path: "docs/user-guide/filesystem-bootstrapper.md"
      provides: "Drift fix (services? no-op) + cross-links to new pages (D-01)"
      contains: "shared-entities.md"
    - path: "UPGRADE.md"
      provides: "Expanded 0.3 to 0.4 section (D-05)"
      contains: "Shared Entities"
  key_links:
    - from: "docs/user-guide/filesystem-bootstrapper.md"
      to: "docs/user-guide/shared-entities.md"
      via: "See-also cross-link"
      pattern: "shared-entities.md"
    - from: "docs/user-guide/cli-commands.md"
      to: "docs/user-guide/shared-entities.md"
      via: "resync stub cross-link"
      pattern: "tenancy:shared:resync"
---

<objective>
Fix the single confirmed filesystem-bootstrapper.md drift (the `services?` key falsely documented
as functional), add See-also cross-links to the two new pages, add a brief `tenancy:shared:resync`
stub + cross-link to cli-commands.md, and expand the existing UPGRADE.md `## 0.3 to 0.4` section.

Purpose: DOC-20 deliverables D-01 (verify-only filesystem page) and D-05 (UPGRADE.md), plus the
discretionary cli-commands.md stub (29-CONTEXT.md Claude's Discretion / RESEARCH Pitfall 5).

Output: 3 existing files edited — no new files.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-docs-refresh/29-CONTEXT.md
@.planning/phases/29-docs-refresh/29-RESEARCH.md
@.planning/phases/29-docs-refresh/29-PATTERNS.md

<canonical_vocabulary>
D-07 (LOCKED). NOTE for this plan: filesystem-bootstrapper.md WILL contain "Shared Entities"
(via the new See-also cross-link), which matches the case-insensitive D-04 pattern
`shared entit(y|ies)`. Therefore filesystem-bootstrapper.md MUST also contain BOTH phrases
verbatim or it will fail Plan 03's D-04 lint check:
- "landlord-side master"
- "tenant-side read-only copy"
Add both in the See-also cross-link's one-line description for shared-entities.md (e.g.
"... the #[Shared] sync model: landlord-side master records fanned out as tenant-side read-only
copies to each tenant database."). UPGRADE.md is NOT scanned by docs-lint (exempt). cli-commands.md:
keep the resync stub phrased around the COMMAND NAME + cross-link so it does NOT contain the literal
phrase "shared entit(y/ies)" and therefore does not trip the D-04 pattern. If the phrase is
unavoidable in cli-commands.md, include both canonical phrases there too.
</canonical_vocabulary>

<verified_facts>
From 29-RESEARCH.md (all VERIFIED against live source):
- DRIFT (filesystem-bootstrapper.md line ~205): the `services?: string[]` row currently reads
  `// limit scoping to these service IDs (empty = all tagged)` but src/Filesystem/TenantFilesystemConfigTrait.php:29
  marks it reserved/no-op in v0.4. This is the ONLY drift. All other content is accurate — DO NOT rewrite the page.
- Existing See-also (filesystem-bootstrapper.md lines 425-434): already links configuration.md,
  UPGRADE.md#03-to-04, mailer-bootstrapper.md, profiler-tab.md. ADD shared-entities.md + phpstan-extension.md.
- UPGRADE.md: `## 0.3 to 0.4` already exists (added Phase 24), covers only Filesystem, ends with a
  blockquote placeholder note: "This section will be expanded ... in the v0.4 docs refresh (Phase 29 / DOC-20)."
  BC verification: line 7 already states "None. TenantInterface is unchanged (DEC-FILE-CONFIG locked)."
  No BC breaks in v0.4 — shared entities are opt-in (#[Shared] tag), PHPStan extension is require-dev opt-in.
- cli-commands.md currently documents tenancy:install, tenancy:init, tenancy:migrate, tenancy:run.
  tenancy:shared:resync is a 5th command not yet listed. Per 29-CONTEXT.md discretion, a one-paragraph
  stub + cross-link to shared-entities.md is sufficient (full docs live on shared-entities.md per D-02).
- tenancy:shared:resync options: `--tenant` (single slug; absent = all tenants), `--dry-run`, `--force`. NO `--all`.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: filesystem-bootstrapper.md drift fix + cross-links (D-01)</name>
  <files>docs/user-guide/filesystem-bootstrapper.md</files>
  <read_first>
    - docs/user-guide/filesystem-bootstrapper.md (the file — focus on the getFilesystemConfig() return-shape block around lines 201-207 and the `## See also` section around lines 425-434; read both ranges)
    - src/Filesystem/TenantFilesystemConfigTrait.php (source-of-truth — line 29 confirms the services? key is a no-op/reserved in v0.4)
    - .planning/phases/29-docs-refresh/29-PATTERNS.md (filesystem-bootstrapper.md drift-fix section: exact line-205 replacement comment + the two See-also bullets to add)
    - .planning/phases/29-docs-refresh/29-RESEARCH.md (Filesystem Bootstrapper Drift Audit — confirms this is the ONLY drift; everything else is accurate)
  </read_first>
  <action>
    Single targeted edit + cross-links. Do NOT rewrite the page (D-01 verify-only).

    (a) On the `services?: string[],` line in the getFilesystemConfig() return-shape code block,
    replace the trailing comment `// limit scoping to these service IDs (empty = all tagged)` with a
    reserved/no-op annotation, e.g. `// reserved — no-op in v0.4; setting this key has no effect in the current release`.
    Keep it as an inline comment inside the existing code block (no prose restructuring).

    (b) In the `## See also` section, add two bullets matching the existing em-dash style. The
    shared-entities.md bullet MUST embed BOTH D-07 canonical phrases (because the link text
    "Shared Entities" makes this file match the D-04 pattern), e.g.
    `- [Shared Entities](shared-entities.md) — the #[Shared] sync model: landlord-side master records fanned out as tenant-side read-only copies to each tenant database.`
    and `- [PHPStan Extension](phpstan-extension.md) — static-analysis rules that catch tenancy + filesystem misconfiguration at edit time.`

    Make no other changes to the file body.
  </action>
  <verify>
    <automated>grep -q 'no-op in v0.4' docs/user-guide/filesystem-bootstrapper.md && [ "$(grep -c 'limit scoping to these service IDs' docs/user-guide/filesystem-bootstrapper.md)" -eq 0 ] && grep -q 'shared-entities.md' docs/user-guide/filesystem-bootstrapper.md && grep -q 'phpstan-extension.md' docs/user-guide/filesystem-bootstrapper.md && grep -q 'landlord-side master' docs/user-guide/filesystem-bootstrapper.md && grep -q 'tenant-side read-only copy' docs/user-guide/filesystem-bootstrapper.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'no-op in v0.4' docs/user-guide/filesystem-bootstrapper.md` >= 1.
    - `grep -c 'limit scoping to these service IDs' docs/user-guide/filesystem-bootstrapper.md` == 0 (stale functional comment removed).
    - `grep -c 'shared-entities.md' docs/user-guide/filesystem-bootstrapper.md` >= 1 AND `grep -c 'phpstan-extension.md' docs/user-guide/filesystem-bootstrapper.md` >= 1.
    - `grep -c 'landlord-side master' docs/user-guide/filesystem-bootstrapper.md` >= 1 AND `grep -c 'tenant-side read-only copy' docs/user-guide/filesystem-bootstrapper.md` >= 1 (required because the See-also link text trips the D-04 pattern).
    - The rest of the page is byte-identical except the one comment line and the two new See-also bullets (verify-only — no rewrite).
  </acceptance_criteria>
  <done>The services? no-op drift is corrected, both new pages are cross-linked, and the file carries both canonical phrases so it stays D-04-green.</done>
</task>

<task type="auto">
  <name>Task 2: cli-commands.md tenancy:shared:resync stub + cross-link (discretion / Pitfall 5)</name>
  <files>docs/user-guide/cli-commands.md</files>
  <read_first>
    - docs/user-guide/cli-commands.md (the file — match the per-command H2 shape: name, usage code, flags table; the resync entry is a STUB, not a full duplicate)
    - .planning/phases/29-docs-refresh/29-RESEARCH.md (tenancy:shared:resync verified facts + Pitfall 5 / Open Q1: one-paragraph stub + cross-link is the chosen approach)
    - .planning/phases/29-docs-refresh/29-CONTEXT.md (Claude's Discretion: comprehensive resync docs live on shared-entities.md)
  </read_first>
  <action>
    Add a brief H2 entry for `tenancy:shared:resync` near the other command sections. Keep it a STUB:
    one-paragraph summary (re-synchronizes the landlord-side master records to every tenant database;
    supports `--tenant`, `--dry-run`, `--force`; NO `--all`) + a one-line bash usage example +
    a cross-link "See [Shared Entities](shared-entities.md) for full options, dry-run output, and behavior."
    Do NOT duplicate the full flags table / output examples — those live on shared-entities.md per D-02.

    D-04 note: phrase the stub around the COMMAND NAME and link text; avoid the literal phrase
    "shared entity"/"shared entities" in this file so it does not trip the D-04 pattern. If the phrase
    is unavoidable, this file must also contain both canonical phrases verbatim — but the cleaner path
    is to keep cli-commands.md phrased around the command + cross-link only.
  </action>
  <verify>
    <automated>grep -q 'tenancy:shared:resync' docs/user-guide/cli-commands.md && grep -q 'shared-entities.md' docs/user-guide/cli-commands.md && if grep -qiE 'shared entit(y|ies)' docs/user-guide/cli-commands.md; then grep -q 'landlord-side master' docs/user-guide/cli-commands.md && grep -q 'tenant-side read-only copy' docs/user-guide/cli-commands.md; else true; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tenancy:shared:resync' docs/user-guide/cli-commands.md` >= 1.
    - `grep -c 'shared-entities.md' docs/user-guide/cli-commands.md` >= 1 (cross-link present).
    - `grep -c -- '--all' docs/user-guide/cli-commands.md` does NOT introduce a resync `--all` flag (the command has none).
    - D-04 safety: IF `grep -qiE 'shared entit(y|ies)' docs/user-guide/cli-commands.md` matches, THEN both 'landlord-side master' and 'tenant-side read-only copy' must be present (the conditional in verify exits 0 either way).
  </acceptance_criteria>
  <done>cli-commands.md has a tenancy:shared:resync stub that cross-links to shared-entities.md and does not break the D-04 check.</done>
</task>

<task type="auto">
  <name>Task 3: Expand UPGRADE.md 0.3 to 0.4 section (D-05)</name>
  <files>UPGRADE.md</files>
  <read_first>
    - UPGRADE.md (the file — the existing `## 0.3 to 0.4` section, including the line-7 "None. TenantInterface is unchanged" BC note, the existing Filesystem subsections, and the trailing placeholder blockquote to remove)
    - .planning/phases/29-docs-refresh/29-PATTERNS.md (UPGRADE.md section: existing structure to modify, H3 heading style `### New: <Feature> (<Req-ID>)`, the exact Shared-Entities + PHPStan subsections to add, the "v0.4 milestone: no breaking changes" block, and the placeholder Note to DELETE)
    - .planning/phases/29-docs-refresh/29-RESEARCH.md (UPGRADE.md verified state + BC verification: no breaking changes in v0.4)
  </read_first>
  <action>
    Expand the existing `## 0.3 to 0.4` section (do NOT create a new section — it already exists).

    (a) Append, after the existing Filesystem "No action required" blocks, two new H3 subsections
    matching the existing `### New: <Feature Name> (<Req-ID>)` style:
    - `### New: Shared Entities (SHARE-01/02/03)` — the #[Shared] Doctrine entity sync model for
      database-per-tenant projects; landlord master fanned out to every tenant DB on postFlush; fully
      opt-in (no schema migration unless adopted); cross-link to docs/user-guide/shared-entities.md;
      a "No action required" note: mark an entity #[Shared] to mirror it; run `bin/console tenancy:shared:resync`
      after the first migration to backfill; no TenantInterface changes; async mode opt-in via `tenancy.shared.async: true`.
    - `### New: PHPStan Extension (DX-03)` — three static-analysis rules for Level-9 projects; distributed
      via `phpstan/extension-installer` (`composer require --dev phpstan/extension-installer`); note that if
      `phpstan/phpstan-doctrine` is present, include `extension-doctrine.neon` INSTEAD OF `extension.neon`;
      cross-link to docs/user-guide/phpstan-extension.md; a "No action required" note: the extension only
      runs if explicitly installed.

    (b) Add a final `### v0.4 milestone: no breaking changes` block stating all four v0.4 capabilities
    (filesystem bootstrapper, shared entities, tenancy:shared:resync, PHPStan extension) are fully opt-in
    and TenantInterface is unchanged — any v0.3 project runs unmodified in v0.4.

    (c) DELETE the trailing placeholder blockquote: "> **Note:** This section will be expanded ... in the
    v0.4 docs refresh (Phase 29 / DOC-20)." Remove the whole blockquote.

    UPGRADE.md is NOT scanned by docs-lint, so the canonical D-07 phrases are NOT required here (terminology exempt).
  </action>
  <verify>
    <automated>grep -q 'New: Shared Entities' UPGRADE.md && grep -q 'New: PHPStan Extension' UPGRADE.md && grep -qi 'no breaking changes' UPGRADE.md && [ "$(grep -c 'will be expanded' UPGRADE.md)" -eq 0 ] && grep -q 'tenancy:shared:resync' UPGRADE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'New: Shared Entities' UPGRADE.md` >= 1 AND `grep -c 'New: PHPStan Extension' UPGRADE.md` >= 1.
    - `grep -ci 'no breaking changes' UPGRADE.md` >= 1 (explicit milestone-wide statement).
    - `grep -c 'will be expanded' UPGRADE.md` == 0 (Phase-29 placeholder blockquote removed).
    - `grep -c 'tenancy:shared:resync' UPGRADE.md` >= 1 (backfill instruction present).
    - The pre-existing `## 0.3 to 0.4` Filesystem content and the BC "None." note are preserved (expand, not replace).
  </acceptance_criteria>
  <done>UPGRADE.md 0.3 to 0.4 section now covers shared entities + PHPStan, removes the placeholder, and states no breaking changes for the v0.4 milestone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs prose → reader's adoption decision | Inaccurate "no breaking changes" or drift claims could mislead an upgrading user. Low severity; mitigated by source cross-reference. |
| (no executable artifact in this plan) | This plan edits only Markdown. No code path; the D-04 lint script is added in Plan 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-INFO | Information Disclosure (docs accuracy) | filesystem-bootstrapper.md services? annotation | mitigate | Annotation matches src/Filesystem/TenantFilesystemConfigTrait.php:29; acceptance criteria assert the stale "functional" comment is removed and the no-op annotation is present. |
| T-29-04-REPUD | Repudiation (false BC claim) | UPGRADE.md "no breaking changes" | mitigate | Claim cross-referenced against UPGRADE.md line-7 "None. TenantInterface is unchanged" and 29-RESEARCH.md BC verification; reviewer confirms against TenantInterface/TenantFilesystemConfigTrait. |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (29-RESEARCH.md Package Legitimacy Audit: none). composer commands in UPGRADE.md are documentation content, not executed. |

No high-severity threats.
</threat_model>

<verification>
- `bash scripts/docs-lint.sh` exits 0 (the D-04 check is added in Plan 03; existing checks must still pass after these edits — note filesystem-bootstrapper.md now carries both D-07 phrases so it will be D-04-green once that check lands).
- Manual cross-reference: services? annotation matches src/Filesystem/TenantFilesystemConfigTrait.php; UPGRADE.md "no breaking changes" matches TenantInterface state (29-VALIDATION.md Manual-Only Verifications).
</verification>

<success_criteria>
- filesystem-bootstrapper.md drift corrected + both new pages cross-linked + both D-07 phrases present.
- cli-commands.md has a tenancy:shared:resync stub cross-linking to shared-entities.md.
- UPGRADE.md 0.3 to 0.4 expanded with shared-entities + PHPStan subsections, placeholder removed, no-breaking-changes statement added.
- No existing docs-lint check regresses.
</success_criteria>

<output>
Create `.planning/phases/29-docs-refresh/29-02-SUMMARY.md` when done.
</output>
