---
phase: 09-oss-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
autonomous: true
requirements:
  - OSS-01

must_haves:
  truths:
    - "composer validate --strict passes without errors"
    - "symfony/process is NOT in the suggest block"
    - "keywords, authors, homepage, support URLs are present for Packagist discoverability"
    - "branch-alias maps dev-master to 1.0.x-dev"
  artifacts:
    - path: "composer.json"
      provides: "Packagist-ready package metadata"
      contains: "branch-alias"
  key_links:
    - from: "composer.json"
      to: "Packagist"
      via: "keywords, homepage, support URLs"
      pattern: "\"keywords\""
---

<objective>
Polish composer.json for Packagist publication: add package metadata (keywords, authors, homepage, support URLs), remove the misleading symfony/process suggest entry, and add branch-alias for pre-release installs.

Purpose: OSS-01 requires the package to be Packagist-ready. The existing composer.json has the right structure but is missing discoverability metadata and has a stale suggest entry.
Output: A composer.json that passes `composer validate --strict` and is ready for `packagist.org/packages/submit`.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-oss-hardening/09-CONTEXT.md
@.planning/phases/09-oss-hardening/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Packagist metadata and clean up composer.json</name>
  <files>composer.json</files>
  <read_first>
    - composer.json (current state — must see exact structure before editing)
    - .planning/phases/09-oss-hardening/09-RESEARCH.md (section "09-01: Packagist-Ready composer.json" for exact field values)
  </read_first>
  <action>
Edit composer.json to make the following specific changes (do NOT rewrite from scratch — modify in place):

1. **Add `keywords` array** after the `license` field:
   ```json
   "keywords": ["symfony", "multitenancy", "multi-tenant", "saas", "bundle", "doctrine", "tenancy"]
   ```

2. **Add `authors` array** after `keywords`:
   ```json
   "authors": [
       {
           "name": "Dan Platon",
           "homepage": "https://github.com/danplaton4",
           "role": "Developer"
       }
   ]
   ```

3. **Add `homepage`** after `authors`:
   ```json
   "homepage": "https://github.com/danplaton4/tenancy-bundle"
   ```

4. **Add `support` block** after `homepage` (per D-14):
   ```json
   "support": {
       "issues": "https://github.com/danplaton4/tenancy-bundle/issues",
       "source": "https://github.com/danplaton4/tenancy-bundle"
   }
   ```

5. **Remove `symfony/process` from `suggest`** (per D-13): Delete the entire line `"symfony/process": "Required for tenancy:run command (^6.4||^7.0)"` from the suggest block. symfony/process is already in `require` since Phase 07 — keeping it in suggest is misleading.

6. **Add `branch-alias` to `extra`** (per D-15): The `extra` block currently has only `symfony`. Add `branch-alias` as a sibling:
   ```json
   "extra": {
       "symfony": {
           "bundles": {
               "Tenancy\\Bundle\\TenancyBundle": ["all"]
           }
       },
       "branch-alias": {
           "dev-master": "1.0.x-dev"
       }
   }
   ```

IMPORTANT: Do NOT add `extra.symfony.require` — that key constrains the application's Symfony version and must NEVER appear in a reusable bundle (see RESEARCH.md Pitfall 1).

Preserve all existing fields (name, description, type, license, minimum-stability, require, require-dev, suggest minus process, autoload, autoload-dev, config). Keep `"sort-packages": true` in config.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && composer validate --strict 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - composer.json contains `"keywords": ["symfony", "multitenancy"`
    - composer.json contains `"name": "Dan Platon"`
    - composer.json contains `"homepage": "https://github.com/danplaton4/tenancy-bundle"`
    - composer.json contains `"issues": "https://github.com/danplaton4/tenancy-bundle/issues"`
    - composer.json contains `"source": "https://github.com/danplaton4/tenancy-bundle"`
    - composer.json does NOT contain `"symfony/process"` in the `suggest` block (grep for `suggest` section should show only doctrine/* and symfony/messenger entries)
    - composer.json contains `"dev-master": "1.0.x-dev"` under `extra.branch-alias`
    - composer.json does NOT contain `"symfony.require"` (must not constrain consumers)
    - `composer validate --strict` exits 0
  </acceptance_criteria>
  <done>composer.json passes strict validation and contains all Packagist metadata (keywords, authors, homepage, support, branch-alias) with symfony/process removed from suggest</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Packagist registry | Package metadata is publicly visible; no secrets should be in composer.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | composer.json authors/support | accept | Author name and GitHub URLs are intentionally public for OSS |
| T-09-02 | Tampering | extra.symfony.require injection | mitigate | Explicitly verified that extra.symfony.require is NOT present — prevents bundle from constraining consumer Symfony versions |
</threat_model>

<verification>
- `composer validate --strict` passes
- `grep -c "symfony/process" composer.json` in suggest block returns 0 (only appears in require)
- `php -r "echo json_decode(file_get_contents('composer.json'))->extra->{'branch-alias'}->{'dev-master'};"` outputs `1.0.x-dev`
</verification>

<success_criteria>
- composer.json is Packagist-ready with all required metadata
- No misleading suggest entries
- Branch alias enables `composer require ^1.0` before first tag
</success_criteria>

<output>
After completion, create `.planning/phases/09-oss-hardening/09-01-SUMMARY.md`
</output>
