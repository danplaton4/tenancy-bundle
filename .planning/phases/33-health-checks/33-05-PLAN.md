---
phase: 33-health-checks
plan: 05
type: execute
wave: 4
depends_on: ["33-03", "33-04"]
files_modified:
  - composer.json
  - src/Health/Liip/TenantConnectivityCheck.php
  - src/DependencyInjection/Compiler/HealthCheckIntegrationPass.php
  - src/TenancyBundle.php
  - config/services.php
  - tests/Unit/DependencyInjection/Compiler/HealthCheckIntegrationPassTest.php
  - tests/Integration/Health/HealthChecksNoLiipTest.php
  - tests/Integration/Health/HealthEndpointsIntegrationTest.php
autonomous: true
requirements: [HEALTH-01, HEALTH-04, HEALTH-05, HEALTH-06, HEALTH-07]
must_haves:
  truths:
    - "The health config node (fleet_default_limit=50, fleet_max_limit=200) exposes tenancy.health.* parameters — no HTTP enabled flag (route-import is the opt-in)"
    - "Controller, command, checker, and sanitizer are wired as container services; the controller is public and the command is tagged console.command"
    - "When liip/monitor-bundle is installed, HealthCheckIntegrationPass registers a bundle check as a liip_monitor.check service (class_exists-guarded)"
    - "When liip/monitor-bundle is absent, the pass no-ops and the self-contained endpoints + command still work"
    - "The imported routes resolve end-to-end: /_tenancy/health/live returns 200, /_tenancy/health/ready/{slug} returns application/health+json"
  artifacts:
    - path: "src/DependencyInjection/Compiler/HealthCheckIntegrationPass.php"
      provides: "class_exists-guarded liip check auto-registration"
      contains: "class_exists"
    - path: "src/Health/Liip/TenantConnectivityCheck.php"
      provides: "Laminas CheckInterface adapter delegating to TenantHealthChecker"
      contains: "CheckInterface"
    - path: "config/services.php"
      provides: "controller (public) + command (console.command tag) wiring"
      contains: "tenancy.health.controller"
    - path: "tests/Integration/Health/HealthChecksNoLiipTest.php"
      provides: "Proof the endpoints/command work with liip absent"
      contains: "NoLiip"
  key_links:
    - from: "src/TenancyBundle.php"
      to: "src/DependencyInjection/Compiler/HealthCheckIntegrationPass.php"
      via: "addCompilerPass(new HealthCheckIntegrationPass()) in build()"
      pattern: "HealthCheckIntegrationPass"
    - from: "config/routes/health.php"
      to: "src/Controller/TenantHealthController.php"
      via: "public controller service resolvable by the router"
      pattern: "tenancy.health.controller"
    - from: "src/Health/Liip/TenantConnectivityCheck.php"
      to: "src/Health/TenantHealthChecker.php"
      via: "check() delegates to checker (never touches TenantContext directly)"
      pattern: "checker"
---

<objective>
Wire everything together and deliver the optional liip integration. Add the `health` config node + `tenancy.health.*` parameters to `TenancyBundle`; wire the controller (public) and command (console.command tag) in `config/services.php`; register `HealthCheckIntegrationPass`; add `liip/monitor-bundle` to require-dev + suggest; build the `class_exists`-guarded liip check adapter; and prove HEALTH-07 both directions (liip present -> auto-register; liip absent -> no-op, endpoints still work) plus an end-to-end HTTP integration test.

Purpose: This is the integration wave. It makes the HTTP routes actually resolve (the controller service must be public), registers the CLI command, and delivers HEALTH-07 (liip auto-registration) without turning liip into a hard require. The no-Doctrine + no-liip CI lanes must stay green.
Output: modified `composer.json`, `src/TenancyBundle.php`, `config/services.php`; new `HealthCheckIntegrationPass` + `TenantConnectivityCheck`; one compiler-pass unit test + two integration tests (no-liip + HTTP end-to-end).
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-health-checks/33-CONTEXT.md
@.planning/phases/33-health-checks/33-RESEARCH.md
@.planning/phases/33-health-checks/33-PATTERNS.md
@.planning/phases/33-health-checks/33-01-SUMMARY.md
@.planning/phases/33-health-checks/33-02-SUMMARY.md
@.planning/phases/33-health-checks/33-03-SUMMARY.md
@.planning/phases/33-health-checks/33-04-SUMMARY.md

<interfaces>
Wiring facts extracted from live source. Follow these exactly.

From src/DependencyInjection/Compiler/FilesystemContractPass.php + MailerTransportContractPass.php (the guard pattern to mirror):
- final class implementing CompilerPassInterface with process(ContainerBuilder): void
- early-return guard first: if not interface_exists(OptionalInterface::class) then return
- then register/tag services via Definition + addTag(...)

From src/TenancyBundle.php:
- configure(DefinitionConfigurator) — the maintenance arrayNode sits ~lines 136-146; add a sibling health node with fleet_default_limit (default 50, min 1) + fleet_max_limit (default 200, min 1). NO enabled flag (D-01).
- loadExtension(array config, ...) — extract config['health'] and set params tenancy.health.fleet_default_limit + tenancy.health.fleet_max_limit (mirror the maintenance param-extraction block ~lines 203-231, same is_int() defensive casting).
- build(ContainerBuilder container) — compiler passes registered ~lines 457-476; add addCompilerPass(new HealthCheckIntegrationPass()) UNCONDITIONALLY (the pass self-guards via class_exists internally, per the FilesystemContractPass precedent). Existing conditional guards use interface_exists(...).

From config/services.php (checker + sanitizer are already wired by plan 33-02):
- public controller: set('tenancy.health.controller', TenantHealthController::class) with public() and args [service('tenancy.health.checker'), service('tenancy.provider')->nullOnInvalid(), service('tenancy.health.sanitizer'), param('tenancy.health.fleet_default_limit'), param('tenancy.health.fleet_max_limit')]
- tagged command: set('tenancy.command.health', TenantHealthCommand::class) with args [service('tenancy.provider')->nullOnInvalid(), service('tenancy.health.checker'), service('tenancy.health.sanitizer')] and tag('console.command')

From src/Health/ (plans 33-01/33-02): TenantHealthChecker::checkOne(): TenantHealthReport, TenantHealthReport{slug,status,results}, HealthStatus{Pass,Warn,Fail}, HealthResponseSanitizer.
From src/Provider/TenantProviderInterface.php: findAll(): TenantInterface[].

liip contract (33-RESEARCH.md Pattern 6): checks implement Laminas\Diagnostics\Check\CheckInterface — check(): ResultInterface (may throw) + getLabel(): string; result types Laminas\Diagnostics\Result\Success|Failure|Warning; auto-register tag = liip_monitor.check.

CRITICAL — liip/laminas are NOT yet in vendor/. RESEARCH Assumption A2 flags the guard FQCN as unverified. After composer require --dev, verify the ACTUAL FQCNs from vendor before hard-coding the guard. Prefer guarding on interface_exists(Laminas\Diagnostics\Check\CheckInterface::class) (always present when liip is installed; more robust than the bundle class name) OR the real LiipMonitorBundle FQCN confirmed from vendor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: composer require-dev liip + health config node + parameters + controller/command wiring</name>
  <files>composer.json, src/TenancyBundle.php, config/services.php</files>
  <read_first>
    - composer.json (require-dev + suggest — liip/laminas are NOT present; must be added)
    - .planning/phases/33-health-checks/33-RESEARCH.md section "Package Legitimacy Audit" (liip/monitor-bundle + laminas/laminas-diagnostics both Approved [VERIFIED: Packagist]) and section "Standard Stack" (liip ^2.25, require-dev + suggest only)
    - src/TenancyBundle.php (configure() maintenance node ~136-146; loadExtension() maintenance param block ~203-231)
    - config/services.php (tenancy.health.checker + tenancy.health.sanitizer wiring from plan 33-02; the console.command tag pattern; the public()+nullOnInvalid() patterns)
    - 33-PATTERNS.md section "src/TenancyBundle.php" + section "config/services.php" (blocks 3 + 4)
    - 33-CONTEXT.md D-01 (no HTTP enabled flag), D-08 (fleet limits)
  </read_first>
  <action>
    Add liip/monitor-bundle ^2.25 to composer.json require-dev (transitively pulls laminas/laminas-diagnostics; both are Approved [VERIFIED: Packagist] in the RESEARCH.md legitimacy audit — no legitimacy checkpoint required) and add a suggest entry keyed liip/monitor-bundle describing it as an optional auto-registration integration for HEALTH-07 (endpoints + command work without it). Run composer update liip/monitor-bundle --with-all-dependencies to install into vendor. In src/TenancyBundle::configure(), add a health arrayNode sibling to maintenance with addDefaultsIfNotSet(), integerNode fleet_default_limit defaultValue(50) min(1), and integerNode fleet_max_limit defaultValue(200) min(1). NO enabled flag (D-01). In loadExtension(), extract config['health'] and set container parameters tenancy.health.fleet_default_limit + tenancy.health.fleet_max_limit (mirror the maintenance param-extraction block with the same is_int() defensive casting). In config/services.php, add the PUBLIC controller service tenancy.health.controller for TenantHealthController (public(), args = checker + provider nullOnInvalid + sanitizer + the two limit params) and the command service tenancy.command.health for TenantHealthCommand (args = provider nullOnInvalid + checker + sanitizer, tag console.command).
  </action>
  <verify>
    <automated>composer validate --strict && vendor/bin/phpunit --testsuite unit</automated>
  </verify>
  <acceptance_criteria>
    - composer validate --strict passes; liip/monitor-bundle present under require-dev in composer.json (grep -q "liip/monitor-bundle" composer.json)
    - vendor/laminas/laminas-diagnostics exists after composer update (ls vendor/laminas/laminas-diagnostics succeeds)
    - grep -q "tenancy.health.controller" config/services.php AND the controller service line contains ->public()
    - grep -q "tenancy.command.health" config/services.php AND that block contains tag('console.command')
    - grep -q "fleet_default_limit" src/TenancyBundle.php AND src/TenancyBundle.php contains no tenancy.health.enabled parameter (D-01)
  </acceptance_criteria>
  <done>liip is a require-dev + suggest optional dep; the health config node + tenancy.health.* params exist with no HTTP enabled flag; controller (public) + command (console.command) are wired.</done>
</task>

<task type="auto">
  <name>Task 2: HealthCheckIntegrationPass + TenantConnectivityCheck liip adapter + registration in build()</name>
  <files>src/Health/Liip/TenantConnectivityCheck.php, src/DependencyInjection/Compiler/HealthCheckIntegrationPass.php, src/TenancyBundle.php, tests/Unit/DependencyInjection/Compiler/HealthCheckIntegrationPassTest.php</files>
  <read_first>
    - src/DependencyInjection/Compiler/FilesystemContractPass.php + MailerTransportContractPass.php (guard + Definition/addTag pattern)
    - tests/Unit/DependencyInjection/Compiler/MaintenanceModeContractPassTest.php + FilesystemContractPassTest.php (the ContainerBuilder unit-test pattern for compiler passes)
    - src/TenancyBundle.php build() ~lines 457-476 (where addCompilerPass calls live)
    - src/Health/TenantHealthChecker.php + src/Provider/TenantProviderInterface.php (the check() adapter delegates to these)
    - 33-RESEARCH.md Pattern 6 (CheckInterface + Success/Failure/Warning + liip_monitor.check tag), Anti-pattern H-A7 (liip check must NOT set/clear TenantContext directly — it calls the checker), Assumption A2 (verify guard FQCN from vendor)
    - 33-PATTERNS.md section "HealthCheckIntegrationPass.php"
    - vendor/liip/monitor-bundle + vendor/laminas/laminas-diagnostics (confirm actual FQCNs installed in Task 1)
  </read_first>
  <action>
    Create src/Health/Liip/TenantConnectivityCheck (final class implementing Laminas\Diagnostics\Check\CheckInterface, declare(strict_types=1), namespace Tenancy\Bundle\Health\Liip). Constructor takes the TenantHealthChecker and the TenantProviderInterface. Implement check(): ResultInterface by iterating findAll() and calling checker->checkOne() per tenant (worst-of across tenants), returning Laminas\Diagnostics\Result\Success when all pass, Warning when any warn (none fail), Failure when any fail — the message must be built from sanitized output only (delegate to HealthResponseSanitizer OR to report data that is already safe; never embed a raw DSN). getLabel() returns a fixed human label like "Tenancy: per-tenant DB connectivity". This class MUST NOT set or clear TenantContext directly (Anti-pattern H-A7) — the checker owns that. This file references Laminas types, so it is only loadable when liip is present; that is fine because it is registered only inside the guarded compiler pass. Create src/DependencyInjection/Compiler/HealthCheckIntegrationPass (final class implementing CompilerPassInterface). process(): early-return if the liip guard FQCN is absent (use interface_exists(Laminas\Diagnostics\Check\CheckInterface::class) — confirmed present iff liip installed, more robust than the bundle class per Assumption A2; verify against vendor). When present, register a Definition for TenantConnectivityCheck with its two service references and add the liip_monitor.check tag. In src/TenancyBundle::build(), add addCompilerPass(new HealthCheckIntegrationPass()) unconditionally after the existing passes (it self-guards). Write tests/Unit/DependencyInjection/Compiler/HealthCheckIntegrationPassTest.php mirroring MaintenanceModeContractPassTest: build a ContainerBuilder, run the pass, and assert that when the guard class is available a service tagged liip_monitor.check is registered (in this repo liip IS installed via Task 1 so the positive path is exercised); include an assertion path documenting the no-op-when-absent behavior (e.g. a separate assertion that the pass does not throw and registers nothing when the guarded branch is not taken, or a note that the no-liip lane covers absence — see Task 3).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter HealthCheckIntegrationPassTest</automated>
  </verify>
  <acceptance_criteria>
    - vendor/bin/phpunit --filter HealthCheckIntegrationPassTest exits 0
    - HealthCheckIntegrationPass::process() starts with a class_exists/interface_exists guard early-return (source assertion: grep -q "interface_exists\|class_exists" src/DependencyInjection/Compiler/HealthCheckIntegrationPass.php)
    - With the guard satisfied, a service tagged liip_monitor.check is present in the compiled container (behavior)
    - TenantConnectivityCheck implements CheckInterface and does NOT reference TenantContext (source assertion: grep -L "TenantContext" src/Health/Liip/TenantConnectivityCheck.php lists the file)
    - src/TenancyBundle.php build() contains addCompilerPass(new HealthCheckIntegrationPass()) (source assertion)
  </acceptance_criteria>
  <done>The liip adapter and its guarded compiler pass exist; the pass registers a liip_monitor.check-tagged service when the laminas contract is present and no-ops otherwise; the pass is wired into build().</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — no-liip lane + end-to-end HTTP endpoints</name>
  <files>tests/Integration/Health/HealthChecksNoLiipTest.php, tests/Integration/Health/HealthEndpointsIntegrationTest.php</files>
  <read_first>
    - 33-VALIDATION.md section "Wave 0 Requirements" (bullet 3 — self-contained works with liip ABSENT; auto-register fires when PRESENT)
    - 33-RESEARCH.md section "Validation for HEALTH-07" + section "Environment Availability Audit"
    - An existing integration test that boots a framework kernel and imports routes / hits the container (search tests/Integration for a kernel that loads the bundle + framework-bundle; reuse it). Also the existing no-doctrine or minimal-kernel test as a template for a lane that excludes an optional dependency.
    - config/routes/health.php + config/routes/health_fleet.php (plan 33-03 — import these in the HTTP test kernel with prefix /_tenancy/health and /_tenancy/health respectively)
    - src/Controller/TenantHealthController.php (plan 33-03)
  </read_first>
  <action>
    Create tests/Integration/Health/HealthEndpointsIntegrationTest.php: boot a test kernel that registers the bundle + symfony/framework-bundle and imports config/routes/health.php with prefix /_tenancy/health (and config/routes/health_fleet.php at /_tenancy/health for the fleet path). Use the browser/kernel HTTP test approach (create a Request and pass it through the kernel, or KernelBrowser if framework-bundle test client is available). Assert: GET /_tenancy/health/live returns HTTP 200, Content-Type application/health+json, body status ok; GET /_tenancy/health/ready/{slug} for a provisioned SQLite tenant returns application/health+json with HTTP 200; GET /_tenancy/health/ready/does-not-exist returns HTTP 404 with an application/health+json body (D-06). This proves the greenfield routes + controller resolve end-to-end (the controller service is public). Create tests/Integration/Health/HealthChecksNoLiipTest.php: prove the self-contained surface works when liip is NOT active — either boot a kernel whose HealthCheckIntegrationPass guard does not fire (simulate absence by asserting that when the guard is bypassed no liip_monitor.check service is required for the endpoints/command to function) OR assert directly that TenantHealthChecker + TenantHealthController + TenantHealthCommand are all resolvable and functional without any liip service present. The key assertion: the container compiles and the health endpoints/command work with zero liip_monitor.check dependency (HEALTH-07 absence direction). Both tests use SQLite consistent with the existing integration kernels; no external DB.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter "HealthEndpointsIntegrationTest|HealthChecksNoLiipTest"</automated>
  </verify>
  <acceptance_criteria>
    - vendor/bin/phpunit --filter "HealthEndpointsIntegrationTest|HealthChecksNoLiipTest" exits 0
    - GET /_tenancy/health/live returns HTTP 200 with Content-Type application/health+json (behavior — HEALTH-01 end-to-end)
    - GET /_tenancy/health/ready/{unknown} returns HTTP 404 with application/health+json body (behavior — D-06 end-to-end)
    - The no-liip test proves the checker/controller/command function with no liip_monitor.check service present (behavior — HEALTH-07 absence)
    - Tests reuse an existing integration kernel; no external DB (source assertion: SQLite only)
  </acceptance_criteria>
  <done>End-to-end HTTP tests confirm the imported routes resolve (live 200, ready health+json, unknown-slug 404) and the no-liip lane proves the self-contained surface works without the optional bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional package supply chain | Adding liip/monitor-bundle (+ transitive laminas/laminas-diagnostics) to require-dev pulls third-party code into the dev/test container. |
| liip runner -> tenant roster | The liip check enumerates tenants and reports connectivity; its result message could carry a DSN if not sanitized. |
| imported route -> public controller | Making the controller a public service exposes it to the router; only the imported routes reach it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-SC | Tampering | require-dev install of liip/monitor-bundle + laminas/laminas-diagnostics | mitigate | Both packages are Approved [VERIFIED: Packagist] in the RESEARCH.md Package Legitimacy Audit (13+ / 10+ yrs, 9M+ installs, canonical repos). No [ASSUMED]/[SUS]/[SLOP] packages -> no blocking human checkpoint required. composer validate --strict + the existing CI composer audit gate cover integrity. |
| T-33-04 | Information Disclosure | TenantConnectivityCheck result message | mitigate | The liip check builds its message from sanitized report data only (delegates to HealthResponseSanitizer / already-safe report fields); never embeds a raw DSN. |
| T-33-07 | Availability | HealthCheckIntegrationPass guard | mitigate | class_exists/interface_exists early-return means absence of liip causes no error; the no-liip integration test proves the self-contained surface still works (HEALTH-07). |
| T-33-CTX | Tampering | TenantConnectivityCheck | mitigate | The liip adapter delegates to TenantHealthChecker and never sets/clears TenantContext directly (Anti-pattern H-A7); the checker's finally owns the clear. |

Package legitimacy gate: RESEARCH.md contains a well-formed "## Package Legitimacy Audit" table; both installed packages are VERIFIED. Gate satisfied; no pre-install checkpoint task needed.
</threat_model>

<verification>
- vendor/bin/phpunit full suite green (unit + integration; SQLite :memory:)
- vendor/bin/phpstan analyse --memory-limit=512M clean at level 9
- vendor/bin/php-cs-fixer check --diff clean
- composer validate --strict passes
- No-Doctrine lane stays green: the controller/command/checker take TenantProviderInterface via nullOnInvalid() and do not import EntityManagerInterface (Pitfall 4)
- No-liip lane stays green: HealthCheckIntegrationPass self-guards; TenantConnectivityCheck is only referenced inside the guarded branch
</verification>

<success_criteria>
- health config node + tenancy.health.* params exist; no HTTP enabled flag (D-01)
- Controller (public) + command (console.command) wired; routes resolve end-to-end — HEALTH-01/HEALTH-02/HEALTH-06
- liip present -> a liip_monitor.check service is auto-registered (class_exists-guarded); liip absent -> no-op, self-contained surface still works — HEALTH-07
- Every liip result message sanitized (no raw DSN) — HEALTH-04
- liip/monitor-bundle added to require-dev + suggest (not a hard require)
- Full suite + PHPStan L9 + cs-fixer + composer validate all green; no-Doctrine and no-liip lanes green
</success_criteria>

<output>
Create `.planning/phases/33-health-checks/33-05-SUMMARY.md` when done.
</output>
