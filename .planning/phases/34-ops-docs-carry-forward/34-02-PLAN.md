---
phase: 34-ops-docs-carry-forward
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - scripts/docs-lint.sh
  - UPGRADE.md
autonomous: true
requirements: [DOC-21]

must_haves:
  truths:
    - "docs-lint.sh gains an ops-terms guard that fires (EXIT=1) on wrong/stale forms of the new ops commands and endpoint paths, docs/-scoped"
    - "docs-lint.sh still exits 0 against the correct ops pages authored in 34-01"
    - "UPGRADE.md has a 0.4 → 0.5 section documenting the isInMaintenance() BC break and the TenantMaintenanceConfigTrait migration path"
    - "The 0.4 → 0.5 section notes the break is a no-op for adopters who use the trait or return false"
  artifacts:
    - path: "scripts/docs-lint.sh"
      provides: "New-ops-terms negative guard using the existing check() helper"
      contains: "OPS_TARGETS"
    - path: "UPGRADE.md"
      provides: "0.4 → 0.5 BC-break upgrade section"
      contains: "TenantMaintenanceConfigTrait"
  key_links:
    - from: "scripts/docs-lint.sh"
      to: "docs/ops/"
      via: "check() invocations scoped to OPS_TARGETS=(docs/)"
      pattern: "check .* \"\\$\\{OPS_TARGETS\\[@\\]\\}\""
    - from: "UPGRADE.md"
      to: "TenantInterface::isInMaintenance()"
      via: "BC-break section documenting the new interface method"
      pattern: "isInMaintenance"
---

<objective>
Extend `scripts/docs-lint.sh` with a docs/-scoped guard for the new ops terms (D-04) and add a
`0.4 → 0.5` section to `UPGRADE.md` covering the `TenantInterface::isInMaintenance()` BC break and
the `TenantMaintenanceConfigTrait` migration path (D-05).

Purpose: DOC-21 Success Criteria 1 (docs-lint guards the new ops terms) and 2 (UPGRADE 0.4→0.5).
Output: an extended `scripts/docs-lint.sh` (still exits 0) and a new `## 0.4 to 0.5` section at the
top of `UPGRADE.md`.

Depends on 34-01: the ops pages must exist so the docs-lint run proves green against the real
authored content, and so the guard's docs/-scope has the actual pages to protect.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ops-docs-carry-forward/34-CONTEXT.md
@.planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md

<interfaces>
<!-- docs-lint.sh check() is a NEGATIVE guard: it sets EXIT=1 when the pattern IS found.
     Guard WRONG/stale forms only — NEVER the correct term (that would fire on every correct doc). -->

check() helper (scripts/docs-lint.sh lines 22-33):
  check(pattern, desc, targets...) — grep -rnE across targets; on match: prints ERROR, sets EXIT=1
Existing invocation style (lines 38-42), scoped to TARGETS=(docs/ src/Command/TenantInitCommand.php):
  check 'wrapperClass' "Found 'wrapperClass' (v0.1 DBAL approach — use doctrine.middleware tag)" "${TARGETS[@]}"

BC break to document (verified src/TenantInterface.php line 20):
  TenantInterface::isInMaintenance(): bool   — new required interface method (Phase 32)
  TenantMaintenanceConfigTrait — drop-in trait: adds `bool $inMaintenance = false` + isInMaintenance()
    returning false by default + the `in_maintenance` Doctrine column
</interfaces>

# The file being modified + the UPGRADE-section analog the executor must mirror:
@scripts/docs-lint.sh
@UPGRADE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ops-terms negative guard to docs-lint.sh</name>
  <files>scripts/docs-lint.sh</files>
  <read_first>
    - scripts/docs-lint.sh — the file to modify; lines 22-33 define `check()`, lines 36-42 are the existing `TARGETS` block + first five `check()` calls (the exact call style to copy)
    - .planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md §scripts/docs-lint.sh — the exact new-guard block to append and the Pitfall-1 warning (never guard the CORRECT term)
    - .planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md §Pitfall 1 and §docs-lint.sh Extension Strategy — the negative-vs-positive check reasoning
    - docs/ops/maintenance-mode.md and docs/ops/health-checks.md (authored in 34-01) — confirm the guarded WRONG forms do NOT appear (so the guard stays green)
  </read_first>
  <action>
    Append a new ops-terms guard block after the existing `sqlite://` check (line 42), before the
    `# D-15` bundles.php-section block. Define `OPS_TARGETS=(docs/)` (docs/-scoped only; UPGRADE.md and
    CHANGELOG.md are outside docs/ and thus excluded, matching the existing D-04/D-15 precedent). Add a
    header comment `# D-04 (Phase 34): Ops-terms consistency guards.` explaining these are NEGATIVE
    guards against WRONG/stale forms. Add at least three `check()` calls guarding WRONG forms that must
    never appear in correct docs (e.g. `tenancy:maintenance:activated` → use enable;
    `tenancy:maintenance:deactivated` → use disable; `health/liveness` → use `/_tenancy/health/live`;
    `cache_control_no_store` → underscore form of the `Cache-Control: no-store` header). Each call passes
    `"${OPS_TARGETS[@]}"` as the target array. CRITICAL: do NOT add a `check()` for a CORRECT term such
    as `/_tenancy/health/live` — `check()` fires EXIT=1 when the pattern IS found, so guarding the correct
    term would fail against the very docs authored in 34-01 (34-RESEARCH.md Pitfall 1). After editing,
    run the whole script and confirm it still exits 0 against the real ops pages.
  </action>
  <verify>
    <automated>bash scripts/docs-lint.sh; test $? -eq 0 && grep -q 'OPS_TARGETS' scripts/docs-lint.sh && grep -q 'tenancy:maintenance:activated' scripts/docs-lint.sh && [ "$(grep -cE 'check .+OPS_TARGETS' scripts/docs-lint.sh)" -ge 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/docs-lint.sh` defines `OPS_TARGETS=(docs/)` and has >= 3 `check ... "${OPS_TARGETS[@]}"` invocations
    - Every new guard matches a WRONG/stale form, not a correct ops term (no `check` for `/_tenancy/health/live`, `Retry-After`, `--parallel`, etc.)
    - `bash scripts/docs-lint.sh` exits 0 against the ops pages authored in 34-01
    - UPGRADE.md and CHANGELOG.md are NOT among the OPS_TARGETS
  </acceptance_criteria>
  <done>docs-lint.sh guards >= 3 wrong ops-term forms via check()/OPS_TARGETS and still exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add the UPGRADE.md 0.4 → 0.5 BC-break section</name>
  <files>UPGRADE.md</files>
  <read_first>
    - UPGRADE.md lines 252-353 (`## 0.2 to 0.3`) — the TenantInterface BC-break template: section intro, Migration path A (trait, recommended), Migration path B (manual), DB migration note
    - UPGRADE.md lines 11-138 (`## 0.3 to 0.4`) — the "No action required if ... not needed" no-op note idiom
    - UPGRADE.md line 3 (`## 0.4.0 to 0.4.1`) — the current top section; the new section inserts ABOVE it (newest-first ordering)
    - .planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md §UPGRADE.md — the exact section shape + insert position
    - The `<interfaces>` block — the `isInMaintenance()` / `TenantMaintenanceConfigTrait` surface
  </read_first>
  <action>
    Insert a new `## 0.4 to 0.5` section at the TOP of UPGRADE.md, immediately BEFORE the existing
    `## 0.4.0 to 0.4.1` section (newest-first ordering, matching the file). Structure it on the
    `## 0.2 to 0.3` template: a one-paragraph intro stating Phase 32 introduces per-tenant maintenance
    mode and the contract change is a BC break on `TenantInterface` (one new required method,
    `isInMaintenance(): bool`); a `### Migration path A: use TenantMaintenanceConfigTrait (recommended)`
    subsection showing `use TenantMaintenanceConfigTrait;` in a Tenant class (the trait provides the
    `isInMaintenance()` method + a `bool $inMaintenance = false` property + the `in_maintenance` Doctrine
    column) and a note that `bin/console doctrine:migrations:diff` after adding the trait generates the
    column migration; a `### Migration path B: manual implementation` subsection showing a minimal
    hand-written `public function isInMaintenance(): bool { return false; }`; and a no-op note per D-05:
    no action is required if you do not use maintenance mode — any class returning `false` from
    `isInMaintenance()` is fully v0.5-compatible, and the trait makes the break a no-op for adopters who
    use it. Guard Doctrine references with the bundle's optional-Doctrine convention in prose (the trait's
    column mapping only matters when Doctrine ORM is present). UPGRADE.md is NOT scanned by docs-lint, so
    no lint constraints apply here — but keep code samples using `declare(strict_types=1)`-consistent PHP.
  </action>
  <verify>
    <automated>grep -qE '^## 0.4 to 0.5' UPGRADE.md && grep -q 'isInMaintenance' UPGRADE.md && grep -q 'TenantMaintenanceConfigTrait' UPGRADE.md && grep -qi 'migration path' UPGRADE.md && awk '/^## 0.4 to 0.5/{s=NR} /^## 0.4.0 to 0.4.1/{e=NR} END{exit !(s>0 && e>0 && s<e)}' UPGRADE.md</automated>
  </verify>
  <acceptance_criteria>
    - `UPGRADE.md` contains a `## 0.4 to 0.5` heading positioned ABOVE the `## 0.4.0 to 0.4.1` section
    - The section contains `isInMaintenance`, `TenantMaintenanceConfigTrait`, both a Migration path A (trait) and Migration path B (manual), and a DB-migration note (`doctrine:migrations:diff`)
    - The section states the break is a no-op for adopters using the trait or returning `false`
  </acceptance_criteria>
  <done>UPGRADE.md has a complete, correctly-positioned 0.4 → 0.5 BC-break section following the 0.2→0.3 template.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → merge | docs-lint.sh is a CI gate; a broken/no-op guard silently weakens doc quality enforcement |
| UPGRADE.md → adopter code | Upgrade recipe is copy-pasted into adopter Tenant classes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Tampering | docs-lint.sh ops guard mistakenly guards a CORRECT term | mitigate | Guards match WRONG/stale forms only (Task 1 acceptance criteria + Pitfall-1 read); the guard is proven green against the real 34-01 pages before commit |
| T-34-05 | Repudiation / Correctness | UPGRADE recipe understates the BC break, leaving adopters with a broken Tenant class | mitigate | Section documents both trait and manual paths + the DB migration step; the "no-op" note is scoped to adopters who actually satisfy the new method, not a blanket "nothing changed" |

No `high`-severity threats — this is a CI-script + docs plan. No credentials, no runtime code, no input handling.
</threat_model>

<verification>
- `bash scripts/docs-lint.sh` exits 0 (new guard added, all ops pages pass)
- `scripts/docs-lint.sh` has `OPS_TARGETS=(docs/)` and >= 3 new `check()` calls guarding wrong forms
- `UPGRADE.md` has a `## 0.4 to 0.5` section above `## 0.4.0 to 0.4.1` with the BC-break content
- The extended docs-lint must not regress the existing v0.1-stale-term / bundles.php guards
</verification>

<success_criteria>
- docs-lint.sh guards the new ops terms (negative guards) and still exits 0 (DOC-21 Success Criterion 1)
- UPGRADE.md documents the 0.4 → 0.5 isInMaintenance BC break + TenantMaintenanceConfigTrait path (DOC-21 Success Criterion 2)
</success_criteria>

<output>
Create `.planning/phases/34-ops-docs-carry-forward/34-02-SUMMARY.md` when done
</output>
