---
phase: 34-ops-docs-carry-forward
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contributor-guide/test-infrastructure.md
  - .planning/phases/31-parallel-migrations/31-VALIDATION.md
autonomous: true
requirements: [GOV-02]

must_haves:
  truths:
    - "A contributor reading the test-infrastructure page finds an explicit, discoverable Nyquist VALIDATION.md policy: it is advisory-only and the live green PHPUnit suite is the real phase gate"
    - "The policy explains that nyquist_validation:true in config.json governs the discovery workflow, not a blocking gate"
    - "Phase 31 has a VALIDATION.md so the v0.5 phase set (31/32/33) is uniform"
  artifacts:
    - path: "docs/contributor-guide/test-infrastructure.md"
      provides: "Nyquist VALIDATION.md advisory-only policy note (GOV-02, D-08/D-09)"
      contains: "VALIDATION.md"
    - path: ".planning/phases/31-parallel-migrations/31-VALIDATION.md"
      provides: "Retrospective Phase 31 validation artifact (D-10 backfill)"
      contains: "status: complete"
  key_links:
    - from: "docs/contributor-guide/test-infrastructure.md"
      to: ".planning/config.json nyquist_validation"
      via: "policy note referencing the config flag as discovery-only"
      pattern: "nyquist_validation|advisory|discovery-only"
---

<objective>
Make the Nyquist `VALIDATION.md` enforcement policy explicit (GOV-02, D-08): document the
discovery-only / advisory-non-blocking stance in the contributor guide (D-09), and backfill Phase 31's
`VALIDATION.md` so the v0.5 phase set is uniform (D-10). No gate is added — `nyquist_validation: true`
in `.planning/config.json` stays as-is; the decision is to state the de-facto policy, not change it.

Purpose: GOV-02 Success Criterion 4 — a written Nyquist enforcement policy for v0.5 is in place,
documenting the discovery-only stance explicitly and discoverably.
Output: a policy section in `docs/contributor-guide/test-infrastructure.md` and a retrospective
`.planning/phases/31-parallel-migrations/31-VALIDATION.md`.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ops-docs-carry-forward/34-CONTEXT.md
@.planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md
@.planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md

<interfaces>
<!-- Verified facts (34-RESEARCH.md §GOV-02 Placement). -->
- Phase 31 (completed 2026-06-26) has NO VALIDATION.md; Phase 32 and 33 each have one → the discovery-only stance is already de-facto.
- `.planning/config.json` has `workflow.nyquist_validation: true` — governs the discovery workflow (surfacing gaps for human review), NOT a phase-complete block. Stays as-is (D-08).
- `docs/contributor-guide/test-infrastructure.md` exists (~9.6 KB): covers PHPUnit, test suites, test kernels, setUpBeforeClass patterns — the natural home for a policy note (D-09).
- Phase 32 VALIDATION.md frontmatter: `nyquist_compliant: false / wave_0_complete: false / status: complete` (advisory format to mirror for the Phase 31 backfill).
</interfaces>

# The file being modified + the backfill analog:
@docs/contributor-guide/test-infrastructure.md
@.planning/phases/32-maintenance-mode/32-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Nyquist VALIDATION.md policy note to the contributor guide</name>
  <files>docs/contributor-guide/test-infrastructure.md</files>
  <read_first>
    - docs/contributor-guide/test-infrastructure.md — the file to extend; append a new `##` section after the existing test-pattern documentation (see 34-PATTERNS.md §test-infrastructure.md for the section skeleton + suggested wording)
    - .planning/phases/34-ops-docs-carry-forward/34-CONTEXT.md D-08/D-09 — the exact policy: advisory-only, live green suite is the real gate, config flag governs discovery not a block
    - .planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md §GOV-02 Placement — the de-facto history (Phase 31 shipped with no VALIDATION.md) that makes this policy accurate
    - .planning/config.json — confirm `workflow.nyquist_validation: true` is the flag to reference
  </read_first>
  <action>
    Append a new section `## Nyquist Validation Artifacts (\`VALIDATION.md\`)` to
    `docs/contributor-guide/test-infrastructure.md` after the existing test-pattern content. State
    explicitly (D-08): each v0.5 phase directory MAY contain a `VALIDATION.md` — a lightweight
    requirement→signal coverage artifact that is ADVISORY ONLY; the live green PHPUnit suite is the real
    phase gate. Explain that `nyquist_validation: true` in `.planning/config.json` governs the Nyquist
    DISCOVERY workflow (surfacing coverage gaps for a human to review), NOT a blocking phase-complete gate;
    a phase that ships a green PHPUnit suite is complete regardless of whether a `VALIDATION.md` was
    authored. Note this was the de-facto policy in v0.4 and is now made explicit for v0.5. Cross-reference
    the format examples at `.planning/phases/32-maintenance-mode/32-VALIDATION.md` and
    `.planning/phases/33-health-checks/33-VALIDATION.md`. Keep the prose concise and in the existing page
    voice. If a new dedicated page were created instead, mkdocs.yml would need a nav entry — but appending
    to the existing (already-nav-registered) test-infrastructure.md needs no mkdocs change; use the
    existing page.
  </action>
  <verify>
    <automated>grep -q 'VALIDATION.md' docs/contributor-guide/test-infrastructure.md && grep -qiE 'advisory|discovery-only|discovery workflow' docs/contributor-guide/test-infrastructure.md && grep -qi 'green' docs/contributor-guide/test-infrastructure.md && grep -q 'nyquist_validation' docs/contributor-guide/test-infrastructure.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/contributor-guide/test-infrastructure.md` contains a section mentioning `VALIDATION.md`, the words `advisory`/`discovery-only`, that the live green suite is the real gate, and the `nyquist_validation` config flag
    - The note states VALIDATION.md is NOT a blocking gate (discovery workflow only)
    - `bash scripts/docs-lint.sh` still exits 0 (the note introduces no stale v0.1 terms; note it is under docs/ so it IS scanned)
  </acceptance_criteria>
  <done>The contributor guide has an explicit, discoverable Nyquist VALIDATION.md advisory-only policy note.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill Phase 31 VALIDATION.md for artifact consistency</name>
  <files>.planning/phases/31-parallel-migrations/31-VALIDATION.md</files>
  <read_first>
    - .planning/phases/32-maintenance-mode/32-VALIDATION.md — the frontmatter + body structure to mirror (nyquist_compliant/wave_0_complete/status fields)
    - .planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md §31-VALIDATION.md — the exact retrospective frontmatter + body to write
    - .planning/phases/31-parallel-migrations/31-VERIFICATION.md — confirm Phase 31 completed/verified 2026-06-26 (the fact the retrospective records)
  </read_first>
  <action>
    Create `.planning/phases/31-parallel-migrations/31-VALIDATION.md` as a minimal RETROSPECTIVE artifact
    mirroring the Phase 32 VALIDATION.md frontmatter schema. Frontmatter: `nyquist_compliant: false`,
    `wave_0_complete: true`, `status: complete` (Phase 31 shipped and was verified 2026-06-26 via its
    VERIFICATION.md; existing test infrastructure covered its requirements). Body: an H1
    `# Phase 31 Parallel Migrations — Validation (Retrospective)`, a status line noting it completed and
    was verified 2026-06-26 via `31-VERIFICATION.md`, and a one-line policy note that this artifact is
    advisory only per `docs/contributor-guide/test-infrastructure.md` (cross-reference the Task-1 policy).
    This is a low-value polish artifact (D-10) — keep it short; do NOT fabricate detailed per-requirement
    coverage tables beyond what 31-VERIFICATION.md already recorded.
  </action>
  <verify>
    <automated>test -f .planning/phases/31-parallel-migrations/31-VALIDATION.md && grep -q 'status: complete' .planning/phases/31-parallel-migrations/31-VALIDATION.md && grep -qi 'retrospective\|advisory' .planning/phases/31-parallel-migrations/31-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/31-parallel-migrations/31-VALIDATION.md` exists with frontmatter `status: complete` and mirrors the Phase 32 schema
    - The body records Phase 31 completed/verified 2026-06-26 and notes the artifact is advisory-only (cross-referencing the contributor-guide policy)
    - The v0.5 phase set (31/32/33) now all have a VALIDATION.md
  </acceptance_criteria>
  <done>Phase 31 has a retrospective VALIDATION.md consistent with 32/33's advisory format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy doc → contributor behavior | The written policy sets the expectation for whether a missing VALIDATION.md blocks a phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Repudiation / Governance | An ambiguous policy lets a phase ship with genuinely-untested behavior under the "advisory" banner | accept | The policy explicitly names the live GREEN PHPUnit suite as the real gate (a phase still cannot ship red), so "advisory VALIDATION.md" never means "untested" — it means the coverage-mapping doc is optional, not that tests are optional |

No `high`-severity threats — this is documentation + a planning artifact, no runtime code, no credentials, no input handling.
</threat_model>

<verification>
- `docs/contributor-guide/test-infrastructure.md` has an explicit VALIDATION.md advisory-only policy referencing `nyquist_validation` and the green-suite gate
- `.planning/phases/31-parallel-migrations/31-VALIDATION.md` exists with `status: complete`
- `bash scripts/docs-lint.sh` exits 0 (the contributor-guide note is docs/-scanned)
</verification>

<success_criteria>
- A written Nyquist VALIDATION.md enforcement policy for v0.5 is in place, documenting the discovery-only stance explicitly and discoverably (GOV-02 Success Criterion 4)
- The v0.5 phase set is uniform (Phase 31 backfill, D-10)
</success_criteria>

<output>
Create `.planning/phases/34-ops-docs-carry-forward/34-04-SUMMARY.md` when done
</output>
