---
phase: 34-ops-docs-carry-forward
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/saas/composer.json
  - examples/saas/composer.lock
autonomous: false
requirements: [DEMO-02]

must_haves:
  truths:
    - "examples/saas/composer.json declares config.platform.php = 8.2.99 (pins Composer resolution to PHP 8.2)"
    - "examples/saas/composer.lock, regenerated under the pin, has zero packages requiring php >=8.4"
    - "composer install against the regenerated lock succeeds on the host despite the host running PHP > 8.2 (platform pin resolves the constraint)"
    - "The Dockerfile base image stays dunglas/frankenphp:1-php8.2-bookworm (unchanged)"
  artifacts:
    - path: "examples/saas/composer.json"
      provides: "config.platform.php pin to 8.2.99"
      contains: "platform"
    - path: "examples/saas/composer.lock"
      provides: "PHP-8.2-coherent regenerated lock"
      contains: "content-hash"
  key_links:
    - from: "examples/saas/composer.json"
      to: "examples/saas/composer.lock"
      via: "composer update regeneration under config.platform.php"
      pattern: "\"platform\""
---

<objective>
Resolve the `examples/saas` PHP-version drift (DEMO-02): the lock was generated on a PHP-8.4 host
without a platform pin, so 15 packages resolved to versions requiring PHP >=8.4 while the Dockerfile
and CI smoke both run PHP 8.2 — meaning `composer install` on the 8.2 CI runner would fail. Add
`config.platform.php = "8.2.99"` to `examples/saas/composer.json` and regenerate the lock so nothing
requires >=8.4 (D-06). Do NOT bump the Dockerfile.

Purpose: DEMO-02 Success Criterion 3 — `bin/smoke.sh` passes on a single coherent PHP version (8.2)
after the Dockerfile ↔ composer.lock drift is reconciled.
Output: `examples/saas/composer.json` with the platform pin, and a regenerated
`examples/saas/composer.lock` with zero `php: >=8.4` requirements.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ops-docs-carry-forward/34-CONTEXT.md
@.planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md
@.planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md

<interfaces>
<!-- Verified environment facts (this planning session). -->
- Host PHP is > 8.2 (8.5.x); `config.platform.php` makes Composer RESOLVE as if PHP were 8.2 regardless of host.
- `.github/workflows/demo-smoke.yml`: sets php-version '8.2', runs `composer install --no-interaction --no-progress --prefer-dist --no-scripts` (line 34), then `docker compose up -d --wait --build`, then `bash bin/smoke.sh` (line 66). The 8.2 `composer install` is exactly what fails today against the 8.4-locked deps.
- smoke.sh (examples/saas/bin/smoke.sh) needs Docker + a running container on port 80 → NOT runnable locally; CI is the real smoke gate.
- No production dep genuinely needs 8.4 (34-RESEARCH.md §DEMO-02 Investigation): all 15 are PHP-version-sensitive resolutions that down-pin to 7.4/last-8.2 forms once the platform is pinned.

Current examples/saas/composer.json "config" block (verified):
  { "allow-plugins": {"symfony/runtime": true, "symfony/flex": false}, "optimize-autoloader": true, "sort-packages": true }
Target: insert "platform": {"php": "8.2.99"} inside "config" (alphabetical: allow-plugins → optimize-autoloader → platform → sort-packages, per sort-packages:true convention).
</interfaces>

# The file being modified + its analog:
@examples/saas/composer.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin examples/saas to PHP 8.2 and regenerate its lock</name>
  <files>examples/saas/composer.json, examples/saas/composer.lock</files>
  <read_first>
    - examples/saas/composer.json lines 44-51 — the existing `config` block (34-PATTERNS.md §composer.json shows the exact before/after)
    - .planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md §DEMO-02 Investigation — the 15 offending packages, the root cause, and the exact fix path (add config.platform.php, `composer update` WITHOUT --no-dev)
    - .planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md §Common Pitfalls (Pitfalls 2 and 5) — run without `--no-dev`; the platform pin only affects RESOLUTION, so re-run `composer update` and verify the lock has no >=8.4 requirement
    - .github/workflows/demo-smoke.yml — confirms the 8.2 composer install + smoke gate that this fix unblocks
  </read_first>
  <action>
    Edit `examples/saas/composer.json`: inside the existing `"config"` block, add
    `"platform": {"php": "8.2.99"}` between `"optimize-autoloader"` and `"sort-packages"` (alphabetical
    key order per the `sort-packages: true` convention). Do not add `platform` at the top level — it
    goes INSIDE `config`. Then regenerate the lock from within the sub-project:
    `cd examples/saas && composer update --no-interaction --no-progress` (run WITHOUT `--no-dev` so
    dev-only packages also down-resolve — 34-RESEARCH.md Pitfall 2). The platform pin instructs Composer
    to resolve as PHP 8.2 even though the host runs 8.5, so `symfony/*` constraints `^7.4||^8.0` resolve to
    their 7.4.x forms and `doctrine/instantiator ^1.3||^2` resolves to 2.0.x (last `^8.2` version). If
    the host's PHP extensions block resolution, use `--ignore-platform-reqs` ONLY for `ext-*` mismatches,
    never to mask a real php-version constraint (the whole point of the pin is to satisfy php resolution).
    After update, audit the regenerated lock: every package's `require.php` (if present) must resolve to a
    `>=8.2`-compatible constraint and NONE may require `>=8.4`/`^8.4`. If any PRODUCTION (non-dev)
    dependency still genuinely requires 8.4, STOP and escalate per D-07 (research expects none). Run
    `cd examples/saas && composer validate --no-check-publish` to confirm the composer.json ↔ composer.lock
    pair is consistent, and `composer install --dry-run --no-interaction` to confirm the pinned lock
    installs cleanly.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('examples/saas/composer.json')); assert d['config'].get('platform',{}).get('php')=='8.2.99', d['config'].get('platform'); lock=json.load(open('examples/saas/composer.lock')); fails=[p['name'] for p in lock['packages']+lock.get('packages-dev',[]) if '8.4' in p.get('require',{}).get('php','')]; assert not fails, 'still require 8.4: '+repr(fails); print('OK: platform pinned, 0 packages require >=8.4')"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/saas/composer.json` `config.platform.php` == `"8.2.99"`
    - `examples/saas/composer.lock` has ZERO packages (in `packages` or `packages-dev`) whose `require.php` contains `8.4`
    - `cd examples/saas && composer validate --no-check-publish` reports the lock is up to date / consistent with composer.json
    - `examples/saas/Dockerfile` still references `dunglas/frankenphp:1-php8.2-bookworm` (unchanged)
  </acceptance_criteria>
  <done>The pin is in composer.json, the lock is regenerated with no >=8.4 requirements, and composer validate confirms consistency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CI/human verification that bin/smoke.sh is green on PHP 8.2</name>
  <action>
    This is a human/CI verification checkpoint — no code is written here. The executor has already
    regenerated the lock and passed the local automated gate; smoke.sh cannot be run locally (needs Docker
    + a live FrankenPHP container on port 80, and it exercises PHP 8.2 while the host runs 8.5). Confirm the
    DEMO-02 fix passes the CI smoke gate (or a local docker smoke run) before treating DEMO-02 as closed.
  </action>
  <what-built>
    `examples/saas/composer.json` now pins `config.platform.php = 8.2.99` and `examples/saas/composer.lock`
    was regenerated so no package requires PHP >=8.4. The local automated gate (lock audit +
    `composer validate` + `composer install --dry-run`) is green. The FULL `bin/smoke.sh` cannot run
    locally — it needs Docker + a running FrankenPHP container on port 80, and it exercises PHP 8.2 in the
    container (the host runs 8.5). The real smoke gate is CI (`.github/workflows/demo-smoke.yml`, which sets
    php-version 8.2, runs `composer install`, `docker compose up --build`, then `bash bin/smoke.sh`).
  </what-built>
  <how-to-verify>
    Confirm the DEMO-02 fix passes the CI smoke gate on PHP 8.2. Either:
    1. After this phase's commit lands on a branch/PR, watch the `demo-smoke` GitHub Actions job go
       green (the `Install demo deps (composer)` step must succeed on the 8.2 runner — this is the step
       that fails today against the 8.4-locked deps), OR
    2. If you have Docker locally: `cd examples/saas && docker compose up -d --wait --build && bash bin/smoke.sh`
       and confirm it prints the per-tenant marker assertions and exits 0, then `docker compose down -v`.
    Expected: the demo builds on the frankenphp 8.2 base, boots, and smoke.sh's landlord + per-tenant
    assertions pass.
  </how-to-verify>
  <verify>
    <human-check>demo-smoke CI job green on PHP 8.2 (composer install step succeeds against the regenerated lock), or a local docker smoke run exits 0</human-check>
  </verify>
  <done>The demo-smoke CI job is green on PHP 8.2 (or a local docker smoke passed), confirming the drift is reconciled.</done>
  <resume-signal>Type "approved" once the demo-smoke CI job is green (or the local docker smoke passed), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer.lock → CI/adopter install | The lock pins exact dependency versions installed in the demo container and by anyone running the demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Tampering (supply chain) | Regenerated composer.lock pulls in unexpected/new package versions | mitigate | Regeneration only DOWN-pins existing packages to their last-8.2-compatible versions; no new top-level requires are added; the lock audit + `composer validate` confirm no unexpected package appears, and the milestone-level `composer audit` CI gate (existing) still applies |
| T-34-07 | Denial of Service | Pinning to a too-old patch strands a security fix | accept | `8.2.99` simulates any 8.2.* patch for RESOLUTION only; the container still runs the real frankenphp 8.2 image and receives OS/PHP patches; production deps are unaffected (demo-only) |

No `high`-severity threats — this is a demo sub-project lock regeneration with no bundle-runtime impact. The "net-zero new production deps" constraint is preserved (no bundle deps change).
</threat_model>

<verification>
- `examples/saas/composer.json` has `config.platform.php = 8.2.99`
- `examples/saas/composer.lock` audit: zero packages require php >=8.4 (automated python gate)
- `cd examples/saas && composer validate --no-check-publish` passes
- `examples/saas/Dockerfile` unchanged (still frankenphp 8.2)
- CI `demo-smoke` job green on PHP 8.2 (human-check / CI-deferred — smoke.sh needs Docker + a live container, not runnable on the host 8.5)
</verification>

<success_criteria>
- The examples/saas Dockerfile ↔ composer.lock PHP-version drift is reconciled to a single coherent PHP 8.2 (DEMO-02 Success Criterion 3)
- `bin/smoke.sh` passes on PHP 8.2 in CI after the fix
</success_criteria>

<output>
Create `.planning/phases/34-ops-docs-carry-forward/34-03-SUMMARY.md` when done
</output>
