---
phase: 34-ops-docs-carry-forward
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/Command/SharedEntityResyncCommandTest.php
  - tests/Unit/PHPStan/ExtensionInstallerContractTest.php
autonomous: true
requirements: [QA-01]

must_haves:
  truths:
    - "A regression test proves the tenancy:shared:resync confirm-YES branch proceeds to apply (not just that --force bypasses and default-no aborts)"
    - "A regression test proves the PHPStan extension-installer zero-config auto-load contract: composer.json#extra.phpstan.includes references extension.neon, the file exists, and it declares all three rule classes"
    - "Both new tests are green and the full PHPUnit suite stays green"
  artifacts:
    - path: "tests/Unit/Command/SharedEntityResyncCommandTest.php"
      provides: "SHARE-02-c confirm-yes regression test (Phase 26 UAT closure)"
      contains: "setInputs"
    - path: "tests/Unit/PHPStan/ExtensionInstallerContractTest.php"
      provides: "PHPStan extension-installer metadata contract test (Phase 28 UAT closure)"
      contains: "extra"
  key_links:
    - from: "tests/Unit/Command/SharedEntityResyncCommandTest.php"
      to: "src/Command/SharedEntityResyncCommand.php confirm gate"
      via: "CommandTester::setInputs(['yes']) + interactive execute"
      pattern: "setInputs\\(\\['yes'\\]\\)"
    - from: "tests/Unit/PHPStan/ExtensionInstallerContractTest.php"
      to: "composer.json extra.phpstan.includes + extension.neon"
      via: "metadata + file-content assertions"
      pattern: "extension.neon"
---

<objective>
Close the two open v0.4 `human_needed` UAT items as permanent regression tests (QA-01, D-11), each
proving the gap is CLOSED (behavior exercised), not merely opened:
- Phase 26: a `tenancy:shared:resync` test that confirms the interactive TTY confirm-YES path proceeds
  to apply (the missing branch — `--force` bypass and default-no abort are already covered).
- Phase 28: a test proving the PHPStan extension-installer zero-config auto-load contract
  (`extra.phpstan.includes` references `extension.neon`; the file exists and declares the three rules).

Purpose: QA-01 Success Criterion 5 — both v0.4 `human_needed` UAT items are closed, each converted to a
code-level testability seam.
Output: a new test method in `tests/Unit/Command/SharedEntityResyncCommandTest.php` and a new
`tests/Unit/PHPStan/ExtensionInstallerContractTest.php`.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ops-docs-carry-forward/34-CONTEXT.md
@.planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md
@.planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md

<interfaces>
<!-- Verified seams (34-RESEARCH.md §QA-01 Seam Analysis). -->

Phase 26 — src/Command/SharedEntityResyncCommand.php:
- line 170: `$isForce = (bool) $input->getOption('force');`
- line 171: `if (!$isForce && !$io->confirm('Proceed with live resync?', false)) { return Command::SUCCESS; }` (clean abort on decline)
- --dry-run exits SUCCESS BEFORE the confirm gate (no prompt in dry-run)
Existing coverage in tests/Unit/Command/SharedEntityResyncCommandTest.php:
- testLiveRunPromptsConfirmDefaultNoAbortsCleanly() (uses ['interactive' => false] → confirm returns default false) — clean-abort covered
- testForceSkipsConfirmation() (uses ['--force' => true]) — bypass covered
GAP: no test uses setInputs(['yes']) + ['interactive' => true] to prove the confirm-YES path proceeds to applyRow().

Phase 28 — composer.json + extension.neon:
- composer.json lines 82-85: `"extra": {"phpstan": {"includes": ["extension.neon"]}}` (what phpstan/extension-installer reads)
- composer.json lines 88-91: `"allow-plugins": {"phpstan/extension-installer": true}`
- extension.neon declares 3 rule classes under phpstan.rules.rule: MutualExclusionRule, TenantIdDriftRule, SharedEntityLeakRule
Existing tests in tests/Unit/PHPStan/Rule/ load extension.neon via getAdditionalConfigFiles() — they prove the rules WORK, NOT that they AUTO-LOAD via the installer metadata.
GAP: no test proves the zero-config auto-load contract (the extra.phpstan.includes metadata + file existence + rule declarations).
Path from tests/Unit/PHPStan/ to package root: `__DIR__.'/../../../'`.

Pitfall (Pitfall 3): setInputs takes STRING tokens — use ['yes'], NOT [true] or [1]; SymfonyStyle::confirm() reads a raw string from stdin.
</interfaces>

# The files being modified + their analogs:
@tests/Unit/Command/SharedEntityResyncCommandTest.php
@tests/Unit/PHPStan/Rule/MutualExclusionRuleTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the confirm-YES regression test to SharedEntityResyncCommandTest</name>
  <files>tests/Unit/Command/SharedEntityResyncCommandTest.php</files>
  <behavior>
    - Given a live (non-dry-run) resync with one tenant and one classifiable shared row, when the
      interactive confirm prompt receives `yes`, then `applyRow()` IS called and the command exits SUCCESS.
    - Distinguishes from existing coverage: default-no (interactive:false) aborts BEFORE applyRow; --force
      bypasses the prompt. The new test proves the third branch (explicit confirm proceeds).
  </behavior>
  <read_first>
    - tests/Unit/Command/SharedEntityResyncCommandTest.php lines 105-181 — the makeCommand()/makeTenant()/wireSharedClasses() harness and the two existing confirm tests (testLiveRunPromptsConfirmDefaultNoAbortsCleanly, testForceSkipsConfirmation) that are the structural predecessors
    - src/Command/SharedEntityResyncCommand.php lines 164-173 — the confirm gate + applyRow call path being exercised
    - .planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md §SharedEntityResyncCommandTest.php — the exact new method body, placement, and the setInputs(['yes']) pitfall
    - The `<interfaces>` block — verified seam + Pitfall 3 (string tokens, not booleans)
  </read_first>
  <action>
    Add a new test method `testLiveRunConfirmYesProceedsToApply()` to
    `tests/Unit/Command/SharedEntityResyncCommandTest.php`, placed after
    `testLiveRunPromptsConfirmDefaultNoAbortsCleanly()` and before `testForceSkipsConfirmation()`. Use the
    existing harness (`makeTenant`, `wireSharedClasses`, `makeCommand`, the mocked `$this->copier`,
    `$this->tenantProvider`, `$this->registry`). Configure the copier to classify a row (e.g. `insert`) and
    set a mock expectation that `applyRow()` is called at least once — this is the load-bearing assertion
    that proves the confirm-YES branch reached the apply pass. Drive it with a `CommandTester`:
    `$tester->setInputs(['yes'])` (STRING token per Pitfall 3, not `[true]`) then
    `$tester->execute([], ['interactive' => true])`, asserting `Command::SUCCESS`. Keep a docblock tagging
    the UAT item (`SHARE-02-c`). Follow @Symfony cs-fixer conventions (no unused imports; the existing file
    already imports CommandTester/EntityManagerInterface — reuse them, do not re-import).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter testLiveRunConfirmYesProceedsToApply tests/Unit/Command/SharedEntityResyncCommandTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/Command/SharedEntityResyncCommandTest.php` contains `testLiveRunConfirmYesProceedsToApply()` using `setInputs(['yes'])` and `['interactive' => true]`
    - The test asserts `applyRow()` is invoked (proving the confirm-YES branch proceeds, not just that the command exits 0)
    - `vendor/bin/phpunit --filter testLiveRunConfirmYesProceedsToApply` passes
    - `vendor/bin/phpunit tests/Unit/Command/SharedEntityResyncCommandTest.php` (whole file) stays green
  </acceptance_criteria>
  <done>The confirm-YES branch is exercised by a passing regression test tagged SHARE-02-c.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the PHPStan extension-installer contract test</name>
  <files>tests/Unit/PHPStan/ExtensionInstallerContractTest.php</files>
  <behavior>
    - `composer.json` `extra.phpstan.includes` contains `extension.neon` (the key phpstan/extension-installer reads for zero-config auto-load).
    - `extension.neon` exists at the package root (the path declared in the includes metadata).
    - `extension.neon` declares all three rule classes: MutualExclusionRule, TenantIdDriftRule, SharedEntityLeakRule.
  </behavior>
  <read_first>
    - tests/Unit/PHPStan/Rule/MutualExclusionRuleTest.php lines 1-30 — namespace, strict_types, #[Group] attribute, and the extension.neon relative-path idiom (the new test sits one level shallower)
    - .planning/phases/34-ops-docs-carry-forward/34-PATTERNS.md §ExtensionInstallerContractTest.php — the exact class skeleton and the three Option-A test-body methods (file existence + string-contains, no live PHPStan invocation)
    - .planning/phases/34-ops-docs-carry-forward/34-RESEARCH.md §QA-01 Phase 28 — Option A vs Option B; Option A (metadata contract) is recommended
    - The `<interfaces>` block — the composer.json metadata keys, the three rule class names, and the `__DIR__.'/../../../'` root path
  </read_first>
  <action>
    Create `tests/Unit/PHPStan/ExtensionInstallerContractTest.php` in namespace
    `Tenancy\Bundle\Tests\Unit\PHPStan` (drop the `Rule` sub-namespace — this is not a rule-execution test).
    It extends `PHPUnit\Framework\TestCase` (NOT `RuleTestCase`), with `declare(strict_types=1)`, marked
    `#[Group('phpstan-extension')]` and `final`. Implement the Option-A metadata-contract tests:
    (1) `testComposerJsonDeclaresExtensionNeonInPhpstanIncludes()` — json_decode the root `composer.json`
    (`__DIR__.'/../../../composer.json'`, with JSON_THROW_ON_ERROR) and assert
    `extra.phpstan.includes` contains `extension.neon`; (2) `testExtensionNeonExistsAtDeclaredPath()` —
    assertFileExists on `__DIR__.'/../../../extension.neon'`; (3)
    `testExtensionNeonDeclaresThreeRuleClasses()` — file_get_contents the neon and
    assertStringContainsString for `MutualExclusionRule`, `TenantIdDriftRule`, `SharedEntityLeakRule`. No
    live PHPStan invocation, no `nette/neon` API dependency (plain string-contains per 34-RESEARCH.md).
    Follow @Symfony cs-fixer (only import what is used: TestCase + the Group attribute).
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/PHPStan/ExtensionInstallerContractTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/PHPStan/ExtensionInstallerContractTest.php` exists, extends `TestCase`, has `declare(strict_types=1)`, and is in namespace `Tenancy\Bundle\Tests\Unit\PHPStan`
    - It asserts `extra.phpstan.includes` contains `extension.neon`, that `extension.neon` exists, and that it names all three rule classes (proves the auto-load contract, not just file existence)
    - `vendor/bin/phpunit tests/Unit/PHPStan/ExtensionInstallerContractTest.php` passes
  </acceptance_criteria>
  <done>The PHPStan zero-config auto-load contract is proven by a passing metadata contract test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → release confidence | These regression tests are the standing proof that two previously-manual UAT behaviors stay working |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-09 | Repudiation / Correctness | A weak test asserts only the exit code and "opens" the gap without proving it closed | mitigate | Task 1 asserts `applyRow()` IS invoked (exercises the confirm-YES branch), not merely `Command::SUCCESS`; Task 2 asserts the metadata contract + rule declarations, not merely file existence — both satisfy the "prove the gap is closed, not just opened" principle (D-11) |
| T-34-10 | Tampering | Confirm-gate bypass (`--force`) could be tested as the only path, masking the interactive requirement | mitigate | The new test drives `['interactive' => true]` with `setInputs(['yes'])`, exercising the genuine TTY confirm path distinct from the already-covered `--force` bypass |

No `high`-severity threats — PHPUnit tests over existing seams; no runtime code changes, no credentials, no input handling beyond the mocked CommandTester stream.
</threat_model>

<verification>
- `vendor/bin/phpunit --filter testLiveRunConfirmYesProceedsToApply tests/Unit/Command/SharedEntityResyncCommandTest.php` passes
- `vendor/bin/phpunit tests/Unit/PHPStan/ExtensionInstallerContractTest.php` passes
- `vendor/bin/phpunit` (full suite) stays green
- `vendor/bin/phpstan analyse --memory-limit=512M` clean and `vendor/bin/php-cs-fixer check --diff` clean (new test files conform to L9 + @Symfony)
</verification>

<success_criteria>
- Both v0.4 human_needed UAT items are closed as passing regression tests, each proving the behavior is exercised (QA-01 Success Criterion 5, D-11)
- Full PHPUnit suite + PHPStan L9 + cs-fixer stay green
</success_criteria>

<output>
Create `.planning/phases/34-ops-docs-carry-forward/34-05-SUMMARY.md` when done
</output>
