---
phase: 26-tenancy-shared-resync-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/Shared/SharedEntityCopierTest.php
  - tests/Unit/Command/SharedEntityResyncCommandTest.php
  - tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php
  - tests/Integration/SharedEntity/Support/MakeSharedEntityResyncServicesPublicPass.php
autonomous: true
requirements: [SHARE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The 4 Nyquist test files named in 26-VALIDATION.md exist and are loadable by PHPUnit 11"
    - "Each stub test is explicitly skipped (markTestSkipped) until its production class exists, so the suite stays green"
    - "MakeSharedEntityResyncServicesPublicPass exposes tenancy.shared_entity_copier + tenancy.command.shared_resync for integration test container access"
  artifacts:
    - path: "tests/Unit/Shared/SharedEntityCopierTest.php"
      provides: "Unit stub for SHARE-02-a + classify correctness"
      contains: "class SharedEntityCopierTest"
    - path: "tests/Unit/Command/SharedEntityResyncCommandTest.php"
      provides: "Unit stub for SHARE-02-b..g, SHARE-02-k"
      contains: "class SharedEntityResyncCommandTest"
    - path: "tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php"
      provides: "Integration stub for SHARE-02-h, -i, -l"
      contains: "class SharedEntityResyncCommandIntegrationTest"
    - path: "tests/Integration/SharedEntity/Support/MakeSharedEntityResyncServicesPublicPass.php"
      provides: "Compiler pass exposing new resync services"
      contains: "tenancy.command.shared_resync"
  key_links:
    - from: "tests/Integration/SharedEntity/Support/MakeSharedEntityResyncServicesPublicPass.php"
      to: "CompilerPassInterface"
      via: "implements"
      pattern: "implements CompilerPassInterface"
---

<objective>
Wave 0 Nyquist scaffolding. Create the 4 test files named in 26-VALIDATION.md as skip-guarded stubs so every downstream task in this phase has an `<automated>` verify target, and create the compiler pass that exposes the new copier + command services for integration tests.

Purpose: establish the feedback-sampling spine for SHARE-02 before any production code exists. Stubs must keep the full suite green (skip-guarded on `class_exists`/service presence) so partial-phase runs never go red for the wrong reason.
Output: 3 new test classes (1 unit copier, 1 unit command, 1 integration) + 1 new test-support compiler pass.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-tenancy-shared-resync-command/26-VALIDATION.md
@.planning/phases/26-tenancy-shared-resync-command/26-PATTERNS.md

<interfaces>
<!-- Service IDs the integration pass must expose (verified from src/TenancyBundle.php) -->
New service IDs introduced by this phase:
  - tenancy.shared_entity_copier        (Plan 26-02)
  - tenancy.command.shared_resync        (Plan 26-03)
Existing service IDs (already public via MakeSharedEntityServicesPublicPass):
  - tenancy.shared_entity_sync_subscriber
  - tenancy.shared_entity_write_protection
  - tenancy.context
  - doctrine.orm.landlord_entity_manager
  - doctrine
  - doctrine.dbal.tenant_connection

Working class FQCNs the stubs reference (do not exist yet — guard with class_exists):
  - Tenancy\Bundle\Shared\SharedEntityCopier               (Plan 26-02)
  - Tenancy\Bundle\Command\SharedEntityResyncCommand        (Plan 26-03)

Existing analog (copy its skip-guard + ContainerBuilder pattern verbatim):
  tests/Integration/SharedEntity/Support/MakeSharedEntityServicesPublicPass.php
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MakeSharedEntityResyncServicesPublicPass + reuse the existing kernel</name>
  <files>tests/Integration/SharedEntity/Support/MakeSharedEntityResyncServicesPublicPass.php</files>
  <read_first>
    - tests/Integration/SharedEntity/Support/MakeSharedEntityServicesPublicPass.php (exact analog — copy structure)
    - tests/Integration/SharedEntity/Support/SharedEntityFailureLoggingTestKernel.php (the kernel to reuse — extends SharedEntitySyncTestKernel, registers InjectRecordingLoggerPass in build())
    - tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php (base kernel: build() registers MakeSharedEntityServicesPublicPass + ReplaceWithStubMultiTenantProviderPass)
    - .planning/phases/26-tenancy-shared-resync-command/26-PATTERNS.md (the MakeSharedEntityResyncServicesPublicPass full pattern, lines 658-711)
  </read_first>
  <action>Create final class MakeSharedEntityResyncServicesPublicPass implements CompilerPassInterface in namespace Tenancy\Bundle\Tests\Integration\SharedEntity\Support. Its process(ContainerBuilder) iterates an $ids list and calls setPublic(true) on each, guarded by hasDefinition()/hasAlias() (tolerate absence — copier + command do not exist until Plans 26-02/26-03). Include these IDs: tenancy.shared_entity_copier, tenancy.command.shared_resync, tenancy.shared_entity_sync_subscriber, tenancy.shared_entity_write_protection, tenancy.context, doctrine.orm.landlord_entity_manager, doctrine, doctrine.dbal.tenant_connection. Do NOT create a new kernel — the integration test (Task 3) reuses SharedEntityFailureLoggingTestKernel; the pass is registered by the integration test class via a one-line subclass OR by adding the new pass IDs to the existing MakeSharedEntityServicesPublicPass — prefer the dedicated new pass class per 26-PATTERNS.md.</action>
  <verify>
    <automated>php -l tests/Integration/SharedEntity/Support/MakeSharedEntityResyncServicesPublicPass.php</automated>
  </verify>
  <acceptance_criteria>
    - MakeSharedEntityResyncServicesPublicPass.php contains "implements CompilerPassInterface"
    - The $ids array contains the literal strings 'tenancy.shared_entity_copier' and 'tenancy.command.shared_resync'
    - process() guards each id with $container->hasDefinition($id) and $container->hasAlias($id) before setPublic(true) (tolerates absent services)
    - php -l on the file exits 0 (no parse error)
  </acceptance_criteria>
  <done>The compiler pass file exists, parses clean, and is structurally identical to MakeSharedEntityServicesPublicPass with the two new service IDs added.</done>
</task>

<task type="auto">
  <name>Task 2: Create the two unit-test stubs (copier + command)</name>
  <files>tests/Unit/Shared/SharedEntityCopierTest.php, tests/Unit/Command/SharedEntityResyncCommandTest.php</files>
  <read_first>
    - tests/Unit/Command/TenantMigrateCommandTest.php (exact analog for the command unit test — CommandTester, makeCommand factory, continue-on-failure spy-bootstrapper, finally-clear assertions)
    - tests/Unit/Subscriber/SharedEntitySyncSubscriberSharedDbTest.php (analog for copier unit test — createMock(EntityManagerInterface), createMock(ClassMetadata), the skip-guard idiom)
    - .planning/phases/26-tenancy-shared-resync-command/26-VALIDATION.md (per-task verification map SHARE-02-a..l — every test method maps to one row)
    - .planning/phases/26-tenancy-shared-resync-command/26-PATTERNS.md (test skeletons, lines 438-575)
  </read_first>
  <action>Create final class SharedEntityCopierTest extends TestCase in namespace Tenancy\Bundle\Tests\Unit\Shared, and final class SharedEntityResyncCommandTest extends TestCase in namespace Tenancy\Bundle\Tests\Unit\Command. Each test method is a skip-guarded stub: first line `if (!class_exists(\Tenancy\Bundle\Shared\SharedEntityCopier::class)) { self::markTestSkipped('SharedEntityCopier not yet extracted (Plan 26-02)'); }` (resp. SharedEntityResyncCommand for the command test). Name one stub method per VALIDATION behavior so the map is traceable: copier test → testEnumeratesSharedClassesViaLandlordMetadata (SHARE-02-a), testClassifyRowReturnsInsertWhenAbsent / testClassifyRowReturnsUpdateOnScalarDrift / testClassifyRowReturnsInSyncWhenIdentical (classify correctness), testApplyRowSetsAndResetsSyncInProgressAroundFlush, testIsSyncInProgressFalseOutsideApply, testIsSharedUsesReflClassProxySafe. Command test → testDryRunNeverWrites (SHARE-02-b), testLiveRunPromptsConfirmDefaultNoAbortsCleanly (SHARE-02-c), testForceSkipsConfirmation (SHARE-02-d), testSharedDbDriverExitsSuccessWithNoOp (SHARE-02-e), testContinueOnFailureExitsFailureWhenAnyTenantFails (SHARE-02-f), testContextAndBootstrapperClearedInFinally (SHARE-02-g), testTenantOptionTargetsSingleTenant (SHARE-02-k). Bodies may be a single markTestSkipped after the class_exists guard for now; the real assertions land in Plans 26-02/26-03. Use declare(strict_types=1).</action>
  <verify>
    <automated>php -l tests/Unit/Shared/SharedEntityCopierTest.php && php -l tests/Unit/Command/SharedEntityResyncCommandTest.php && vendor/bin/phpunit tests/Unit/Shared/SharedEntityCopierTest.php tests/Unit/Command/SharedEntityResyncCommandTest.php</automated>
  </verify>
  <acceptance_criteria>
    - SharedEntityCopierTest.php contains "class SharedEntityCopierTest" and a method named testEnumeratesSharedClassesViaLandlordMetadata
    - SharedEntityResyncCommandTest.php contains "class SharedEntityResyncCommandTest" and methods testDryRunNeverWrites, testForceSkipsConfirmation, testSharedDbDriverExitsSuccessWithNoOp, testContinueOnFailureExitsFailureWhenAnyTenantFails, testTenantOptionTargetsSingleTenant
    - Every test method begins with a class_exists skip-guard on the not-yet-existing production class
    - `vendor/bin/phpunit tests/Unit/Shared/SharedEntityCopierTest.php tests/Unit/Command/SharedEntityResyncCommandTest.php` exits 0 (all skipped, none failed)
  </acceptance_criteria>
  <done>Both unit-test classes exist with one skip-guarded stub method per VALIDATION behavior; PHPUnit reports them skipped, not failed.</done>
</task>

<task type="auto">
  <name>Task 3: Create the integration-test stub reusing SharedEntityFailureLoggingTestKernel</name>
  <files>tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php</files>
  <read_first>
    - tests/Integration/SharedEntity/SharedEntitySyncIntegrationTest.php (exact analog — setUpBeforeClass/tearDownAfterClass kernel lifecycle, landlord+2-tenant SQLite paths, switchTenantManager helper, setUp context reset)
    - tests/Integration/SharedEntity/Support/SharedEntityFailureLoggingTestKernel.php (the kernel to reuse — no new kernel)
    - tests/Integration/SharedEntity/Support/MakeSharedEntityResyncServicesPublicPass.php (the pass created in Task 1 — register it via a one-line kernel subclass in this test file, or document reliance on the existing kernel build())
    - .planning/phases/26-tenancy-shared-resync-command/26-VALIDATION.md (SHARE-02-h idempotency, -i write-protection bypass, -l classify correctness rows)
  </read_first>
  <action>Create final class SharedEntityResyncCommandIntegrationTest extends TestCase in namespace Tenancy\Bundle\Tests\Integration\SharedEntity. Mirror SharedEntitySyncIntegrationTest's static kernel-lifecycle scaffold (setUpBeforeClass boots a kernel, pre-creates schemas via SchemaTool on landlord + both tenants, @unlink cleanup; tearDownAfterClass shutdown + @unlink; setUp resets tenant context). Reuse SharedEntityFailureLoggingTestKernel — register MakeSharedEntityResyncServicesPublicPass either by defining a tiny in-file kernel subclass that adds the pass in build() OR rely on it being added in Task 1's chosen registration point. Add one skip-guarded stub method per integration behavior: testResyncIsIdempotent (SHARE-02-h), testResyncWritesBypassWriteProtection (SHARE-02-i), testInSyncRowsNotCountedAsUpdate (SHARE-02-l). Each method's first line guards on `if (!class_exists(\Tenancy\Bundle\Command\SharedEntityResyncCommand::class)) { self::markTestSkipped('SharedEntityResyncCommand not yet built (Plan 26-03)'); }`. Bodies are markTestSkipped stubs for now. Use declare(strict_types=1).</action>
  <verify>
    <automated>php -l tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php && vendor/bin/phpunit tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php</automated>
  </verify>
  <acceptance_criteria>
    - SharedEntityResyncCommandIntegrationTest.php contains "class SharedEntityResyncCommandIntegrationTest" and methods testResyncIsIdempotent, testResyncWritesBypassWriteProtection, testInSyncRowsNotCountedAsUpdate
    - The test reuses SharedEntityFailureLoggingTestKernel (grep shows SharedEntityFailureLoggingTestKernel referenced; no brand-new top-level kernel class with its own DB-path setup)
    - Every test method begins with a class_exists skip-guard on SharedEntityResyncCommand
    - `vendor/bin/phpunit tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php` exits 0 (all skipped)
  </acceptance_criteria>
  <done>Integration test class exists, reuses the Phase 25 kernel, has 3 skip-guarded stubs, and PHPUnit reports them skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Wave 0 creates only test scaffolding — no production code, no input handling, no cross-tenant writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-TEST | Tampering | Skip-guards on stub tests | accept | Stubs guard on class_exists/service presence so they cannot silently pass before the production class exists — false-green is structurally prevented; no runtime risk (test-only files) |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — 26-RESEARCH.md Package Legitimacy Audit confirms zero new external packages |
</threat_model>

<verification>
- `vendor/bin/phpunit --testsuite unit` exits 0 (new unit stubs skipped, no regressions)
- `vendor/bin/phpunit` exits 0 (new integration stub skipped, full suite green)
- `php -l` clean on all 4 new files
</verification>

<success_criteria>
- 4 new files exist (1 compiler pass + 3 test classes)
- Every stub test method is skip-guarded on its not-yet-existing production class
- Full PHPUnit suite stays green (stubs skipped, never failed)
- The compiler pass exposes tenancy.shared_entity_copier + tenancy.command.shared_resync
</success_criteria>

<output>
Create `.planning/phases/26-tenancy-shared-resync-command/26-01-SUMMARY.md` when done
</output>
