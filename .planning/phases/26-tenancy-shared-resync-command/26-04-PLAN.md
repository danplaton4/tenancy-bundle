---
phase: 26-tenancy-shared-resync-command
plan: 04
type: execute
wave: 4
depends_on: [26-02, 26-03]
files_modified:
  - tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php
autonomous: true
requirements: [SHARE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Running tenancy:shared:resync writes #[Shared] landlord rows into a tenant DB WITHOUT SharedEntityWriteInTenantContextException (the copier bypass works end-to-end)"
    - "Running resync twice produces no duplicate tenant rows and the same final state (idempotent find-or-new, D-02)"
    - "A tenant copy carries the SAME primary key as its landlord master (CR-01 cross-DB key equality)"
    - "Drift classification is correct: in-sync rows are not counted as would-update (D-03/SHARE-02-l)"
    - "The full PHPUnit suite is green (unit + integration), including the Phase 25 SHARE-01 suite"
  artifacts:
    - path: "tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php"
      provides: "End-to-end SQLite proof of SHARE-02-h, -i, -l"
      contains: "testResyncWritesBypassWriteProtection"
  key_links:
    - from: "tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php"
      to: "tenancy.command.shared_resync"
      via: "CommandTester on the container-resolved command"
      pattern: "tenancy.command.shared_resync"
---

<objective>
Prove the end-to-end behaviors that unit tests with mocks cannot: the write-protection bypass actually works against a live tenant EM flush, the resync is idempotent across runs with cross-DB key equality, and drift classification is accurate. Fill the Wave 0 integration stubs with real assertions against a full SQLite kernel, then gate on the full suite being green.

Purpose: close SHARE-02-h (idempotency), SHARE-02-i (write-protection bypass LANDMINE — the highest-risk behavior in the phase), and SHARE-02-l (classify correctness) with real DB round-trips. Re-confirm SHARE-02-j (Phase 25 regression) at the suite level.
Output: filled-in SharedEntityResyncCommandIntegrationTest; full green suite.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-tenancy-shared-resync-command/26-VALIDATION.md
@.planning/phases/26-tenancy-shared-resync-command/26-RESEARCH.md
@.planning/phases/26-tenancy-shared-resync-command/26-PATTERNS.md

<interfaces>
<!-- Verified from Phase 25 integration test + kernel. Reuse these directly — no new kernel. -->

Kernel: SharedEntityFailureLoggingTestKernel (extends SharedEntitySyncTestKernel) already provides landlord + 2-tenant SQLite DBs + RecordingLogger. The Wave 0 integration test (Plan 26-01) already wires MakeSharedEntityResyncServicesPublicPass to expose the new services.

Container service IDs (now public via the resync pass): tenancy.command.shared_resync, tenancy.shared_entity_copier, tenancy.context, doctrine.orm.landlord_entity_manager, doctrine, doctrine.dbal.tenant_connection.

Shared test entities (tests/Integration/SharedEntity/Support/Entity/): TestPlan, TestPlanCategory, TestPlanWithAssociation.

switchTenantManager helper (copy verbatim from SharedEntitySyncIntegrationTest lines ~907-918):
  setTenant + getConnection('tenant')->close() + resetManager('tenant') → returns tenant EntityManagerInterface.

Test execution pattern: resolve $command = $container->get('tenancy.command.shared_resync'); $tester = new CommandTester($command); $tester->execute(['--force' => true]).

PK-equality invariant (CR-01): the tenant copy's id MUST equal the landlord master's id (the GENERATOR_TYPE_NONE trick in copier->applyRow preserves it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the write-protection-bypass + idempotency integration tests (SHARE-02-h, -i)</name>
  <files>tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php</files>
  <read_first>
    - tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php (the Wave 0 stub from Plan 26-01 — replace markTestSkipped with real DB round-trips)
    - tests/Integration/SharedEntity/SharedEntitySyncIntegrationTest.php (analog — testSyncWriteBypassesWriteProtection pattern, switchTenantManager helper ~907-918, schema setup)
    - src/Command/SharedEntityResyncCommand.php (Plan 26-03 — command under test)
    - src/Shared/SharedEntityCopier.php (Plan 26-02 — the bypass flag owner)
    - .planning/phases/26-tenancy-shared-resync-command/26-PATTERNS.md (idempotency + bypass test patterns, lines 616-654)
  </read_first>
  <action>Fill testResyncWritesBypassWriteProtection (SHARE-02-i): insert one #[Shared] entity (e.g. TestPlan) on the landlord EM; resolve tenancy.command.shared_resync from the container; run via CommandTester with ['--force' => true]; assert NO SharedEntityWriteInTenantContextException is thrown (the command exits Command::SUCCESS) and the row lands in each tenant DB (switch to a tenant via switchTenantManager and find() the copy). This is the LANDMINE proof — the copier-owned syncInProgress flag must let applyRow's tenant flush through. Fill testResyncIsIdempotent (SHARE-02-h): run the command with --force, assert the tenant copy exists with id EQUAL to the landlord master id (CR-01 cross-DB key equality); run the command a second time with --force; assert exactly one tenant row exists for that identifier (no duplicate) and the final scalar state is unchanged. Remove the class_exists skip-guards (command now exists). Use the same @unlink/SchemaTool lifecycle the analog establishes; reset tenant context in setUp.</action>
  <verify>
    <automated>vendor/bin/phpunit tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php -x</automated>
  </verify>
  <acceptance_criteria>
    - testResyncWritesBypassWriteProtection runs CommandTester on the container command with --force, asserts Command::SUCCESS, and asserts the #[Shared] row is found in a tenant DB (no SharedEntityWriteInTenantContextException)
    - testResyncIsIdempotent asserts the tenant copy id equals the landlord master id (CR-01) and a second --force run produces no duplicate row
    - Neither method calls markTestSkipped (guards removed)
    - `vendor/bin/phpunit tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php -x` exits 0 with these two methods running
  </acceptance_criteria>
  <done>The write-protection bypass and idempotency are proven against a live SQLite tenant flush with cross-DB key equality.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the classify-correctness integration test (SHARE-02-l) + full-suite gate</name>
  <files>tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php</files>
  <read_first>
    - tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php (Task 1 additions — extend with the classify test)
    - src/Shared/SharedEntityCopier.php (Plan 26-02 — classifyRow returns insert/update/in-sync)
    - .planning/phases/26-tenancy-shared-resync-command/26-VALIDATION.md (SHARE-02-l row: in-sync rows not counted as update)
  </read_first>
  <action>Fill testInSyncRowsNotCountedAsUpdate (SHARE-02-l): seed a #[Shared] entity on the landlord and run the command once with --force so the tenant copy is in sync; then run the command with ['--dry-run' => true] and assert the printed drift summary classifies that row as in-sync (NOT would-update) — assert the would-update count for an unchanged row is 0 and the in-sync count reflects it. Optionally also assert a would-update appears after mutating the landlord scalar field (drives the update branch). Use the command display (CommandTester::getDisplay()) and/or directly call copier->classifyRow against the live tenant EM to assert 'in-sync' for an unchanged row and 'update' after a scalar change. Remove any remaining skip-guard. Then run the FULL suite as the wave gate to confirm no regression (SHARE-02-j included).</action>
  <verify>
    <automated>vendor/bin/phpunit tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php -x && vendor/bin/phpunit && vendor/bin/phpstan analyse && vendor/bin/php-cs-fixer check</automated>
  </verify>
  <acceptance_criteria>
    - testInSyncRowsNotCountedAsUpdate asserts an unchanged synced row classifies as 'in-sync' (would-update count 0 for that row) and a scalar-mutated row classifies as 'update'
    - `vendor/bin/phpunit tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php -x` exits 0 with all three integration methods running (no skips)
    - `vendor/bin/phpunit` (full suite) exits 0 — unit + integration, including tests/Integration/SharedEntity/SharedEntitySyncIntegrationTest.php (SHARE-02-j regression)
    - `vendor/bin/phpstan analyse` level 9 exits 0
    - `vendor/bin/php-cs-fixer check` reports no violations
  </acceptance_criteria>
  <done>Classify correctness is proven, and the full suite (unit + integration + PHPStan + cs-fixer) is green — the phase verification gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command → live tenant EM flush | The integration test exercises the real cross-tenant write boundary against SQLite tenant DBs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-BYPASS | Elevation of Privilege | write-protection bypass at runtime | mitigate | testResyncWritesBypassWriteProtection is the live proof that the copier-owned syncInProgress flag scopes the bypass to sync writes only — asserts no SharedEntityWriteInTenantContextException AND the row lands (covers the highest-risk LANDMINE) |
| T-26-04-DRIFT | Information Disclosure | inaccurate drift diagnostic | mitigate | testInSyncRowsNotCountedAsUpdate proves classifyRow does not misreport in-sync rows as updates — the diagnostic value of --dry-run depends on this |
| T-26-04-KEY | Tampering | cross-DB primary-key divergence | mitigate | testResyncIsIdempotent asserts tenant copy id == landlord master id (CR-01) so update/delete lookups by landlord id remain valid |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs — 26-RESEARCH.md Package Legitimacy Audit confirms zero new external packages |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Integration/SharedEntity/SharedEntityResyncCommandIntegrationTest.php -x` green (SHARE-02-h, -i, -l)
- `vendor/bin/phpunit` full suite green (SHARE-02-j regression included)
- `vendor/bin/phpstan analyse` level 9 clean
- `vendor/bin/php-cs-fixer check` clean
- Manual-only (per 26-VALIDATION.md): interactive `[y/N]` prompt rendering in a real TTY — run `bin/console tenancy:shared:resync` against a dev fixture and confirm the drift summary + prompt render and default-No aborts (SHARE-02-c TTY UX)
</verification>

<success_criteria>
- All three integration behaviors (SHARE-02-h, -i, -l) proven against a live SQLite kernel with no skips
- Write-protection bypass works end-to-end (no exception, rows land)
- Idempotent re-run produces no duplicates with cross-DB key equality
- Full suite + PHPStan level 9 + cs-fixer all green (phase gate)
</success_criteria>

<output>
Create `.planning/phases/26-tenancy-shared-resync-command/26-04-SUMMARY.md` when done
</output>
