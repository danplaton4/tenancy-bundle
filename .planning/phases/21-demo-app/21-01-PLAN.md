---
phase: 21-demo-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/saas/composer.json
  - examples/saas/.env
  - examples/saas/.env.example
  - examples/saas/.dockerignore
  - examples/saas/.gitignore
  - examples/saas/bin/console
  - examples/saas/public/index.php
  - examples/saas/src/Kernel.php
  - examples/saas/config/bundles.php
  - examples/saas/config/routes.yaml
  - examples/saas/config/packages/framework.yaml
  - examples/saas/config/packages/twig.yaml
  - examples/saas/config/packages/doctrine.yaml
  - examples/saas/config/packages/tenancy.yaml
  - examples/saas/config/packages/mailer.yaml
  - examples/saas/config/packages/web_profiler.yaml
autonomous: true
requirements: [DEMO-01]

must_haves:
  truths:
    - "Demo Symfony 7.4 skeleton has a Composer root with a path repository back to the bundle (D-16)"
    - "Demo `config/bundles.php` is hand-wired (D-10) and includes TenancyBundle, DoctrineBundle, TwigBundle, FrameworkBundle, DoctrineFixturesBundle (dev/test), WebProfilerBundle (dev/test)"
    - "Demo `tenancy.yaml` configures `database_per_tenant` driver (D-04), `landlord_connection: landlord`, `host.app_domain: tenancy.localhost`, resolvers `[host, origin, header, query_param, console]`, origin allow-list for `http(s)://*.tenancy.localhost` (D-08), `strict_mode: true` (CLAUDE.md), `tenant_entity_class: App\\Entity\\Landlord\\DemoTenant`"
    - "Demo `doctrine.yaml` defines two connections (landlord + tenant) and two EMs, MariaDB pdo_mysql driver"
    - "Demo `mailer.yaml` sets default DSN to `smtp://mailpit:1025` (D-09)"
    - "Demo `web_profiler.yaml` enables WDT under `when@dev` (D-07)"
  artifacts:
    - path: "examples/saas/composer.json"
      provides: "Composer root + path repo `../../` symlink:true + require @dev"
      contains: '"type": "path"'
    - path: "examples/saas/config/bundles.php"
      provides: "Hand-wired bundle list (D-10)"
      contains: "Tenancy\\\\Bundle\\\\TenancyBundle::class"
    - path: "examples/saas/config/packages/tenancy.yaml"
      provides: "driver/host/origin/resolvers/strict_mode/tenant_entity_class"
      contains: "tenant_entity_class: App\\\\Entity\\\\Landlord\\\\DemoTenant"
    - path: "examples/saas/src/Kernel.php"
      provides: "MicroKernelTrait Symfony 7.4 kernel"
    - path: "examples/saas/public/index.php"
      provides: "Front controller"
  key_links:
    - from: "examples/saas/composer.json"
      to: "../../ (this bundle's composer.json)"
      via: 'repositories: [{ type: path, url: "../../", options: { symlink: true } }]'
      pattern: '"type":\\s*"path"'
    - from: "examples/saas/config/packages/tenancy.yaml"
      to: "src/Resolver/OriginHeaderResolver.php (bundle)"
      via: "origin.allow_list config"
      pattern: 'allow_list:'
    - from: "examples/saas/config/packages/tenancy.yaml"
      to: "App\\Entity\\Landlord\\DemoTenant (Plan 02)"
      via: "tenant_entity_class"
      pattern: 'tenant_entity_class:\\s*App\\\\Entity\\\\Landlord\\\\DemoTenant'
---

<objective>
Scaffold the Symfony 7.4 demo skeleton at `examples/saas/`: Composer root with path-repo back to the bundle (D-16), hand-wired bundle list (D-10), and idiomatic config for FrameworkBundle, Doctrine (two-EM landlord+tenant on MariaDB), Twig, Tenancy (database_per_tenant + Origin allow-list per D-08), Mailer (Mailpit default DSN per D-09), and Web Profiler (D-07).

Purpose: this is the foundation Plan 02 (PHP source) and Plan 03 (container stack) build on. Nothing in this plan boots a container; everything is on-disk PHP / YAML.

Output: `examples/saas/{composer.json, .env*, public/, bin/console, src/Kernel.php, config/**}` ready for `composer install` (which runs in the Dockerfile during Plan 03).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-demo-app/21-CONTEXT.md
@.planning/phases/21-demo-app/21-RESEARCH.md
@.planning/phases/21-demo-app/21-PATTERNS.md
@.planning/phases/21-demo-app/21-VALIDATION.md

<interfaces>
<!-- Bundle config keys consumed by this plan's tenancy.yaml -->
<!-- All verified against bundle source on 2026-05-22 -->

From src/Resolver/OriginHeaderResolver.php:
- Priority: 25
- Service ID: tenancy.resolver.origin (conditional on 'origin' in tenancy.resolvers)
- Reads `Origin` header; matches against compile-normalized allow-list (exact host or `*.suffix`)

From src/Resolver/HostResolver.php:
- Priority: 30
- Consumes tenancy.host.app_domain

From src/DependencyInjection/Compiler/OriginHeaderResolverConfigPass.php:
- Empty allow-list -> RuntimeException at compile time
- Mid-string wildcards rejected; only leftmost-label `*.suffix` allowed

From src/Entity/Tenant.php:
- Columns: slug (string 63 PK), name (255), domain (253 nullable), connectionConfig (json), isActive (bool), mailerDsn (255 nullable), mailerFrom (255 nullable), mailerReplyTo (255 nullable), createdAt, updatedAt
- Table: tenancy_tenants
- Implements TenantInterface (getSlug, getName, getDomain, getConnectionConfig, isActive, getMailerDsn, getMailerFrom, getMailerReplyTo)
</interfaces>

<deviations_from_context>
**CONTEXT.md D-05 and D-06 reference APIs that do not exist in the bundle.** Resolution per RESEARCH §"Critical Discrepancy" Option 1:
- `tenancy:migrate --create-dbs` flag does NOT exist on `src/Command/TenantMigrateCommand.php`. The demo will introduce a local `App\Command\SeedDemoCommand` (in Plan 02) that creates tenant DBs via a root-credential DBAL connection and seeds schema+posts. The Dockerfile entrypoint (Plan 03) invokes `bin/console app:seed-demo` (not `tenancy:migrate --create-dbs`).
- `TenantContextOrchestrator::executeAs($tenant, fn() => ...)` does NOT exist. `SeedDemoCommand` will use the explicit `$tenantContext->setTenant() + $bootstrapperChain->boot()` / try/finally / `clear()` pattern lifted verbatim from `src/Command/TenantMigrateCommand.php` lines 97–108.

This plan ships the config that points the demo at `database_per_tenant` driver with two EMs (landlord + tenant) so Plan 02's `SeedDemoCommand` can wire to them.

Other locked decisions honored here:
- `actions/checkout@v5` (Plan 04 — not v4 as the RESEARCH excerpt at line 757 showed); this plan touches no workflow.
- `strict_mode: true` in tenancy.yaml (CLAUDE.md: do NOT disable in demo).
- Database-per-tenant driver only (D-04).
- Web Profiler ON by default via `APP_ENV=dev` (D-07).
- OriginHeaderResolver configured alongside HostResolver in `resolvers` list (D-08).
</deviations_from_context>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/curl -> Caddy (Plan 03) | Untrusted HTTP request; Host header is the tenant signal |
| App -> MariaDB landlord (this plan's doctrine.yaml) | Trusted credential; localhost-only network in compose |
| App -> MariaDB tenant_<slug> | Per-tenant DB; isolation boundary owned by bundle's DatabaseSwitchBootstrapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | Cross-tenant data leak via wrong DB connection | mitigate | `strict_mode: true` in `config/packages/tenancy.yaml`; `database_per_tenant` driver isolates physically; bundle integration suite (Phases 1–20) proves invariants. NEVER set `strict_mode: false` in demo. |
| T-21-02 | Spoofing | `Host`/`Origin` header spoofing | accept | INTENDED behavior — smoke script relies on `Host:` injection as a feature. README (Plan 04) documents this as deliberate. No mitigation in app config. |
| T-21-SC | Tampering | npm/pip/cargo supply chain | n/a | Phase is PHP-only; no npm/pip/cargo installs. Composer packages audited in RESEARCH.md §"Package Legitimacy Audit" (all approved). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: composer.json, .env, .gitignore, .dockerignore, bin/console, public/index.php</name>
  <files>examples/saas/composer.json, examples/saas/.env, examples/saas/.env.example, examples/saas/.gitignore, examples/saas/.dockerignore, examples/saas/bin/console, examples/saas/public/index.php</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/composer.json" (lines 499-529 — path-repo shape, drop flex/migrations, add mailer + fixtures-bundle)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Installation (demo composer.json require block)" (lines 160-178)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-16 Composer / Path-Repo Workflow" (lines 268-283)
    - /Users/danplaton/dev/hype/tests/symfony74-demo/composer.json (external reference per CONTEXT D-17 — lines 1-89 for shape)
    - ./composer.json (this bundle's manifest — referenced by demo's path repo)
  </read_first>
  <action>
    Create `examples/saas/composer.json` as a Symfony 7.4 skeleton Composer root.
    - "name": "danplaton4/tenancy-demo-saas", "type": "project", "license": "MIT", "require-dev" + "require" blocks.
    - "minimum-stability": "dev", "prefer-stable": true.
    - "repositories": single entry of type=path, url="../../", options.symlink=true, options.versions.{"danplaton4/tenancy-bundle":"0.3.x-dev"}. The container build will COPY the bundle source into the same relative position (Plan 03's Dockerfile handles this — do NOT use `/bundle` here per the PATTERNS.md "Recommend Option B").
    - "require": "php":">=8.2", "danplaton4/tenancy-bundle":"@dev", "doctrine/doctrine-bundle":"^2.13", "doctrine/orm":"^3.0", "doctrine/dbal":"^4.0", "symfony/framework-bundle":"7.4.*", "symfony/runtime":"7.4.*", "symfony/dotenv":"7.4.*", "symfony/yaml":"7.4.*", "symfony/twig-bundle":"7.4.*", "symfony/mailer":"7.4.*", "symfony/console":"7.4.*".
    - "require-dev": "symfony/web-profiler-bundle":"7.4.*", "doctrine/doctrine-fixtures-bundle":"^4".
    - "autoload": "App\\\\": "src/" PSR-4.
    - "config.allow-plugins": symfony/runtime=true, symfony/flex=false (we are not using Flex per RESEARCH §"State of the Art").
    - "extra.symfony.allow-contrib": false, "extra.symfony.require": "7.4.*".
    - "scripts.auto-scripts": empty object (no Flex recipes).

    Create `examples/saas/.env` and `.env.example` with identical contents (the demo is secrets-free per RESEARCH §"Runtime State Inventory"):
    - APP_ENV=dev
    - APP_SECRET=demo-secret-not-for-production
    - APP_DEBUG=1
    - TENANCY_DOMAIN_BASE=tenancy.localhost
    - MARIADB_ROOT_PASSWORD=root
    - MARIADB_USER=tenancy
    - MARIADB_PASSWORD=tenancy
    - DATABASE_URL_LANDLORD="mysql://tenancy:tenancy@db:3306/landlord?serverVersion=mariadb-11.0.0&charset=utf8mb4"
    - MAILER_DSN=smtp://mailpit:1025
    - PORT_HTTP=80
    - PORT_HTTPS=443

    Create `examples/saas/.gitignore` ignoring `/var/`, `/vendor/`, `/.env.local`, `/.env.*.local`, `/public/bundles/`.

    Create `examples/saas/.dockerignore` ignoring `var/`, `vendor/`, `.git/`, `.env.local*`, `node_modules/`.

    Create `examples/saas/bin/console` as the standard Symfony 7.4 console entry — opens `vendor/autoload_runtime.php` and returns `function (array $context) { return new \App\Kernel($context['APP_ENV'], (bool) $context['APP_DEBUG']); };`.

    Create `examples/saas/public/index.php` as the standard Symfony 7.4 front controller using `Runtime` — `require_once dirname(__DIR__).'/vendor/autoload_runtime.php';` then return the same closure.

    Mark `bin/console` executable (chmod +x via the Write tool is impossible — leave it as a plain file; Plan 03's Dockerfile/entrypoint will chmod).
  </action>
  <verify>
    <automated>php -r 'json_decode(file_get_contents("examples/saas/composer.json"), false, 512, JSON_THROW_ON_ERROR); echo "ok\n";' && grep -q '"type": "path"' examples/saas/composer.json && grep -q '"url": "../../"' examples/saas/composer.json && grep -q '"symlink": true' examples/saas/composer.json && grep -q 'doctrine/doctrine-fixtures-bundle' examples/saas/composer.json && grep -q 'symfony/web-profiler-bundle' examples/saas/composer.json && grep -q '^TENANCY_DOMAIN_BASE=tenancy.localhost$' examples/saas/.env && grep -q '^MAILER_DSN=smtp://mailpit:1025$' examples/saas/.env && test -f examples/saas/bin/console && test -f examples/saas/public/index.php && grep -q 'autoload_runtime.php' examples/saas/public/index.php</automated>
  </verify>
  <acceptance_criteria>
    - `examples/saas/composer.json` is valid JSON (php -r json_decode passes)
    - `composer.json` contains `"type": "path"` AND `"url": "../../"` AND `"symlink": true` AND `"danplaton4/tenancy-bundle":"0.3.x-dev"` in `repositories[0].options.versions`
    - `composer.json` requires `doctrine/doctrine-fixtures-bundle:^4` in `require-dev`
    - `composer.json` requires `symfony/web-profiler-bundle:7.4.*` in `require-dev`
    - `composer.json` requires `symfony/mailer:7.4.*` in `require`
    - `composer.json` does NOT contain `symfony/flex` (we're not using it per RESEARCH §"State of the Art")
    - `composer.json` does NOT contain `doctrine/doctrine-migrations-bundle` (CONTEXT D-05: no migrations)
    - `.env` and `.env.example` both contain `TENANCY_DOMAIN_BASE=tenancy.localhost`, `MAILER_DSN=smtp://mailpit:1025`, `DATABASE_URL_LANDLORD` pointing at host `db`
    - `bin/console` and `public/index.php` both reference `autoload_runtime.php`
    - `.gitignore` lists `/vendor/` and `/var/`
  </acceptance_criteria>
  <done>Demo Composer root with path-repo back to the bundle is in place; env defaults are committed (no secrets); Symfony Runtime entry points exist.</done>
</task>

<task type="auto">
  <name>Task 2: Kernel + bundles.php + framework/twig/web_profiler config</name>
  <files>examples/saas/src/Kernel.php, examples/saas/config/bundles.php, examples/saas/config/routes.yaml, examples/saas/config/packages/framework.yaml, examples/saas/config/packages/twig.yaml, examples/saas/config/packages/web_profiler.yaml</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/config/bundles.php" (lines 480-495 — full file shape, drop migrations-bundle)
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/src/Kernel.php" (lines 28 — MicroKernelTrait kernel)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-07" (Web Profiler always on; APP_ENV=dev in compose)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-10" (config/bundles.php hand-wired, not via tenancy:install)
    - /Users/danplaton/dev/hype/tests/symfony74-demo/config/bundles.php (external — for shape only)
    - /Users/danplaton/dev/hype/tests/symfony74-demo/src/Kernel.php (external — MicroKernelTrait reference)
    - tests/Integration/TestKernel.php (in-repo — for framework config shape lines 44-53)
  </read_first>
  <action>
    Create `examples/saas/src/Kernel.php` per Symfony 7.4 idiom — `namespace App;`, `final class Kernel extends \Symfony\Component\HttpKernel\Kernel { use \Symfony\Bundle\FrameworkBundle\Kernel\MicroKernelTrait; }`. No method overrides needed; MicroKernelTrait auto-loads `config/bundles.php`, `config/packages/*.yaml`, and `config/routes.yaml`.

    Create `examples/saas/config/bundles.php` returning an array of bundle FQCNs (PATTERNS.md lines 487-495):
    - Symfony\Bundle\FrameworkBundle\FrameworkBundle::class => ['all' => true]
    - Tenancy\Bundle\TenancyBundle::class => ['all' => true]
    - Doctrine\Bundle\DoctrineBundle\DoctrineBundle::class => ['all' => true]
    - Symfony\Bundle\TwigBundle\TwigBundle::class => ['all' => true]
    - Symfony\Bundle\WebProfilerBundle\WebProfilerBundle::class => ['dev' => true, 'test' => true]
    - Doctrine\Bundle\FixturesBundle\DoctrineFixturesBundle::class => ['dev' => true, 'test' => true]

    Create `examples/saas/config/routes.yaml` — attribute-route loader pointed at `../src/Controller/`:
    ```
    controllers:
        resource:
            path: ../src/Controller/
            namespace: App\Controller
        type: attribute
    when@dev:
        _wdt:
            resource: '@WebProfilerBundle/Resources/config/routing/wdt.xml'
            prefix: /_wdt
        _profiler:
            resource: '@WebProfilerBundle/Resources/config/routing/profiler.xml'
            prefix: /_profiler
    ```
    (Use the standard Symfony skeleton routes.yaml shape; the attribute loader picks up Plan 02's controllers.)

    Create `examples/saas/config/packages/framework.yaml`:
    - framework.secret: '%env(APP_SECRET)%'
    - framework.http_method_override: false
    - framework.handle_all_throwables: true
    - framework.php_errors.log: true
    - framework.session.handler_id: null, cookie_secure: 'auto', cookie_samesite: 'lax', storage_factory_id: 'session.storage.factory.native'
    - when@dev: framework.profiler.{collect: true, only_exceptions: false}
    - when@test: framework.test: true, framework.session.storage_factory_id: 'session.storage.factory.mock_file'

    Create `examples/saas/config/packages/twig.yaml`:
    - twig.default_path: '%kernel.project_dir%/templates'
    - when@test: twig.strict_variables: true

    Create `examples/saas/config/packages/web_profiler.yaml`:
    - when@dev: web_profiler.{toolbar: true, intercept_redirects: false}; framework.profiler.{collect_serializer_data: true}
    - when@test: web_profiler.{toolbar: false, intercept_redirects: false}; framework.profiler.{collect: false}
  </action>
  <verify>
    <automated>php -l examples/saas/src/Kernel.php && php -l examples/saas/config/bundles.php && grep -q 'Tenancy\\\\Bundle\\\\TenancyBundle::class' examples/saas/config/bundles.php && grep -q 'WebProfilerBundle::class.*dev.*true' examples/saas/config/bundles.php && grep -q 'DoctrineFixturesBundle::class' examples/saas/config/bundles.php && ! grep -q 'doctrine/migrations' examples/saas/config/bundles.php && grep -q 'MicroKernelTrait' examples/saas/src/Kernel.php && test -f examples/saas/config/routes.yaml && grep -q 'when@dev' examples/saas/config/packages/web_profiler.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `php -l examples/saas/src/Kernel.php` exits 0
    - `php -l examples/saas/config/bundles.php` exits 0
    - `bundles.php` contains `Tenancy\Bundle\TenancyBundle::class => ['all' => true]`
    - `bundles.php` contains `WebProfilerBundle::class => ['dev' => true, 'test' => true]` (D-07)
    - `bundles.php` contains `DoctrineFixturesBundle::class => ['dev' => true, 'test' => true]`
    - `bundles.php` does NOT contain `DoctrineMigrationsBundle` (CONTEXT D-05)
    - `Kernel.php` uses `MicroKernelTrait`
    - `routes.yaml` references `../src/Controller/` with `type: attribute`
    - `framework.yaml` references `%env(APP_SECRET)%`
    - `web_profiler.yaml` has `when@dev` block enabling toolbar
  </acceptance_criteria>
  <done>Symfony 7.4 kernel boots from on-disk config (verified at container-build time in Plan 03); bundle list is hand-wired per D-10; WDT is wired for `APP_ENV=dev` per D-07.</done>
</task>

<task type="auto">
  <name>Task 3: tenancy.yaml + doctrine.yaml + mailer.yaml (cross-phase wiring)</name>
  <files>examples/saas/config/packages/tenancy.yaml, examples/saas/config/packages/doctrine.yaml, examples/saas/config/packages/mailer.yaml</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/config/packages/tenancy.yaml" (lines 307-333 — combined target shape)
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/config/packages/doctrine.yaml" (lines 336-345 — two-EM split)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Phase 17 OriginHeaderResolver" (lines 252-287 — origin allow-list shape)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Phase 20: Mailer" (lines 309-329 — mailer.yaml shape, transport_cache_size, async:false)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-04, D-08, D-09" (driver, origin, mailer)
    - src/Resolver/OriginHeaderResolver.php (bundle — verify allow-list grammar)
    - src/DependencyInjection/Compiler/OriginHeaderResolverConfigPass.php (bundle — empty allow-list rejection)
    - src/Resolver/HostResolver.php (bundle — host.app_domain consumption)
    - /Users/danplaton/dev/hype/tests/symfony74-demo/config/packages/doctrine.yaml (external — two-EM landlord+tenant pattern)
  </read_first>
  <action>
    Create `examples/saas/config/packages/tenancy.yaml` with the following top-level keys (combine PATTERNS.md lines 313-327 with RESEARCH §"Phase 17" lines 264-277):
    - `tenancy.driver: database_per_tenant`
    - `tenancy.database.enabled: true`
    - `tenancy.landlord_connection: landlord`
    - `tenancy.host.app_domain: tenancy.localhost`
    - `tenancy.strict_mode: true` (per CLAUDE.md: "do NOT disable in demo")
    - `tenancy.tenant_entity_class: App\\Entity\\Landlord\\DemoTenant` (Plan 02 creates this class at `src/Entity/Landlord/DemoTenant.php` with `namespace App\\Entity\\Landlord;`)
    - `tenancy.resolvers: [host, origin, header, query_param, console]`
    - `tenancy.origin.allow_list:` two entries — `{ origin: 'https://*.tenancy.localhost' }` and `{ origin: 'http://*.tenancy.localhost' }` (per D-08; HTTP allowed because demo's primary walkthrough is HTTP per D-11)

    Create `examples/saas/config/packages/doctrine.yaml` with two connections + two EMs (PATTERNS.md lines 336-345 + external prototype lines 1-37). Top-level `doctrine`:
    - `doctrine.dbal.default_connection: landlord`
    - `doctrine.dbal.connections.landlord`: url='%env(resolve:DATABASE_URL_LANDLORD)%', driver='pdo_mysql', server_version='mariadb-11.0.0', charset='utf8mb4'
    - `doctrine.dbal.connections.tenant`: same shape with url='%env(resolve:DATABASE_URL_LANDLORD)%' (the bundle's DatabaseSwitchBootstrapper rewrites this at request time per Phase 3 D-03), driver='pdo_mysql', server_version='mariadb-11.0.0', charset='utf8mb4', wrapper_class='Tenancy\\Bundle\\Doctrine\\TenantConnection'
    - `doctrine.orm.default_entity_manager: landlord`
    - `doctrine.orm.entity_managers.landlord`: connection='landlord', mappings.{App: { is_bundle: false, dir: '%kernel.project_dir%/src/Entity/Landlord', prefix: 'App\\Entity\\Landlord', alias: 'App' }, Tenancy: { is_bundle: false, type: 'attribute', dir: '%kernel.project_dir%/vendor/danplaton4/tenancy-bundle/src/Entity', prefix: 'Tenancy\\Bundle\\Entity', alias: 'Tenancy' }}
    - `doctrine.orm.entity_managers.tenant`: connection='tenant', mappings.{App: { is_bundle: false, dir: '%kernel.project_dir%/src/Entity/Tenant', prefix: 'App\\Entity\\Tenant', alias: 'AppTenant' }}
    - `when@dev/when@test` blocks turn off auto_generate_proxy_classes only in prod (idiomatic)

    Notes for executor:
    - `App\Entity\Landlord\DemoTenant` lives at `src/Entity/Landlord/DemoTenant.php` (Plan 02) — that's why the landlord EM maps `src/Entity/Landlord` with prefix `App\Entity\Landlord`. The FQCN configured in `tenancy.tenant_entity_class` is `App\Entity\Landlord\DemoTenant` (NOT `App\Entity\DemoTenant`); this matches Plan 02's `namespace App\Entity\Landlord;` declaration.
    - `App\Entity\Tenant\Post` lives at `src/Entity/Tenant/Post.php` (Plan 02) — mapped by the tenant EM only.
    - This split keeps `Post.php` out of the landlord EM (prevents it from trying to create the `post` table in the landlord DB).
    - The bundle's `Tenancy\Bundle\Entity\Tenant` is mapped on the LANDLORD EM via the explicit `Tenancy` mapping entry above so the bundle's compiler passes find it (bundle Phase 3 prependExtension wires this normally — explicit mapping here ensures it works with our two-EM split).

    Create `examples/saas/config/packages/mailer.yaml` per RESEARCH §"Phase 20" (lines 322-329):
    - `framework.mailer.dsn: '%env(MAILER_DSN)%'`
    - No `framework.messenger.transports.async` (CONTEXT D-09 plus RESEARCH "tenancy.mailer.async: false" — demo dispatches synchronously)
    - No `tenancy.mailer.transport_cache_size` override (default 32 is fine for 3 tenants)
  </action>
  <verify>
    <automated>php -r 'use Symfony\Component\Yaml\Yaml; require "vendor/autoload.php"; Yaml::parseFile("examples/saas/config/packages/tenancy.yaml"); Yaml::parseFile("examples/saas/config/packages/doctrine.yaml"); Yaml::parseFile("examples/saas/config/packages/mailer.yaml"); echo "ok\n";' 2>/dev/null || (cd examples/saas && php -r 'echo "yaml parse skipped - vendor not installed (expected at this stage)\n";') ; grep -v '^#' examples/saas/config/packages/tenancy.yaml | grep -q 'driver: database_per_tenant' && grep -v '^#' examples/saas/config/packages/tenancy.yaml | grep -q 'app_domain: tenancy.localhost' && grep -v '^#' examples/saas/config/packages/tenancy.yaml | grep -q 'strict_mode: true' && grep -v '^#' examples/saas/config/packages/tenancy.yaml | grep -q 'tenant_entity_class: App\\\\Entity\\\\Landlord\\\\DemoTenant' && grep -v '^#' examples/saas/config/packages/tenancy.yaml | grep -qE 'origin.*tenancy\.localhost' && grep -v '^#' examples/saas/config/packages/doctrine.yaml | grep -q 'default_connection: landlord' && grep -v '^#' examples/saas/config/packages/doctrine.yaml | grep -q 'wrapper_class.*TenantConnection' && grep -v '^#' examples/saas/config/packages/mailer.yaml | grep -q "dsn.*'%env(MAILER_DSN)%'"</automated>
  </verify>
  <acceptance_criteria>
    - `tenancy.yaml` contains `driver: database_per_tenant`
    - `tenancy.yaml` contains `app_domain: tenancy.localhost`
    - `tenancy.yaml` contains `strict_mode: true` (CLAUDE.md)
    - `tenancy.yaml` contains `tenant_entity_class: App\Entity\Landlord\DemoTenant`
    - `tenancy.yaml` `origin.allow_list` has BOTH `http://*.tenancy.localhost` and `https://*.tenancy.localhost` entries (D-11 HTTP-default)
    - `tenancy.yaml` `resolvers` list contains `host`, `origin`, `header`, `query_param`, `console` (in this order or with `host` first)
    - `doctrine.yaml` `default_connection` is `landlord`
    - `doctrine.yaml` `connections.tenant.wrapper_class` is `Tenancy\Bundle\Doctrine\TenantConnection`
    - `doctrine.yaml` defines TWO entity managers (`landlord` and `tenant`)
    - `doctrine.yaml` landlord EM maps `src/Entity/Landlord` directory
    - `doctrine.yaml` tenant EM maps `src/Entity/Tenant` directory
    - `mailer.yaml` sets `framework.mailer.dsn` to `%env(MAILER_DSN)%`
  </acceptance_criteria>
  <done>Bundle cross-phase wiring is committed: Phase 3 driver (database_per_tenant), Phase 17 OriginHeaderResolver allow-list, Phase 20 Mailer DSN. Plan 02 will land the App entities + commands the Doctrine mappings point at.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. Filesystem check: `tree -L 3 examples/saas` shows config/{bundles.php, routes.yaml, packages/}, public/index.php, bin/console, src/Kernel.php, .env*, composer.json, .gitignore, .dockerignore
2. `php -l` passes for every PHP file created
3. YAML files are well-formed (`php-yaml-lint` or `python -c "import yaml; yaml.safe_load(open('...'))"`)
4. `grep -c "Tenancy\\\\Bundle\\\\TenancyBundle::class" examples/saas/config/bundles.php` returns 1
5. `grep -c "tenant_entity_class: App\\\\Entity\\\\Landlord\\\\DemoTenant" examples/saas/config/packages/tenancy.yaml` returns 1
</verification>

<success_criteria>
- Demo Composer root at `examples/saas/composer.json` with path repo back to `../../` (D-16)
- Demo `config/bundles.php` is hand-wired with TenancyBundle + DoctrineBundle + TwigBundle + WebProfilerBundle + DoctrineFixturesBundle (D-07, D-10)
- Demo `tenancy.yaml` wires database_per_tenant driver + Host/Origin resolvers + strict_mode + DemoTenant entity class (D-04, D-08, CLAUDE.md)
- Demo `doctrine.yaml` defines landlord+tenant EMs with `TenantConnection` wrapper on the tenant connection (D-04)
- Demo `mailer.yaml` points at Mailpit DSN (D-09)
- No invalid PHP, no malformed YAML
</success_criteria>

<output>
Create `.planning/phases/21-demo-app/21-01-SUMMARY.md` documenting:
- Composer manifest shape (path-repo, require/require-dev lists)
- `bundles.php` shape
- `tenancy.yaml` config keys (driver, host, origin allow-list, resolvers, strict_mode, tenant_entity_class)
- `doctrine.yaml` two-EM split (landlord at `src/Entity/Landlord`, tenant at `src/Entity/Tenant`)
- Any deviations from PATTERNS.md analogs and the rationale
</output>
</content>
</invoke>