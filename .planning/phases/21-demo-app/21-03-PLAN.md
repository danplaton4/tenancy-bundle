---
phase: 21-demo-app
plan: 03
type: execute
wave: 2
depends_on: ["21-01", "21-02"]
files_modified:
  - examples/saas/compose.yaml
  - examples/saas/Dockerfile
  - examples/saas/Caddyfile
  - examples/saas/docker/entrypoint.sh
  - examples/saas/docker/php.ini
autonomous: true
requirements: [DEMO-01]

must_haves:
  truths:
    - "`docker compose up -d --wait --build` brings up three services (db: mariadb:11, mailpit: axllent/mailpit, php: FrankenPHP) and exits 0 within ~90s on a clean machine (DEMO-01.1)"
    - "MariaDB healthcheck uses `healthcheck.sh --connect --innodb_initialized` (RESEARCH Pitfall 2 mitigation — not bare `mysqladmin ping`)"
    - "FrankenPHP healthcheck is `curl -fsS http://localhost/health` and only goes healthy AFTER `app:seed-demo` completes because the entrypoint runs `bin/console app:seed-demo --no-interaction` BEFORE `exec frankenphp run` (RESEARCH Pitfall 5)"
    - "Caddy serves both HTTP (:80) and HTTPS (:443) for `*.tenancy.localhost`, `tenancy.localhost`, `localhost`; `auto_https disable_redirects` lets HTTP-default walkthrough work (RESEARCH Pitfall 1; D-11)"
    - "Dockerfile installs Composer deps at BUILD time (image is self-contained for CI per D-16); bind-mount strategy keeps bundle source dev-loop working (RESEARCH Pattern 3)"
    - "OPcache configured `validate_timestamps=1` + `revalidate_freq=0` for dev source-edit reflection (RESEARCH Pattern 4; CONTEXT 'Critical Edge: Bundle-Source Dev Loop')"
    - "Mailpit is published on `127.0.0.1:8025` only (NOT 0.0.0.0 — security per RESEARCH §Security)"
  artifacts:
    - path: "examples/saas/compose.yaml"
      provides: "Three-service compose (db, mailpit, php) with healthchecks and depends_on"
    - path: "examples/saas/Dockerfile"
      provides: "FrankenPHP build with composer install + bundle source copy"
    - path: "examples/saas/Caddyfile"
      provides: "Wildcard vhost for *.tenancy.localhost + tls internal"
    - path: "examples/saas/docker/entrypoint.sh"
      provides: "Seed-before-serve entrypoint (Pitfall 5 mitigation)"
    - path: "examples/saas/docker/php.ini"
      provides: "OPcache dev config (Pattern 4)"
  key_links:
    - from: "examples/saas/docker/entrypoint.sh"
      to: "examples/saas/src/Command/SeedDemoCommand.php (Plan 02)"
      via: "bin/console app:seed-demo --no-interaction"
      pattern: 'app:seed-demo'
    - from: "examples/saas/compose.yaml"
      to: "examples/saas/src/Controller/HealthController.php (Plan 02)"
      via: "healthcheck.test: curl /health"
      pattern: '/health'
    - from: "examples/saas/Dockerfile"
      to: "../../ (bundle source root)"
      via: "COPY bundle source into build context for `composer install`"
      pattern: 'COPY.*bundle'
---

<objective>
Land the container stack: `compose.yaml` (db + mailpit + php), `Dockerfile` (FrankenPHP image with build-time composer install), `Caddyfile` (wildcard vhost with `tls internal` + HTTP-default), `docker/entrypoint.sh` (seed BEFORE exec frankenphp), and `docker/php.ini` (OPcache dev tuning).

Purpose: Make `docker compose up -d --wait --build` from `examples/saas/` boot the demo app to a state where curl returns 200 with per-tenant body markers. This satisfies DEMO-01.1 and primes Plan 04 (smoke + CI).

Output: 5 infra files that, combined with Plans 01-02 source, produce a runnable demo.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/21-demo-app/21-CONTEXT.md
@.planning/phases/21-demo-app/21-RESEARCH.md
@.planning/phases/21-demo-app/21-PATTERNS.md

# Prior plans (this plan wires their outputs)
@.planning/phases/21-demo-app/21-01-PLAN.md
@.planning/phases/21-demo-app/21-02-PLAN.md

<interfaces>
<!-- Files this plan must invoke from the prior plans -->

From Plan 02 — examples/saas/src/Command/SeedDemoCommand.php:
- Console command name: `app:seed-demo`
- Idempotent on re-run
- Issues CREATE DATABASE IF NOT EXISTS via root credentials (reads $_ENV['MARIADB_ROOT_PASSWORD'])
- Connects to host 'db' (compose service name)

From Plan 02 — examples/saas/src/Controller/HealthController.php:
- Route: GET /health
- Returns 200 OK
- No host constraint (responds to `Host: localhost`)

From Plan 01 — examples/saas/composer.json:
- Path repo at `../../` with symlink:true
- Container must COPY the bundle source into the SAME relative position before `composer install`

From Plan 01 — examples/saas/.env:
- DATABASE_URL_LANDLORD points at host `db` (compose service name)
- MAILER_DSN=smtp://mailpit:1025 (compose service name)
- MARIADB_ROOT_PASSWORD=root, MARIADB_USER=tenancy, MARIADB_PASSWORD=tenancy
</interfaces>

<deviations_from_context>
- CONTEXT D-05 said "tenancy:migrate --create-dbs" in entrypoint. Replaced with `bin/console app:seed-demo --no-interaction` (per Plan 02 + RESEARCH §"Critical Discrepancy" Option 1).
- Per RESEARCH §"Pitfall 5" the seed runs BEFORE `exec frankenphp run` (not in parallel with healthcheck) so `/health` only goes green after seeding completes.
- Per RESEARCH §"Pattern 3" the path-repo strategy uses Dockerfile COPY of the bundle source into a position that matches the `../../` relative URL in composer.json, AND a runtime bind-mount of bundle source over that position so live edits reflect in the container. This means the bundle source ends up at `/bundle-src/` in the image (during build) and is bind-mounted at the same path at runtime — the demo's composer.json `url: "../../"` resolves to `/bundle-src/` because composer is run from `/app/` and `../../` from `/app/` is `/bundle-src/` (if `/app` is the demo and `/bundle-src` is one level up). To make `../../` work cleanly, we place the demo at `/app/demo/` and the bundle at `/app/` inside the image — but a simpler scheme: place demo at `/srv/app/demo/` and bundle at `/srv/app/`. Final choice: demo at `/srv/demo/` and bundle at `/srv/` so `../../` from `/srv/demo/` resolves to `/srv/`. Executor finalizes the exact paths during implementation.
- `actions/checkout@v5` is in Plan 04 (this plan touches no workflows).
</deviations_from_context>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host -> Docker compose ports | :80, :443 (public on localhost), :8025 (Mailpit, 127.0.0.1 only) |
| Container -> MariaDB | Internal compose network only; not published to host |
| Caddy internal CA | Per-container ephemeral; not committed to repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-MP | Information Disclosure | Mailpit UI exposes test emails | mitigate | `ports: ['127.0.0.1:8025:8025']` in compose.yaml (NOT `0.0.0.0`); Mailpit SMTP `1025` NOT published at all (internal network only) |
| T-21-DB | Information Disclosure | MariaDB credentials | accept | Credentials are committed demo defaults (root/root); MariaDB port `3306` NOT published to host; intra-compose only |
| T-21-CA | Spoofing | Caddy internal CA root cert leakage | mitigate | CA root generated inside Caddy's volume on first run; ephemeral; never committed. README (Plan 04) HTTPS section documents `caddy trust` as optional. |
| T-21-PI | Tampering | OPcache `validate_timestamps=1` in dev | accept | Required for the bundle-source dev loop (CONTEXT 'Critical Edge'). README (Plan 04) documents these are dev-only knobs, NOT production-shaped. |
| T-21-SC | Tampering | Container image supply chain | mitigate | Pin image tags: `mariadb:11` (major-pinned), `axllent/mailpit:v1.20` (minor-pinned per RESEARCH A3), `dunglas/frankenphp:1-php8.2-bookworm` (minor-pinned). Re-evaluate pins on each Docker pull. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Caddyfile + docker/php.ini (edge + runtime config)</name>
  <files>examples/saas/Caddyfile, examples/saas/docker/php.ini</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/Caddyfile" (lines 372-389 — verbatim block)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pattern 2: Caddyfile" (lines 515-535)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pitfall 1" (lines 620-628 — auto_https disable_redirects)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pattern 4: OPcache Dev Loop" (lines 572-582)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-11" (HTTP-default + HTTPS optional)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"Critical Edge: Bundle-Source Dev Loop" (lines 497-503)
  </read_first>
  <action>
    Create `examples/saas/Caddyfile`:
    ```
    {
        frankenphp
        auto_https disable_redirects
    }

    *.tenancy.localhost, tenancy.localhost, localhost {
        tls internal

        root * public/
        encode zstd br gzip

        php_server
    }
    ```

    Why these specific lines:
    - Global `frankenphp` directive enables the FrankenPHP module (required by `dunglas/frankenphp` image).
    - `auto_https disable_redirects` keeps HTTPS available on :443 BUT prevents HTTP->HTTPS 301s on :80, so the README's HTTP walkthrough works without cert friction (D-11; RESEARCH Pitfall 1).
    - The matcher list `*.tenancy.localhost, tenancy.localhost, localhost` covers per-tenant subdomains (TenantController), landlord apex (LandlordController), AND the in-container healthcheck path (`curl http://localhost/health` resolves via the `localhost` matcher).
    - `tls internal` directs Caddy to use its internal CA — wildcard cert is auto-generated for the entire matcher list. No external ACME, no DNS challenge.
    - `root * public/` points Caddy at the Symfony front-controller directory. The CWD inside the container will be `/srv/demo/` (the demo app root) so this resolves to `/srv/demo/public/`.
    - `php_server` invokes FrankenPHP's `php_server` directive (idiomatic for Symfony per https://frankenphp.dev/docs/symfony/).

    Create `examples/saas/docker/php.ini`:
    ```
    ; OPcache — DEV tuning, NOT production-shaped (CONTEXT Critical Edge)
    opcache.enable=1
    opcache.validate_timestamps=1
    opcache.revalidate_freq=0

    ; Symfony recommended (dev)
    memory_limit=256M
    max_execution_time=60
    date.timezone=UTC

    ; Realpath cache (modest dev sizing)
    realpath_cache_size=4M
    realpath_cache_ttl=600
    ```

    Why:
    - `revalidate_freq=0` + `validate_timestamps=1` makes OPcache check file mtime on every request — required for the bundle-source dev loop (edit `src/Resolver/HostResolver.php`, no rebuild needed).
    - `memory_limit=256M` accommodates Symfony dev profiler.
    - `date.timezone=UTC` silences a PHP warning.
  </action>
  <verify>
    <automated>test -f examples/saas/Caddyfile && grep -q 'auto_https disable_redirects' examples/saas/Caddyfile && grep -q '\*.tenancy.localhost' examples/saas/Caddyfile && grep -q 'tls internal' examples/saas/Caddyfile && grep -q 'php_server' examples/saas/Caddyfile && grep -q 'root \* public/' examples/saas/Caddyfile && test -f examples/saas/docker/php.ini && grep -q '^opcache.validate_timestamps=1$' examples/saas/docker/php.ini && grep -q '^opcache.revalidate_freq=0$' examples/saas/docker/php.ini</automated>
  </verify>
  <acceptance_criteria>
    - `Caddyfile` contains `auto_https disable_redirects` (Pitfall 1)
    - `Caddyfile` matcher list includes `*.tenancy.localhost`, `tenancy.localhost`, AND `localhost` (the last is for in-container healthcheck)
    - `Caddyfile` contains `tls internal` (no external ACME)
    - `Caddyfile` contains `php_server` directive
    - `php.ini` contains `opcache.validate_timestamps=1` AND `opcache.revalidate_freq=0` (dev source-edit loop)
    - `php.ini` is dev-shaped (not production); a comment documents this
  </acceptance_criteria>
  <done>Caddy serves wildcard vhost + tls internal + HTTP-default; PHP runtime OPcache tuned for dev source-edit loop.</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile + docker/entrypoint.sh</name>
  <files>examples/saas/Dockerfile, examples/saas/docker/entrypoint.sh</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/docker/entrypoint.sh" (lines 393-407 — seed-before-serve)
    - .planning/phases/21-demo-app/21-PATTERNS.md §"No Analog Found" §Dockerfile (lines 611 — RESEARCH refs)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pattern 3: Composer Path Repository in Docker" (lines 538-569 — bind-mount strategy)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pitfall 5" (lines 658-670 — healthcheck-vs-seed race)
    - examples/saas/composer.json (Plan 01 — to know the path-repo `../../` shape)
    - examples/saas/src/Command/SeedDemoCommand.php (Plan 02 — `app:seed-demo` is invoked from entrypoint)
    - https://frankenphp.dev/docs/symfony/ (FrankenPHP Symfony integration — for `SERVER_NAME` and base image conventions)
  </read_first>
  <action>
    Create `examples/saas/Dockerfile`:
    ```
    # syntax=docker/dockerfile:1.7
    FROM dunglas/frankenphp:1-php8.2-bookworm AS base

    # System deps for pdo_mysql + git (composer needs it for path repos sometimes)
    RUN apt-get update && apt-get install -y --no-install-recommends \
            git \
            unzip \
            curl \
            && rm -rf /var/lib/apt/lists/*

    # PHP extensions: pdo_mysql, opcache (already in base), intl (Symfony recommends)
    RUN install-php-extensions \
            pdo_mysql \
            intl \
            opcache \
            zip

    # Composer from official image
    COPY --from=composer:2 /usr/bin/composer /usr/bin/composer

    # Custom php.ini
    COPY docker/php.ini /usr/local/etc/php/conf.d/zz-demo.ini

    # Working dirs
    # Bundle source lives at /srv/ (so composer.json's `url: "../../"` from /srv/demo/ resolves to /srv/)
    WORKDIR /srv

    # Copy the bundle source first (build context is the demo dir; Dockerfile uses an extra build arg / docker compose build context to reach the bundle)
    # In compose.yaml the build context is the examples/saas/ dir, but we need the bundle source one level up.
    # SOLUTION: use a multi-stage trick — bundle source is provided via build context = ../../ (configured in compose.yaml `build.context: ../../` with `dockerfile: examples/saas/Dockerfile`).
    # With build.context: ../../ the entire repo is the build context, so we COPY paths relative to the repo root.

    # 1) Bundle source -> /srv/ (excluding examples/saas/)
    COPY src/ /srv/src/
    COPY config/ /srv/config/
    COPY composer.json composer.lock* /srv/

    # 2) Demo source -> /srv/demo/
    WORKDIR /srv/demo
    COPY examples/saas/composer.json examples/saas/composer.lock* ./
    COPY examples/saas/ /srv/demo/

    # 3) Install demo composer deps (will use the path repo at ../../ which is /srv/)
    RUN composer install --no-interaction --no-progress --prefer-dist --no-scripts

    # 4) Set up entrypoint
    RUN chmod +x bin/console docker/entrypoint.sh

    # OPcache + Caddy config
    ENV SERVER_NAME=:80,:443
    ENV APP_ENV=dev
    ENV APP_DEBUG=1

    # Caddy config is loaded from /srv/demo/Caddyfile via FrankenPHP env
    ENV CADDY_GLOBAL_OPTIONS="frankenphp"

    # Expose ports
    EXPOSE 80 443

    # Healthcheck calls /health which is satisfied by Plan 02's HealthController
    HEALTHCHECK --interval=5s --timeout=3s --start-period=120s --retries=5 \
        CMD curl -fsS http://localhost/health || exit 1

    ENTRYPOINT ["/srv/demo/docker/entrypoint.sh"]
    ```

    Notes for executor:
    - The build context in `compose.yaml` (Task 3) MUST be set to `../../` (repo root) with `dockerfile: examples/saas/Dockerfile` so the COPY paths above resolve correctly.
    - The `# syntax=docker/dockerfile:1.7` directive at the top enables BuildKit features. With BuildKit the trailing `*` in `composer.lock*` makes the lock file optional — the `COPY composer.json composer.lock* /srv/` (and the demo equivalent `COPY examples/saas/composer.json examples/saas/composer.lock* ./`) succeeds whether or not the bundle root (or the demo) commits a lockfile. The demo's `composer install` runs against `composer.json` only when no lock is present (this is fine for a demo). No executor decision required.
    - The `SERVER_NAME=:80,:443` env tells FrankenPHP/Caddy to listen on both ports (HTTP-default + HTTPS optional, D-11).
    - `APP_ENV=dev` enables the Web Profiler (D-07).
    - `start-period=120s` gives the entrypoint time to seed before healthcheck failures count against the container.

    Create `examples/saas/docker/entrypoint.sh`:
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail

    # Per RESEARCH §"Pitfall 5": run app:seed-demo BEFORE exec'ing FrankenPHP so /health
    # only goes green after seed completes (no race between healthcheck and seed).
    # Compose's depends_on: service_healthy already waits for MariaDB.

    cd /srv/demo

    echo "==> Seeding demo (idempotent)"
    bin/console app:seed-demo --no-interaction

    echo "==> Starting FrankenPHP"
    exec frankenphp run --config /srv/demo/Caddyfile
    ```

    Notes:
    - `set -euo pipefail` — exit on any failure (cascades to container exit).
    - `--no-interaction` — never prompt; CI-safe.
    - `exec` replaces the bash process so signals (SIGTERM from `docker stop`) reach FrankenPHP cleanly.
    - The script is also chmod +x'd in the Dockerfile (`RUN chmod +x`).
  </action>
  <verify>
    <automated>test -f examples/saas/Dockerfile && test -f examples/saas/docker/entrypoint.sh && grep -q 'dunglas/frankenphp' examples/saas/Dockerfile && grep -q 'pdo_mysql' examples/saas/Dockerfile && grep -q 'COPY docker/php.ini' examples/saas/Dockerfile && grep -q 'composer install' examples/saas/Dockerfile && grep -q 'HEALTHCHECK' examples/saas/Dockerfile && grep -q 'curl -fsS http://localhost/health' examples/saas/Dockerfile && grep -q 'ENTRYPOINT.*entrypoint.sh' examples/saas/Dockerfile && grep -q '^# syntax=docker/dockerfile:1' examples/saas/Dockerfile && grep -q 'COPY composer.json composer.lock\* /srv/' examples/saas/Dockerfile && grep -q '^set -euo pipefail$' examples/saas/docker/entrypoint.sh && grep -q 'app:seed-demo --no-interaction' examples/saas/docker/entrypoint.sh && grep -q '^exec frankenphp run' examples/saas/docker/entrypoint.sh && ! grep -q 'tenancy:migrate.*--create-dbs' examples/saas/docker/entrypoint.sh</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` first line is `# syntax=docker/dockerfile:1.7` (enables BuildKit features including the optional-lock pattern)
    - `Dockerfile` uses `dunglas/frankenphp:1-php8.2-bookworm` base (pinned, RESEARCH A3 supply-chain stance)
    - `Dockerfile` installs `pdo_mysql` extension (MariaDB driver)
    - `Dockerfile` runs `composer install` at build time (D-16: image self-contained for CI)
    - `Dockerfile` uses the optional-lock COPY pattern `COPY composer.json composer.lock* /srv/` for the bundle root (BuildKit trailing `*` makes the lock optional — works whether the bundle commits a lockfile or not)
    - `Dockerfile` uses the optional-lock COPY pattern `COPY examples/saas/composer.json examples/saas/composer.lock* ./` for the demo (same rationale)
    - `Dockerfile` defines a HEALTHCHECK calling `http://localhost/health` (matches Plan 02 HealthController)
    - `Dockerfile` HEALTHCHECK has `start-period >= 60s` (RESEARCH Pitfall 5: gives entrypoint time to seed)
    - `Dockerfile` ENTRYPOINT points at `docker/entrypoint.sh`
    - `entrypoint.sh` uses `set -euo pipefail`
    - `entrypoint.sh` invokes `bin/console app:seed-demo --no-interaction` (matches Plan 02's command name)
    - `entrypoint.sh` ends with `exec frankenphp run --config /srv/demo/Caddyfile`
    - `entrypoint.sh` does NOT invoke `tenancy:migrate --create-dbs` (CONTEXT D-05 discrepancy mitigated)
  </acceptance_criteria>
  <done>Image builds with composer deps + entrypoint seeds before serving + healthcheck honors seed completion.</done>
</task>

<task type="auto">
  <name>Task 3: compose.yaml (three services + healthchecks + volumes)</name>
  <files>examples/saas/compose.yaml</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/compose.yaml" (lines 347-369 — diverge table)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pitfall 2" (lines 630-638 — MariaDB healthcheck.sh)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pattern 3" (lines 540-557 — bind-mount strategy)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-15" (CI readiness gate — `docker compose up -d --wait`)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Open Questions §4" (lines 815-818 — port 80 default)
    - /Users/danplaton/dev/hype/tests/symfony74-demo/compose.yaml (external — depends_on/healthcheck shape; diverge per PATTERNS)
    - examples/saas/.env (Plan 01 — env var names this compose reads)
    - examples/saas/Dockerfile (Task 2 above — build context paths)
  </read_first>
  <action>
    Create `examples/saas/compose.yaml`:
    ```yaml
    services:
      db:
        image: mariadb:11
        environment:
          MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root}
          MARIADB_USER: ${MARIADB_USER:-tenancy}
          MARIADB_PASSWORD: ${MARIADB_PASSWORD:-tenancy}
          MARIADB_DATABASE: landlord
        healthcheck:
          test: ['CMD', 'healthcheck.sh', '--connect', '--innodb_initialized']
          interval: 5s
          timeout: 3s
          retries: 20
          start_period: 30s
        # MariaDB port NOT published — internal compose network only (security; T-21-DB)
        volumes:
          - db-data:/var/lib/mysql

      mailpit:
        image: axllent/mailpit:v1.20
        ports:
          # SMTP NOT published (T-21-MP): internal network only
          - '127.0.0.1:8025:8025'   # UI bound to loopback only (security)
        environment:
          MP_MAX_MESSAGES: 5000
          MP_DATABASE: /data/mailpit.db
        volumes:
          - mailpit-data:/data
        healthcheck:
          test: ['CMD', 'wget', '-q', '-O', '/dev/null', 'http://localhost:8025/api/v1/info']
          interval: 5s
          timeout: 3s
          retries: 10

      php:
        build:
          context: ../../        # repo root — Dockerfile COPYs both bundle src and demo
          dockerfile: examples/saas/Dockerfile
        depends_on:
          db:
            condition: service_healthy
          mailpit:
            condition: service_healthy
        ports:
          - '${PORT_HTTP:-80}:80'
          - '${PORT_HTTPS:-443}:443'
        environment:
          APP_ENV: dev
          APP_DEBUG: '1'
          APP_SECRET: ${APP_SECRET:-demo-secret-not-for-production}
          DATABASE_URL_LANDLORD: 'mysql://${MARIADB_USER:-tenancy}:${MARIADB_PASSWORD:-tenancy}@db:3306/landlord?serverVersion=mariadb-11.0.0&charset=utf8mb4'
          MAILER_DSN: 'smtp://mailpit:1025'
          MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root}
          DATABASE_HOST: db
          TENANCY_DOMAIN_BASE: tenancy.localhost
        volumes:
          # Bundle-source dev loop (CONTEXT Critical Edge): mount the bundle source over
          # the image's /srv position so edits to ../../src/** reflect in the container.
          - ../../src:/srv/src:rw
          # Optionally mount demo source too for tweaking templates without rebuild
          - ./src:/srv/demo/src:rw
          - ./templates:/srv/demo/templates:rw
          - ./config:/srv/demo/config:rw
          # Caddy data (CA cert lives here, ephemeral)
          - caddy-data:/data
          - caddy-config:/config

    volumes:
      db-data:
      mailpit-data:
      caddy-data:
      caddy-config:
    ```

    Why these specific lines:
    - `db.healthcheck`: uses `healthcheck.sh --connect --innodb_initialized` (RESEARCH Pitfall 2 — `mysqladmin ping` reports healthy too early; `healthcheck.sh` from the MariaDB image waits for innodb init).
    - `db` does NOT publish port `3306` — intra-compose network only.
    - `mailpit.ports`: `'127.0.0.1:8025:8025'` binds UI to loopback only (T-21-MP). SMTP `1025` is NOT in `ports:` so it's internal-network-only (php service reaches it via `smtp://mailpit:1025`).
    - `php.build.context: ../../` — Dockerfile needs both bundle source AND demo source; using repo root as context gives access to both via paths `examples/saas/...` and `src/...`.
    - `php.depends_on` uses `condition: service_healthy` so `--wait` actually means "everything is ready".
    - `php.ports` defaults to 80/443 (RESEARCH §"Open Questions §4") with env-var override for users on platforms where port 80 needs sudo.
    - `php.volumes` bind-mount the bundle's `src/` over `/srv/src/` in the container — this is the dev-loop knob. Edit `src/Resolver/HostResolver.php` on the host, `docker compose restart php` (or just hit the URL — OPcache revalidate_freq=0 picks it up), changes visible.
    - `caddy-data` volume persists the internal CA root cert across `docker compose up`/`down`, so `caddy trust` only needs to run once.

    Notes for executor:
    - `mailpit:v1.20` is pinned (per RESEARCH §A3 — `latest` is not safe).
    - The Dockerfile's optional-lock COPY pattern (`composer.lock*` with BuildKit `# syntax=docker/dockerfile:1.7`) handles the bundle-root-may-not-have-a-lockfile case — no compose-level adaptation needed.
  </action>
  <verify>
    <automated>test -f examples/saas/compose.yaml && grep -q 'image: mariadb:11' examples/saas/compose.yaml && grep -q 'image: axllent/mailpit:v1.20' examples/saas/compose.yaml && grep -q 'healthcheck.sh.*--connect.*--innodb_initialized' examples/saas/compose.yaml && grep -q "127.0.0.1:8025:8025" examples/saas/compose.yaml && ! grep -E '^\s+- .3306:3306' examples/saas/compose.yaml && grep -q 'condition: service_healthy' examples/saas/compose.yaml && grep -q 'context: ../../' examples/saas/compose.yaml && grep -q 'dockerfile: examples/saas/Dockerfile' examples/saas/compose.yaml && grep -q 'MAILER_DSN.*smtp://mailpit:1025' examples/saas/compose.yaml && grep -q '../../src:/srv/src' examples/saas/compose.yaml && grep -q 'PORT_HTTP:-80' examples/saas/compose.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `compose.yaml` defines exactly three services: `db`, `mailpit`, `php`
    - `db` uses `mariadb:11` (DEC-DEMO-01) with `healthcheck.sh --connect --innodb_initialized` (Pitfall 2)
    - `db` port `3306` is NOT published to host
    - `mailpit` uses `axllent/mailpit:v1.20` (pinned, RESEARCH A3)
    - `mailpit` UI port is bound to `127.0.0.1:8025` (NOT `0.0.0.0`)
    - `mailpit` SMTP `1025` is NOT in `ports:` (internal only)
    - `php` `depends_on` has `db: { condition: service_healthy }` AND `mailpit: { condition: service_healthy }`
    - `php.build.context` is `../../` (repo root) with `dockerfile: examples/saas/Dockerfile`
    - `php` publishes both `80:80` and `443:443` (with PORT_HTTP/HTTPS overrides)
    - `php` bind-mounts `../../src:/srv/src` (bundle-source dev loop)
    - `php` environment has `MAILER_DSN=smtp://mailpit:1025` and `DATABASE_URL_LANDLORD` pointing at host `db`
  </acceptance_criteria>
  <done>`docker compose up -d --wait --build` from `examples/saas/` brings up db + mailpit + php with healthchecks; `--wait` returns after php is healthy (which requires seed completion).</done>
</task>

</tasks>

<verification>
After all three tasks (with prior plans landed):
1. `cd examples/saas && docker compose config` parses without error (yaml + compose schema valid)
2. `cd examples/saas && docker compose up -d --wait --build` exits 0 within 90s on a warm cache
3. `docker compose ps` shows three services with state `running` and health `healthy`
4. `curl -fsS -H "Host: tenancy.localhost" http://localhost/` returns 200 with body containing all three tenant slugs
5. `curl -fsS -H "Host: acme.tenancy.localhost" http://localhost/` returns 200 with body containing `Acme Corporation`
6. `docker compose logs php | grep -q "Seeding demo"` (entrypoint banner visible)
7. `docker compose logs php | grep -v "tenancy:migrate.*--create-dbs"` returns no match (CONTEXT D-05 fix in place)
8. `docker compose down -v` cleans up volumes
</verification>

<success_criteria>
- `docker compose up -d --wait --build` brings up three services healthy within 90s (DEMO-01.1)
- MariaDB uses `healthcheck.sh` (not bare ping) — Pitfall 2 mitigated
- FrankenPHP healthcheck only goes green after seed — Pitfall 5 mitigated
- Caddy serves both HTTP (default walkthrough per D-11) and HTTPS (optional)
- Mailpit UI bound to `127.0.0.1:8025` only (T-21-MP mitigated)
- Bundle source bind-mount makes the dev loop work (CONTEXT Critical Edge)
- Entrypoint invokes `app:seed-demo` (NOT `tenancy:migrate --create-dbs`)
- Container images pinned (mariadb:11, mailpit:v1.20, frankenphp:1-php8.2-bookworm)
</success_criteria>

<output>
Create `.planning/phases/21-demo-app/21-03-SUMMARY.md` documenting:
- Three-service compose topology (db + mailpit + php) with healthcheck choices
- Dockerfile build strategy (repo-root build context; bundle source COPY + bind-mount; composer install at build time; BuildKit optional-lock pattern)
- Caddyfile shape (`auto_https disable_redirects`, `tls internal`, wildcard matcher)
- Entrypoint ordering (seed BEFORE exec frankenphp — Pitfall 5)
- Volume strategy (db-data persist, caddy CA persist, bundle-src bind-mount for dev loop)
- Image pins and rationale
- Any deviations from PATTERNS.md / RESEARCH and why
</output>
</content>
</invoke>
