---
phase: 21-demo-app
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified:
  - examples/saas/bin/smoke.sh
  - examples/saas/README.md
  - .github/workflows/demo-smoke.yml
  - .gitignore
  - README.md
autonomous: false
requirements: [DEMO-01]

must_haves:
  truths:
    - "D-13: `bin/smoke.sh` runs on the HOST (not in-container), curls `localhost` with `Host:` headers; same script local + CI. `bash examples/saas/bin/smoke.sh` against a running demo (after `docker compose up -d --wait`) exits 0 (DEMO-01.4)"
    - "Smoke script asserts per-tenant body markers: Acme Corporation, Globex Industries, Initech LLC (DEMO-01.2; CONTEXT D-14)"
    - "Smoke script asserts the OriginHeaderResolver path: `-H 'Host: tenancy.localhost' -H 'Origin: https://acme.tenancy.localhost'` resolves to acme content (Phase 17 extension; D-14)"
    - "Smoke script asserts landlord page lists all three tenant slugs (DEMO-01.2; D-14)"
    - "`.github/workflows/demo-smoke.yml` runs on push to master + PRs, uses `actions/checkout@v5`, builds the compose stack with `docker compose up -d --wait --build`, runs `bash bin/smoke.sh`, tears down with `docker compose down -v`, and dumps logs on failure (DEMO-01.5)"
    - "Workflow uses `actions/checkout@v5` (NOT v4 as the RESEARCH excerpt erroneously showed; matches repo convention from `.github/workflows/ci.yml` line 21)"
    - "`examples/saas/README.md` has a `## Three-step fallback` section documenting (1) curl with Host header, (2) /etc/hosts line, (3) browser-native *.tenancy.localhost (DEMO-01.3; CONTEXT D-12)"
    - "README documents: HTTP-default walkthrough (D-11), optional HTTPS section with `caddy trust` and Firefox CA-import quirk (RESEARCH Pitfall 4), Mailer/Mailpit walkthrough (D-09), Profiler walkthrough (D-07), Origin-resolver curl example (D-08), `tenancy:install` reference for downstream adoption (D-10), bundle-source dev-loop instructions (CONTEXT Critical Edge)"
    - "D-17: external verification assets at `/Users/danplaton/dev/hype/tests/symfony74-demo/` and `symfony8x-demo/` are NOT modified by this phase (intentional non-scope — they remain as the user's external scratch space for post-build parity validation)"
    - "Root README points to `examples/saas/README.md` under a 'Try the demo' heading"
    - "Root `.gitignore` ignores `examples/saas/var/` and `examples/saas/vendor/` (RESEARCH Runtime State Inventory)"
  artifacts:
    - path: "examples/saas/bin/smoke.sh"
      provides: "Host-side DNS-independent curl smoke; exit non-zero on any failure"
      contains: "set -euo pipefail"
    - path: "examples/saas/README.md"
      provides: "User-facing walkthrough + three-step fallback ladder + walkthroughs for Mailpit / Profiler / Origin"
      contains: "## Three-step fallback"
    - path: ".github/workflows/demo-smoke.yml"
      provides: "Required CI gate — failure blocks merge to master"
    - path: "README.md"
      provides: "Pointer to `examples/saas/README.md` under 'Try the demo'"
    - path: ".gitignore"
      provides: "Ignore examples/saas/var, vendor"
  key_links:
    - from: "examples/saas/bin/smoke.sh"
      to: "examples/saas/src/Controller/HealthController.php (Plan 02)"
      via: "curl /health readiness probe"
      pattern: '/health'
    - from: "examples/saas/bin/smoke.sh"
      to: "examples/saas/src/Controller/{Landlord,Tenant}Controller.php (Plan 02)"
      via: "curl with Host:/Origin: headers"
      pattern: 'Host:.*tenancy.localhost'
    - from: ".github/workflows/demo-smoke.yml"
      to: "examples/saas/bin/smoke.sh"
      via: "bash bin/smoke.sh step"
      pattern: 'bash bin/smoke.sh'
    - from: ".github/workflows/demo-smoke.yml"
      to: "examples/saas/compose.yaml (Plan 03)"
      via: "docker compose up -d --wait --build"
      pattern: 'docker compose up -d --wait'
---

<objective>
Land the verification and adoption surface for the demo: `bin/smoke.sh` (host-side curl smoke per D-13/D-14), `.github/workflows/demo-smoke.yml` (CI gate per DEMO-01.5), the long-form `README.md` walkthrough (D-12 three-step fallback ladder, D-09 Mailpit walkthrough, D-07 Profiler walkthrough, D-08 Origin curl example, D-10 tenancy:install pointer, D-11 HTTPS-optional), root README pointer, and a `.gitignore` entry for `examples/saas/{var,vendor}`.

Purpose: This plan closes the DEMO-01 success criteria (acceptance lines 2-5 in REQUIREMENTS.md) and the human-checkpoint step for the README's three-step ladder. The CI workflow becomes a required status check after the first successful run.

Output: Smoke script (executable), CI workflow, README files, .gitignore update.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/21-demo-app/21-CONTEXT.md
@.planning/phases/21-demo-app/21-RESEARCH.md
@.planning/phases/21-demo-app/21-PATTERNS.md
@.planning/phases/21-demo-app/21-VALIDATION.md

# Prior plans (this plan invokes their outputs)
@.planning/phases/21-demo-app/21-01-PLAN.md
@.planning/phases/21-demo-app/21-02-PLAN.md
@.planning/phases/21-demo-app/21-03-PLAN.md

<interfaces>
<!-- Outputs from prior plans this plan invokes -->

From Plan 02:
- HealthController @ GET /health — returns "OK" status 200 (no host constraint)
- LandlordController @ GET / (host: tenancy.localhost) — renders 3 tenants
- TenantController @ GET / (host: {slug}.tenancy.localhost) — renders tenant.name marker
- DemoMailController @ POST /_demo/send-test-mail — fires Mailer (D-09)

From Plan 03:
- compose.yaml services: db, mailpit, php
- php publishes :80 and :443 (env-var override PORT_HTTP/HTTPS)
- mailpit UI on 127.0.0.1:8025 (host)
- Caddy serves *.tenancy.localhost + tenancy.localhost + localhost

From existing repo:
- .github/workflows/ci.yml line 21 uses `actions/checkout@v5` — match this version exactly
- README.md (repo root) — has a "Try the demo" placement target (or near "Quick Start" section)
</interfaces>

<deviations_from_context>
- `actions/checkout@v5` (matches `.github/workflows/ci.yml` line 21) — NOT v4 which the RESEARCH §"CI workflow" excerpt at lines 757 erroneously showed. The PATTERNS.md §"Pattern D" calls this out explicitly.
- README's HTTPS subsection documents the Firefox-specific manual CA import (RESEARCH Pitfall 4), which CONTEXT D-11 covers only in passing.
- This plan is `autonomous: false` because the final task is a `checkpoint:human-verify` step asking the user to walk the README's three-step ladder on their actual machine + browser (Chrome/Safari/Firefox depending on OS) — DEMO-01.3 acceptance cannot be fully automated.
</deviations_from_context>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> Docker compose | Ephemeral Linux runner; full root; pulls images from Docker Hub |
| `bin/smoke.sh` -> localhost:80 | Local loopback; no network egress beyond image pulls |
| README copy-paste -> User's shell | Trusted user environment; document destructive ops clearly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-CI | Tampering | CI workflow could leak secrets to logs | mitigate | Workflow uses NO secrets (demo is secrets-free per RESEARCH §Runtime State Inventory). `docker compose logs` on failure can dump container logs, but env vars are all committed demo defaults — no leak path. |
| T-21-IM | Spoofing | Docker image supply chain in CI | mitigate | Workflow pulls images by tag (mariadb:11, mailpit:v1.20, frankenphp:1-php8.2-bookworm) — pinned in compose.yaml. Re-evaluate pins quarterly. Consider digest-pinning for v0.4. |
| T-21-RM | Tampering | README copy-paste commands | accept | All commands are read-only (curl, docker compose up/down). The ONLY mutating command is `docker compose down -v` (volumes wiped, documented). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: bin/smoke.sh + repo-root .gitignore update</name>
  <files>examples/saas/bin/smoke.sh, .gitignore</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"examples/saas/bin/smoke.sh" (lines 411-423 — features summary)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Smoke script (bin/smoke.sh)" (lines 686-738 — full verbatim block)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-13, D-14" (host-side, body markers, exit non-zero)
    - .planning/phases/21-demo-app/21-VALIDATION.md (per-task verification map — smoke covers DEMO-01.2/04 + Phase 17 ext)
    - examples/saas/src/Controller/HealthController.php (Plan 02 — /health endpoint)
    - examples/saas/src/Controller/TenantController.php (Plan 02 — tenant slug routing)
    - ./.gitignore (existing repo-root gitignore — to know current shape before appending)
  </read_first>
  <action>
    Create `examples/saas/bin/smoke.sh` per RESEARCH §"Smoke script" lines 687-738 (verbatim block; PATTERNS §"smoke.sh" calls this out as the source of truth). Per D-13 the script MUST run on the host (not via `docker compose exec`) and curl `localhost` with `Host:` headers — same script local + CI:
    ```bash
    #!/usr/bin/env bash
    # bin/smoke.sh — DNS-independent demo smoke test.
    # Runs on host (CI runner or dev machine). Caddy must publish :80 on localhost.
    set -euo pipefail

    CURL='curl --fail --max-time 10 --retry 5 --retry-all-errors --retry-connrefused -sS'
    BASE='http://localhost'

    # Wait for /health to be ready (max 30s)
    echo "==> Waiting for app readiness…"
    for i in $(seq 1 30); do
        if curl -sf --max-time 2 "$BASE/health" >/dev/null 2>&1; then
            echo "    ready"
            break
        fi
        sleep 1
        [ "$i" -eq 30 ] && { echo "    timeout"; exit 1; }
    done

    # Landlord index — should list all three tenant slugs
    echo "==> Landlord root"
    body=$($CURL -H "Host: tenancy.localhost" "$BASE/")
    for slug in acme globex initech; do
        grep -q "$slug" <<<"$body" || { echo "FAIL: landlord index missing $slug"; exit 1; }
    done

    # Per-tenant marker assertions
    echo "==> Per-tenant landing pages"
    declare -A markers=(
        [acme]='Acme Corporation'
        [globex]='Globex Industries'
        [initech]='Initech LLC'
    )
    for slug in "${!markers[@]}"; do
        body=$($CURL -H "Host: $slug.tenancy.localhost" "$BASE/")
        grep -q "${markers[$slug]}" <<<"$body" || {
            echo "FAIL: $slug page missing '${markers[$slug]}'"; exit 1;
        }
    done

    # Origin-resolver path (Phase 17 invariant)
    echo "==> OriginHeaderResolver"
    body=$($CURL \
        -H "Host: tenancy.localhost" \
        -H "Origin: https://acme.tenancy.localhost" \
        "$BASE/")
    grep -q 'Acme Corporation' <<<"$body" || {
        echo "FAIL: Origin-resolver did not resolve acme"; exit 1;
    }

    echo "==> All smoke assertions PASSED"
    ```

    Notes:
    - The script MUST be executable. Write the file, then run `chmod +x examples/saas/bin/smoke.sh` via Bash.
    - The CURL flags `--retry 5 --retry-all-errors --retry-connrefused` handle transient errors without sleep loops (RESEARCH §"Don't Hand-Roll").
    - Per-tenant marker assertions use the `body marker` strings from CONTEXT D-14 — these match the fixture names from Plan 02.
    - The Origin-resolver curl uses `Host: tenancy.localhost` (landlord — HostResolver returns null) + `Origin: https://acme.tenancy.localhost` (OriginHeaderResolver matches against the allow-list and resolves acme). Body assertion is `Acme Corporation`.

    Append to repo-root `.gitignore` (read existing file first; do NOT overwrite — append the new entries if not already present):
    ```
    # Demo app generated files
    /examples/saas/var/
    /examples/saas/vendor/
    /examples/saas/.env.local
    /examples/saas/.env.*.local
    ```
    (Verify these aren't already covered by global patterns before appending.)
  </action>
  <verify>
    <automated>test -f examples/saas/bin/smoke.sh && test -x examples/saas/bin/smoke.sh && head -1 examples/saas/bin/smoke.sh | grep -q '^#!/usr/bin/env bash$' && grep -q '^set -euo pipefail$' examples/saas/bin/smoke.sh && grep -q 'curl --fail --max-time 10 --retry 5' examples/saas/bin/smoke.sh && grep -q 'Acme Corporation' examples/saas/bin/smoke.sh && grep -q 'Globex Industries' examples/saas/bin/smoke.sh && grep -q 'Initech LLC' examples/saas/bin/smoke.sh && grep -q 'Origin: https://acme.tenancy.localhost' examples/saas/bin/smoke.sh && grep -q 'examples/saas/var/' .gitignore && grep -q 'examples/saas/vendor/' .gitignore && bash -n examples/saas/bin/smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `examples/saas/bin/smoke.sh` exists and is executable (`test -x`)
    - Script shebang is `#!/usr/bin/env bash`
    - `bash -n examples/saas/bin/smoke.sh` exits 0 (syntactically valid)
    - Script has `set -euo pipefail`
    - Script uses `--retry 5 --retry-all-errors --retry-connrefused` (RESEARCH "Don't Hand-Roll")
    - Script asserts ALL THREE markers: `Acme Corporation`, `Globex Industries`, `Initech LLC` (D-14)
    - Script asserts the OriginHeaderResolver curl with `Host: tenancy.localhost` AND `Origin: https://acme.tenancy.localhost`
    - Script asserts landlord page lists all three slugs (acme, globex, initech)
    - Repo-root `.gitignore` contains `examples/saas/var/` and `examples/saas/vendor/`
  </acceptance_criteria>
  <done>Host-side smoke script ready and executable; works locally and in CI; .gitignore prevents committing generated files.</done>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/demo-smoke.yml + repo-root README pointer</name>
  <files>.github/workflows/demo-smoke.yml, README.md</files>
  <read_first>
    - .planning/phases/21-demo-app/21-PATTERNS.md §"Pattern D: CI workflow trigger + checkout" (lines 583-590 — checkout@v5 NOT v4)
    - .planning/phases/21-demo-app/21-PATTERNS.md §".github/workflows/demo-smoke.yml" (lines 427-475)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"CI workflow" (lines 742-768)
    - .github/workflows/ci.yml (in-repo — line 21 uses checkout@v5; verify versions match)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-15" (CI readiness gate: docker compose up -d --wait + curl-retry)
    - ./README.md (existing repo-root README — to know where to add the "Try the demo" pointer)
    - examples/saas/bin/smoke.sh (Task 1 above)
    - examples/saas/compose.yaml (Plan 03 — docker compose up target)
  </read_first>
  <action>
    Create `.github/workflows/demo-smoke.yml`:
    ```yaml
    name: demo-smoke

    on:
      push:
        branches: [master]
      pull_request:
        branches: [master]

    jobs:
      smoke:
        runs-on: ubuntu-latest
        timeout-minutes: 10
        defaults:
          run:
            working-directory: examples/saas
        steps:
          - name: Checkout
            uses: actions/checkout@v5

          - name: Build and start demo
            run: docker compose up -d --wait --build

          - name: Run smoke
            run: bash bin/smoke.sh

          - name: Dump container logs on failure
            if: failure()
            run: docker compose logs

          - name: Tear down
            if: always()
            run: docker compose down -v
    ```

    Why these specifics:
    - `actions/checkout@v5` — matches `.github/workflows/ci.yml` line 21 (PATTERNS §"Pattern D" calls this out; RESEARCH excerpt at line 757 erroneously showed v4 which we deliberately reject).
    - `timeout-minutes: 10` is the job timeout. Cold demo boot is ~110s per CONTEXT §"Two-minute promise"; well under.
    - `working-directory: examples/saas` keeps all `docker compose` invocations + `bash bin/smoke.sh` short.
    - `docker compose up -d --wait --build` (D-15): `--wait` blocks until all healthchecks pass; `--build` ensures the image is rebuilt on every CI run (catches Dockerfile drift).
    - `if: failure()` log dump aids debugging without blocking on success.
    - `if: always()` teardown ensures clean runner state.

    Update `README.md` (repo root) — append (or find existing demo section and replace) a "Try the demo" subsection. Read the current file first; place the new content near the top "Quick Start" / installation section, or just before the API reference:

    ```markdown
    ## Try the demo

    A runnable two-+-landlord Symfony app lives under [`examples/saas/`](examples/saas/README.md):

    ```bash
    git clone <this repo>
    cd <repo>/examples/saas
    docker compose up -d --wait --build
    open http://acme.tenancy.localhost/          # or curl with -H 'Host:'
    ```

    Three tenants (acme, globex, initech) on FrankenPHP + Caddy + MariaDB 11 with the Profiler tab and Mailpit always-up. See [`examples/saas/README.md`](examples/saas/README.md) for the full walkthrough + three-step fallback ladder + CI smoke details.
    ```

    Notes:
    - Use the EXACT existing README.md heading shape (don't change the doc's tone).
    - Do not delete anything from README.md — only ADD this subsection in an appropriate place.
    - D-17 reminder: external verification assets at `/Users/danplaton/dev/hype/tests/symfony74-demo/` and `symfony8x-demo/` are NOT modified by this phase or its README updates (intentional non-scope per CONTEXT D-17).
  </action>
  <verify>
    <automated>test -f .github/workflows/demo-smoke.yml && grep -q '^name: demo-smoke$' .github/workflows/demo-smoke.yml && grep -q 'actions/checkout@v5' .github/workflows/demo-smoke.yml && ! grep -q 'actions/checkout@v4' .github/workflows/demo-smoke.yml && grep -q 'docker compose up -d --wait --build' .github/workflows/demo-smoke.yml && grep -q 'bash bin/smoke.sh' .github/workflows/demo-smoke.yml && grep -q 'docker compose down -v' .github/workflows/demo-smoke.yml && grep -q "if: failure()" .github/workflows/demo-smoke.yml && grep -q "working-directory: examples/saas" .github/workflows/demo-smoke.yml && grep -q 'Try the demo' README.md && grep -q 'examples/saas/README.md' README.md && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/demo-smoke.yml'))" 2>/dev/null || ruby -ryaml -e "YAML.load_file('.github/workflows/demo-smoke.yml')" 2>/dev/null || echo 'note: yaml parser not installed, skipping schema check'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/demo-smoke.yml` exists with `name: demo-smoke`
    - Uses `actions/checkout@v5` (matches `.github/workflows/ci.yml` line 21)
    - Does NOT contain `actions/checkout@v4` (RESEARCH excerpt erratum corrected)
    - `working-directory: examples/saas` set at the job's `defaults.run`
    - Step order: checkout → up --wait --build → run smoke → (if failure) dump logs → (if always) down -v
    - Triggers on `push: branches: [master]` AND `pull_request: branches: [master]`
    - Has `timeout-minutes: 10`
    - YAML schema valid (parses without error)
    - Repo-root `README.md` contains a "Try the demo" section
    - Repo-root `README.md` links to `examples/saas/README.md`
  </acceptance_criteria>
  <done>CI workflow ready to gate master merges on demo smoke. After first successful push the workflow becomes eligible to add to required status checks (manual repo-admin step, documented in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 3: examples/saas/README.md (the long-form user walkthrough)</name>
  <files>examples/saas/README.md</files>
  <read_first>
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-12 Three-step fallback ladder" (lines 224-232)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-09" Mailer/Mailpit walkthrough
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-07" Profiler walkthrough
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-08" Origin curl example
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-10" tenancy:install reference (NOT exercised, just mentioned)
    - .planning/phases/21-demo-app/21-CONTEXT.md §"D-11" HTTPS-optional + caddy trust
    - .planning/phases/21-demo-app/21-CONTEXT.md §"Critical Edge: Bundle-Source Dev Loop" (lines 497-503)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Domain Research → Caddy" (lines 196-219 — Firefox CA-import quirk, Pitfall 4)
    - .planning/phases/21-demo-app/21-RESEARCH.md §"Pitfall 4, 6" (Firefox + WSL2)
    - .planning/phases/21-demo-app/21-VALIDATION.md §"Manual-Only Verifications" (lines 72-78 — what humans verify)
    - /Users/danplaton/dev/hype/tests/symfony74-demo/README.md (external — landlord dashboard UX pattern reference)
  </read_first>
  <action>
    Create `examples/saas/README.md` as the user-facing walkthrough. Sections required (verifier will grep for headings):

    ```markdown
    # Tenancy Bundle Demo — `examples/saas/`

    A runnable two-+-landlord Symfony app demonstrating the `danplaton4/tenancy-bundle` end-to-end. FrankenPHP + Caddy + MariaDB 11 + Mailpit, single `docker compose up`, three tenants (acme, globex, initech) on the same code path with isolated data.

    ## Two-minute boot

    ```bash
    cd examples/saas
    docker compose up -d --wait --build
    open http://acme.tenancy.localhost/
    ```

    On a warm image cache the stack boots in ~50s; cold (first image pull) ~110s. `--wait` returns only when every healthcheck has passed (db: innodb initialized, mailpit: ready, php: `/health` 200 — which only goes green after `app:seed-demo` completes).

    Once up:
    - `http://tenancy.localhost/` — landlord page (lists the three tenants)
    - `http://acme.tenancy.localhost/` — Acme tenant landing
    - `http://globex.tenancy.localhost/` — Globex tenant landing
    - `http://initech.tenancy.localhost/` — Initech tenant landing

    Each tenant page shows the tenant's brand name, accent color, and 2-3 seeded posts — distinct per tenant.

    ## Three-step fallback

    `*.tenancy.localhost` is RFC 6761 §6.3 magic — most modern browsers resolve it to 127.0.0.1 without DNS. If yours doesn't, walk the ladder:

    ### Step 1 — curl with `Host:` header (works everywhere)
    ```bash
    curl -fsS -H "Host: acme.tenancy.localhost" http://localhost/
    # Body should contain "Acme Corporation"
    ```
    This is exactly what `bin/smoke.sh` does, and what CI runs. DNS-independent.

    ### Step 2 — `/etc/hosts` (Safari + WSL2 fallback)
    Add to `/etc/hosts` (macOS/Linux) or `C:\Windows\System32\drivers\etc\hosts` (Windows / WSL2 — note: edit the **Windows-host** hosts file, not the WSL2 distro's):
    ```
    127.0.0.1 tenancy.localhost acme.tenancy.localhost globex.tenancy.localhost initech.tenancy.localhost
    ```

    ### Step 3 — Browser-native `*.tenancy.localhost`
    Out of the box on Chrome/Chromium (all OS), Firefox 84+ (since 2020), Microsoft Edge. Open `http://acme.tenancy.localhost/` and it just works.

    | OS | Browser | Works out of the box? |
    |---|---|---|
    | macOS | Chrome / Chromium / Edge | ✓ |
    | macOS | Safari | ✗ — use Step 2 |
    | macOS | Firefox 84+ | ✓ |
    | Linux | Chrome / Firefox | ✓ |
    | Windows | Chrome / Edge | ✓ |
    | Windows + WSL2 | Chrome on Windows | usually ✓; if not → Step 2 (Windows-host hosts file) |

    ## The Profiler walkthrough (Phase 19)

    The demo runs `APP_ENV=dev`, so the Symfony Web Debug Toolbar is on by default.

    1. Open `http://acme.tenancy.localhost/` in Chrome.
    2. At the bottom of the page, click the **Tenancy** icon in the WDT.
    3. Observe: `slug: acme`, `tenant_label: Acme Corporation`, `driver: database_per_tenant`, `connection_name: tenant`, `resolved_by: Tenancy\Bundle\Resolver\HostResolver`, and the list of bootstrappers that ran.
    4. Visit `http://tenancy.localhost/` (the landlord). The Tenancy panel still renders, but in its "no tenant" state — confirming null-resolution is a first-class state, not an error.

    ## The Mailer walkthrough (Phase 20)

    Mailpit is always up. Mailpit UI lives at `http://localhost:8025/`.

    Send a test email FROM each tenant (the ONLY write path in this demo):
    ```bash
    curl -X POST -H "Host: acme.tenancy.localhost" http://localhost/_demo/send-test-mail
    curl -X POST -H "Host: globex.tenancy.localhost" http://localhost/_demo/send-test-mail
    curl -X POST -H "Host: initech.tenancy.localhost" http://localhost/_demo/send-test-mail
    ```
    Open `http://localhost:8025/`. Three messages appear, with distinct `From:` and `Reply-To:` addresses (`noreply@acme.example`, `noreply@globex.example`, `noreply@initech.example`). The controller code is the same for every tenant; the bundle's `MailerBootstrapper` + `TenantMessageDecorator` inject the correct From/Reply-To per resolved tenant.

    ## The OriginHeaderResolver scenario (Phase 17)

    For SPAs that send `Origin` but not `X-Tenant-ID`, the bundle resolves the tenant from `Origin`:
    ```bash
    curl -fsS \
         -H "Host: tenancy.localhost" \
         -H "Origin: https://acme.tenancy.localhost" \
         http://localhost/
    # Body contains "Acme Corporation" — resolved via Origin chain (priority 25)
    ```
    The Host header points at the landlord (no leftmost-label slug); OriginHeaderResolver fires next in the chain and picks up the cross-origin Origin against the allow-list configured in `config/packages/tenancy.yaml`. See the bundle docs for `Origin`'s trust model — browser-protected for cross-origin XHR, but spoofable from non-browser clients.

    ## Optional: HTTPS with Caddy's local CA

    The default walkthrough uses HTTP — zero cert friction. To enable HTTPS:

    1. Trust Caddy's internal CA on your host:
       ```bash
       docker compose exec php caddy trust
       ```
       This installs the CA into the system trust store (Keychain on macOS, `/usr/local/share/ca-certificates/` on Linux). You'll be prompted for sudo.

    2. **Firefox** uses its own trust store. After `caddy trust`, also manually import the CA into Firefox:
       - `about:preferences#privacy` → View Certificates → Authorities → Import
       - Path: `docker compose exec php cat /data/caddy/pki/authorities/local/root.crt` (copy/paste into a temp file)

    3. Visit `https://acme.tenancy.localhost/` — cert is wildcard, covers all three tenants.

    ## What if I want to install Tenancy Bundle in MY app?

    Two commands (replace `my-app/` with your Symfony app dir):
    ```bash
    cd my-app
    composer require danplaton4/tenancy-bundle
    bin/console tenancy:install
    ```
    `tenancy:install` registers `TenancyBundle::class` in your `config/bundles.php` and runs `tenancy:init` to scaffold `config/packages/tenancy.yaml`. See the [main bundle README](../../README.md) for full docs.

    > The demo's own `config/bundles.php` is hand-committed — we don't run `tenancy:install` during demo boot because path-repo'd setups need a few extra wiring bits (Doctrine two-EM split etc.) that the standard installer doesn't generate.

    ## Bundle-source dev loop

    Path-repo with `symlink: true` + bind-mount: edits to `../../src/**` (the bundle's source) reflect inside the container immediately.

    ```bash
    # Edit a bundle file, e.g. add error_log() in src/Resolver/HostResolver.php
    docker compose restart php   # OPcache picks up the change (validate_timestamps=1, revalidate_freq=0)
    curl -H "Host: acme.tenancy.localhost" http://localhost/
    docker compose logs php | tail
    ```

    No image rebuild required. The compose.yaml bind-mounts `../../src` into `/srv/src` in the container.

    ## Smoke + CI

    `bin/smoke.sh` runs host-side curl assertions:
    ```bash
    cd examples/saas
    docker compose up -d --wait --build
    bash bin/smoke.sh
    # Asserts landlord lists 3 tenants, each tenant page has its body marker,
    # and the Origin-resolver path resolves correctly.
    docker compose down -v
    ```

    The same script runs on every push to `master` and on every PR via `.github/workflows/demo-smoke.yml`. Failure blocks merge.

    ## Security notes

    - This is a **local-dev demo**, not a production blueprint.
    - `POST /_demo/send-test-mail` accepts unauthenticated requests — deliberate; localhost-only.
    - MariaDB credentials in `.env` are committed defaults (`root`/`tenancy`); fine for localhost; **never reuse for production**.
    - Mailpit UI binds to `127.0.0.1:8025` only — not exposed beyond loopback.
    - `Host:` header injection by `bin/smoke.sh` is intended — tenant resolution by host is the bundle's job; production-shaped authn is YOUR app's job (see the bundle's docs).

    ## File layout

    ```
    examples/saas/
    ├── bin/smoke.sh                 # host-side curl smoke; what CI runs
    ├── compose.yaml                 # FrankenPHP + MariaDB + Mailpit
    ├── Dockerfile                   # FrankenPHP base + composer install at build time
    ├── Caddyfile                    # *.tenancy.localhost + tls internal
    ├── docker/{entrypoint.sh,php.ini}
    ├── composer.json                # path-repo to ../../ (the bundle)
    ├── config/{bundles.php,packages/*.yaml,routes.yaml}
    ├── src/
    │   ├── Command/SeedDemoCommand.php       # CREATE DATABASE + schema + posts (idempotent)
    │   ├── Controller/{Landlord,Tenant,DemoMail,Health}Controller.php
    │   ├── DataFixtures/LandlordTenantsFixture.php
    │   ├── Entity/Landlord/DemoTenant.php    # adds brandColor to bundle's Tenant
    │   ├── Entity/Tenant/Post.php
    │   └── Repository/PostRepository.php
    ├── templates/{base,landlord/index,tenant/index}.html.twig
    └── README.md                    # ← you are here
    ```
    ```

    Notes for executor:
    - Ensure `## Three-step fallback` heading is present EXACTLY as the smoke-checker greps for it (VALIDATION.md §Wave 0 Requirements line 5).
    - Keep the document scannable: short paragraphs, code blocks with copy-paste-ready commands.
    - Do NOT add Phase 22 long-form prose here — Phase 22 will lift screenshots and detailed copy.
  </action>
  <verify>
    <automated>test -f examples/saas/README.md && grep -q '^## Three-step fallback$' examples/saas/README.md && grep -q '^# Tenancy Bundle Demo' examples/saas/README.md && grep -q 'docker compose up -d --wait --build' examples/saas/README.md && grep -q 'tenancy:install' examples/saas/README.md && grep -q 'Mailpit' examples/saas/README.md && grep -q 'localhost:8025' examples/saas/README.md && grep -q 'OriginHeaderResolver' examples/saas/README.md && grep -q 'caddy trust' examples/saas/README.md && grep -q 'Firefox' examples/saas/README.md && grep -q 'Profiler' examples/saas/README.md && grep -q 'WDT' examples/saas/README.md && grep -q 'bundle-source dev loop\|Bundle-source dev loop' examples/saas/README.md && grep -q 'security' examples/saas/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `examples/saas/README.md` exists
    - Section heading `## Three-step fallback` is present (DEMO-01.3; VALIDATION.md grep gate)
    - Three-step ladder content: curl `Host:` snippet, `/etc/hosts` line, browser-native section
    - HTTPS section documents `caddy trust` + Firefox manual CA import (Pitfall 4)
    - Profiler walkthrough section (D-07)
    - Mailer/Mailpit walkthrough section with `localhost:8025` (D-09)
    - OriginHeaderResolver curl example (D-08) with `Host: tenancy.localhost` + `Origin: https://acme.tenancy.localhost`
    - `tenancy:install` reference for downstream adoption (D-10) — described, not exercised
    - Bundle-source dev-loop section explaining bind-mount + OPcache (CONTEXT Critical Edge)
    - Security notes section explaining demo-only nature (T-21-DM, T-21-MP context)
  </acceptance_criteria>
  <done>User-facing walkthrough complete; satisfies DEMO-01.3 (three-step fallback ladder documented) and seeds Phase 22's longer docs work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the three-step fallback ladder + WDT/Mailpit walkthroughs</name>
  <what-built>
    Plans 21-01 through 21-04 have produced a runnable `examples/saas/` demo with:
    - 3 tenants (acme, globex, initech) on FrankenPHP + Caddy + MariaDB + Mailpit
    - Smoke script + CI workflow + comprehensive README walkthrough
    - All automated assertions in `bin/smoke.sh` pass

    DEMO-01.3 ("README documents three-step fallback ladder for Firefox/Safari/WSL2") and DEMO-01.6 ("bundle-source change reflects on demo after rebuild") require a real human eye on a real machine — they cannot be fully automated from a CI runner.
  </what-built>
  <how-to-verify>
    From the repo root on your machine (not a CI runner):

    1. **Bring up the demo:**
       ```bash
       cd examples/saas
       docker compose up -d --wait --build
       ```
       Expect: exits 0 within ~110s on first run (cold image pull), ~50s warm.

    2. **Run the smoke script:**
       ```bash
       bash bin/smoke.sh
       ```
       Expect: "==> All smoke assertions PASSED" final line; exit 0.

    3. **Browser walkthrough — Three-step ladder (DEMO-01.3):**
       - Open `http://tenancy.localhost/` in your default browser. Expect: landlord page lists three tenants with subdomain links.
       - Click the Acme link → `http://acme.tenancy.localhost/`. Expect: orange-accent page titled "Acme Corporation" with seeded posts.
       - Repeat for Globex (blue) and Initech (green).
       - If `*.tenancy.localhost` does NOT resolve in your browser, walk the README's `## Three-step fallback` Steps 1-2 (curl, then /etc/hosts).

    4. **WDT walkthrough (Phase 19 — DX-02 visibility):**
       - On `http://acme.tenancy.localhost/`, click the Tenancy panel icon in the WDT (bottom bar).
       - Expect: panel shows `slug: acme`, `driver: database_per_tenant`, `connection_name: tenant`, `resolved_by: Tenancy\Bundle\Resolver\HostResolver`, bootstrapper list.
       - On `http://tenancy.localhost/` (landlord), the panel renders in "no tenant" state — does NOT throw.

    5. **Mailpit walkthrough (Phase 20 — D-09):**
       ```bash
       curl -X POST -H "Host: acme.tenancy.localhost" http://localhost/_demo/send-test-mail
       curl -X POST -H "Host: globex.tenancy.localhost" http://localhost/_demo/send-test-mail
       curl -X POST -H "Host: initech.tenancy.localhost" http://localhost/_demo/send-test-mail
       ```
       Open `http://localhost:8025/`. Expect: three messages with distinct `From:` addresses (noreply@acme.example, noreply@globex.example, noreply@initech.example).

    6. **Bundle-source dev loop (DEMO-01.6):**
       - Edit `../../src/Resolver/HostResolver.php` — add `error_log('TOUCHED FROM DEMO');` at the start of `resolve()`.
       - `docker compose restart php`
       - Hit `http://acme.tenancy.localhost/` again.
       - `docker compose logs php | grep "TOUCHED FROM DEMO"` — expect: matches.
       - REVERT the edit (`git checkout ../../src/Resolver/HostResolver.php`).

    7. **Tear down:**
       ```bash
       docker compose down -v
       ```

    Report:
    - Step 1: cold-boot time observed? (target <120s)
    - Step 3: which browser(s) tested? did `*.tenancy.localhost` resolve natively, or did you need Step 2?
    - Step 4: WDT Tenancy panel renders correctly?
    - Step 5: Mailpit shows three distinct From addresses?
    - Step 6: bundle-source dev loop works?
    - Any deviations from expected behavior?
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe issues (which step failed, on which OS/browser, with what error).</resume-signal>
</task>

</tasks>

<verification>
After all tasks (with Plans 01-03 landed):
1. `bash -n examples/saas/bin/smoke.sh` exits 0
2. `test -x examples/saas/bin/smoke.sh` (executable)
3. `.github/workflows/demo-smoke.yml` parses as valid YAML
4. `cd examples/saas && docker compose up -d --wait --build && bash bin/smoke.sh && docker compose down -v` completes the full cycle exit 0 (this is the CI-equivalent flow)
5. `grep -c 'examples/saas/README.md' README.md` returns 1 or higher (root README points at demo README)
6. `grep -c "## Three-step fallback" examples/saas/README.md` returns 1
7. Human checkpoint (Task 4) confirms three-step ladder works on a real browser and Mailpit/Profiler render
</verification>

<success_criteria>
- DEMO-01.2 + DEMO-01.4: `bash bin/smoke.sh` exits 0 with per-tenant + Origin-resolver assertions
- DEMO-01.3: README has `## Three-step fallback` heading; documents curl/etc-hosts/browser-native ladder
- DEMO-01.5: `.github/workflows/demo-smoke.yml` runs on push to master; uses checkout@v5; fails block merge
- DEMO-01.6: bundle-source dev loop verified by human (Task 4 step 6)
- Repo-root README points at demo README under "Try the demo"
- Repo-root `.gitignore` ignores `examples/saas/var/` and `vendor/`
- Human walkthrough confirms WDT panel (D-07), Mailpit (D-09), Origin (D-08) all work in a real browser
</success_criteria>

<output>
Create `.planning/phases/21-demo-app/21-04-SUMMARY.md` documenting:
- Smoke script structure (readiness loop + landlord + per-tenant + Origin)
- CI workflow shape (checkout@v5, `--wait --build`, log dump on failure, teardown always)
- README sections covered (two-minute boot, three-step fallback, Profiler, Mailer, Origin, HTTPS-optional, tenancy:install pointer, dev-loop, security, file layout)
- Human verification outcome (which browsers tested; any quirks observed)
- Required follow-up: add `demo-smoke` as a required status check on the GitHub repo (manual repo-admin action per RESEARCH §"Runtime State Inventory")
</output>
