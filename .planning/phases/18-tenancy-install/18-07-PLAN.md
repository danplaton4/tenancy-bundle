---
phase: 18-tenancy-install
plan: 07
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03, 18-04, 18-05, 18-06]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [DX-06]
must_haves:
  truths:
    - "CHANGELOG.md `## [Unreleased]` → `### Added` section lists the tenancy:install entry"
    - "Entry cites the DX-06 requirement ID + DEC-INST-01 + DEC-INST-02 ratified decisions"
    - "Entry mentions: nikic/php-parser require-dev, refuse-on-nonstandard, .bak + php -l + restore, --dry-run + --force flags"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Phase 18 release-note entry in the [Unreleased] block"
      contains: "tenancy:install"
  key_links:
    - from: "CHANGELOG.md `[Unreleased]` `### Added`"
      to: "REQUIREMENTS.md DX-06"
      via: "explicit DX-06 citation in the bullet text"
      pattern: "DX-06"
---

<objective>
Document the Phase 18 deliverables in `CHANGELOG.md` under the existing `## [Unreleased]` → `### Added` section, alongside the Phase 17 OriginHeaderResolver entry.

Purpose: Keep-a-Changelog discipline — every shipped feature appears in `[Unreleased]` until a release is cut. DX-06's user-visible contract is documented for v0.3.0's eventual `[Unreleased]` → `[0.3.0]` promotion. No new architectural decisions are ratified in this phase (the install command implements DEC-INST-01 and DEC-INST-02 — both already ratified in REQUIREMENTS.md § Architectural Decisions); the changelog entry references those IDs for traceability.

Output: 1 edit to `CHANGELOG.md` — append a bullet to the existing `### Added` subheading.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-tenancy-install/18-CONTEXT.md
@.planning/phases/18-tenancy-install/18-PATTERNS.md
@.planning/REQUIREMENTS.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 18-07-01: Append the tenancy:install entry to CHANGELOG.md `[Unreleased]` → `### Added`</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-30 (current `[Unreleased]` block + the analog Phase 17 `OriginHeaderResolver` bullet)
    - .planning/phases/18-tenancy-install/18-PATTERNS.md § 11 lines 611-649 (exact bullet format, mirror Phase 17 style, cite DX-06 at the end of the entry)
    - .planning/REQUIREMENTS.md § DX-06 (acceptance criteria summary — extract terse language for the changelog bullet)
    - .planning/REQUIREMENTS.md § "Architectural Decisions (Ratified)" — DEC-INST-01 ("programmatically forwards --force") and DEC-INST-02 ("nikic, refuse-on-nonstandard")
    - .planning/phases/18-tenancy-install/18-CONTEXT.md § D-22 (composer additions — require-dev + suggest, NOT require)
  </read_first>
  <action>
    EDIT `CHANGELOG.md`. Locate the existing `## [Unreleased]` → `### Added` block (currently lines 8-26, containing the Phase 17 `OriginHeaderResolver` + `OriginHeaderResolverConfigPass` bullets). APPEND the following new bullet AFTER the existing `OriginHeaderResolverConfigPass` bullet, BEFORE the closing of the `[Unreleased]` block. Match the existing bullet's formatting: 2-space hanging indent on continuation lines, ~80-column wrap, inline-code backticks around symbol names, file paths, and command names.

    NEW BULLET TO APPEND:

    ```markdown
    - **`tenancy:install` console command** — one-command bundle setup for fresh
      Symfony apps. Runs `composer require danplaton4/tenancy-bundle && bin/console
      tenancy:install` and the bundle is registered + configured with zero manual
      `config/bundles.php` editing. Uses `nikic/php-parser` (declared `require-dev`
      + `suggest` only — never `require`) to AST-detect a Flex-canonical
      `bundles.php` shape; refuses to mutate non-standard shapes (DDD
      `registerBundles()` override, env-conditional registration, parser-rejected
      files) and prints a clean copy-paste snippet — refusal is a clean exit, not a
      tool failure. On the standard shape: takes a timestamped
      `config/bundles.php.bak.YYYYMMDD-HHMMSS` sidecar BEFORE write, atomic write
      via `Symfony\Component\Filesystem\Filesystem::dumpFile()`, post-mutation
      `php -l` syntax check, automatic restore via `Filesystem::copy()` (NOT
      `rename` — the `.bak` outlives every failure path) on lint failure. Then
      programmatically delegates to `tenancy:init` (forwarding `--force`) for a
      single continuous transcript. Supports `--dry-run` (preview-only; no write,
      no `tenancy:init` invocation) and `--force` (forwarded to `tenancy:init` to
      permit overwrite of an existing `tenancy.yaml`); the two flags are mutually
      exclusive (exit code 2 on conflict). Fixture corpus of ≥6 real-world
      `bundles.php` shapes (Symfony skeleton, API Platform, Sulu CMS,
      DDD-override, with-comments, env-conditional) plus a malformed sample
      gates the AST detector in CI. Implements DEC-INST-01 (programmatic
      delegation) and DEC-INST-02 (nikic-detect + refuse-on-nonstandard). Closes
      DX-06.
    - **`BundlesPhpInstaller`** — `final` collaborator powering `tenancy:install`.
      Pure value-returning detector + writer with a typed `InstallResult`
      (`WROTE` / `ALREADY_REGISTERED` / `REFUSED_NON_STANDARD` /
      `LINT_FAILED_RESTORED` / `DEV_DEPENDENCY_MISSING` enum cases). Unit-testable
      against the fixture corpus without a kernel boot.
    ```

    Do NOT modify the `[0.2.1]` block or anything below it. Do NOT modify the compare-links footer at the bottom of the file (`[Unreleased]: ...v0.2.0...HEAD` remains valid until v0.3.0 is tagged).

    Run `vendor/bin/php-cs-fixer check --diff CHANGELOG.md` — Markdown is not in php-cs-fixer's scope so this is a no-op; explicitly skip.

    Confirm the file remains valid Markdown by piping to a renderer or visually verifying no list-item indentation is broken.
  </action>
  <verify>
    <automated>grep -q "tenancy:install" CHANGELOG.md && grep -q "DX-06" CHANGELOG.md && grep -q "BundlesPhpInstaller" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains the `tenancy:install` entry under `[Unreleased]`: `awk '/## \[Unreleased\]/{flag=1; next} /## \[0\./{flag=0} flag' CHANGELOG.md | grep -q "tenancy:install console command"` exits 0
    - Entry cites the DX-06 requirement ID: `grep -q "Closes DX-06" CHANGELOG.md` exits 0 OR `grep -q "DX-06" CHANGELOG.md` exits 0
    - Entry cites both ratified architectural decisions: `grep -q "DEC-INST-01" CHANGELOG.md && grep -q "DEC-INST-02" CHANGELOG.md` exits 0
    - Entry mentions key implementation details: `grep -q "nikic/php-parser" CHANGELOG.md && grep -q "require-dev" CHANGELOG.md && grep -q "php -l" CHANGELOG.md && grep -q "Filesystem::copy" CHANGELOG.md && grep -q "\.bak" CHANGELOG.md` exits 0
    - Entry mentions both flags: `grep -q -- "--dry-run" CHANGELOG.md && grep -q -- "--force" CHANGELOG.md` exits 0
    - Entry mentions the fixture corpus: `grep -q "fixture corpus" CHANGELOG.md && grep -qE "Symfony skeleton.*API Platform.*Sulu" CHANGELOG.md` exits 0 (the third grep is loose — the executor may order the shape list differently)
    - `BundlesPhpInstaller` collaborator entry present: `grep -q "BundlesPhpInstaller" CHANGELOG.md` exits 0
    - The `[Unreleased]` section header still exists exactly once: `grep -c "^## \[Unreleased\]" CHANGELOG.md` returns 1
    - The `[0.2.1]` section header is unchanged: `grep -c "^## \[0\.2\.1\] — 2026-04-21" CHANGELOG.md` returns 1
    - Compare links footer is unchanged: `grep -q "compare/v0.2.0...HEAD" CHANGELOG.md` exits 0
    - Only CHANGELOG.md is modified by this plan: `git status --porcelain CHANGELOG.md | grep -q "^.M\|^M."` exits 0 AND `git status --porcelain src/ tests/ config/ composer.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    CHANGELOG.md documents the Phase 18 deliverables. DX-06 + DEC-INST-01 + DEC-INST-02 cited for traceability. The bullet reads in the same voice as the Phase 17 entry directly above it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(No new trust boundaries introduced by docs-only changes.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DOC-01 | Information Disclosure — stale CHANGELOG misrepresents shipped surface | CHANGELOG.md `[Unreleased]` block | accept | Manually maintained; the acceptance-criteria grep gates catch missing key terms (DX-06, DEC-INST-*, flag names, fixture corpus). No machine enforcement beyond CI grep. |
</threat_model>

<verification>
- `CHANGELOG.md` is the only file touched
- All grep acceptance criteria pass
- Full suite still green: `vendor/bin/phpunit` exits 0 (docs-only change cannot break tests)
- `vendor/bin/phpstan analyse` exits 0 (unchanged)
- `vendor/bin/php-cs-fixer check --diff` exits 0 (Markdown not in scope)
</verification>

<success_criteria>
- `CHANGELOG.md` `[Unreleased]` section lists `tenancy:install` and `BundlesPhpInstaller`
- DX-06 + DEC-INST-01 + DEC-INST-02 cited
- All key implementation details (nikic require-dev, .bak, php -l, copy-not-rename, --dry-run, --force, fixture corpus) mentioned
- No other files modified
</success_criteria>

<output>
After completion, create `.planning/phases/18-tenancy-install/18-07-SUMMARY.md` per the standard template.
</output>
