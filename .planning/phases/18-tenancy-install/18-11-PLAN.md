---
phase: 18-tenancy-install
plan: 11
type: execute
wave: 3
depends_on:
  - 18-09
  - 18-10
files_modified:
  - CHANGELOG.md
  - README.md
autonomous: true
gap_closure: true
requirements:
  - DX-06
must_haves:
  truths:
    - "Canary regression test `tests/Integration/ZeroConfigKernelBootTest.php` is GREEN end-to-end on the post-fix codebase — all 3 test methods pass."
    - "Full PHPUnit suite (`vendor/bin/phpunit`) exits 0 across unit + integration testsuites — no regressions introduced by the 6-site fix."
    - "PHPStan level 9 is clean across the entire `src/` tree."
    - "php-cs-fixer @Symfony is clean across the entire source tree."
    - "CHANGELOG.md `[Unreleased]` block has a `### Fixed` subsection describing the zero-config boot regression, listing all 6 fix sites, citing DX-06, and naming the affected versions (v0.1.0, v0.2.0, v0.2.1)."
    - "README quick-start section flags `composer require --dev nikic/php-parser` as a prerequisite for `bin/console tenancy:install` so users don't hit DEC-INST-02's intended failure path without context."
  artifacts:
    - path: CHANGELOG.md
      provides: "Public changelog entry documenting the zero-config boot fix"
      contains: "### Fixed"
    - path: README.md
      provides: "Quick-start callout for the nikic/php-parser dev-dep prerequisite"
      contains: "nikic/php-parser"
  key_links:
    - from: CHANGELOG.md `[Unreleased] ### Fixed`
      to: DX-06 + 18-VERIFICATION.md
      via: "User-readable summary citing the requirement ID and the verification report"
      pattern: "DX-06"
---

<objective>
Cap the gap-closure work with the GREEN-bar verification + the public-facing documentation that turns this fix into a credible release note. This is the wave-3 plan: depends on both 18-09 and 18-10 because it asserts the integrated outcome (all 6 sites fixed, canary fully green).

Purpose: (a) prove the regression is closed end-to-end via the canary + full suite; (b) document the fix in CHANGELOG.md so when v0.2.2 (or whatever patch goes out) ships, users can see what was broken and what is now safe; (c) add the README callout that was flagged as a "lower priority, optional" secondary item in 18-VERIFICATION.md's gap summary — the user explicitly noted that `bin/console tenancy:install` requires `nikic/php-parser` in dev and surfacing this prominently helps onboarding.

Output: A CHANGELOG.md `[Unreleased] ### Fixed` entry, a small README quick-start addition, and a green-bar test transcript captured in the SUMMARY.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-tenancy-install/18-VERIFICATION.md
@.planning/phases/18-tenancy-install/18-08-PLAN.md
@.planning/phases/18-tenancy-install/18-09-PLAN.md
@.planning/phases/18-tenancy-install/18-10-PLAN.md
@CHANGELOG.md
@README.md
@CLAUDE.md

<interfaces>
<!-- Reference: the existing CHANGELOG format to mirror. -->

From CHANGELOG.md head:
- Format: Keep a Changelog 1.1.0; SemVer.
- Existing `## [Unreleased]` has an `### Added` subsection containing the Phase 17 OriginHeaderResolver, Phase 18 tenancy:install, and (presumably further down) other v0.3 entries.
- We ADD a sibling `### Fixed` subsection. Convention per Keep a Changelog: order is `Added`, `Changed`, `Deprecated`, `Removed`, `Fixed`, `Security`. `### Fixed` goes after `### Added` (and any `### Changed`/`### Deprecated`/`### Removed` already present in `[Unreleased]`).

The defect-site list to enumerate in the entry (canonical from 18-VERIFICATION.md `defect_inventory`):
1. `Tenancy\Bundle\Resolver\HostResolver`
2. `Tenancy\Bundle\Resolver\HeaderResolver`
3. `Tenancy\Bundle\Resolver\QueryParamResolver`
4. `Tenancy\Bundle\Resolver\ConsoleResolver`
5. `Tenancy\Bundle\Command\TenantRunCommand`
6. `Tenancy\Bundle\Messenger\TenantWorkerMiddleware`

Affected versions (canonical from 18-VERIFICATION.md `versions_affected`):
- v0.1.0, v0.2.0, v0.2.1 (defect predates Phase 18; current master HEAD also affected pre-fix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final green-bar verification of the canary + full suite</name>
  <files></files>
  <read_first>
    - .planning/phases/18-tenancy-install/18-VERIFICATION.md (the `## Gaps` block — confirm all 6 defect sites + the canary regression item are now CLOSED)
    - tests/Integration/ZeroConfigKernelBootTest.php (canary; should be GREEN after plans 18-09 + 18-10 landed)
    - src/Resolver/HostResolver.php (confirm `?TenantProviderInterface ... = null` from plan 18-09)
    - src/Resolver/HeaderResolver.php (same)
    - src/Resolver/QueryParamResolver.php (same)
    - src/Resolver/ConsoleResolver.php (same)
    - src/Command/TenantRunCommand.php (confirm `?TenantProviderInterface` + RuntimeException from plan 18-10)
    - src/Messenger/TenantWorkerMiddleware.php (same)
    - CLAUDE.md (test commands)
  </read_first>
  <action>
    Run the full verification matrix and capture transcripts in the upcoming SUMMARY:

    1. Canary: `vendor/bin/phpunit tests/Integration/ZeroConfigKernelBootTest.php --testdox` — must exit 0 with all 3 test methods reported as passing. This is the headline assertion: the regression that 18-08 captured as RED is now GREEN.

    2. Full PHPUnit suite: `vendor/bin/phpunit` (no testsuite filter) — must exit 0. Capture the test/assertion counts from the trailing summary line for inclusion in the SUMMARY.

    3. Unit testsuite isolation: `vendor/bin/phpunit --testsuite unit` — must exit 0 with ≥ 287 tests (matches the baseline noted in 18-VERIFICATION.md behavioral spot-checks).

    4. Integration testsuite isolation: `vendor/bin/phpunit --testsuite integration` (or whatever the suite name is per `phpunit.xml.dist`) — must exit 0.

    5. PHPStan level 9: `vendor/bin/phpstan analyse --memory-limit=512M` — must exit 0 with 0 errors.

    6. php-cs-fixer @Symfony: `vendor/bin/php-cs-fixer check --diff` — must exit 0 (no style violations on the modified files).

    If ANY of the above fail: STOP and surface the failure to the user. Do not advance to task 2 until all 6 commands green-bar. This task is a verification gate — no code changes happen here.
  </action>
  <verify>
    <automated>
      cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" \
        && vendor/bin/phpunit tests/Integration/ZeroConfigKernelBootTest.php --testdox 2>&1 | tee /tmp/18-11-canary.log | tail -10 \
        && vendor/bin/phpunit 2>&1 | tee /tmp/18-11-full.log | tail -5 \
        && vendor/bin/phpstan analyse --memory-limit=512M 2>&1 | tail -3 \
        && vendor/bin/php-cs-fixer check --diff 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/Integration/ZeroConfigKernelBootTest.php` canary: `vendor/bin/phpunit tests/Integration/ZeroConfigKernelBootTest.php` exits 0 AND `/tmp/18-11-canary.log` contains `OK (` (PHPUnit's success marker) AND does NOT contain `FAILURES!` or `ERRORS!`
    - Full suite: `vendor/bin/phpunit` exits 0 AND `/tmp/18-11-full.log` trailing summary line shows `OK` (PHPUnit success) with a test count ≥ the pre-fix baseline (current master had 545+ tests per 18-VERIFICATION.md notes; canary adds 3 → ≥ 548)
    - PHPStan: `vendor/bin/phpstan analyse --memory-limit=512M` exits 0 (the line "[OK] No errors" or equivalent in PHPStan's output)
    - php-cs-fixer: `vendor/bin/php-cs-fixer check --diff` exits 0
    - Existence check for each fixed file's nullable signature (sanity grep, single command):
      `grep -lE "\?TenantProviderInterface" src/Resolver/HostResolver.php src/Resolver/HeaderResolver.php src/Resolver/QueryParamResolver.php src/Resolver/ConsoleResolver.php src/Command/TenantRunCommand.php src/Messenger/TenantWorkerMiddleware.php | wc -l`
      must return `6` (all 6 fix sites have the nullable signature)
  </acceptance_criteria>
  <done>
    All 6 verification commands exit 0. Test transcripts captured at `/tmp/18-11-canary.log` and `/tmp/18-11-full.log` are ready to quote in the SUMMARY. The 18-VERIFICATION.md blocker is now empirically closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the fix in CHANGELOG.md and surface the nikic/php-parser prereq in README</name>
  <files>CHANGELOG.md, README.md</files>
  <read_first>
    - CHANGELOG.md (the entire file — confirm `[Unreleased]` section ordering, existing subsection conventions, sibling `Added`/`Changed` placement)
    - README.md (the Quick Start / Install section — the executor needs to locate the right spot to insert the nikic/php-parser callout; mirror existing markdown idioms)
    - .planning/phases/18-tenancy-install/18-VERIFICATION.md (the `## Gaps` block — canonical defect inventory + versions_affected list)
    - .planning/phases/18-tenancy-install/18-HUMAN-UAT.md (canonical record of the human UAT failure on 2026-05-21 — the "evidence" line for the changelog)
    - CLAUDE.md (project conventions for docs phrasing — terse, no marketing tone)
  </read_first>
  <action>
    **Part A — CHANGELOG.md:**

    Locate the `## [Unreleased]` section. After the existing `### Added` (and any `### Changed`/`### Removed`) subsections, add a `### Fixed` subsection. Inside:

    - Single bullet entry titled with the symptom (e.g., **"Fresh-skeleton boot regression — bundle now constructs cleanly with no `tenancy:` config block"** — executor refines wording).
    - Sub-bullets describing:
      - The root cause: 6 services were wired `service('tenancy.provider')->nullOnInvalid()` but their constructors declared non-nullable `TenantProviderInterface`. On a fresh `composer require` against a Symfony skeleton (no `tenancy:` config present), `nullOnInvalid()` resolves to null and PHP 8.x strict typing throws `TypeError` during `cache:clear`.
      - The fix policy: constructor params are now `?TenantProviderInterface`. The 4 resolvers (HostResolver / HeaderResolver / QueryParamResolver / ConsoleResolver) early-return null on the null path (resolver chain falls through). The 2 write-path consumers (TenantRunCommand / TenantWorkerMiddleware) throw `RuntimeException` with an actionable message directing the user to `bin/console tenancy:install`.
      - Versions affected: v0.1.0, v0.2.0, v0.2.1 (defect predates Phase 18). All users on those tags should upgrade.
      - Closes DX-06 contract (zero-config bootability of the install command flow). Cite `.planning/phases/18-tenancy-install/18-VERIFICATION.md` as the audit source.
      - Regression coverage: new `tests/Integration/ZeroConfigKernelBootTest.php` exercises the previously-uncovered zero-config code path.

    Match the prose density / bullet style of the existing `### Added` entries (terse, technical, no marketing). Do not invent a version number — leave the entry under `[Unreleased]`; the release decision is outside this plan.

    **Part B — README.md:**

    Locate the Quick Start / Install section (likely under a heading like `## Installation` or `## Quick Start`). Find the line(s) describing `bin/console tenancy:install`. Add a one-sentence callout (a markdown blockquote or a `> Note:` line) noting:

    > Running `bin/console tenancy:install` requires `nikic/php-parser` (used to AST-parse `config/bundles.php`). It is listed in the bundle's `composer suggest`, so install it explicitly on your application: `composer require --dev nikic/php-parser`. Without it the command prints a clear error and exits 1 (per DEC-INST-02).

    Executor refines wording for tone consistency with the surrounding README. Keep the callout SHORT (≤ 4 lines including blockquote prefix). Do not duplicate the install command's own runtime error message verbatim.

    Do NOT modify any other section of README.md in this task. Do NOT touch docs/ — that belongs to Phase 22 (DOC-19) per the project Out-of-Scope notes.
  </action>
  <verify>
    <automated>
      cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" \
        && grep -E "^### Fixed" CHANGELOG.md | head -3 \
        && grep -cE "HostResolver|HeaderResolver|QueryParamResolver|ConsoleResolver|TenantRunCommand|TenantWorkerMiddleware" CHANGELOG.md \
        && grep -cE "nikic/php-parser" README.md \
        && grep -cE "DX-06" CHANGELOG.md
    </automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains a new `### Fixed` heading under `## [Unreleased]` (verified: `grep -E "^### Fixed" CHANGELOG.md` returns ≥ 1 line)
    - The new `### Fixed` entry mentions all 6 defect-site class names — verified: `grep -cE "HostResolver|HeaderResolver|QueryParamResolver|ConsoleResolver|TenantRunCommand|TenantWorkerMiddleware" CHANGELOG.md` returns ≥ 6 (any one of these may already exist elsewhere in the file; what matters is that all 6 names appear in the file overall, and that the `### Fixed` block enumerates them — manual diff review confirms placement)
    - The new entry cites `DX-06` — verified: `grep -c "DX-06" CHANGELOG.md` returns ≥ 1 in the changed file (existing DX-06 references in the `### Added` block may already match; verify the `### Fixed` block adds at least one new citation by reading the diff)
    - The new entry references versions affected — verified: `grep -cE "v0\\.2\\.1|v0\\.2\\.0|v0\\.1\\.0" CHANGELOG.md` returns ≥ 3 (at least the three version tags appear in the new `### Fixed` block; existing changelog may already mention them elsewhere — confirm via diff)
    - README.md contains a `nikic/php-parser` callout — verified: `grep -c "nikic/php-parser" README.md` returns ≥ 1 (may already match if README mentions the package elsewhere; the NEW callout must be in the Quick Start / Install section — confirm via diff review)
    - README.md callout mentions `composer require --dev nikic/php-parser` exactly — verified: `grep -c "composer require --dev nikic/php-parser" README.md` returns ≥ 1
    - php-cs-fixer / phpstan unchanged: docs-only changes have no automated PHP impact, but rerun `vendor/bin/php-cs-fixer check` to confirm no collateral edits — exits 0.
  </acceptance_criteria>
  <done>
    CHANGELOG.md `[Unreleased] ### Fixed` documents the regression + the fix + the 6 site list + DX-06 + the affected versions. README.md Quick Start has a one-paragraph callout pointing users at `composer require --dev nikic/php-parser` before running `tenancy:install`. No source code touched.
  </done>
</task>

</tasks>

<verification>
- Canary: `vendor/bin/phpunit tests/Integration/ZeroConfigKernelBootTest.php` exits 0 (all 3 tests pass)
- Full suite: `vendor/bin/phpunit` exits 0
- PHPStan level 9: `vendor/bin/phpstan analyse --memory-limit=512M` exits 0
- php-cs-fixer: `vendor/bin/php-cs-fixer check --diff` exits 0
- CHANGELOG.md `### Fixed` exists with all 6 site classes named, DX-06 cited, versions 0.1.0/0.2.0/0.2.1 enumerated
- README.md has the `composer require --dev nikic/php-parser` callout in the install section
</verification>

<success_criteria>
- All 6 verification commands from task 1 exit 0.
- The 18-08 canary test, RED on master at the start of this gap closure, is now GREEN.
- The DX-06 zero-config bootability contract is empirically restored.
- CHANGELOG.md documents the fix in user-facing prose, ready for the next patch release.
- README.md Quick Start surfaces the `nikic/php-parser` dev-dep prerequisite so users hit DEC-INST-02's clean exit-1 path with full context, not surprise.
</success_criteria>

<output>
Create `.planning/phases/18-tenancy-install/18-11-SUMMARY.md` documenting:
- The full green-bar transcript (paste the relevant tail of `/tmp/18-11-canary.log` and `/tmp/18-11-full.log`).
- Final test counts (X tests, Y assertions) — quoted from the PHPUnit summary lines.
- CHANGELOG diff (the new `### Fixed` block) and README diff (the new callout) for traceability.
- Explicit statement: "Phase 18 verification status flips from `gaps_found` to `verified`. Recommend `/gsd:verify-phase 18` (re-run) to update 18-VERIFICATION.md formally, OR have a human update the frontmatter `status:` field to `verified`."
- Suggest the next external step: tag a `v0.2.2` patch release including this fix (decision belongs to the user — do not tag automatically).
</output>
