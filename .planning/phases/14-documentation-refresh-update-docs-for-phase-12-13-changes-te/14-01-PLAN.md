---
phase: 14-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flex/danplaton4/tenancy-bundle/1.0/config/packages/tenancy.yaml
  - flex/danplaton4/tenancy-bundle/1.0/manifest.json
  - composer.json
  - docs/user-guide/installation.md
  - docs/index.md
  - docs/user-guide/index.md
  - README.md
autonomous: true
requirements: [DOC-REFRESH]

must_haves:
  truths:
    - "The flex/ directory does not exist in the repository"
    - "composer.json has no extra.symfony block"
    - "No documentation page mentions Symfony Flex"
    - "installation.md has no tab-based With Flex / Without Flex layout"
    - "installation.md presents a single standard installation path"
    - "installation.md mentions tenancy:init as the recommended setup method"
    - "index.md comparison table has no Flex row"
    - "README.md comparison table has no Flex row"
    - "cache_prefix_separator default shown as '.' not ':' in installation.md"
  artifacts:
    - path: "docs/user-guide/installation.md"
      provides: "Single installation path with tenancy:init tip"
    - path: "docs/index.md"
      provides: "Landing page with no Flex references, tenancy:init as setup path"
    - path: "docs/user-guide/index.md"
      provides: "User guide index with no Flex reference in Installation description"
    - path: "README.md"
      provides: "README with no Flex references, tenancy:init as setup path"
    - path: "composer.json"
      provides: "composer.json with extra.symfony block removed"
  key_links:
    - from: "docs/user-guide/installation.md"
      to: "docs/user-guide/cli-commands.md"
      via: "tenancy:init cross-reference"
      pattern: "tenancy:init"
---

<objective>
Remove all Symfony Flex artifacts and references from the project and documentation. Replace the Flex-based onboarding path with `tenancy:init` as the primary zero-config setup method.

Purpose: The user has decided that Symfony Flex is NOT used at all. The `tenancy:init` command (Phase 12) replaces Flex as the primary onboarding path. All references to Flex must be purged from code, config, and documentation.

Output: Clean repository with no Flex directory, no `extra.symfony` in composer.json, and all docs/README updated to present `tenancy:init` as the standard setup method.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-refresh-update-docs-for-phase-12-13-changes-te/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Flex directory and remove extra.symfony from composer.json</name>
  <files>
    flex/danplaton4/tenancy-bundle/1.0/config/packages/tenancy.yaml
    flex/danplaton4/tenancy-bundle/1.0/manifest.json
    composer.json
  </files>
  <read_first>
    composer.json
    flex/danplaton4/tenancy-bundle/1.0/config/packages/tenancy.yaml
    flex/danplaton4/tenancy-bundle/1.0/manifest.json
  </read_first>
  <action>
1. Delete the entire `flex/` directory recursively: `rm -rf flex/`

2. Edit `composer.json` to remove ONLY the `"symfony"` key from the `"extra"` block. The current `"extra"` block is:
```json
"extra": {
    "symfony": {
        "bundles": {
            "Tenancy\\Bundle\\TenancyBundle": ["all"]
        }
    },
    "branch-alias": {
        "dev-master": "1.0.x-dev"
    }
}
```

After the edit, the `"extra"` block must be:
```json
"extra": {
    "branch-alias": {
        "dev-master": "1.0.x-dev"
    }
}
```

IMPORTANT: Keep the `"branch-alias"` key. Only remove the `"symfony"` key and its contents. Do NOT change any other part of composer.json. Do NOT run `composer update` or any other composer command.
  </action>
  <verify>
    <automated>test ! -d flex/ && ! grep -q '"symfony"' composer.json && grep -q '"branch-alias"' composer.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `flex/` directory does not exist (verified by `test ! -d flex/`)
    - `composer.json` does NOT contain the string `"symfony"` inside the `"extra"` block
    - `composer.json` DOES still contain `"branch-alias"` with `"dev-master": "1.0.x-dev"`
    - `composer.json` remains valid JSON (verified by `php -r 'json_decode(file_get_contents("composer.json"), true); echo json_last_error() === 0 ? "valid" : "invalid";'`)
    - No other changes to composer.json besides the `extra.symfony` removal
  </acceptance_criteria>
  <done>flex/ directory deleted, composer.json extra.symfony block removed, branch-alias preserved, valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite installation.md — remove Flex tab, single installation path, add tenancy:init tip</name>
  <files>docs/user-guide/installation.md</files>
  <read_first>
    docs/user-guide/installation.md
    src/Command/TenantInitCommand.php
  </read_first>
  <action>
Rewrite the "## 2. Bundle Registration" section of `docs/user-guide/installation.md`. The current section uses a tabbed layout (`=== "With Flex"` / `=== "Without Flex"`). Replace the entire section with a single standard path (no tabs). Also fix `cache_prefix_separator: ':'` to `cache_prefix_separator: '.'` in the YAML example.

The new "## 2. Bundle Registration" section must be exactly:

```markdown
## 2. Bundle Registration

Add the bundle to `config/bundles.php`:

```php
return [
    // ... other bundles
    Tenancy\Bundle\TenancyBundle::class => ['all' => true],
];
```

Then create `config/packages/tenancy.yaml` with the full defaults:

```yaml
tenancy:
    driver: database_per_tenant
    strict_mode: true
    landlord_connection: default
    tenant_entity_class: Tenancy\Bundle\Entity\Tenant
    cache_prefix_separator: '.'
    database:
        enabled: false
    resolvers:
        - host
        - header
        - query_param
        - console
    host:
        app_domain: ~
```

!!! tip "Or use tenancy:init"
    Instead of creating the config manually, run `bin/console tenancy:init` to generate a fully
    commented `config/packages/tenancy.yaml` with all keys and Doctrine-aware driver recommendations.
    See [CLI Commands](cli-commands.md#tenancyinit) for details.
```

Key changes:
- NO tab groups (`=== "With Flex"` / `=== "Without Flex"`) — single path only
- `cache_prefix_separator` value changed from `':'` to `'.'`
- Added `tenancy:init` tip admonition after the YAML block
- All other sections of the file (1. Composer Install, 3. Optional Dependencies, 4. Requirements Summary, 5. Verification, Next Steps) remain UNCHANGED
  </action>
  <verify>
    <automated>! grep -q 'With Flex' docs/user-guide/installation.md && ! grep -q 'Without Flex' docs/user-guide/installation.md && grep -q "cache_prefix_separator: '.'" docs/user-guide/installation.md && grep -q 'tenancy:init' docs/user-guide/installation.md && ! grep -q "cache_prefix_separator: ':'" docs/user-guide/installation.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - No occurrence of "With Flex" in installation.md
    - No occurrence of "Without Flex" in installation.md
    - No occurrence of `=== "With Flex"` or `=== "Without Flex"` tab syntax
    - `cache_prefix_separator: '.'` appears (dot separator, not colon)
    - `cache_prefix_separator: ':'` does NOT appear
    - `tenancy:init` is mentioned in a tip admonition
    - The string `cli-commands.md#tenancyinit` appears as a cross-reference link
    - Sections 1, 3, 4, 5, and "Next Steps" are unchanged
    - The word "Flex" does not appear anywhere in the file
  </acceptance_criteria>
  <done>installation.md has a single registration path with correct separator default and tenancy:init tip, no Flex references</done>
</task>

<task type="auto">
  <name>Task 3: Remove Flex references from index.md, user-guide/index.md, and README.md</name>
  <files>
    docs/index.md
    docs/user-guide/index.md
    README.md
  </files>
  <read_first>
    docs/index.md
    docs/user-guide/index.md
    README.md
  </read_first>
  <action>
**docs/index.md** — Three changes:

1. Replace line 22 (Quick Start step 1 description):
   - OLD: `Symfony Flex auto-registers the bundle and creates `config/packages/tenancy.yaml`.`
   - NEW: `Register the bundle in `config/bundles.php`, then run `bin/console tenancy:init` to generate `config/packages/tenancy.yaml` with commented defaults.`

2. Remove line 125 from the comparison table:
   - DELETE: `| Flex recipe (zero-config) | :material-check: | N/A | :material-close: | :material-close: |`
   Replace with:
   - NEW: `| `tenancy:init` scaffolding | :material-check: | N/A | :material-close: | :material-close: |`

3. Add a row for `tenancy:init` to the Features table (line 76 area) — replace the existing CLI commands row:
   - OLD: `| **CLI commands** | `tenancy:migrate` and `tenancy:run` |`
   - NEW: `| **CLI commands** | `tenancy:init`, `tenancy:migrate`, and `tenancy:run` |`

**docs/user-guide/index.md** — One change:

1. Replace line 9:
   - OLD: `1. [Installation](installation.md) — composer require, Flex auto-config, manual registration`
   - NEW: `1. [Installation](installation.md) — composer require, bundle registration, tenancy:init`

**README.md** — Three changes:

1. Replace line 21 (Quick Start step 1 description):
   - OLD: `Symfony Flex auto-registers the bundle and drops a `config/packages/tenancy.yaml` stub in your project.`
   - NEW: `Register the bundle in `config/bundles.php`, then run `bin/console tenancy:init` to generate `config/packages/tenancy.yaml`.`

2. Replace line 98 in the comparison table:
   - OLD: `| Flex recipe (zero-config install) | Yes | N/A (Laravel) | No | No |`
   - NEW: `| \`tenancy:init\` scaffolding | Yes | N/A (Laravel) | No | No |`

3. Update the CLI commands line in the Features section (line 54):
   - OLD: `- **CLI commands** — `tenancy:migrate` (run migrations per tenant), `tenancy:run` (wrap any command with tenant context)`
   - NEW: `- **CLI commands** — `tenancy:init` (scaffold config), `tenancy:migrate` (run migrations per tenant), `tenancy:run` (wrap any command with tenant context)`
  </action>
  <verify>
    <automated>! grep -qi 'flex' docs/index.md && ! grep -qi 'flex' docs/user-guide/index.md && ! grep -qi 'flex' README.md && grep -q 'tenancy:init' docs/index.md && grep -q 'tenancy:init' docs/user-guide/index.md && grep -q 'tenancy:init' README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - The word "Flex" (case-insensitive) does NOT appear in docs/index.md
    - The word "Flex" (case-insensitive) does NOT appear in docs/user-guide/index.md
    - The word "Flex" (case-insensitive) does NOT appear in README.md
    - `tenancy:init` appears in docs/index.md (in Quick Start description and comparison table)
    - `tenancy:init` appears in docs/user-guide/index.md (in Installation description)
    - `tenancy:init` appears in README.md (in Quick Start description and comparison table)
    - The comparison table in docs/index.md has a row with "tenancy:init scaffolding" instead of "Flex recipe"
    - The comparison table in README.md has a row with "tenancy:init scaffolding" instead of "Flex recipe"
  </acceptance_criteria>
  <done>All three files have no Flex references, tenancy:init is the promoted setup method in Quick Start and comparison tables</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation content | User-facing text; no code execution path changed |
| composer.json extra block | Removing auto-registration metadata; does not affect runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | composer.json | accept | Removing `extra.symfony` only affects Flex auto-registration which is being intentionally removed; `branch-alias` preserved; no runtime behavior change |
| T-14-02 | Information Disclosure | flex/ directory deletion | accept | Removing recipe files reduces attack surface (no config stubs shipped); positive security impact |
| T-14-03 | Denial of Service | Bundle registration | accept | Without Flex auto-registration, users must manually register the bundle or use `tenancy:init`; documented in installation.md |
</threat_model>

<verification>
1. `flex/` directory does not exist: `test ! -d flex/`
2. composer.json is valid and has no `extra.symfony`: `php -r 'json_decode(file_get_contents("composer.json"), true); echo json_last_error() === 0 ? "valid" : "invalid";'`
3. No "Flex" word in any modified doc: `! grep -riq 'flex' docs/user-guide/installation.md docs/index.md docs/user-guide/index.md README.md`
4. `tenancy:init` mentioned in all onboarding paths: `grep -l 'tenancy:init' docs/user-guide/installation.md docs/index.md docs/user-guide/index.md README.md | wc -l` returns 4
5. Test suite unaffected: `vendor/bin/phpunit --testsuite unit` passes (no source code changes)
</verification>

<success_criteria>
- flex/ directory completely removed from repository
- composer.json has no extra.symfony block, retains branch-alias, valid JSON
- installation.md presents a single (non-tabbed) installation path with cache_prefix_separator: '.' and tenancy:init tip
- index.md, user-guide/index.md, README.md have zero Flex references and promote tenancy:init
- All comparison tables show "tenancy:init scaffolding" instead of "Flex recipe"
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-refresh-update-docs-for-phase-12-13-changes-te/14-01-SUMMARY.md`
</output>
