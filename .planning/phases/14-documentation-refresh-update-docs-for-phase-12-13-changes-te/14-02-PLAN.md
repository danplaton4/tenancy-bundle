---
phase: 14-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user-guide/cli-commands.md
  - docs/user-guide/configuration.md
  - docs/user-guide/cache-isolation.md
  - docs/user-guide/resolvers.md
  - docs/user-guide/database-per-tenant.md
  - docs/architecture/di-compilation.md
  - src/Command/TenantInitCommand.php
autonomous: true
requirements: [DOC-REFRESH]

must_haves:
  truths:
    - "cli-commands.md documents three commands: tenancy:init, tenancy:migrate, and tenancy:run"
    - "cli-commands.md tenancy:init section documents --force flag, Doctrine detection, overwrite protection, and next-steps output"
    - "configuration.md shows cache_prefix_separator default as '.' not ':'"
    - "configuration.md example shows 'acme.user.123' not 'acme:user.123'"
    - "cache-isolation.md namespace examples use '.' separator not ':'"
    - "resolvers.md documents that custom resolvers always pass through the config filter"
    - "database-per-tenant.md EntityManagerResetListener section specifies which EM is reset per driver mode"
    - "di-compilation.md ResolverChainPass code block shows the config filtering logic"
    - "di-compilation.md always-registered services table includes TenantInitCommand"
    - "TenantInitCommand.php YAML template shows cache_prefix_separator: '.' not ':'"
  artifacts:
    - path: "docs/user-guide/cli-commands.md"
      provides: "Complete CLI reference including tenancy:init command"
      contains: "tenancy:init"
    - path: "docs/user-guide/configuration.md"
      provides: "Configuration reference with correct cache_prefix_separator default ('.')"
      contains: "cache_prefix_separator"
    - path: "docs/user-guide/cache-isolation.md"
      provides: "Cache isolation docs with correct separator examples"
    - path: "docs/user-guide/resolvers.md"
      provides: "Resolver docs with custom resolver pass-through note"
    - path: "docs/user-guide/database-per-tenant.md"
      provides: "Database driver docs with scoped EM reset description"
    - path: "docs/architecture/di-compilation.md"
      provides: "DI compilation reference with updated ResolverChainPass and TenantInitCommand"
    - path: "src/Command/TenantInitCommand.php"
      provides: "Correct cache_prefix_separator default in YAML template"
      contains: "cache_prefix_separator: '.'"
  key_links:
    - from: "docs/user-guide/cli-commands.md"
      to: "src/Command/TenantInitCommand.php"
      via: "documents the command behavior"
      pattern: "tenancy:init"
    - from: "docs/architecture/di-compilation.md"
      to: "src/DependencyInjection/Compiler/ResolverChainPass.php"
      via: "shows simplified source code"
      pattern: "BUILT_IN_RESOLVER_MAP"
---

<objective>
Update six documentation files to reflect Phase 12 (tenancy:init command) and Phase 13 (resolver config filtering, cache_prefix_separator default change to '.', EntityManagerResetListener EM targeting) changes. Fix all stale passages identified in the research audit.

Purpose: Documentation currently references pre-Phase-13 behavior (wrong separator default, missing command, stale code blocks). Users following these docs will misconfigure their applications.

Output: Accurate documentation across all six files, matching the as-shipped v1.0 codebase.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-refresh-update-docs-for-phase-12-13-changes-te/14-RESEARCH.md

<interfaces>
<!-- Source of truth files the executor must read for accurate content -->

From src/Command/TenantInitCommand.php:
- Command name: tenancy:init
- Options: --force (VALUE_NONE)
- Doctrine detection: interface_exists(\Doctrine\ORM\EntityManagerInterface::class)
- Creates: config/packages/tenancy.yaml
- Overwrite protection: exits FAILURE if file exists and --force not passed
- Output: "[OK] Created config/packages/tenancy.yaml" + driver recommendation + next steps

From src/Cache/TenantAwareCacheAdapter.php line 18:
- Default separator: '.' (NOT ':')

From src/DependencyInjection/Compiler/ResolverChainPass.php:
- BUILT_IN_RESOLVER_MAP: host->HostResolver, header->HeaderResolver, query_param->QueryParamResolver, console->ConsoleResolver
- Filtering: reads tenancy.resolvers parameter, builds allowedFqcns list, skips built-ins not in list, custom resolvers always pass through

From src/EventListener/EntityManagerResetListener.php:
- managersToReset defaults to [null] (resets default EM)
- Overridden to ['tenant'] in database_per_tenant mode via loadExtension
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenancy:init section to cli-commands.md</name>
  <files>docs/user-guide/cli-commands.md</files>
  <read_first>
    docs/user-guide/cli-commands.md
    src/Command/TenantInitCommand.php
  </read_first>
  <action>
Two changes to `docs/user-guide/cli-commands.md`:

**Change 1:** Update the opening paragraph (line 3). Replace:
```
The bundle provides two console commands for tenant-aware operations: `tenancy:migrate` for
running Doctrine Migrations across all tenants, and `tenancy:run` for executing any Symfony
console command within a specific tenant's context.
```
With:
```
The bundle provides three console commands: `tenancy:init` for scaffolding configuration,
`tenancy:migrate` for running Doctrine Migrations across all tenants, and `tenancy:run` for
executing any Symfony console command within a specific tenant's context.
```

**Change 2:** Insert a new `## tenancy:init` section BEFORE the existing `## tenancy:migrate` section (i.e., after the updated opening paragraph, before line 7's `## tenancy:migrate`). The new section must be:

```markdown
## tenancy:init

Scaffold a fully commented `config/packages/tenancy.yaml` with all configuration keys, Doctrine
detection, and next-steps guidance.

### Usage

```bash
# First-time setup — creates config/packages/tenancy.yaml
bin/console tenancy:init

# Regenerate (overwrite existing file)
bin/console tenancy:init --force
```

### Behavior

- **File creation**: Creates `config/packages/tenancy.yaml` in the project root. If the
  `config/packages/` directory does not exist, it is created automatically.
- **Overwrite protection**: If `config/packages/tenancy.yaml` already exists and `--force` is
  NOT passed, the command prints a warning and exits with failure. Pass `--force` to overwrite.
- **Doctrine detection**: The command checks `interface_exists(EntityManagerInterface::class)`.
  If Doctrine ORM is installed, it recommends `database_per_tenant` as the driver and
  uncomments the `driver:` line in the generated YAML. If Doctrine is absent, it recommends
  `shared_db` and leaves the driver line commented.
- **Next-steps guidance**: After creating the file, the command prints actionable next steps
  (review config, create Tenant entity, configure app_domain, run schema update).
- **No dependencies**: The command is always registered — no optional packages required.

### Output

When Doctrine ORM is detected:

```
 [OK] Created config/packages/tenancy.yaml

 Doctrine ORM detected — recommended driver: database_per_tenant
 Uncomment driver and set database.enabled: true in your config.

 Next Steps
 ----------
  * Review and uncomment the configuration values in config/packages/tenancy.yaml
  * Create your Tenant entity implementing Tenancy\Bundle\TenantInterface
  * Configure your host.app_domain if using subdomain-based resolution
  * Run bin/console doctrine:schema:update or create migrations for the Tenant entity
  * Visit https://github.com/danplaton4/tenancy-bundle for full documentation
```

When Doctrine ORM is NOT detected:

```
 [OK] Created config/packages/tenancy.yaml

 Doctrine ORM not detected — recommended driver: shared_db
 Install doctrine/orm to use database_per_tenant mode.

 Next Steps
 ----------
  * Review and uncomment the configuration values in config/packages/tenancy.yaml
  * Create your Tenant entity implementing Tenancy\Bundle\TenantInterface
  * Configure your host.app_domain if using subdomain-based resolution
  * Run bin/console doctrine:schema:update or create migrations for the Tenant entity
  * Visit https://github.com/danplaton4/tenancy-bundle for full documentation
```

---
```

After the new section, the existing `## tenancy:migrate` section continues unchanged.

Also update the "## See Also" section at the bottom — add a reference:
```markdown
- [Installation](installation.md) — initial setup with tenancy:init
```
  </action>
  <verify>
    <automated>grep -q 'three console commands' docs/user-guide/cli-commands.md && grep -q '## tenancy:init' docs/user-guide/cli-commands.md && grep -q '\-\-force' docs/user-guide/cli-commands.md && grep -q 'Doctrine detection' docs/user-guide/cli-commands.md && grep -q 'Overwrite protection' docs/user-guide/cli-commands.md && grep -q '## tenancy:migrate' docs/user-guide/cli-commands.md && grep -q '## tenancy:run' docs/user-guide/cli-commands.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Opening paragraph says "three console commands" not "two"
    - `## tenancy:init` section exists and appears BEFORE `## tenancy:migrate`
    - The tenancy:init section documents `--force` flag
    - The tenancy:init section documents Doctrine detection behavior
    - The tenancy:init section documents overwrite protection behavior
    - The tenancy:init section shows output examples for both Doctrine-detected and not-detected cases
    - The tenancy:init section states "always registered — no optional packages required"
    - `## tenancy:migrate` and `## tenancy:run` sections are unchanged
    - See Also section includes Installation link with tenancy:init reference
  </acceptance_criteria>
  <done>cli-commands.md documents all three commands including tenancy:init with --force, Doctrine detection, overwrite protection, and output examples</done>
</task>

<task type="auto">
  <name>Task 2: Fix cache_prefix_separator default in configuration.md and cache-isolation.md</name>
  <files>
    docs/user-guide/configuration.md
    docs/user-guide/cache-isolation.md
  </files>
  <read_first>
    docs/user-guide/configuration.md
    docs/user-guide/cache-isolation.md
    src/Cache/TenantAwareCacheAdapter.php
  </read_first>
  <action>
**docs/user-guide/configuration.md** — Four changes:

1. Line 72 — Update the Default cell in the `cache_prefix_separator` table:
   - OLD: `| `string` | `:` |`
   - NEW: `| `string` | `.` |`

2. Lines 75-76 — Update the prose description:
   - OLD: `The separator inserted between the tenant slug and the cache key when the cache bootstrapper namespaces the cache pool. For example, with the default separator and slug `acme`, a cache key `user.123` becomes `acme:user.123`.`
   - NEW: `The separator inserted between the tenant slug and the cache key when the cache bootstrapper namespaces the cache pool. For example, with the default separator and slug `acme`, a cache key `user.123` becomes `acme.user.123`.`

3. Line 155 in the Full Example YAML block:
   - OLD: `        cache_prefix_separator: ':'`
   - NEW: `        cache_prefix_separator: '.'`

4. Line 177 in the Full Example PHP block:
   - OLD: `            'cache_prefix_separator' => ':',`
   - NEW: `            'cache_prefix_separator' => '.',`

**docs/user-guide/cache-isolation.md** — One change:

Lines 50-53 — Update the namespace isolation examples:
   - OLD:
```
No tenant active:    cache key  →  app:my_key
Tenant 'acme':       cache key  →  app/acme:my_key
Tenant 'demo':       cache key  →  app/demo:my_key
```
   - NEW:
```
No tenant active:    cache key  →  app:my_key
Tenant 'acme':       cache key  →  app/acme.my_key
Tenant 'demo':       cache key  →  app/demo.my_key
```

Note: The "No tenant active" line keeps the colon because `app:my_key` is the Symfony cache namespace delimiter (not the tenancy separator). Only the tenant-scoped lines change from `:` to `.` after the slug.
  </action>
  <verify>
    <automated>grep -q "| .string. | \`\\.\` |" docs/user-guide/configuration.md && grep -q "acme.user.123" docs/user-guide/configuration.md && grep -q "cache_prefix_separator: '.'" docs/user-guide/configuration.md && ! grep -q "cache_prefix_separator: ':'" docs/user-guide/configuration.md && grep -q "app/acme.my_key" docs/user-guide/cache-isolation.md && grep -q "app/demo.my_key" docs/user-guide/cache-isolation.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - configuration.md `cache_prefix_separator` table shows default as `.` not `:`
    - configuration.md prose example says `acme.user.123` not `acme:user.123`
    - configuration.md Full Example YAML block shows `cache_prefix_separator: '.'`
    - configuration.md Full Example PHP block shows `'cache_prefix_separator' => '.'`
    - configuration.md has zero occurrences of `cache_prefix_separator: ':'`
    - cache-isolation.md shows `app/acme.my_key` and `app/demo.my_key` (dot separator)
    - cache-isolation.md "No tenant active" line still shows `app:my_key` (unchanged — that's a Symfony namespace colon)
    - No other sections of either file are changed
  </acceptance_criteria>
  <done>Both configuration.md and cache-isolation.md show correct '.' separator default and examples</done>
</task>

<task type="auto">
  <name>Task 3: Update resolvers.md, database-per-tenant.md, and di-compilation.md for Phase 13 accuracy</name>
  <files>
    docs/user-guide/resolvers.md
    docs/user-guide/database-per-tenant.md
    docs/architecture/di-compilation.md
  </files>
  <read_first>
    docs/user-guide/resolvers.md
    docs/user-guide/database-per-tenant.md
    docs/architecture/di-compilation.md
    src/DependencyInjection/Compiler/ResolverChainPass.php
    src/EventListener/EntityManagerResetListener.php
  </read_first>
  <action>
**docs/user-guide/resolvers.md** — One addition:

After the existing admonition at lines 184-185 (`!!! note "ConsoleResolver is always active"` block), add a new admonition:

```markdown

!!! note "Custom resolvers always pass through"
    The `tenancy.resolvers` config list only filters the four built-in resolvers (`host`,
    `header`, `query_param`, `console`). Custom resolvers that implement
    `TenantResolverInterface` are **never** filtered — they are always added to the chain
    regardless of the `resolvers` config value. This means you cannot accidentally disable
    a custom resolver by omitting it from the config list.
```

**docs/user-guide/database-per-tenant.md** — Replace the EntityManagerResetListener section:

Replace lines 203-210 (the "### Entity Manager Isolation" subsection including the warning admonition):

OLD:
```markdown
### Entity Manager Isolation

`EntityManagerResetListener` listens for `TenantContextCleared` and calls `resetManager()` on the
registry. This clears the tenant EM's identity map, preventing entity objects from leaking between
requests.

!!! warning "Stale EM References"
    `resetManager()` is called on every tenant switch. Any `EntityManagerInterface` reference
    obtained before the switch may be invalid after. Always retrieve the EM from the registry
    (e.g., `$doctrine->getManager('tenant')`) rather than caching it as a class property.
```

NEW:
```markdown
### Entity Manager Isolation

`EntityManagerResetListener` listens for `TenantContextCleared` and resets entity managers to
prevent identity map pollution across tenant switches. The behavior depends on the active driver:

- **`database_per_tenant` mode**: Only the `tenant` EM is reset via `resetManager('tenant')`.
  The `landlord` EM is never reset — it remains stable across tenant switches.
- **`shared_db` / single-EM mode**: The default EM is reset via `resetManager(null)`.

!!! warning "Stale EM References"
    `resetManager()` is called on every tenant switch. Any `EntityManagerInterface` reference
    to the **tenant** EM obtained before the switch may be invalid after. Always retrieve the
    tenant EM from the registry (e.g., `$doctrine->getManager('tenant')`) rather than caching
    it as a class property. The `landlord` EM is not affected.
```

**docs/architecture/di-compilation.md** — Two changes:

**Change 1:** Replace the ResolverChainPass code block (lines 88-99). Replace the entire code block with:

```php
// src/DependencyInjection/Compiler/ResolverChainPass.php (simplified)
public function process(ContainerBuilder $container): void
{
    $definition = $container->findDefinition(ResolverChain::class);

    // Build allowed FQCN set from config short-names (e.g. 'host', 'header')
    $allowedFqcns = null;
    if ($container->hasParameter('tenancy.resolvers')) {
        $allowedFqcns = [];
        foreach ($container->getParameter('tenancy.resolvers') as $name) {
            if (isset(self::BUILT_IN_RESOLVER_MAP[$name])) {
                $allowedFqcns[] = self::BUILT_IN_RESOLVER_MAP[$name];
            }
        }
    }

    $resolvers = $this->findAndSortTaggedServices('tenancy.resolver', $container);

    foreach ($resolvers as $resolver) {
        $serviceId = (string) $resolver;
        if (null !== $allowedFqcns) {
            $fqcn = $container->findDefinition($serviceId)->getClass() ?? $serviceId;
            // Built-in resolvers must be in the allowed list
            if (in_array($fqcn, self::BUILT_IN_RESOLVER_MAP, true)
                && !in_array($fqcn, $allowedFqcns, true)) {
                continue; // Skip — not in config
            }
            // Custom resolvers (not in built-in map) always pass through
        }
        $definition->addMethodCall('addResolver', [$resolver]);
    }
}
```

Also update the paragraph after the code block to explain the filtering:

After the code block, replace the existing paragraph (which currently describes unconditional addition) with:

```markdown
The pass reads the `tenancy.resolvers` config parameter to determine which built-in resolvers
are active. Built-in resolvers not listed in the config are skipped. Custom resolvers (any class
implementing `TenantResolverInterface` that is not in the `BUILT_IN_RESOLVER_MAP`) always pass
through the filter — they cannot be accidentally disabled by configuration. If no
`tenancy.resolvers` parameter exists, all resolvers are added unconditionally (backward
compatible).
```

**Change 2:** Add `TenantInitCommand` to the "Always Registered" services table (around line 229). Add a new row after the `EntityManagerResetListener` row:

```markdown
| `tenancy.command.init` | `TenantInitCommand` | Scaffolds `config/packages/tenancy.yaml` |
```
  </action>
  <verify>
    <automated>grep -q 'Custom resolvers always pass through' docs/user-guide/resolvers.md && grep -q "resetManager('tenant')" docs/user-guide/database-per-tenant.md && grep -q 'landlord EM is never reset' docs/user-guide/database-per-tenant.md && grep -q 'BUILT_IN_RESOLVER_MAP' docs/architecture/di-compilation.md && grep -q 'TenantInitCommand' docs/architecture/di-compilation.md && grep -q 'tenancy.command.init' docs/architecture/di-compilation.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - resolvers.md contains a "Custom resolvers always pass through" admonition note
    - resolvers.md admonition explains that custom resolvers are never filtered by config
    - database-per-tenant.md EntityManagerResetListener section distinguishes between database_per_tenant mode (resets tenant EM only) and shared_db mode (resets default EM)
    - database-per-tenant.md states "landlord EM is never reset"
    - database-per-tenant.md warning admonition specifies "tenant EM" not just generic EM
    - di-compilation.md ResolverChainPass code block contains `BUILT_IN_RESOLVER_MAP`
    - di-compilation.md ResolverChainPass code block contains `allowedFqcns`
    - di-compilation.md ResolverChainPass code block shows the filtering logic (not the old unconditional loop)
    - di-compilation.md explanation paragraph mentions "custom resolvers always pass through"
    - di-compilation.md always-registered table includes `tenancy.command.init` / `TenantInitCommand`
    - No unrelated sections of any file are changed
  </acceptance_criteria>
  <done>resolvers.md has custom pass-through note, database-per-tenant.md has scoped EM reset description, di-compilation.md has updated ResolverChainPass code and TenantInitCommand in services table</done>
</task>

<task type="auto">
  <name>Task 4: Fix stale cache_prefix_separator default in TenantInitCommand YAML template</name>
  <files>src/Command/TenantInitCommand.php</files>
  <read_first>
    src/Command/TenantInitCommand.php
    src/Cache/TenantAwareCacheAdapter.php
  </read_first>
  <action>
In `src/Command/TenantInitCommand.php`, line 113, the YAML template has the old separator default:

OLD:
```php
"    # cache_prefix_separator: ':'",
```

NEW:
```php
"    # cache_prefix_separator: '.'",
```

This aligns the scaffolded YAML with the actual default in `TenantAwareCacheAdapter.php` line 18 (which is `'.'`).
  </action>
  <verify>
    <automated>grep -q "cache_prefix_separator: '.'" src/Command/TenantInitCommand.php && ! grep -q "cache_prefix_separator: ':'" src/Command/TenantInitCommand.php && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - TenantInitCommand.php YAML template shows `cache_prefix_separator: '.'` not `':'`
    - No other lines in TenantInitCommand.php are changed
    - `vendor/bin/phpunit --testsuite unit` passes
  </acceptance_criteria>
  <done>TenantInitCommand YAML template shows correct '.' separator default matching TenantAwareCacheAdapter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation content | User-facing text only; no code execution path changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | Documentation | accept | Documentation updates are text-only, no secrets or credentials involved; all content references are to public source code |
| T-14-05 | Tampering | Documentation accuracy | mitigate | Every updated value is verified against source code (TenantAwareCacheAdapter.php line 18, ResolverChainPass.php, EntityManagerResetListener.php, TenantInitCommand.php); read_first blocks ensure executor reads source before writing docs |
</threat_model>

<verification>
1. cli-commands.md has three documented commands: `grep -c '^## tenancy:' docs/user-guide/cli-commands.md` returns 3
2. cache_prefix_separator is '.' everywhere: `! grep -r "cache_prefix_separator: ':'" docs/` (no matches)
3. ResolverChainPass code block is updated: `grep -q 'BUILT_IN_RESOLVER_MAP' docs/architecture/di-compilation.md`
4. TenantInitCommand in DI table: `grep -q 'tenancy.command.init' docs/architecture/di-compilation.md`
5. EM reset scoped correctly: `grep -q "resetManager('tenant')" docs/user-guide/database-per-tenant.md`
6. Custom resolver note added: `grep -q 'Custom resolvers always pass through' docs/user-guide/resolvers.md`
7. Test suite unaffected: `vendor/bin/phpunit --testsuite unit` passes (no source code changes)
</verification>

<success_criteria>
- cli-commands.md documents tenancy:init with --force, Doctrine detection, overwrite protection, output examples
- configuration.md and cache-isolation.md show '.' as cache_prefix_separator default everywhere
- resolvers.md documents that custom resolvers always pass through the config filter
- database-per-tenant.md EntityManagerResetListener section distinguishes per-driver EM targeting
- di-compilation.md has the real ResolverChainPass implementation (with filtering) and TenantInitCommand in services table
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-refresh-update-docs-for-phase-12-13-changes-te/14-02-SUMMARY.md`
</output>