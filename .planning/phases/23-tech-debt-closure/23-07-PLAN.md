---
phase: 23-tech-debt-closure
plan: 07
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
  - 23-03
  - 23-04
  - 23-05
  - 23-06
files_modified: []
autonomous: false
requirements:
  - GREEN-BAR-VERIFICATION-01
must_haves:
  truths:
    - "vendor/bin/phpunit --no-coverage exits 0 with 562+ tests (existing 559 + ~3 from Plan 23-01 + ~7 from Plan 23-02 contract data provider + ~5 from Plan 23-02 LogicException + Plan 23-03 guard-ordering = ~15 new minimum)"
    - "vendor/bin/phpstan analyse --memory-limit=512M returns [OK] No errors"
    - "vendor/bin/php-cs-fixer check --diff exits 0 with no files changed"
    - "scripts/docs-lint.sh exits 0"
    - "If feasible: examples/saas/bin/smoke.sh run against a live docker compose stack returns exit 0 INCLUDING the new mailer-isolation section"
  artifacts: []
  key_links: []
---

<objective>
Wave-3 integration green-bar verification. This plan does NOT modify source — it only runs the full verification matrix against the bundle's tree after Plans 23-01..23-06 have landed. The checkpoint task asks the user to optionally run the demo's `bin/smoke.sh` against a live `docker compose up` stack to prove the new Mailpit assertion works against real Mailpit.

Purpose: The audit's success criteria #6 says "Full PHPUnit suite + PHPStan level 9 + php-cs-fixer all green." This plan is the explicit gate. CONTEXT.md flagged this as Wave 3; collapsing it into Wave 2 is permitted by the orchestrator, but a dedicated plan with a final human-verify checkpoint for the live-stack smoke run is cleaner: live-stack runs cost a developer ~3 minutes of `docker compose up` and they're the only way to prove the Plan 23-04 jq assertion works against real Mailpit.

Output: No file changes. Plan completion produces a SUMMARY documenting:
- PHPUnit final test count + assertion count
- PHPStan baseline (clean)
- php-cs-fixer baseline (clean)
- docs-lint.sh result
- Live-stack smoke.sh result (or note: skipped per user, deferred to CI)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.3-MILESTONE-AUDIT.md
@.planning/phases/23-tech-debt-closure/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification matrix</name>
  <files></files>
  <read_first>
    - CHANGELOG.md (confirm Plan 23-05 landed — the new 0.3.2 + 0.3.3 sections exist)
    - .planning/REQUIREMENTS.md (confirm Plan 23-06 landed — three checkboxes are [x])
  </read_first>
  <action>
    Run each verification command in sequence; STOP on first failure and report the failing command's output verbatim in the SUMMARY. The intent is a single uninterrupted green run; partial passes are NOT a success.

    1. `vendor/bin/phpunit --no-coverage` — full PHPUnit suite (expect 562+ tests, exit 0).
       Record: Tests count, Assertions count, any deprecation warnings (a benign PHPUnit Deprecations: 1 already exists from prior phases — note if it changes), exit code.

    2. `vendor/bin/phpstan analyse --memory-limit=512M` — expect `[OK] No errors`, exit 0.
       Record: total files analyzed, exit code.

    3. `vendor/bin/php-cs-fixer check --diff` — expect exit 0 with no files changed.
       Record: number of files checked, exit code.

    4. `bash scripts/docs-lint.sh` — expect exit 0.
       Record: exit code. If the script exits non-zero with a `bundles.php` false positive against the new CHANGELOG 0.3.0 section (which legitimately references `config/bundles.php` in the "Adoption Surface" prose), investigate whether the script's whitelist already covers CHANGELOG headers — if not, this is a NEW FAILING — report it as a regression and do NOT silently extend the whitelist (out of scope for this plan; surface to the user).

    5. `bash -n examples/saas/bin/smoke.sh` — expect exit 0. Re-confirm the Plan 23-04 syntax pass.

    Compile a verification matrix into the SUMMARY:

       | Check | Command | Result | Output snippet |
       |-------|---------|--------|----------------|
       | PHPUnit | `vendor/bin/phpunit --no-coverage` | PASS / FAIL | `Tests: N, Assertions: M` |
       | PHPStan | `vendor/bin/phpstan analyse --memory-limit=512M` | PASS / FAIL | `[OK] No errors` |
       | cs-fixer | `vendor/bin/php-cs-fixer check --diff` | PASS / FAIL | `... files were correctly formatted` |
       | docs-lint | `bash scripts/docs-lint.sh` | PASS / FAIL | (exit code) |
       | smoke.sh syntax | `bash -n examples/saas/bin/smoke.sh` | PASS / FAIL | (exit code) |

    Do NOT modify any source file. If any check fails, report it and STOP — do not attempt to auto-fix.
  </action>
  <acceptance_criteria>
    - All five commands exit 0.
    - PHPUnit `Tests:` count is ≥ 562 (559 baseline + ~3 from Plan 23-01 rendered-HTML tests + ~7 from Plan 23-02 contract data-provider expansion + ~3 from Plan 23-02 LogicException tests + ~2 from Plan 23-03 guard-ordering tests + ~1 from Plan 23-03 whitespace-slug regression = ~16 minimum new). The exact number is not material to acceptance; what matters is the suite is green.
    - PHPStan `[OK] No errors` appears in output.
    - `php-cs-fixer check --diff` output contains `Found 0 files that need to be fixed` OR equivalent zero-changed wording.
    - docs-lint and smoke.sh syntax checks both green.
  </acceptance_criteria>
  <verify>
    <automated>vendor/bin/phpunit --no-coverage &amp;&amp; vendor/bin/phpstan analyse --memory-limit=512M &amp;&amp; vendor/bin/php-cs-fixer check --diff &amp;&amp; bash scripts/docs-lint.sh &amp;&amp; bash -n examples/saas/bin/smoke.sh</automated>
  </verify>
  <done>Full automated verification matrix green; SUMMARY records exact test/assertion counts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 23 closure is mechanically complete after Plans 23-01..23-06. Plan 23-04 added a per-tenant mailer-isolation section to `examples/saas/bin/smoke.sh` that queries Mailpit's `/api/v1/messages` API. The Plan 23-04 verify gate only proved bash syntax; it did NOT prove the new assertion actually catches a TenantMessageDecorator regression against a live Mailpit instance.
  </what-built>
  <how-to-verify>
    OPTIONAL — recommended but skippable. The CI pipeline at `.github/workflows/demo-smoke.yml` will run this same check on the next push to master, so failure to run locally is recoverable. But running locally now catches issues before the tag.

    Steps:

    1. Open a terminal at the repo root.
    2. Choose ports that don't conflict with anything else running on the dev machine. For example:
       `export PORT_HTTP=8081 PORT_HTTPS=8444 PORT_MAILPIT_UI=8026 BASE_PORT=8081`
    3. Bring up the demo stack (~60-90s build + boot):
       `cd examples/saas && docker compose up -d --wait --build`
    4. When all three services report healthy, run the smoke script:
       `bash bin/smoke.sh`
    5. Confirm the output includes (in order):
       - `==> Per-tenant mailer isolation (Mailpit assertion)`
       - `    PASS: acme + globex From: addresses isolated correctly`
       - `==> All smoke assertions PASSED`
       (Plus the prior existing PASS lines for app readiness, landlord root, per-tenant landing pages, resolver priority.)
    6. Exit code 0.
    7. (Sanity check, optional): manually curl `http://127.0.0.1:8026/api/v1/messages` and confirm at least 2 messages with the expected From: addresses.
    8. Tear down: `docker compose down -v` (the `-v` removes the named volumes; recommended between local runs to avoid stale Mailpit data).

    If `bin/smoke.sh` exits non-zero on the mailer assertion specifically, report the failure mode to the user — DO NOT auto-fix (that would be out of scope for a verification plan).

    If the user skips this step (acceptable per the audit's "let CI run it" recommendation), record `live_stack_run: skipped (deferred to .github/workflows/demo-smoke.yml on next push)` in the SUMMARY frontmatter and move on.
  </how-to-verify>
  <resume-signal>Type "approved" if smoke.sh passed end-to-end including the mailer-isolation section. Type "skipped" if deferring to CI. Type any other message to flag a real failure for triage.</resume-signal>
</task>

</tasks>

<verification>
- Task 1 automated matrix green (all 5 commands exit 0).
- Task 2 either passes live-stack verification OR is documented as "deferred to CI" with explicit user acknowledgment.
</verification>

<success_criteria>
1. PHPUnit + PHPStan + php-cs-fixer + docs-lint + smoke.sh syntax all green.
2. Either: live-stack smoke.sh confirmed exit-0 with the new mailer-isolation section present, OR: user explicitly defers to CI with the deferred status logged.
3. SUMMARY records the verification matrix verbatim so the v0.3.3 tag has a clean audit trail.
</success_criteria>

<output>
Create `.planning/phases/23-tech-debt-closure/23-07-SUMMARY.md` when done. Include:
- Frontmatter: `phase: 23-tech-debt-closure`, `plan: 07`, `requirements: [GREEN-BAR-VERIFICATION-01]`, `subsystem: verification`.
- Body: the verification matrix table; PHPUnit test count delta vs the 559 baseline; explicit note on whether the live-stack smoke.sh ran or was deferred.
- A closing "Ready to tag v0.3.3" line if all gates green.
</output>
