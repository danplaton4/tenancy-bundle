---
phase: 23-tech-debt-closure
plan: 06
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
  - 23-03
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-CHECKBOX-REFRESH-01
must_haves:
  truths:
    - "RESV-06 checkbox in REQUIREMENTS.md flipped from [ ] to [x]"
    - "DEMO-01 checkbox in REQUIREMENTS.md flipped from [ ] to [x]"
    - "DOC-19 checkbox in REQUIREMENTS.md flipped from [ ] to [x]"
    - "Traceability table at the bottom of REQUIREMENTS.md shows Complete status for RESV-06, DEMO-01, DOC-19"
    - "GOV-01 traceability row preserves ⊘ Skipped status"
    - "Already-complete requirements (DX-06, DX-02, BOOT-04) remain unchanged"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Up-to-date v0.3 traceability checkboxes"
      contains: "- [x] **RESV-06**"
  key_links:
    - from: ".planning/REQUIREMENTS.md ## v0.3 Requirements"
      to: "RESV-06/DEMO-01/DOC-19 sections"
      via: "Checkbox flip [ ]→[x]"
      pattern: "- \\[x\\] \\*\\*(RESV-06|DEMO-01|DOC-19)\\*\\*"
    - from: ".planning/REQUIREMENTS.md ## Traceability"
      to: "Status column for RESV-06, DEMO-01, DOC-19"
      via: "Status text changed from Pending to Complete"
      pattern: "(RESV-06|DEMO-01|DOC-19) \\| Phase \\d+ .* \\| Complete"
---

<objective>
Refresh three v0.3 traceability checkboxes in `.planning/REQUIREMENTS.md` from `[ ]` to `[x]`: RESV-06 (Phase 17 — shipped), DEMO-01 (Phase 21 — shipped), DOC-19 (Phase 22 — shipped). Update the Traceability table at the bottom of the file so the Status column reads `Complete` for those three rows.

Purpose: Cosmetic pre-tag housekeeping. Per `.planning/v0.3-MILESTONE-AUDIT.md` lines 100-105, all three requirements are SATISFIED in their phase VERIFICATION.md and SUMMARY frontmatter — only REQUIREMENTS.md was never updated in lockstep. The `/gsd:complete-milestone` workflow normally handles this in archival, but doing it inline here means the eventual milestone-archive commit is a pure file move (cleaner git history) per CONTEXT.md.

Output: One file modified — six edits total (three checkbox flips in the v0.3 Requirements section, three Status column updates in the Traceability table).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.3-MILESTONE-AUDIT.md
@.planning/phases/23-tech-debt-closure/23-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Current REQUIREMENTS.md state (read confirmed): -->

Checkboxes in the v0.3 Requirements section:
  - L13: `- [x] **DX-06**: …` — ALREADY COMPLETE (do not touch)
  - L20: `- [ ] **DEMO-01**: …` — FLIP TO [x]
  - L30: `- [x] **DX-02**: …` — ALREADY COMPLETE (do not touch)
  - L40: `- [x] **BOOT-04**: …` — ALREADY COMPLETE (do not touch)
  - L52: `- [ ] **RESV-06**: …` — FLIP TO [x]
  - L63: `- [ ] **DOC-19**: …` — FLIP TO [x]
  - L75: `- ⊘ **GOV-01 (SKIPPED — non-functional)**` — DO NOT TOUCH

Traceability table (L108-116, 7 rows):
  | Requirement | Phase | Status |
  |-------------|-------|--------|
  | GOV-01 | Phase 16 — Governance Carry-Forward | ⊘ Skipped (non-functional, 2026-05-15) |
  | RESV-06 | Phase 17 — OriginHeaderResolver | Pending          ← UPDATE to Complete
  | DX-06 | Phase 18 — tenancy:install | Complete
  | DX-02 | Phase 19 — Profiler Tab | Complete
  | BOOT-04 | Phase 20 — Mailer Bootstrapper | Complete
  | DEMO-01 | Phase 21 — Demo App | Pending                  ← UPDATE to Complete
  | DOC-19 | Phase 22 — Docs Refresh | Pending                ← UPDATE to Complete

Coverage block (L118-122) — note: includes "Active: 6, mapped to phases 17–22 (100%)" — the wording does NOT need to change because Pending→Complete is a status delta, not a coverage delta.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip RESV-06 / DEMO-01 / DOC-19 checkboxes and update Traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — confirm current line numbers per the &lt;interfaces&gt; section)
  </read_first>
  <action>
    SIX precise edits to `.planning/REQUIREMENTS.md`. Each edit replaces the exact pre-existing text with the exact replacement; do not modify any other text on the line, do not reformat surrounding paragraphs.

    EDIT A — Line ~20 (DEMO-01 checkbox):
      FIND:  `- [ ] **DEMO-01**: `
      REPLACE WITH:  `- [x] **DEMO-01**: `

    EDIT B — Line ~52 (RESV-06 checkbox):
      FIND:  `- [ ] **RESV-06**: `
      REPLACE WITH:  `- [x] **RESV-06**: `

    EDIT C — Line ~63 (DOC-19 checkbox):
      FIND:  `- [ ] **DOC-19**: `
      REPLACE WITH:  `- [x] **DOC-19**: `

    EDIT D — Traceability table RESV-06 row:
      FIND:  `| RESV-06 | Phase 17 — OriginHeaderResolver | Pending |`
      REPLACE WITH:  `| RESV-06 | Phase 17 — OriginHeaderResolver | Complete |`

    EDIT E — Traceability table DEMO-01 row:
      FIND:  `| DEMO-01 | Phase 21 — Demo App | Pending |`
      REPLACE WITH:  `| DEMO-01 | Phase 21 — Demo App | Complete |`

    EDIT F — Traceability table DOC-19 row:
      FIND:  `| DOC-19 | Phase 22 — Docs Refresh | Pending |`
      REPLACE WITH:  `| DOC-19 | Phase 22 — Docs Refresh | Complete |`

    Do NOT touch:
    - The DX-06 checkbox (already [x])
    - The DX-02 checkbox (already [x])
    - The BOOT-04 checkbox (already [x])
    - The GOV-01 row (preserve ⊘ Skipped status)
    - The Coverage paragraph below the Traceability table — the "Active: 6, mapped to phases 17–22 (100%)" wording is correct because it reports coverage, not status.
    - The Architectural Decisions Ratified table at the bottom of the file.
    - Any other Markdown formatting, spacing, or text.

    The exact pre-existing strings above were verified by reading the file. If the Read shows slightly different whitespace (e.g. trailing spaces), use the actual on-disk strings. The edit MUST be byte-precise — Edit tool will fail on mismatch.
  </action>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*RESV-06\*\*:" .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c "^- \[x\] \*\*DEMO-01\*\*:" .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c "^- \[x\] \*\*DOC-19\*\*:" .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c "^- \[ \] \*\*\(RESV-06\|DEMO-01\|DOC-19\)\*\*:" .planning/REQUIREMENTS.md` returns `0` (no Pending checkboxes remain for these three).
    - `grep -c "| RESV-06 | Phase 17 — OriginHeaderResolver | Complete |" .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c "| DEMO-01 | Phase 21 — Demo App | Complete |" .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c "| DOC-19 | Phase 22 — Docs Refresh | Complete |" .planning/REQUIREMENTS.md` returns `1`.
    - `grep -c "| RESV-06 .* | Pending |" .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c "| DEMO-01 .* | Pending |" .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c "| DOC-19 .* | Pending |" .planning/REQUIREMENTS.md` returns `0`.
    - GOV-01 row UNCHANGED: `grep -c "| GOV-01 | Phase 16 — Governance Carry-Forward | ⊘ Skipped" .planning/REQUIREMENTS.md` returns `1`.
    - DX-06 / DX-02 / BOOT-04 rows UNCHANGED (still `Complete` in the table; checkboxes still `[x]`).
    - Total line count of REQUIREMENTS.md is unchanged ± 0 (every edit is a same-length replacement). Verify via `wc -l .planning/REQUIREMENTS.md` before and after.
  </acceptance_criteria>
  <verify>
    <automated>[ $(grep -c "^- \\[x\\] \\*\\*RESV-06\\*\\*" .planning/REQUIREMENTS.md) -eq 1 ] &amp;&amp; [ $(grep -c "^- \\[x\\] \\*\\*DEMO-01\\*\\*" .planning/REQUIREMENTS.md) -eq 1 ] &amp;&amp; [ $(grep -c "^- \\[x\\] \\*\\*DOC-19\\*\\*" .planning/REQUIREMENTS.md) -eq 1 ] &amp;&amp; [ $(grep -c "| Pending |" .planning/REQUIREMENTS.md) -eq 0 ] &amp;&amp; grep -q "| GOV-01 | Phase 16 — Governance Carry-Forward | ⊘ Skipped" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All three checkboxes flipped; all three traceability rows updated to Complete; GOV-01 and already-complete requirements untouched.</done>
</task>

</tasks>

<verification>
- All Task 1 acceptance gates pass.
- The file remains syntactically valid Markdown (no broken tables, no orphan list items).
- No PHP / test side-effects expected.
</verification>

<success_criteria>
1. RESV-06, DEMO-01, DOC-19 checkboxes flipped to `[x]` in the v0.3 Requirements section.
2. Same three rows show `Complete` in the Traceability table.
3. GOV-01 retains `⊘ Skipped`; DX-06/DX-02/BOOT-04 untouched.
4. File total line count unchanged.
</success_criteria>

<output>
Create `.planning/phases/23-tech-debt-closure/23-06-SUMMARY.md` when done. Include:
- Frontmatter: `phase: 23-tech-debt-closure`, `plan: 06`, `requirements: [REQ-CHECKBOX-REFRESH-01]`, `subsystem: traceability`.
- Body: list the six edits made (3 checkbox flips + 3 table-status updates) with before/after one-liners.
</output>
