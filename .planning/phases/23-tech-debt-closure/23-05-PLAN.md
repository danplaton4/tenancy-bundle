---
phase: 23-tech-debt-closure
plan: 05
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
  - 23-03
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - CHANGELOG-PROMOTION-01
must_haves:
  truths:
    - "CHANGELOG.md has a `## [0.3.2] — 2026-05-22` section between Unreleased and 0.3.1"
    - "CHANGELOG.md has a `## [0.3.3] — 2026-05-29` section between Unreleased and 0.3.2"
    - "## [0.3.2] enumerates the AbstractTenant split, BOOT-01..BOOT-07 docker layout fixes, and demo-bundles boot path fixes from Phase 21 live-stack pass 3"
    - "## [0.3.3] enumerates the nikic require move from Phase 22 (DEC-INST-02 reversal) AND the Phase 23 closure items"
    - "## [Unreleased] section is left empty (placeholder for v0.4 work)"
    - "Bottom-of-file compare-link footnotes are updated for the new tags (0.3.2 + 0.3.3)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Versioned 0.3.2 + 0.3.3 sections promoted from Unreleased"
      contains: "## [0.3.3] — 2026-05-29"
  key_links:
    - from: "CHANGELOG.md ## [0.3.2]"
      to: "Phase 21 VERIFICATION.md BOOT-01..BOOT-07"
      via: "Verbatim copy of the 7 BOOT-* gap-closure entries"
      pattern: "BOOT-01\\|BOOT-02\\|BOOT-03\\|BOOT-04\\|BOOT-05\\|BOOT-06\\|BOOT-07"
    - from: "CHANGELOG.md ## [0.3.3]"
      to: "Phase 22 SUMMARY (nikic require move) + Phase 23 plans (closure items)"
      via: "Section bullets sourced from Phase 22 SUMMARY frontmatter (D-09/D-10) + Phase 23 PLAN audit IDs"
      pattern: "nikic/php-parser"
---

<objective>
Promote the empty `## [Unreleased]` section into two dated, versioned releases: `## [0.3.2] — 2026-05-22` and `## [0.3.3] — 2026-05-29`. This is the pre-tag housekeeping item from `.planning/v0.3-MILESTONE-AUDIT.md` lines 187-188. Without this promotion, `git tag v0.3.3` would ship with an empty Unreleased section and no canonical record of what changed.

Purpose: Keep-a-Changelog convention requires dated section headers tied to git tags. v0.3.2 has been the shipped state since 2026-05-22 (Phase 21 live-stack pass 3 + AbstractTenant split landed in commit `aa6ae37` / `c968e2f` / earlier — see git log). v0.3.3 is what Phase 22 nikic require move + Phase 23 closure adds. The Unreleased section was left empty after these phases shipped because their SUMMARYs front-load the detail and the closure phase was supposed to consolidate. Today, today.

Output: One file modified — CHANGELOG.md gains two new section headers + content blocks + updated footnote compare links. No source-code changes in this plan.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.3-MILESTONE-AUDIT.md
@.planning/phases/23-tech-debt-closure/23-CONTEXT.md
@.planning/phases/21-demo-app/21-VERIFICATION.md
@.planning/phases/22-docs-refresh/22-01-SUMMARY.md

<interfaces>
<!-- Current CHANGELOG.md structure (read confirmed, 269 lines): -->
- L1-7: title + Keep-a-Changelog intro
- L8: `## [Unreleased]` (CURRENTLY EMPTY — no bullets between this and the next section)
- L10-24: `## [0.3.1] — 2026-05-22` (CI prefer-lowest matrix fix)
- L26-125: `## [0.3.0] — 2026-05-22` (Adoption Surface batch 1)
- L127-137: `## [0.2.1] — 2026-04-21`
- L139-201: `## [0.2.0] — 2026-04-20`
- L203-265: `## [0.1.0] - 2026-04-19`
- L266-268: compare-link footnotes (`[Unreleased]:`, `[0.2.0]:`, `[0.1.0]:`) — note these are stale; they reference v0.2.0 not v0.3.x

<!-- BOOT-01..BOOT-07 source text (verbatim from 21-VERIFICATION.md L24-31): -->
- BOOT-01: DemoTenant could not extend bundle Tenant — Doctrine refused two #[ORM\Entity] root classes on tenancy_tenants table. Fixed by splitting bundle Tenant into AbstractTenant (MappedSuperclass) + thin concrete Tenant. DemoTenant now extends AbstractTenant.
- BOOT-02: Demo composer path repo url `../../` did not resolve inside Docker because the layout `/srv/` + `/srv/demo/` made `../../` point at `/`, not the bundle root. Composer silently fell through to Packagist's published v0.3.1 — the demo was never running the dev tree. Fixed by mirroring the host layout: `/srv/bundle/` + `/srv/bundle/examples/saas/`.
- BOOT-03: doctrine.yaml referenced `wrapper_class: Tenancy\Bundle\Doctrine\TenantConnection` which does not exist in the dev tree (the bundle now auto-registers TenantDriverMiddleware via DBAL 4). Removed.
- BOOT-04: `final class Post` rejected by Doctrine ORM 3 lazy-ghost proxy generation. Removed `final`.
- BOOT-05: `config/services.yaml` did not exist — controllers were never autoconfigured as services. Every route returned 500 'has no container set'. Added the standard Symfony services skeleton.
- BOOT-06: `bin/console` returned Kernel instead of Application. Symfony Runtime under FrankenPHP's SERVER_NAME env then dispatched the CLI as an HTTP request and threw 'Invalid Host ":80,"'. Fixed to return Application + wrap in `env -u SERVER_NAME` in entrypoint as belt-and-braces.
- BOOT-07: Caddyfile served HTTPS only (`tls internal` on the wildcard block), but smoke.sh + the Dockerfile healthcheck use plain HTTP. Split into explicit `http://` and `https://` site blocks.

<!-- AbstractTenant split source (memory: project_tenant_entity_split.md): -->
- 2026-05-22: bundle Tenant split into AbstractTenant (MappedSuperclass) + thin concrete Tenant.
- Custom tenant entities MUST extend AbstractTenant.

<!-- Phase 22 nikic require move source (22-01-SUMMARY.md frontmatter D-09/D-10): -->
- D-09: nikic/php-parser ^5.0 added to composer.json#require (was require-dev + suggest)
- D-10: nikic/php-parser entry removed from composer.json#suggest

<!-- Phase 23 closure items (this phase): -->
- INT-01 Twig mailer subsection hoisted out of resolved-only branch (Plan 23-01)
- CR-01 nullable-provider drift guard — all 6 sites use `= null` default; reflection contract test strengthened (Plan 23-02)
- WR-01 MissingTenantProviderException LogicException unit tests for both throw sites (Plan 23-02)
- WR-02 ConsoleResolver guard-ordering defensive comment + source-order test (Plan 23-03)
- WR-03 QueryParamResolver empty-string pattern aligned with ConsoleResolver (trim-aware) (Plan 23-03)
- WR-04 TenantRunCommand @security trust-boundary docblock (Plan 23-03)
- IN-01..IN-05 ZeroConfigKernelBootTest cosmetic + safety cleanup + explicit `use TenantStamp` in middleware (Plan 23-03)
- smoke.sh per-tenant mailer assertion via Mailpit /api/v1/messages (Plan 23-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ## [0.3.2] and ## [0.3.3] sections + update compare links</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — note L8 is `## [Unreleased]`, L10 is `## [0.3.1] — 2026-05-22`, L266-268 are compare-link footnotes)
    - .planning/phases/21-demo-app/21-VERIFICATION.md (L22-31 — verbatim source for the 7 BOOT-* bullets; quotation marks must be preserved)
    - .planning/phases/22-docs-refresh/22-01-SUMMARY.md (frontmatter D-09 / D-10 for nikic require/suggest move)
  </read_first>
  <action>
    Edit `CHANGELOG.md` as follows:

    A) INSERT two new section headers between `## [Unreleased]` (L8) and `## [0.3.1]` (L10).

       New section 1 — placed IMMEDIATELY AFTER the `## [Unreleased]` line and ITS following blank line, BEFORE `## [0.3.2]`:

       ```
       ## [0.3.3] — 2026-05-29

       v0.3.3 closes the v0.3 milestone tech-debt audit and finalizes the install
       ergonomics promotion. This is the tag that ships v0.3 Adoption Surface in its
       final shape. The next milestone is v0.4 Storage & Shared Entities.

       ### Changed

       - **`nikic/php-parser` promoted from `require-dev`+`suggest` to `require`** —
         the bundle now hard-requires `nikic/php-parser ^5.0`. Rationale (DEC-INST-02
         reversal, Phase 22): `tenancy:install` is the canonical onboarding path and
         it depends on the AST detector. Asking users to `composer require --dev
         nikic/php-parser` before running `tenancy:install` was a stumbling block on
         the install funnel; the dep is small (~150 KB), zero-runtime-cost when
         unused, and is already a transitive dep of every Symfony-Flex-aware
         project. The bundle's own runtime never touches the parser at request time
         — it loads only during the one-time install command.

       ### Fixed

       - **Profiler mailer subsection now renders on all panel states** — the
         `{% if collector.data.mailer is defined %}` block in
         `src/Resources/views/Collector/tenant.html.twig` was previously nested
         inside `{% if collector.data.state == 'resolved' %}`, hiding cache
         hit/eviction counters on landlord, public, and health-check routes where
         operators most need to see them. Hoisted to the top level of the panel
         block. INT-01 from `.planning/v0.3-MILESTONE-AUDIT.md`. Affects DX-02 + BOOT-04.
       - **Nullable-provider drift guard strengthened** — all six
         `tenancy.provider->nullOnInvalid()` consumer constructors now use the
         identical signature `?TenantProviderInterface $tenantProvider = null`.
         Previously three of six (ConsoleResolver, TenantRunCommand,
         TenantWorkerMiddleware) declared the parameter nullable but omitted the
         `= null` default, allowing a future contributor to drop the `?` without
         tripping a downstream caller. `NullableProviderInjectionContractTest` now
         locks the default-null contract via reflection across all seven registered
         sites (the six listed plus TenantAwareTransportsDecorator). CR-01.
       - **Messenger retry semantics for misconfiguration** — `TenantRunCommand` and
         `TenantWorkerMiddleware` were already throwing
         `MissingTenantProviderException` (which extends `\LogicException`, NOT
         `\RuntimeException`) since v0.3.0, but unit tests asserted only the
         concrete class. New tests pin the `\LogicException` base class so a future
         refactor cannot regress Messenger's retry semantics: Symfony Messenger's
         default retry strategy excludes `LogicException` from re-queue, treating
         it as a permanent config error. WR-01.

       ### Changed (internal)

       - **`ConsoleResolver` guard-ordering tripwire** — defensive comment block
         marks the WR-02 invariant (`null === $this->tenantProvider` guard MUST
         precede the Application::addOption mutation that adds `--tenant` to the
         global definition); `ConsoleResolverGuardOrderingTest` asserts the source
         order via file-read line scan. WR-02.
       - **`QueryParamResolver` empty-string check tightened** — changed from
         `null === $slug || '' === $slug` to `!is_string($slug) || '' === trim($slug)`,
         matching `ConsoleResolver`'s pattern and additionally rejecting
         whitespace-only `?_tenant=   ` query strings. Behavior is strictly stricter;
         any tenant slug that survived the prior check survives this one. WR-03.
       - **`TenantRunCommand` `@security` trust-boundary docblock** added above the
         `new Process($command)` call to document the array-argv defense (the
         actual shell-injection vector was closed in v0.3.0 by switching from
         `Process::fromShellCommandline()` to array-argv `new Process()`). WR-04.
       - **`ZeroConfigKernelBootTest` housekeeping** — dropped the stale
         `@group canary-red` annotation and historical RED-bar framing (the canary
         has been green since plans 18-09/18-10 landed); removed
         `setCatchExceptions(false)` from the `bin/console list` regression test so
         the captured output surfaces in the failure message; added `getmypid()` to
         the cache-dir hash to prevent parallel-PHPUnit cache-dir collisions;
         deduped the parent-directory removal loop in `tearDownAfterClass`.
         IN-01..IN-04.
       - **`TenantWorkerMiddleware` explicit `use TenantStamp` import** — the
         class now imports `Tenancy\Bundle\Messenger\TenantStamp` explicitly
         instead of relying on same-namespace resolution; aligns with the rest of
         the bundle's import style and future-proofs the class against a stamp
         relocation. IN-05.
       - **`examples/saas/bin/smoke.sh` per-tenant mailer assertion** — the demo
         smoke script now POSTs `/_demo/send-test-mail` for acme + globex, queries
         Mailpit's `/api/v1/messages` API, and asserts distinct `From:` addresses
         per tenant via `jq -e`. A regression in `TenantMessageDecorator` that
         broke per-tenant `From:` injection would now fail demo-smoke CI loudly
         instead of being caught only by human UAT. Closure of the audit's
         "smoke.sh has no per-tenant mailer assertion" tech-debt item.

       ## [0.3.2] — 2026-05-22

       Phase 21 live-stack pass 3 hardening. The demo app would not boot end-to-end
       on a fresh clone before this release; seven docker-layout + Doctrine ORM 3
       + FrankenPHP integration bugs were discovered only when `docker compose up`
       was finally run as part of phase verification. All seven are fixed here.

       ### Changed

       - **`Tenant` entity split into `AbstractTenant` + concrete `Tenant`** —
         Doctrine ORM 3 refuses two `#[ORM\Entity]` root classes pointing at the
         same `tenancy_tenants` table, which prevented the demo's `DemoTenant`
         from extending the bundle's `Tenant`. The split moves the slug/name/active
         column definitions onto an `#[ORM\MappedSuperclass]` abstract base
         (`Tenancy\Bundle\AbstractTenant`); the concrete bundle `Tenant` becomes
         a thin `#[ORM\Entity]` carrying nothing of its own. **Custom tenant
         entities MUST extend `AbstractTenant`, not `Tenant`** — see `UPGRADE.md`
         § 0.3.1 → 0.3.2 for the trivial migration. BOOT-01.

       ### Fixed

       - **Demo Composer path-repo broken inside Docker** — `examples/saas/composer.json`
         declared a path repository at `../../` which resolved to the bundle root
         on the host but to `/` inside the container, silently falling through to
         Packagist's published `v0.3.1` and running the demo against the published
         tag instead of the dev tree. Fixed by mirroring the host layout in the
         container: `/srv/bundle/` (bundle root) + `/srv/bundle/examples/saas/`
         (demo root). BOOT-02.
       - **Stale `wrapper_class:` reference in demo `doctrine.yaml`** — pointed at
         `Tenancy\Bundle\Doctrine\TenantConnection`, a class removed in v0.2.0
         when the bundle migrated to DBAL 4 `TenantDriverMiddleware` connection
         switching. Removed. BOOT-03.
       - **`final class Post` rejected by Doctrine ORM 3 lazy-ghost proxy
         generation** in the demo. Removed `final`. BOOT-04.
       - **Demo `config/services.yaml` missing** — controllers had no
         autoconfiguration, every route returned 500 `"has no container set"`.
         Added the standard Symfony services skeleton with controller resource
         tagging. BOOT-05.
       - **`bin/console` returned Kernel instead of Application** — Symfony
         Runtime under FrankenPHP's `SERVER_NAME` env then dispatched the CLI as
         an HTTP request and threw `Invalid Host ":80,"`. Fixed by returning
         `Application` from `bin/console` and wrapping the entrypoint script in
         `env -u SERVER_NAME` as belt-and-braces. BOOT-06.
       - **Caddyfile served HTTPS only** — `tls internal` on the wildcard block
         meant `smoke.sh` (plain HTTP) and the Dockerfile healthcheck both got
         redirects they could not follow. Split into explicit `http://` and
         `https://` site blocks. BOOT-07.

       ### Changed (demo packaging)

       - **Mailpit UI port parameterized** — `${PORT_MAILPIT_UI:-8025}` in
         `compose.yaml`, `.env`, and `.env.example` so the demo coexists with
         other dev stacks on the same host. `smoke.sh` now accepts a `BASE_PORT`
         env override for the same reason.

       ```

       Note the careful structure: 0.3.3 sits ABOVE 0.3.2 (newest first per Keep-a-Changelog convention). Both are inserted between the empty `## [Unreleased]` (still at L8) and the existing `## [0.3.1]` section.

    B) LEAVE `## [Unreleased]` empty — do NOT add bullets to it. Future v0.4 work will populate it. Confirm there is exactly one blank line between `## [Unreleased]` and `## [0.3.3]`.

    C) UPDATE the compare-link footnotes at the bottom of the file (currently L266-268). The existing block is:
       ```
       [Unreleased]: https://github.com/danplaton4/tenancy-bundle/compare/v0.2.0...HEAD
       [0.2.0]: https://github.com/danplaton4/tenancy-bundle/releases/tag/v0.2.0
       [0.1.0]: https://github.com/danplaton4/tenancy-bundle/releases/tag/v0.1.0
       ```
       Replace the `[Unreleased]:` line and insert lines for the new tags:
       ```
       [Unreleased]: https://github.com/danplaton4/tenancy-bundle/compare/v0.3.3...HEAD
       [0.3.3]: https://github.com/danplaton4/tenancy-bundle/compare/v0.3.2...v0.3.3
       [0.3.2]: https://github.com/danplaton4/tenancy-bundle/compare/v0.3.1...v0.3.2
       [0.3.1]: https://github.com/danplaton4/tenancy-bundle/compare/v0.3.0...v0.3.1
       [0.3.0]: https://github.com/danplaton4/tenancy-bundle/compare/v0.2.1...v0.3.0
       [0.2.1]: https://github.com/danplaton4/tenancy-bundle/compare/v0.2.0...v0.2.1
       [0.2.0]: https://github.com/danplaton4/tenancy-bundle/releases/tag/v0.2.0
       [0.1.0]: https://github.com/danplaton4/tenancy-bundle/releases/tag/v0.1.0
       ```
       This preserves the existing v0.2.0/v0.1.0 links AND adds the missing v0.2.1/v0.3.0/v0.3.1/v0.3.2/v0.3.3 footnotes.

    Do NOT modify any of the existing version sections (0.3.1, 0.3.0, 0.2.1, 0.2.0, 0.1.0) in any way. Do NOT alter the title/intro at the top of the file.
  </action>
  <acceptance_criteria>
    - `grep -c "^## \[0.3.3\] — 2026-05-29" CHANGELOG.md` returns `1`.
    - `grep -c "^## \[0.3.2\] — 2026-05-22" CHANGELOG.md` returns `1`.
    - The line `## [0.3.3] — 2026-05-29` appears BEFORE `## [0.3.2] — 2026-05-22` (newest first). Verify via `awk '/^## \[0.3.3\]/{a=NR} /^## \[0.3.2\]/{b=NR} END{exit (a<b)?0:1}' CHANGELOG.md`.
    - `## [0.3.2]` section contains all SEVEN BOOT-* IDs: `grep -cE "BOOT-0[1-7]" CHANGELOG.md` returns at least `7`.
    - `## [0.3.3]` section enumerates the Phase 23 closure IDs: `grep -cE "INT-01|CR-01|WR-01|WR-02|WR-03|WR-04|IN-01" CHANGELOG.md` returns at least `7`.
    - `## [0.3.3]` section references the nikic require move: `grep -c "nikic/php-parser" CHANGELOG.md` returns at least `2` (existing v0.3.0 section already mentions it once; the new 0.3.3 section adds one more).
    - `## [Unreleased]` section is empty: the lines between `## [Unreleased]` and the next `## [` heading contain only blank lines. Verify via `awk '/^## \[Unreleased\]/{u=1; next} u && /^## \[/{exit} u {print}' CHANGELOG.md` returns no non-blank lines.
    - Compare-link footnotes block contains all required tags: `grep -cE "^\[(Unreleased|0\.3\.3|0\.3\.2|0\.3\.1|0\.3\.0|0\.2\.1|0\.2\.0|0\.1\.0)\]:" CHANGELOG.md` returns `8`.
    - `[Unreleased]:` compare link points at v0.3.3...HEAD: `grep -q "^\[Unreleased\]: https://github.com/danplaton4/tenancy-bundle/compare/v0.3.3\.\.\.HEAD" CHANGELOG.md`.
    - File still ends with a single newline (no trailing whitespace issues).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^## \[0.3.3\] — 2026-05-29" CHANGELOG.md &amp;&amp; grep -q "^## \[0.3.2\] — 2026-05-22" CHANGELOG.md &amp;&amp; awk '/^## \[0.3.3\]/{a=NR} /^## \[0.3.2\]/{b=NR} END{exit (a&amp;&amp;b&amp;&amp;a&lt;b)?0:1}' CHANGELOG.md &amp;&amp; [ $(grep -cE "BOOT-0[1-7]" CHANGELOG.md) -ge 7 ] &amp;&amp; grep -q "^\[Unreleased\]: https://github.com/danplaton4/tenancy-bundle/compare/v0.3.3\.\.\.HEAD" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has dated 0.3.2 + 0.3.3 sections; Unreleased is empty; compare-link footnotes are complete; existing sections untouched.</done>
</task>

</tasks>

<verification>
- All Task 1 acceptance gates pass.
- `vendor/bin/phpunit --no-coverage` — full suite still green (CHANGELOG edits are documentation-only; no code touched).
- `vendor/bin/phpstan analyse --memory-limit=512M` — `[OK] No errors` (no PHP changes).
- `scripts/docs-lint.sh` — exit 0 (the lint extension from Phase 22 enforces no `bundles.php` install-path references outside permitted sections; CHANGELOG sections are typically permitted — re-run to confirm no false positive on the new content).
</verification>

<success_criteria>
1. CHANGELOG.md has dated `## [0.3.3] — 2026-05-29` and `## [0.3.2] — 2026-05-22` sections, ordered newest-first.
2. 0.3.2 section enumerates AbstractTenant split + BOOT-01..BOOT-07.
3. 0.3.3 section enumerates nikic require move + Phase 23 closure items (INT-01, CR-01, WR-01..04, IN-01..05, smoke.sh).
4. Unreleased section is empty.
5. Compare-link footnotes are complete for all v0.2.x and v0.3.x tags.
6. Existing version sections (0.3.1 and earlier) untouched.
</success_criteria>

<output>
Create `.planning/phases/23-tech-debt-closure/23-05-SUMMARY.md` when done. Include:
- Frontmatter: `phase: 23-tech-debt-closure`, `plan: 05`, `requirements: [CHANGELOG-PROMOTION-01]`, `subsystem: release-notes`.
- Body: summary of the two added sections; cross-reference to 21-VERIFICATION.md (BOOT-* source) and 22-01-SUMMARY.md (nikic require source).
</output>
