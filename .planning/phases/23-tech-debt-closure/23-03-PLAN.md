---
phase: 23-tech-debt-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 23-02
files_modified:
  - src/Resolver/ConsoleResolver.php
  - src/Resolver/QueryParamResolver.php
  - src/Command/TenantRunCommand.php
  - src/Messenger/TenantWorkerMiddleware.php
  - tests/Integration/ZeroConfigKernelBootTest.php
  - tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php
autonomous: true
requirements:
  - DX-06
  - WR-02
  - WR-03
  - WR-04
  - IN-01
  - IN-02
  - IN-03
  - IN-04
  - IN-05
must_haves:
  truths:
    - "ConsoleResolver has a defensive comment block above the Application-mutation site noting guard ordering must stay above mutation"
    - "A reflection-based unit test fails if anyone reorders the guard below the Application-mutation site in ConsoleResolver"
    - "QueryParamResolver empty-string check matches the ConsoleResolver `is_string() && '' !== trim()` pattern and rejects whitespace-only slugs"
    - "TenantRunCommand has a `@security` docblock above the Process invocation documenting the trust boundary"
    - "TenantWorkerMiddleware has an explicit `use Tenancy\\Bundle\\Messenger\\TenantStamp;` import"
    - "ZeroConfigKernelBootTest drops the stale @group canary-red and stale class-docblock framing"
    - "ZeroConfigKernelBootTest replaces setCatchExceptions(false)+assertSame pattern with expectException for the console-exit-zero test"
    - "ZeroConfigTestKernel cache-dir hash includes getmypid() — no parallel-PHPUnit cache-dir collisions"
    - "ZeroConfigKernelBootTest::tearDownAfterClass removes the parent dir exactly once per path (no duplicate Filesystem::remove)"
  artifacts:
    - path: "src/Resolver/ConsoleResolver.php"
      provides: "Guard-ordering defensive comment + pattern-aligned slug check (already aligned, retained)"
      contains: "// GUARD ORDERING"
    - path: "src/Resolver/QueryParamResolver.php"
      provides: "is_string() + trim() empty-string check aligned with ConsoleResolver"
      contains: "is_string"
    - path: "src/Command/TenantRunCommand.php"
      provides: "@security trust-boundary docblock above Process site"
      contains: "@security"
    - path: "src/Messenger/TenantWorkerMiddleware.php"
      provides: "Explicit `use Tenancy\\Bundle\\Messenger\\TenantStamp;` import"
      contains: "use Tenancy\\Bundle\\Messenger\\TenantStamp"
    - path: "tests/Integration/ZeroConfigKernelBootTest.php"
      provides: "Cleaned canary test — no stale @group annotation, no duplicate dir removal, PID in cache-dir hash, expectException pattern"
      contains: "getmypid"
    - path: "tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php"
      provides: "Reflection-based source-order check that ConsoleResolver's null-guard precedes the Application-mutation site"
      contains: "ReflectionMethod"
  key_links:
    - from: "src/Resolver/QueryParamResolver.php"
      to: "is_string check + trim()"
      via: "type narrow before truthiness"
      pattern: "is_string\\(\\$slug\\)"
    - from: "tests/Integration/ZeroConfigKernelBootTest.php"
      to: "getmypid() in cache-dir hash"
      via: "ZeroConfigTestKernel::getCacheDir/getLogDir"
      pattern: "getmypid"
    - from: "tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php"
      to: "ConsoleResolver source order"
      via: "ReflectionMethod::getStartLine + file source scan"
      pattern: "addOption"
---

<objective>
Close five intra-bundle consistency advisory findings from the 18-VERIFICATION.md advisory section (WR-02 ConsoleResolver guard-ordering, WR-03 QueryParamResolver pattern alignment, WR-04 TenantRunCommand shell-injection trust-boundary docblock, IN-01..IN-05 ZeroConfigKernelBootTest cosmetics + the explicit `use TenantStamp` import).

Purpose: These are bounded mechanical edits with high signal-to-noise. WR-02 hardens a fragile ordering invariant inside ConsoleResolver. WR-03 unifies the empty-string handling pattern across the two cli-style resolvers. WR-04 documents the trust boundary at the Process call site (the actual shell-injection vector closed in v0.3.0, but the trust assumption still deserves a comment). IN-01..IN-05 are pure test-harness cleanup that prevents the canary test from misleading future readers and from racing on parallel PHPUnit runners.

WR-04 nuance (per CONTEXT.md D-04): The shell-injection vector was closed in v0.3.0 (CHANGELOG entry: `tenancy:run shell-injection vector — switched to new Process(array)`). What remains is a documentation gap: the `@security` docblock at the Process site that explains the trust boundary now that the unescaped-shell vector is gone. The CONTEXT.md text "Process::fromShellCommandline" refers to the historical state; today the call site is `new Process($command)` with array argv at L71. Anchor the docblock to that line.

Output: 4 source-file edits (small), 1 test-file edit (medium), 1 new unit test (small).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.3-MILESTONE-AUDIT.md
@.planning/phases/23-tech-debt-closure/23-CONTEXT.md
@.planning/phases/18-tenancy-install/18-VERIFICATION.md

<interfaces>
<!-- Current ConsoleResolver shape (read confirmed): -->
ConsoleResolver::onConsoleCommand() outline:
  L31  if (null === $this->tenantProvider) { return; }     ← THE GUARD (must stay above mutation)
  L35  $command = $event->getCommand();
  L36  $input = $event->getInput();
  L38  if (null === $command) { return; }
  L42  $application = $command->getApplication();
  L43  if (null === $application) { return; }
  L52  $appDefinition = $application->getDefinition();
  L53  if (!$appDefinition->hasOption('tenant')) {
  L54      $appDefinition->addOption(                                 ← THE MUTATION (adds --tenant to global Application definition)
  L55          new InputOption('tenant', null, InputOption::VALUE_OPTIONAL, 'Tenant slug to resolve')
  L56      );
  L57  }
  L60  $command->mergeApplicationDefinition();
  L61  $input->bind($command->getDefinition());
  L63  $slug = $input->getOption('tenant');
  L65  if (!\is_string($slug) || '' === $slug) { return; }
  L69  $tenant = $this->tenantProvider->findBySlug($slug);

ConsoleResolver slug check pattern (L65 — already aligned with what WR-03 wants):
  `if (!\is_string($slug) || '' === $slug) { return; }`

QueryParamResolver slug check (L30 — CURRENT, needs WR-03 alignment):
  `if (null === $slug || '' === $slug) { return null; }`

QueryParamResolver new desired pattern (per CONTEXT.md D-03):
  `if (!is_string($slug) || '' === trim($slug)) { return null; }`
  (Rejects whitespace-only as null; uses single-source pattern across both resolvers; the trim() differs from ConsoleResolver's plain `'' === $slug` — CONTEXT.md explicitly calls out the trim addition.)

TenantRunCommand current Process call site (L57-71 — read confirmed):
  Comment block at L56-60 already documents the array-argv inertness for shell metacharacters. The `@security` docblock per WR-04 should sit ABOVE this (after the `findBySlug($tenantSlug);` line at L53 and before the existing comment at L56).

TenantWorkerMiddleware imports (top of file — read confirmed):
  Already imports MissingTenantProviderException at L14. Currently uses `TenantStamp::class` at L29 — implicit same-namespace resolution. IN-05 wants an explicit `use Tenancy\Bundle\Messenger\TenantStamp;` even though it's same-namespace (consistency + future-proofing if the stamp moves).

ZeroConfigKernelBootTest current shape (read confirmed):
  L23-37 — stale class docblock + `@group canary-red` annotation
  L62-68 — `foreach ([$cacheDir, $logDir] as $dir) { $parent = \dirname($dir); if ($fs->exists($parent)) { $fs->remove($parent); } }` — iterates TWO paths but `$cacheDir` and `$logDir` share the SAME parent dir (`sys_get_temp_dir().'/tenancy_bundle_test_'.md5(...).'_'.env`), so the parent is removed twice. The second removal is a no-op. IN-04 asks to dedupe.
  L139-150 — `setCatchExceptions(false)` + `assertSame(0, $tester->getStatusCode())` pattern. IN-02 asks to replace with `expectException(\Symfony\Component\Console\Exception\RuntimeException::class)` … but that wording in CONTEXT.md is INCORRECT: this test is currently the GREEN path (zero-config kernel SHOULD boot and `list` SHOULD exit zero). The IN-02 advisory in 18-VERIFICATION.md (read confirmed L41) talks about "may suppress diagnostic assertion message if an exception propagates" — i.e. the failure mode is that an unexpected exception propagates and obscures the assertSame message. The cleanup is to KEEP the assertSame pattern but DROP `setCatchExceptions(false)` so the ApplicationTester catches exceptions and the assertion message surfaces clearly with the captured output. Re-read 18-VERIFICATION.md L41 carefully and follow that intent, NOT the CONTEXT.md misquote. Implementation: remove `$application->setCatchExceptions(false);` (L141) — keep everything else.
  L207-215 — `getCacheDir()` returns `sys_get_temp_dir().'/tenancy_bundle_test_'.md5(static::class).'_'.$this->environment.'/cache'`. IN-03 asks to add `getmypid()` to the hash input: `md5(static::class.$this->environment.getmypid())`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-02 + WR-03 + WR-04 — Source-level consistency edits in three files</name>
  <files>
    src/Resolver/ConsoleResolver.php,
    src/Resolver/QueryParamResolver.php,
    src/Command/TenantRunCommand.php
  </files>
  <read_first>
    - src/Resolver/ConsoleResolver.php (full file — note L28-75 contains the full onConsoleCommand body; the guard is at L31, the mutation at L54)
    - src/Resolver/QueryParamResolver.php (full file — L21-40)
    - src/Command/TenantRunCommand.php (full file — L40-80; Process site at L69-72)
  </read_first>
  <action>
    THREE source-level edits, all small:

    A) WR-02 — ConsoleResolver guard-ordering defensive comment.
       In `src/Resolver/ConsoleResolver.php`, ADD a multi-line comment block IMMEDIATELY BEFORE the `if (!$appDefinition->hasOption('tenant'))` line (currently L53). Comment text MUST include the literal token `GUARD ORDERING` and the literal token `MUST` so the comment-anchored test (Task 2) can locate it. Suggested wording:

       ```
       // GUARD ORDERING (WR-02): the null-tenantProvider guard at the top of this method
       // MUST stay ABOVE this Application-definition mutation. The mutation adds
       // --tenant to the global Application options; running it in zero-config mode
       // (no provider bound) would pollute every console command with a useless flag.
       // ConsoleResolverGuardOrderingTest verifies the guard precedes the mutation.
       ```

       Do NOT change any executable code in this file (the slug check at L65 already uses the `is_string()`-first pattern WR-03 wants).

    B) WR-03 — QueryParamResolver pattern alignment.
       In `src/Resolver/QueryParamResolver.php`, REPLACE the current empty-string check:

         `if (null === $slug || '' === $slug) {`

       with the trim-aware is_string-first form (per CONTEXT.md D-03):

         `if (!is_string($slug) || '' === trim($slug)) {`

       This narrows the type early (`Request::query->get()` returns `string|null|array` for repeated params; the prior pattern handled null but not arrays) AND rejects whitespace-only slugs. The `(string) $slug` cast at L35 is now superfluous because the if-block has already proven `$slug` is a non-empty trimmed string — but leave the cast for explicitness; PHPStan level 9 may demand it depending on inference.

       Do NOT modify the constructor signature (that is Plan 23-02's responsibility). Do NOT change the `try/catch (TenantNotFoundException)` block.

    C) WR-04 — TenantRunCommand @security trust-boundary docblock.
       In `src/Command/TenantRunCommand.php`, ADD a `@security`-prefixed docblock IMMEDIATELY BEFORE the existing inline comment block that starts `// Tokenize on whitespace; NO shell interpretation.` (currently around L56). The docblock should be a PHPDoc-style `/** … */` comment block placed ABOVE the comment but BELOW the `$this->tenantProvider->findBySlug($tenantSlug);` line. Suggested content:

       ```
       /**
        * @security Trust boundary (WR-04): the caller of `tenancy:run` is a developer at the CLI.
        * `$commandString` is read from `$input->getArgument('command_string')` and is NOT escaped.
        * The v0.3.0 fix switched from Process::fromShellCommandline() (shell-quoted) to
        * `new Process(array $argv)` (execve-direct), making shell metacharacters inert in any
        * single token. Do NOT expose this command via HTTP, queued jobs, or any context where
        * untrusted input can reach `$commandString` — the array-argv defense covers shell metas
        * but a malicious token could still invoke unintended bin/console commands.
        */
       ```

       Do NOT modify any executable code. The existing inline comment block at L56-60 stays as-is — the new docblock supplements it.
  </action>
  <acceptance_criteria>
    - WR-02: `grep -c "GUARD ORDERING" src/Resolver/ConsoleResolver.php` returns at least `1`.
    - WR-02: The line containing `GUARD ORDERING` precedes the line containing `addOption` (Application mutation) in the file. Verify via `awk '/GUARD ORDERING/{g=NR} /addOption\(/{a=NR} END{exit (g<a)?0:1}' src/Resolver/ConsoleResolver.php`.
    - WR-03: `grep -c "is_string(\$slug)" src/Resolver/QueryParamResolver.php` returns at least `1`.
    - WR-03: `grep -c "null === \$slug || '' === \$slug" src/Resolver/QueryParamResolver.php` returns `0` (old pattern fully replaced).
    - WR-03: `grep -c "trim(\$slug)" src/Resolver/QueryParamResolver.php` returns at least `1`.
    - WR-04: `grep -c "@security" src/Command/TenantRunCommand.php` returns at least `1`.
    - PHP lint clean: `for f in src/Resolver/ConsoleResolver.php src/Resolver/QueryParamResolver.php src/Command/TenantRunCommand.php; do php -l "$f" || exit 1; done` exits 0.
    - PHPUnit: existing tests for QueryParamResolver, ConsoleResolver, and TenantRunCommand all still pass. Whitespace-only slug regression: if a pre-existing test covers `?_tenant=   `, the trim() change MUST keep it green (the prior pattern would also return null for that input — both old and new patterns reject `'   '` if it casts to non-empty string; the new pattern is stricter on whitespace-only AFTER trim). If no such test exists, add one to `tests/Unit/Resolver/QueryParamResolverTest.php` asserting that `?_tenant=%20%20%20` (URL-encoded spaces) resolves to null.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "GUARD ORDERING" src/Resolver/ConsoleResolver.php &amp;&amp; grep -q "is_string(\$slug)" src/Resolver/QueryParamResolver.php &amp;&amp; grep -q "@security" src/Command/TenantRunCommand.php &amp;&amp; vendor/bin/phpunit tests/Unit/Resolver --no-coverage &amp;&amp; vendor/bin/phpunit tests/Unit/Command --no-coverage</automated>
  </verify>
  <done>Three source files carry the required comments/pattern; PHPUnit + lint clean; no executable behavior changes except the trim()-aware whitespace rejection in QueryParamResolver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ConsoleResolverGuardOrderingTest (source-order reflection check)</name>
  <files>tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php</files>
  <read_first>
    - src/Resolver/ConsoleResolver.php (confirm method `onConsoleCommand` exists; confirm Reflection-detectable line numbers for the guard at L31 vs the mutation at L54)
    - tests/Unit/Resolver/HostResolverTest.php (if exists; pattern-match the namespace + base class used by existing resolver tests)
  </read_first>
  <behavior>
    - The new test fails RED if a developer moves the `null === $this->tenantProvider` guard below the `addOption('tenant', …)` Application mutation in `ConsoleResolver::onConsoleCommand`.
    - The new test does NOT exercise the resolver's runtime behavior — that is covered by existing tests. It checks the SOURCE ORDER via file read + line-number scan.
  </behavior>
  <action>
    Create new file `tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php` in namespace `Tenancy\Bundle\Tests\Unit\Resolver`, extending `PHPUnit\Framework\TestCase`. The file MUST be declared `final class` and use `declare(strict_types=1);` to match project convention (PHPStan level 9).

    Add ONE test method `testGuardPrecedesApplicationMutation`:
    1. Resolve the path to `src/Resolver/ConsoleResolver.php` via `\dirname(__DIR__, 3).'/src/Resolver/ConsoleResolver.php'`.
    2. Read the file with `file_get_contents`.
    3. Split into lines via `explode("\n", $source)`.
    4. Find the line index of the FIRST occurrence of `'null === $this->tenantProvider'` — this is the guard.
    5. Find the line index of the FIRST occurrence of `'addOption('` — this is the Application mutation.
    6. Assert both indexes were found (not false).
    7. Assert `$guardLine < $mutationLine` with a message that includes both line numbers and the literal string: "WR-02 invariant violated — the null-tenantProvider guard MUST precede the Application::addOption mutation. Moving the guard below the mutation pollutes every console command's --help/--list output with a stale --tenant flag in zero-config mode. See WR-02 / .planning/phases/23-tech-debt-closure/23-CONTEXT.md."

    Add a SECOND test method `testGuardOrderingCommentBlockExists`:
    1. Read the same file.
    2. Assert via `assertStringContainsString` that the source contains BOTH `'GUARD ORDERING'` AND `'MUST'` (both literal strings, case-sensitive).
    3. The presence of this comment is itself a tripwire — a refactor that drops the comment is the first sign someone is about to move the guard.

    Do NOT exercise the resolver's runtime behavior. Do NOT mock the container. The whole point is a static source-level check.
  </action>
  <acceptance_criteria>
    - File exists at the specified path.
    - `vendor/bin/phpunit tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php --no-coverage` exits 0 with 2 tests passing.
    - `grep -c "ReflectionMethod\|file_get_contents" tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php` returns at least `1`.
    - RED-bar proof: mentally swap L31 and L54-57 of ConsoleResolver.php — both new tests must fail.
    - PHPStan level 9 clean on the new file.
    - php-cs-fixer clean.
  </acceptance_criteria>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php --no-coverage &amp;&amp; vendor/bin/phpstan analyse tests/Unit/Resolver/ConsoleResolverGuardOrderingTest.php --memory-limit=512M</automated>
  </verify>
  <done>New test file exists with 2 passing tests pinning the WR-02 invariant; PHPStan + cs-fixer clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IN-01..IN-05 — ZeroConfigKernelBootTest cosmetic + safety cleanup</name>
  <files>
    tests/Integration/ZeroConfigKernelBootTest.php,
    src/Messenger/TenantWorkerMiddleware.php
  </files>
  <read_first>
    - tests/Integration/ZeroConfigKernelBootTest.php (full file)
    - src/Messenger/TenantWorkerMiddleware.php (top imports L1-15; line referencing `TenantStamp::class` at L29)
  </read_first>
  <action>
    FIVE small edits, all within two files:

    IN-01 — Drop stale framing from ZeroConfigKernelBootTest.
       - Remove the `@group canary-red` line in the class docblock (currently L36).
       - Edit the class docblock prose to reflect the test's current role: GREEN-bar permanent regression gate for the zero-config kernel boot path. Replace the lines stating "MUST fail on master before plans 18-09/18-10 land" with a single line: `/** Permanent GREEN-bar regression gate: zero-config kernel boot (no `tenancy:` extension block). */` and keep the architectural narration about the canary's purpose (the second paragraph at L25-32 explaining what the kernel does). Strike only the historical "RED-bar" + "MUST fail before plans 18-09/18-10" framing.

    IN-02 — Replace the setCatchExceptions(false) pattern in `testConsoleApplicationVersionCommandExitsZero`.
       Per the audit (18-VERIFICATION.md L41): the issue is that `setCatchExceptions(false)` lets exceptions propagate BEFORE the `assertSame(0, $tester->getStatusCode())` runs, suppressing the diagnostic message captured in the test output. Fix by removing `$application->setCatchExceptions(false);` (L141). The ApplicationTester's default exception handling will catch and return a non-zero status code, AND the `'Output: '.$tester->getDisplay()` portion of the assertion message will surface the actual error text. Result: clearer failure messages without losing the regression check.

    IN-03 — Add getmypid() to ZeroConfigTestKernel cache/log dir hash.
       Modify `getCacheDir()` (L207) and `getLogDir()` (L212) to include `getmypid()` in the md5 input:
         `return sys_get_temp_dir().'/tenancy_bundle_test_'.md5(static::class.$this->environment.getmypid()).'/cache';`
         `return sys_get_temp_dir().'/tenancy_bundle_test_'.md5(static::class.$this->environment.getmypid()).'/logs';`
       The string concatenation deliberately combines `static::class`, `$this->environment`, and `getmypid()` into one md5 input — keeps the path stable within a single process run, varies across parallel PHPUnit processes.

    IN-04 — Dedupe parent-dir removal in tearDownAfterClass.
       Currently L63-68 iterates `[$cacheDir, $logDir]` and removes `\dirname($dir)` for each. Both share the same parent. Replace the loop with a single-path version that computes the unique parent once:
         ```
         $parent = \dirname($cacheDir);
         $fs = new Filesystem();
         if ($fs->exists($parent)) {
             $fs->remove($parent);
         }
         ```
       (Both `$cacheDir = $parent.'/cache'` and `$logDir = $parent.'/logs'` resolve to the same parent — removing the parent once cleans both subdirs in one shot.)

    IN-05 — Add explicit `use Tenancy\Bundle\Messenger\TenantStamp;` to TenantWorkerMiddleware.
       In `src/Messenger/TenantWorkerMiddleware.php`, ADD the line `use Tenancy\Bundle\Messenger\TenantStamp;` to the import block at the top of the file (after the existing `Tenancy\Bundle\…` imports, in alphabetical order — should land between `TenantContext` (no, it's `Context\TenantContext`) — alphabetical insertion: the existing imports include `Tenancy\Bundle\Event\TenantContextCleared`, `Tenancy\Bundle\Exception\MissingTenantProviderException`, `Tenancy\Bundle\Provider\TenantProviderInterface`. Insert `use Tenancy\Bundle\Messenger\TenantStamp;` AFTER `MissingTenantProviderException` and BEFORE `TenantProviderInterface` per PSR-12 + php-cs-fixer @Symfony alphabetization). The `TenantStamp::class` reference at L29 (now in the same namespace) becomes redundant — but explicit import is the cs-fixer-preferred form and is what IN-05 asks for.

    Do NOT change any test logic in ZeroConfigKernelBootTest beyond the four IN-01..IN-04 items above. Do NOT add new test methods (the canary is sufficient).
  </action>
  <acceptance_criteria>
    - IN-01: `grep -c "@group canary-red" tests/Integration/ZeroConfigKernelBootTest.php` returns `0`.
    - IN-01: `grep -c "MUST fail on master" tests/Integration/ZeroConfigKernelBootTest.php` returns `0`.
    - IN-01: `grep -c "Permanent GREEN-bar regression gate" tests/Integration/ZeroConfigKernelBootTest.php` returns at least `1`.
    - IN-02: `grep -c "setCatchExceptions(false)" tests/Integration/ZeroConfigKernelBootTest.php` returns `0`.
    - IN-03: `grep -c "getmypid()" tests/Integration/ZeroConfigKernelBootTest.php` returns at least `2` (once each for getCacheDir + getLogDir).
    - IN-04: `grep -cE "foreach \(\\\[\\\$cacheDir, \\\$logDir\\\]" tests/Integration/ZeroConfigKernelBootTest.php` returns `0` (the duplicate-removal loop is gone).
    - IN-05: `grep -c "^use Tenancy\\\\Bundle\\\\Messenger\\\\TenantStamp;" src/Messenger/TenantWorkerMiddleware.php` returns `1` (the explicit import exists at top-of-file).
    - All three canary tests still pass: `vendor/bin/phpunit tests/Integration/ZeroConfigKernelBootTest.php --no-coverage` exits 0 with Tests: 3.
    - Full suite green: `vendor/bin/phpunit --no-coverage`.
    - PHPStan + cs-fixer clean.
  </acceptance_criteria>
  <verify>
    <automated>vendor/bin/phpunit tests/Integration/ZeroConfigKernelBootTest.php --no-coverage &amp;&amp; grep -q "getmypid()" tests/Integration/ZeroConfigKernelBootTest.php &amp;&amp; ! grep -q "@group canary-red" tests/Integration/ZeroConfigKernelBootTest.php &amp;&amp; grep -q "^use Tenancy\\\\Bundle\\\\Messenger\\\\TenantStamp;" src/Messenger/TenantWorkerMiddleware.php &amp;&amp; vendor/bin/phpstan analyse tests/Integration/ZeroConfigKernelBootTest.php src/Messenger/TenantWorkerMiddleware.php --memory-limit=512M</automated>
  </verify>
  <done>All five IN-* items closed; canary still green; PHPStan + cs-fixer clean; no stale annotations remain.</done>
</task>

</tasks>

<verification>
- `vendor/bin/phpunit --no-coverage` — full suite green; 3+ new tests added across Tasks 1-3 (1 WR-03 whitespace test if absent, 2 guard-ordering tests).
- `vendor/bin/phpstan analyse --memory-limit=512M` — `[OK] No errors`.
- `vendor/bin/php-cs-fixer check --diff` — exit 0.
- All grep gates from the three tasks' acceptance criteria pass.
</verification>

<success_criteria>
1. WR-02: ConsoleResolver has the GUARD ORDERING comment AND a unit test that fails if the guard is reordered below the mutation.
2. WR-03: QueryParamResolver uses `is_string($slug) && '' !== trim($slug)` — pattern-aligned with ConsoleResolver and stricter on whitespace.
3. WR-04: TenantRunCommand carries a `@security` docblock at the Process call site documenting the trust boundary.
4. IN-01..IN-05: Canary test is cleaned up — no stale `@group canary-red`, no duplicate dir removal, PID in cache-dir hash, no `setCatchExceptions(false)`, explicit `use TenantStamp` in middleware.
5. Full PHPUnit + PHPStan + php-cs-fixer green.
</success_criteria>

<output>
Create `.planning/phases/23-tech-debt-closure/23-03-SUMMARY.md` when done. Include:
- Frontmatter: `phase: 23-tech-debt-closure`, `plan: 03`, `requirements: [DX-06, WR-02, WR-03, WR-04, IN-01..IN-05]`, `subsystem: resolver+command+messenger+canary-test`.
- Body: brief change list per advisory finding; before/after one-liners; line counts.
</output>
