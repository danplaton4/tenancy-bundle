---
phase: 23-tech-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Resources/views/Collector/tenant.html.twig
  - tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php
autonomous: true
requirements:
  - DX-02
  - BOOT-04
  - INT-01
must_haves:
  truths:
    - "Mailer subsection in tenant.html.twig renders on null state when LruTransportCache is wired"
    - "Mailer subsection in tenant.html.twig renders on error state when LruTransportCache is wired"
    - "Mailer subsection in tenant.html.twig renders on resolved state when LruTransportCache is wired (no regression)"
    - "TenantDataCollectorMailerSectionTest asserts rendered HTML, not only data array, for the null+cache-wired case"
  artifacts:
    - path: "src/Resources/views/Collector/tenant.html.twig"
      provides: "Profiler panel template with mailer block hoisted out of the resolved-only branch"
      contains: "{% if collector.data.mailer is defined %}"
    - path: "tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php"
      provides: "Mailer subsection rendered-HTML contract test"
      contains: "Environment"
  key_links:
    - from: "src/Resources/views/Collector/tenant.html.twig"
      to: "collector.data.mailer"
      via: "top-level Twig guard outside resolved/error/null branching"
      pattern: "{% if collector.data.mailer is defined %}"
    - from: "tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php"
      to: "@Tenancy/Collector/tenant.html.twig"
      via: "Twig\\Environment::render"
      pattern: "render\\(.*tenant\\.html\\.twig.*\\)"
---

<objective>
Close INT-01 (Twig contract drift between Phase 19 Profiler panel and Phase 20 mailer subsection). Move the mailer block out of the `state == 'resolved'` Twig branch so cache metrics render on null/error states whenever `LruTransportCache` is wired. Strengthen the unit test from a data-shape assertion to a rendered-HTML assertion.

Purpose: `TenantDataCollector::collectMailerState()` already populates the 10-key `mailer` scalar array on every request when the LruTransportCache dep is present, including null/error states. The current Twig nesting hides cache hit/eviction counters on landlord/health-check routes where operators most need them. This is a cosmetic but operator-impactful drift between data and presentation.

Output: Twig template + unit test that prove the data/presentation contract end-to-end.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.3-MILESTONE-AUDIT.md
@.planning/phases/23-tech-debt-closure/23-CONTEXT.md

<interfaces>
<!-- Key shape of $collector->data from TenantDataCollector. Confirmed via reads of:
       src/Profiler/TenantDataCollector.php (state assignment L63-67, $this->data L82-)
       src/Resources/views/Collector/tenant.html.twig (current rendering L1-165)
       tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php (10-key shape) -->

collector.data keys (always present):
  - state: 'resolved' | 'error' | 'null'
  - driver: string|null
  - slug, tenant_label, connection_name, resolved_by, bootstrappers (resolved only)
  - error: array{class,message} (error only)

collector.data.mailer (10-key sub-array — present ONLY when LruTransportCache dep wired):
  - from: ?string
  - reply_to: ?string
  - dsn_redacted: ?string
  - cache_size: int
  - cache_max: int
  - cache_hits: int
  - cache_evictions: int
  - strategy: string ('x_transport')
  - async_detected: ?string ('auto'|'true'|'false'|null)
  - badge: 'OK' | 'MISSING' | 'ERROR'

Existing Twig block currently lives in tenant.html.twig L104-149 inside `{% if collector.data.state == 'resolved' %}` (L70 opens, L163 closes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoist mailer subsection out of resolved-only branch</name>
  <files>src/Resources/views/Collector/tenant.html.twig</files>
  <read_first>
    - src/Resources/views/Collector/tenant.html.twig (full file — confirm current L70 opens resolved, L104-149 hosts mailer block, L151 elseif error, L158 else null, L163 endif, L164 endblock)
    - src/Profiler/TenantDataCollector.php (confirm `state` values 'resolved'|'error'|'null' and that `mailer` key is populated regardless of state when LruTransportCache injected)
  </read_first>
  <action>
    Restructure the `{% block panel %}` section of `src/Resources/views/Collector/tenant.html.twig` so the mailer subsection renders unconditionally whenever `collector.data.mailer is defined` — independent of `collector.data.state`. Plan: keep the existing resolved/error/null three-way branching intact for the tenant-identity rendering (metrics div, Resolved by, Bootstrappers, error panel, empty panel). Move the entire `{% if collector.data.mailer is defined %} … {% endif %}` block (currently L104-149) OUT of the resolved branch and place it AFTER the closing `{% endif %}` of the state branching, but BEFORE `{% endblock %}`. Preserve the existing leading docblock comment (L104-107 `{# Phase 20 (D-08): Mailer subsection … #}`) and update its text to clarify the new placement: it now renders on every state when the LruTransportCache dep is wired. Do NOT touch the `{% block toolbar %}` or `{% block menu %}` sections. Do NOT change any CSS classes, badge logic, metric values, or table markup — only relocate the block. Per D-01 (CONTEXT.md INT-01): rejected the collector-side guard alternative.
  </action>
  <acceptance_criteria>
    - `grep -c "{% if collector.data.mailer is defined %}" src/Resources/views/Collector/tenant.html.twig` returns exactly `1` (no accidental duplication during the move).
    - The line containing `{% if collector.data.mailer is defined %}` appears AFTER the line containing `{% else %}` that opens the null branch AND AFTER its closing `{% endif %}`. Verify via: `awk '/\{% if collector.data.mailer/{m=NR} /\{% endif %\}/{e=NR} END{print m, e}' src/Resources/views/Collector/tenant.html.twig` — mailer-if line number must be greater than the line of the resolved/error/null closing `{% endif %}`.
    - The `{% block panel %}` opens once (`grep -c '{% block panel %}'` = 1) and the closing `{% endblock %}` count for the panel block is unchanged.
    - The relocated comment block (lines starting `{# Phase 20 (D-08):`) now states explicitly that the subsection renders on all three states.
    - No other file is modified.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "{% if collector.data.mailer is defined %}" src/Resources/views/Collector/tenant.html.twig | grep -q '^1$' &amp;&amp; vendor/bin/phpunit tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php --no-coverage</automated>
  </verify>
  <done>Twig file has the mailer guard at top-level of `{% block panel %}` (outside state branching); existing 7 mailer-section unit tests still pass green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rendered-HTML assertion to TenantDataCollectorMailerSectionTest</name>
  <files>tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php</files>
  <read_first>
    - tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php (the existing 7-test file — confirm setUp(), makeCollector(), and `testMailerKeyPresentWhenNoTenantButCacheWired` shape)
    - src/Resources/views/Collector/tenant.html.twig (post-Task-1 state — confirm template path `@Tenancy/Collector/tenant.html.twig` and that the mailer block is now state-independent)
  </read_first>
  <behavior>
    - Test A (new): when no tenant resolved BUT cache wired → render `@Tenancy/Collector/tenant.html.twig` and assert the rendered HTML contains a `<h3>Mailer</h3>` heading AND the literal `Transport cache` label AND `x_transport` strategy string.
    - Test B (new): when error state BUT cache wired → render the template and assert the same three substrings appear.
    - Test C (new): when resolved state with cache wired → render the template and assert the mailer block still renders (regression guard for Task 1).
    - The existing 7 data-shape tests must keep passing unchanged.
  </behavior>
  <action>
    Add THREE new test methods to `tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php`:

    1. `testMailerBlockRendersWhenNoTenantButCacheWired` — null state + cache wired
    2. `testMailerBlockRendersOnErrorStateWithCacheWired` — error state + cache wired
    3. `testMailerBlockRendersOnResolvedStateWithCacheWired` — resolved state + cache wired (regression guard)

    Each method must:
    - Build a `TenantDataCollector` via the existing `makeCollector()` helper with an `LruTransportCache` instance, then call `$collector->collect()` via the existing `collect()` helper.
    - For the error-state test, force `state == 'error'` by populating `$this->stash` with an error before calling collect (read `TenantDataCollector::collect()` to find the exact stash key/method, e.g. `$this->stash->stashError(...)`). If the stash API does not expose a setter, call `collect()` then use Reflection to mutate `$collector->data['state'] = 'error'` AFTER collect (allowed: the test goal is to exercise the Twig path, not the collector's state machine — which is already covered by Phase 19 tests). Document the chosen approach in a code comment.
    - Construct a `Twig\Environment` whose `Twig\Loader\FilesystemLoader` registers `__DIR__.'/../../../src/Resources/views'` under the namespace `Tenancy`. Render `@Tenancy/Collector/tenant.html.twig` with `['collector' => $collector]`. Because the WebProfiler base template (`@WebProfiler/Profiler/layout.html.twig`) is not present in this minimal environment, use a stub loader that registers a no-op `@WebProfiler/Profiler/layout.html.twig` defining the `toolbar`, `menu`, and `panel` blocks (and `@WebProfiler/Profiler/toolbar_item.html.twig` as an empty template). The goal is to render `{% block panel %}` for assertion.
    - Assert the rendered HTML contains `<h3>Mailer</h3>` AND `Transport cache` AND `x_transport` via `self::assertStringContainsString(...)`.
    - On the resolved-state test, additionally assert the rendered HTML contains the resolved-state identity markup (e.g. the tenant slug from the stub), proving the existing branching still renders.

    Add a `markTestSkipped` guard at the top of each new test if `class_exists(\Twig\Environment::class)` is false — `symfony/twig-bundle` is in require-dev, so the class is always present in CI but the guard documents the dep.

    Do NOT modify the existing 7 tests. Do NOT touch any other file.

    Constructor signature note (per CONTEXT.md D-01): the new tests prove the data-Twig contract end-to-end, not just the data array. The existing `testMailerKeyPresentWhenNoTenantButCacheWired` stays — it is now the finer-grained data-only check; the new tests are the coarser Twig-rendering check.
  </action>
  <acceptance_criteria>
    - `vendor/bin/phpunit tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php --no-coverage` exits 0.
    - Test count for this file increases by exactly 3 (from 7 to 10), confirmable via `vendor/bin/phpunit tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php --list-tests | grep -c '^ - '` against pre/post snapshots, or simply via the PHPUnit `Tests:` summary line.
    - At least one of the three new tests asserts `<h3>Mailer</h3>` literal in the rendered output.
    - Empirical proof of Task 1 fix: if a developer reverts the Twig change (mailer block back inside resolved branch), `testMailerBlockRendersWhenNoTenantButCacheWired` and `testMailerBlockRendersOnErrorStateWithCacheWired` MUST fail. (Verify by mentally running the test logic against pre-Task-1 template.)
    - PHPStan level 9 clean on the test file: `vendor/bin/phpstan analyse tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php --memory-limit=512M` returns `[OK] No errors`.
  </acceptance_criteria>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php --no-coverage &amp;&amp; vendor/bin/phpstan analyse tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php --memory-limit=512M</automated>
  </verify>
  <done>Test file has 10 tests (7 existing + 3 new); all green; PHPStan clean; rendered-HTML assertions cover null/error/resolved states with mailer cache wired.</done>
</task>

</tasks>

<verification>
- `vendor/bin/phpunit tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php --no-coverage` — 10/10 green.
- `vendor/bin/phpunit --no-coverage` — full suite still passes (no regressions in adjacent profiler tests).
- `vendor/bin/phpstan analyse --memory-limit=512M` — `[OK] No errors`.
- `vendor/bin/php-cs-fixer check src/Resources/views/Collector/tenant.html.twig tests/Unit/Profiler/TenantDataCollectorMailerSectionTest.php` — clean (php-cs-fixer ignores Twig but verifies the PHP test file).
- `grep -c "{% if collector.data.mailer is defined %}" src/Resources/views/Collector/tenant.html.twig` returns `1`.
</verification>

<success_criteria>
1. Mailer subsection block lives at the top level of `{% block panel %}` in `tenant.html.twig`, outside the resolved/error/null state branching.
2. `TenantDataCollectorMailerSectionTest` has at least 3 new rendered-HTML assertion tests covering null, error, and resolved states when LruTransportCache is wired.
3. Full PHPUnit suite green; PHPStan level 9 clean; php-cs-fixer clean.
4. The unit-test comment about "STILL renders with cache metrics + strategy" is no longer a lie — the Twig template now actually renders the block on null state.
</success_criteria>

<output>
Create `.planning/phases/23-tech-debt-closure/23-01-SUMMARY.md` when done. Include:
- Frontmatter: `phase: 23-tech-debt-closure`, `plan: 01`, `requirements: [DX-02, BOOT-04, INT-01]`, `subsystem: profiler+twig`, dependency_graph (provides: "mailer subsection visible on all three Profiler states"), `key_files.modified`.
- Body: what was hoisted, how the test was strengthened, before/after line numbers in `tenant.html.twig`.
</output>
