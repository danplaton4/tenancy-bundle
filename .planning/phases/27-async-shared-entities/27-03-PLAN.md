---
phase: 27-async-shared-entities
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - tests/Integration/SharedEntity/Support/SharedEntityAsyncTestKernel.php
  - tests/Integration/SharedEntity/Support/MakeSharedEntityAsyncServicesPublicPass.php
  - tests/Integration/SharedEntity/SharedEntityAsyncCanaryTest.php
autonomous: true
requirements: [SHARE-03]
must_haves:
  truths:
    - "Dispatching SharedEntityChangedMessage on the bus (sync:// transport) reaches the handler and upserts the entity into EVERY tenant DB"
    - "The handler never writes one tenant's data into another tenant's DB (wrong-tenant isolation holds)"
    - "Dispatching with an ACTIVE tenant on TenantContext still fans out to ALL tenants (stamp-clearing proven end-to-end — D-01)"
    - "A vanished landlord row (insert dispatched, master deleted before handle) results in the tenant copies being deleted (D-04 end-to-end)"
    - "Any tenant failure causes the handler to throw SharedEntityAsyncFanOutException (Messenger retry contract observable)"
    - "Re-handling the same message is idempotent (copier find-or-new) — retry leaves tenants in the same correct state"
  artifacts:
    - path: "tests/Integration/SharedEntity/SharedEntityAsyncCanaryTest.php"
      provides: "SHARE-03 round-trip acceptance test (handler-reach + per-tenant DB state, NOT serialization)"
      contains: "messenger.bus.default"
    - path: "tests/Integration/SharedEntity/Support/SharedEntityAsyncTestKernel.php"
      provides: "Kernel with sync:// transport, SharedEntityChangedMessage routing, tenancy.shared.async: true"
      contains: "shared"
    - path: "tests/Integration/SharedEntity/Support/MakeSharedEntityAsyncServicesPublicPass.php"
      provides: "Exposes the handler + bus + EMs for test inspection"
      contains: "tenancy.shared_entity_changed_handler"
  key_links:
    - from: "tests/Integration/SharedEntity/SharedEntityAsyncCanaryTest.php"
      to: "tenancy.shared_entity_changed_handler"
      via: "bus->dispatch routed through sync:// transport to the handler"
      pattern: "bus->dispatch\\(new SharedEntityChangedMessage"
    - from: "tests/Integration/SharedEntity/Support/SharedEntityAsyncTestKernel.php"
      to: "framework.messenger.routing"
      via: "SharedEntityChangedMessage::class => 'sync'"
      pattern: "SharedEntityChangedMessage::class"
---

<objective>
Ship the SHARE-03 acceptance proof: a Messenger transport round-trip canary that boots a real kernel with a `sync://` transport, dispatches `SharedEntityChangedMessage` on the bus, and asserts the handler reached EVERY tenant DB with the correct change and never touched the wrong tenant (RESEARCH Pattern 4 — assert handler-reach + per-tenant DB state, NOT PhpSerializer survival). Also prove the D-04 vanished-row→delete convergence, the D-02 throw-to-retry contract, and the D-01 stamp-clearing isolation invariant (active-tenant-at-dispatch still fans out to ALL tenants) end-to-end. Close the phase with a full-suite + PHPStan L9 + cs-fixer green gate.

Purpose: The acceptance criterion "integration test proves async mode survives Messenger transport round-trip (matches Phase 20 AsyncCanaryTest pattern)" is satisfied here, plus the D-01/D-04/D-05 landmine behaviors are pinned at integration level.
Output: New async test kernel, make-public pass, and the canary test covering SHARE-03-d/-e/-f/-g/-h/-j and the D-01 stamp-clearing integration proof.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-async-shared-entities/27-CONTEXT.md
@.planning/phases/27-async-shared-entities/27-RESEARCH.md
@.planning/phases/27-async-shared-entities/27-PATTERNS.md
@.planning/phases/27-async-shared-entities/27-01-SUMMARY.md
@.planning/phases/27-async-shared-entities/27-02-SUMMARY.md

<interfaces>
<!-- Shipped by 27-01/27-02 — the test dispatches against these. -->

src/Message/SharedEntityChangedMessage.php:
  new SharedEntityChangedMessage(string $entityClass, array $identifier, string $changeType)

src/MessageHandler/SharedEntityChangedMessageHandler.php (service id: tenancy.shared_entity_changed_handler):
  __invoke(SharedEntityChangedMessage): void  // re-fetch + all-tenant fan-out + throw-to-retry

src/Exception/SharedEntityAsyncFanOutException.php:
  final class SharedEntityAsyncFanOutException extends \RuntimeException

config: tenancy.shared.async (boolean) -> set tenancy.shared.async parameter; wires the bus into the subscriber (named `$bus` arg) + registers the handler when Messenger present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SharedEntityAsyncTestKernel + MakeSharedEntityAsyncServicesPublicPass</name>
  <files>tests/Integration/SharedEntity/Support/SharedEntityAsyncTestKernel.php, tests/Integration/SharedEntity/Support/MakeSharedEntityAsyncServicesPublicPass.php</files>
  <read_first>
    - tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php (FULL FILE — the base kernel to extend: FrameworkBundle+DoctrineBundle+TenancyBundle, landlord/tenant SQLite connections, landlord+tenant entity mappings, getCacheDir() md5(static::class) isolation; tenancy config block lines 71-73 to extend with shared.async)
    - tests/Integration/Mailer/MailerTestKernel.php lines 101-114 (the framework.messenger config block to adapt: sync:// transport + routing — replace SendEmailMessage::class with SharedEntityChangedMessage::class; default_bus + allow_no_handlers middleware)
    - tests/Integration/SharedEntity/Support/MakeSharedEntityServicesPublicPass.php (FULL FILE — exact structural template; copy + add the async service ids)
    - tests/Integration/SharedEntity/Support/StubMultiTenantProvider.php + ReplaceWithStubMultiTenantProviderPass.php (the multi-tenant provider the canary needs for >1 tenant fan-out)
    - tests/Integration/SharedEntity/Support/Entity/ (the #[Shared] test entity classes — reuse for fixtures, e.g. TestPlan)
    - 27-PATTERNS.md "SharedEntityAsyncTestKernel" (lines 450-487) + "MakeSharedEntityAsyncServicesPublicPass" (lines 490-521): key differences + service id list
  </read_first>
  <action>
    Create `tests/Integration/SharedEntity/Support/SharedEntityAsyncTestKernel.php` (namespace `Tenancy\Bundle\Tests\Integration\SharedEntity\Support`) — structurally a copy of `SharedEntitySyncTestKernel` with these deltas (27-PATTERNS.md lines 456-460): (1) in the `framework` config add a `messenger` block adapted from `MailerTestKernel` lines 101-114 — `default_bus: messenger.bus.default`, a `messenger.bus.default` bus with `allow_no_handlers` default middleware, a `sync` transport `'sync' => 'sync://'`, and `routing: [SharedEntityChangedMessage::class => 'sync']`; (2) extend the `tenancy` config block to `['database' => ['enabled' => true], 'shared' => ['async' => true]]`; (3) in `build()` add `$container->addCompilerPass(new MakeSharedEntityAsyncServicesPublicPass());` (alongside `ReplaceWithStubMultiTenantProviderPass` so >1 tenant is available); (4) use distinct SQLite DB file paths AND a distinct cache dir (the `md5(static::class)` getCacheDir() pattern already differentiates by class — keep it; use unique DB filenames like `tenancy_shared_async_test_*.db` to avoid colliding with the sync kernel's files). Import `Tenancy\Bundle\Message\SharedEntityChangedMessage`.
    Create `tests/Integration/SharedEntity/Support/MakeSharedEntityAsyncServicesPublicPass.php` — copy `MakeSharedEntityServicesPublicPass` structure exactly, with the `$ids` array extended to include `'tenancy.shared_entity_changed_handler'` and `'messenger.bus.default'` in addition to the existing shared-entity + EM ids (`tenancy.shared_entity_sync_subscriber`, `tenancy.shared_entity_copier`, `tenancy.context`, `doctrine.orm.landlord_entity_manager`, `doctrine`, `doctrine.dbal.tenant_connection`). Keep the `hasDefinition`/`hasAlias` tolerance guard so a missing service does not break compilation.
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse tests/Integration/SharedEntity/Support/SharedEntityAsyncTestKernel.php tests/Integration/SharedEntity/Support/MakeSharedEntityAsyncServicesPublicPass.php --level=9 --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - SharedEntityAsyncTestKernel registers a `framework.messenger` block with `transports: ['sync' => 'sync://']` and `routing: [SharedEntityChangedMessage::class => 'sync']`
    - SharedEntityAsyncTestKernel's tenancy config contains `'shared' => ['async' => true]`
    - SharedEntityAsyncTestKernel build() adds both MakeSharedEntityAsyncServicesPublicPass and ReplaceWithStubMultiTenantProviderPass
    - SharedEntityAsyncTestKernel uses DB filenames distinct from SharedEntitySyncTestKernel (no `tenancy_shared_test_*.db` collision)
    - MakeSharedEntityAsyncServicesPublicPass `$ids` includes `tenancy.shared_entity_changed_handler` AND `messenger.bus.default`
    - `vendor/bin/phpstan analyse tests/Integration/SharedEntity/Support --level=9 --no-progress` exits 0
  </acceptance_criteria>
  <done>An async test kernel boots TenancyBundle with sync:// transport + SharedEntityChangedMessage routing + shared.async:true and exposes the handler/bus/EMs for inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Write SharedEntityAsyncCanaryTest (round-trip + all-tenant fan-out + wrong-tenant isolation w/ active-dispatch-tenant + vanished-row→delete + throw-to-retry + idempotency)</name>
  <files>tests/Integration/SharedEntity/SharedEntityAsyncCanaryTest.php</files>
  <read_first>
    - tests/Integration/Mailer/AsyncCanaryTest.php (FULL FILE — class lifecycle lines 71-105 setUpBeforeClass/tearDownAfterClass + markTestSkipped(MessageBusInterface) guard; setUp reset pattern lines 106-132; bus dispatch pattern; cache-dir cleanup. CRITICAL PRECEDENT: lines 231-330 testAsyncDispatchInWorkerUsesTenantDsnNotLandlord sets an ACTIVE tenant on TenantContext (context->setTenant) BEFORE bus->dispatch — this is exactly the active-dispatch-tenant pattern Task 2's wrong-tenant isolation test below must follow to prove stamp-clearing at integration level). NOTE: do NOT copy its PhpSerializer-survival framing — RESEARCH Pattern 4 says SyncTransport does NOT serialize; assert handler-reach + DB state instead.
    - tests/Integration/SharedEntity/* (the existing sync integration test — schema creation after chain->boot(), per-tenant DB assertions, SQLite :memory:/file fixture setup, TenantContext reset, the #[Shared] test entity persistence flow on the landlord EM, how each tenant's DB is queried directly)
    - 27-RESEARCH.md Pattern 4 (lines 269-296) — the SyncTransport re-dispatch flow + the 6 things the canary MUST assert (lines 273-281); and Validation Architecture Test Map SHARE-03-d/-e/-f/-g/-h/-j (lines 740-746)
    - 27-PATTERNS.md "SharedEntityAsyncCanaryTest" (lines 397-447) — class lifecycle, bus dispatch, canary assertions list, and the explicit "NOT the Mailer PhpSerializer pattern" note
    - 27-CONTEXT.md D-01 / D-02 / D-04 / D-05 (the exact convergence + stamp-clearing isolation behaviors to assert)
    - tests/Integration/SharedEntity/Support/StubMultiTenantProvider.php (to know how many tenants + their slugs/DBs the canary fans to)
  </read_first>
  <action>
    Create `tests/Integration/SharedEntity/SharedEntityAsyncCanaryTest.php` (namespace `Tenancy\Bundle\Tests\Integration\SharedEntity`, extends `PHPUnit\Framework\TestCase`). Lifecycle mirrors `AsyncCanaryTest`: `setUpBeforeClass()` calls `markTestSkipped` when `!interface_exists(MessageBusInterface::class)`, clears stale cache/DB files, boots `SharedEntityAsyncTestKernel`; `tearDownAfterClass()` shuts down + unlinks the test DB files; `setUp()` resets `TenantContext`, resets the tenant manager, and (re)creates the landlord + per-tenant schemas (reuse the existing sync integration test's schema-after-boot setup so DatabaseSwitchBootstrapper does not destroy a :memory: DB — use the file-based DBs the kernel defines). Tests (each retrieving services via the public-ified container per RESEARCH Pattern 4 / 27-PATTERNS lines 438-446):
      - testAsyncRoundTripCanary (SHARE-03-j + SHARE-03-d): persist a `#[Shared]` entity on the landlord EM (subscriber dispatches via async branch through sync:// → handler runs inline); assert the row exists in EVERY tenant DB with the correct scalar field values. This is the round-trip proof — handler reached via the transport, latest state applied.
      - testHandlerFansOutToAllTenants (SHARE-03-f): with ≥2 tenants from StubMultiTenantProvider, assert ALL tenant DBs received the row (not just one).
      - testWrongTenantIsolationWithActiveDispatchTenant (BLOCKER #3 resolution — D-01 stamp-clearing integration proof): BEFORE the flush/dispatch, set ONE specific tenant active on `TenantContext` via `$context->setTenant($tenantA)` (mirroring AsyncCanaryTest lines 261-273 which sets the dispatch-time tenant). Then persist/flush a `#[Shared]` entity on the landlord EM so the subscriber's async branch dispatches with a tenant active at dispatch time. ASSERT: (1) the change reached ALL tenant DBs (the would-be-stamped tenantA AND every OTHER tenant) — proving the subscriber cleared the TenantContext before dispatch so `TenantSendingMiddleware` did NOT stamp a single `TenantStamp` that would have limited the handler to tenantA only; (2) no tenant DB contains another tenant's marker/data (the negative leak assertion). If only tenantA had received the row, the stamp-clearing regressed — this is THE integration-level canary for the D-01 invariant. Add a method docblock citing AsyncCanaryTest::testAsyncDispatchInWorkerUsesTenantDsnNotLandlord as the established active-dispatch-tenant precedent and 27-CONTEXT.md D-01.
      - testVanishedRowPropagatesToTenantDelete (SHARE-03-e / D-04): seed the tenant copies (apply an insert), then dispatch a `SharedEntityChangedMessage(..., 'insert')` AFTER deleting the landlord row (so the handler's re-fetch returns null); assert the handler deleted the tenant copies.
      - testHandlerThrowsOnTenantFailure (SHARE-03-g / D-02): induce a tenant failure with a CONCRETE, idempotent mechanism (WARNING fix) — pick ONE tenant from StubMultiTenantProvider, switch to its EM (via switchToTenant or the public tenant connection), and `DROP TABLE` the shared entity's table on that tenant's DB only, wrapped in a `try { $conn->executeStatement('DROP TABLE IF EXISTS <shared_table>'); } catch (\Throwable) {}` for idempotent setup (so a re-run that already dropped it does not fail setup). Restore tenant context, then dispatch the message directly on `messenger.bus.default`. Assert a `SharedEntityAsyncFanOutException` is thrown (or its `Symfony\Component\Messenger\Exception\HandlerFailedException` wrapper from the bus — unwrap and assert the inner exception is `SharedEntityAsyncFanOutException`). ALSO assert a HEALTHY tenant (one whose table was NOT dropped) still received the change before the throw (best-effort attempt-all, D-02). In a finally / tearDown, recreate the dropped table so later tests are unaffected (or rely on setUp() schema recreation — confirm setUp recreates all tenant schemas).
      - testHandlerIdempotentOnRetry (SHARE-03-h): dispatch the same message twice; assert exactly one row per tenant (find-or-new idempotency), no duplicate/PK conflict.
    Add a class-level PHPDoc that explicitly states (per RESEARCH Pattern 4 / Pitfall 2) the canary asserts handler-reach + per-tenant DB state, NOT PhpSerializer survival, because `SyncTransport::send()` re-dispatches the envelope on the bus without serialization — distinguishing it from the Phase 20 mailer canary.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite integration --filter SharedEntityAsyncCanaryTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --testsuite integration --filter SharedEntityAsyncCanaryTest` exits 0
    - The test dispatches via `messenger.bus.default` (NOT by calling the handler directly) so the sync:// transport + middleware chain is exercised (round-trip proof)
    - testHandlerFansOutToAllTenants asserts the row exists in ≥2 distinct tenant DBs
    - STAMP-CLEARING INTEGRATION PROOF (BLOCKER #3): testWrongTenantIsolationWithActiveDispatchTenant sets a specific tenant ACTIVE on TenantContext BEFORE the flush/dispatch, then asserts ALL tenants (the active one AND every other) received the change AND no cross-tenant data is present — proving the subscriber cleared the context so no single TenantStamp limited the handler to one tenant (D-01)
    - testVanishedRowPropagatesToTenantDelete asserts tenant copies are DELETED when the landlord row is gone at handle time (D-04)
    - testHandlerThrowsOnTenantFailure induces the failure by DROP TABLE on exactly ONE tenant's shared-entity table (idempotent try/catch setup), asserts a SharedEntityAsyncFanOutException (or the bus HandlerFailedException wrapping it) is thrown, AND a healthy tenant still got the change before the throw (best-effort, D-02)
    - testHandlerIdempotentOnRetry asserts re-dispatch yields exactly one row per tenant (SHARE-03-h)
    - The class PHPDoc states the canary asserts handler-reach + DB state, not PhpSerializer survival (RESEARCH Pattern 4)
  </acceptance_criteria>
  <done>The SHARE-03 round-trip canary is green: async dispatch reaches the handler via the transport and converges every tenant to latest state; wrong-tenant isolation WITH an active dispatch-time tenant (D-01 stamp-clearing), vanished-row→delete (D-04), throw-to-retry via a concrete DROP TABLE failure (D-02), and idempotency are all proven.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite + PHPStan L9 + cs-fixer + SHARE-03 traceability green</name>
  <files>tests/Integration/SharedEntity/SharedEntityAsyncCanaryTest.php</files>
  <read_first>
    - CLAUDE.md "Commands" section (the canonical full-suite / PHPStan / cs-fixer invocations)
    - 27-RESEARCH.md Validation Architecture -> Sampling Rate "Phase gate" (line 752): full suite green + PHPStan L9 + cs-fixer before verify-work
    - 27-RESEARCH.md Phase Requirements -> Test Map (lines 735-746): confirm every SHARE-03-* bullet has a passing automated command across Plans 27-01/27-02/27-03
    - MEMORY note "Worktree test-cache staleness": if MappingException/project_dir failures appear, purge `$(php -r 'echo sys_get_temp_dir();')/tenancy_*` and re-run — these are stale-cache artifacts, not code bugs
  </read_first>
  <action>
    Run the full quality gate in the main checkout: `vendor/bin/phpunit` (full suite — unit + integration), `vendor/bin/phpstan analyse` (level 9), `vendor/bin/php-cs-fixer check --diff` (@Symfony ruleset). If stale-cache MappingException/project_dir failures appear, purge `$(php -r 'echo sys_get_temp_dir();')/tenancy_*` and re-run (per MEMORY note — not code bugs). Cross-check the RESEARCH Test Map (SHARE-03-a..-j) so every acceptance bullet maps to a passing test: -a/-b (subscriber async test, Plan 27-02), -c/-i (message + guard unit tests, Plan 27-01), -d/-e/-f/-g/-h/-j (canary, this plan). This task is a verification gate — fix only test/wiring breakage surfaced by the gate; do not add new behavior. If any SHARE-03-* bullet lacks a passing test, add the missing assertion to SharedEntityAsyncCanaryTest (the only file this task may modify).
  </action>
  <verify>
    <automated>vendor/bin/phpunit && vendor/bin/phpstan analyse --level=9 --no-progress && vendor/bin/php-cs-fixer check --diff</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit` (full suite) exits 0
    - `vendor/bin/phpstan analyse --level=9 --no-progress` exits 0
    - `vendor/bin/php-cs-fixer check --diff` reports no fixes needed
    - Every SHARE-03 acceptance bullet has a corresponding passing test: opt-in default-sync (-a/-b), scalar message (-c), latest-state re-fetch (-d), vanished-row→delete (-e), all-tenant fan-out (-f), throw-to-retry (-g), idempotency (-h), compile-time guard (-i), transport round-trip canary (-j)
  </acceptance_criteria>
  <done>Full suite + PHPStan L9 + cs-fixer all green; every SHARE-03 acceptance bullet is covered by a passing test. Phase 27 is ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bus dispatch → sync:// transport → handler | The canary must exercise the real transport+middleware chain, not call the handler directly, to prove the round-trip |
| dispatch-time tenant context → fan-to-all message | A tenant active at dispatch must NOT cause a single-tenant TenantStamp; the subscriber clears context first (D-01) |
| per-tenant DB query in assertions | Each tenant DB is queried independently to confirm correct + isolated state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-LEAK | Information Disclosure | handler fan-out under test | mitigate | `testWrongTenantIsolationWithActiveDispatchTenant` is the negative leak canary — it sets a tenant ACTIVE on TenantContext before dispatch and asserts (a) ALL tenants still received the change (stamp-clearing held — no single-tenant TenantStamp shortcut limited the loop) and (b) no tenant DB holds another tenant's data; `testHandlerFansOutToAllTenants` proves the loop reached every tenant. This is the integration-level proof of T-27-02-LEAK / T-27-02-STAMP (the D-01 invariant) |
| T-27-03-DRIFT | Tampering | vanished-row convergence under test | mitigate | `testVanishedRowPropagatesToTenantDelete` proves D-04 prevents permanent orphan drift (a deleted master deletes the mirror), the only repair path given Phase 26's deferred orphan-batch deletion |
| T-27-03-RETRY | Denial of Service | partial fan-out failure under test | mitigate | `testHandlerThrowsOnTenantFailure` induces a CONCRETE single-tenant failure (DROP TABLE on one tenant) and asserts the aggregate exception is thrown (retry contract observable) while a healthy tenant still converged (best-effort, D-02) — proving one tenant's breakage neither aborts the others nor silently swallows the failure |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `vendor/bin/phpunit` full suite exits 0 (canary + all prior-phase tests)
- `vendor/bin/phpstan analyse --level=9 --no-progress` exits 0
- `vendor/bin/php-cs-fixer check --diff` reports no fixes needed
- The canary dispatches on the bus (round-trip), not directly on the handler
- The wrong-tenant isolation test dispatches with an ACTIVE tenant and asserts all-tenant fan-out (D-01 stamp-clearing)
- All SHARE-03-a..-j acceptance bullets map to passing tests
</verification>

<success_criteria>
- SHARE-03 acceptance "integration test proves async mode survives Messenger transport round-trip (matches Phase 20 AsyncCanaryTest pattern)" satisfied by `SharedEntityAsyncCanaryTest` (RESEARCH Pattern 4: handler-reach + DB state)
- All-tenant fan-out + wrong-tenant isolation WITH an active dispatch-time tenant proven (D-01 stamp-clearing cross-tenant leak threat closed at integration level)
- D-04 vanished-row→delete and D-02 throw-to-retry (concrete DROP TABLE failure) + idempotency proven end-to-end
- Full suite + PHPStan L9 + cs-fixer green — phase ready for verification
</success_criteria>

<output>
Create `.planning/phases/27-async-shared-entities/27-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
