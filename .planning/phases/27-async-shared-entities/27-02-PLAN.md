---
phase: 27-async-shared-entities
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/Shared/SharedEntityCopierInterface.php
  - src/Shared/SharedEntityCopier.php
  - src/MessageHandler/SharedEntityChangedMessageHandler.php
  - src/Subscriber/SharedEntitySyncSubscriber.php
  - src/TenancyBundle.php
  - tests/Unit/Subscriber/SharedEntitySyncSubscriberAsyncTest.php
autonomous: true
requirements: [SHARE-03]
must_haves:
  truths:
    - "When tenancy.shared.async=true, SharedEntitySyncSubscriber::postFlush() dispatches one SharedEntityChangedMessage per changed entity instead of fanning out synchronously"
    - "When tenancy.shared.async=false (default), postFlush() runs the existing Phase 25 sync fan-out unchanged and dispatches nothing"
    - "The subscriber clears tenant context before dispatching so TenantSendingMiddleware does not stamp the fan-to-all-tenants message with a single TenantStamp"
    - "The handler re-fetches the LATEST landlord state at handle time; insert/update collapse into an upsert, delete is a distinct branch (D-05)"
    - "A vanished landlord row at handle time (insert/update whose master row is gone) propagates a tenant-side delete (D-04)"
    - "The handler attempts every tenant best-effort, logs each failure with tenant_slug/entity_class/identifier/error, then throws SharedEntityAsyncFanOutException if ANY tenant failed (D-02)"
    - "The handler can delete a tenant copy by id without passing a non-null entity object to applyRow() (OQ-1 resolution)"
  artifacts:
    - path: "src/MessageHandler/SharedEntityChangedMessageHandler.php"
      provides: "Per-tenant async fan-out handler (__invoke), re-fetch + upsert/delete + best-effort + throw-to-retry"
      contains: "public function __invoke(SharedEntityChangedMessage"
    - path: "src/Shared/SharedEntityCopier.php"
      provides: "deleteRow(tenantEm, class, capturedIds) helper resolving the non-nullable applyRow() entity-param problem"
      contains: "public function deleteRow"
  key_links:
    - from: "src/Subscriber/SharedEntitySyncSubscriber.php"
      to: "MessageBusInterface::dispatch(SharedEntityChangedMessage)"
      via: "postFlush() async branch when bus is non-null"
      pattern: "bus->dispatch\\(new SharedEntityChangedMessage"
    - from: "src/MessageHandler/SharedEntityChangedMessageHandler.php"
      to: "tenant EMs via SharedEntityCopier"
      via: "per-tenant switchToTenant() loop calling copier applyRow()/deleteRow()"
      pattern: "copier->(applyRow|deleteRow)"
    - from: "src/TenancyBundle.php"
      to: "tenancy.shared_entity_changed_handler"
      via: "messenger.message_handler tag inside interface_exists(MessageBusInterface) block"
      pattern: "messenger\\.message_handler"
---

<objective>
Implement the async fan-out: branch `SharedEntitySyncSubscriber::postFlush()` to dispatch `SharedEntityChangedMessage` when the bus is wired (D-07), build `SharedEntityChangedMessageHandler` that re-fetches latest landlord state and fans out to all tenants reusing the proven Phase 25 `switchToTenant()` mechanics (D-01), resolve the `applyRow()` non-nullable-entity delete-path problem (OQ-1) by adding a `deleteRow()` helper to `SharedEntityCopier`, and wire the subscriber's nullable bus arg + handler registration in `TenancyBundle`.

Purpose: This is the actual SHARE-03 behavior — async dispatch on the request thread, per-tenant convergence on the worker thread, with best-effort + throw-to-retry semantics and D-04/D-05 latest-state handling.
Output: New handler class; `deleteRow()` on copier + interface; modified subscriber (nullable bus + postFlush branch); `TenancyBundle` wiring; subscriber unit test for the dispatch branch.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-async-shared-entities/27-CONTEXT.md
@.planning/phases/27-async-shared-entities/27-RESEARCH.md
@.planning/phases/27-async-shared-entities/27-PATTERNS.md
@.planning/phases/27-async-shared-entities/27-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 27-01 (already shipped) — implement against these directly. -->

src/Message/SharedEntityChangedMessage.php (from 27-01):
  final class SharedEntityChangedMessage
  - public readonly string $entityClass
  - public readonly array $identifier      // array<string, mixed>
  - public readonly string $changeType     // 'insert'|'update'|'delete'

src/Exception/SharedEntityAsyncFanOutException.php (from 27-01):
  final class SharedEntityAsyncFanOutException extends \RuntimeException

src/Shared/SharedEntityCopierInterface.php (EXISTING — this plan ADDS deleteRow()):
  applyRow(EntityManagerInterface $landlordEm, EntityManagerInterface $tenantEm, object $entity, string $type='insert', ?array $capturedIds=null): void
  classifyRow(...): string
  findSharedClasses(EntityManagerInterface $landlordEm): array
  isShared(object $entity, EntityManagerInterface $em): bool
  isSyncInProgress(): bool

src/Subscriber/SharedEntitySyncSubscriber.php (EXISTING — current constructor, lines 76-84, has 6 promoted args at positions 0-5):
  __construct(TenantContext $tenantContext, TenantProviderInterface $tenantProvider, ManagerRegistry $registry, LoggerInterface $logger, string $driver, SharedEntityCopier $copier)
  // this plan ADDS `?MessageBusInterface $bus = null` as the 7th promoted arg, parameter name `$bus` (position 6)
  private switchToTenant(TenantInterface): EntityManagerInterface     // lines 198-215
  private restoreTenantContext(?TenantInterface): void                // lines 282-295
  pendingChanges buffer shape: array{entity: object, type: 'insert'|'update'|'delete', ids?: array<string,mixed>}

src/Provider/TenantProviderInterface.php:
  findAll(): iterable<TenantInterface>   // (confirm exact return type in read_first)

src/Context/TenantContext.php:
  hasTenant(): bool ; getTenant(): TenantInterface ; setTenant(TenantInterface): void ; clear(): void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deleteRow() helper to SharedEntityCopier (OQ-1 resolution) + interface</name>
  <files>src/Shared/SharedEntityCopierInterface.php, src/Shared/SharedEntityCopier.php</files>
  <read_first>
    - src/Shared/SharedEntityCopier.php lines 59-94 (applyRow's delete sub-path: dereferences `$entity::class` at line 66 BEFORE the `if ('delete' === $type)` check at line 69 — THIS is why the handler cannot pass null; lines 82-91 are the find->remove->flush delete body to extract; syncInProgress flag at lines 85-90)
    - src/Shared/SharedEntityCopier.php lines 42, 130-136 (syncInProgress flag ownership + the flush/finally pattern the write-protection listener depends on)
    - src/Shared/SharedEntityCopierInterface.php (FULL FILE — the contract to extend)
    - 27-PATTERNS.md "Critical Open Items for Planner -> OQ-1" (lines 620-637): recommended resolution — add deleteRow() so the handler never passes null to applyRow()
    - 27-RESEARCH.md Open Questions #1 (lines 687-690) + Assumptions Log A1 (line 679): the non-nullable `object $entity` PHPStan L9 constraint
    - src/Subscriber/SharedEntityWriteProtectionListener.php (the consumer of isSyncInProgress() — deleteRow's flush MUST set the flag so the listener bypasses correctly, identical to applyRow's delete branch)
  </read_first>
  <action>
    DECISION (resolves OQ-1 / RESEARCH finding #4): add a dedicated `deleteRow()` method to `SharedEntityCopierInterface` and `SharedEntityCopier` rather than passing a dummy entity to `applyRow()`. `applyRow()` dereferences `$entity::class` at line 66 before the delete-type check, and `$entity` is typed non-nullable `object` — so the handler's vanished-row/delete path cannot reuse `applyRow()` without a real entity. The copier remains the single source of truth (Phase 26 D-02) and the write-protection re-entrancy flag stays copier-owned. This explicitly OVERRIDES the RESEARCH Handler Skeleton's `$landlordEntity ?? new \stdClass()` placeholder (27-RESEARCH.md line 568, flagged as a placeholder at line 601): the handler in Task 3 NEVER constructs a `\stdClass` — it calls `deleteRow()` for the delete path and `applyRow()` only with a live, re-fetched, non-null entity.
    Add to `SharedEntityCopierInterface`: `public function deleteRow(EntityManagerInterface $tenantEm, string $class, array $capturedIds): void;` with PHPDoc `@param class-string $class` and `@param array<string, mixed> $capturedIds`. Document it deletes the tenant-side copy by captured id, is idempotent (no-op when the copy is absent), and sets the syncInProgress flag around the flush.
    Implement `deleteRow()` in `SharedEntityCopier` by EXTRACTING the existing delete body from `applyRow()` lines 74-93: the missing/empty-`$capturedIds` guard with the `tenancy.shared_entity_sync_missing_delete_id` error log + early return (WR-03), then `$existing = $tenantEm->find($class, $capturedIds)`, and if non-null: `$tenantEm->remove($existing)` + set `$this->syncInProgress = true` + `try { $tenantEm->flush(); } finally { $this->syncInProgress = false; }`. Then REFACTOR `applyRow()`'s `'delete' === $type` branch (lines 69-94) to delegate to `$this->deleteRow($tenantEm, $class, $capturedIds ?? [])` so there is a single delete implementation (DRY — no behavioral change for the sync path). Keep the upsert path (lines 96-137) untouched.
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse src/Shared --level=9 --no-progress && vendor/bin/phpunit --testsuite unit --filter SharedEntityCopierTest && vendor/bin/phpunit --testsuite integration --filter SharedEntity</automated>
  </verify>
  <acceptance_criteria>
    - src/Shared/SharedEntityCopierInterface.php contains `public function deleteRow(EntityManagerInterface $tenantEm, string $class, array $capturedIds): void;`
    - src/Shared/SharedEntityCopier.php contains `public function deleteRow(` whose body sets `$this->syncInProgress = true` around `$tenantEm->flush()` in a try/finally and is a no-op when the copy is absent
    - applyRow()'s delete branch delegates to deleteRow() (single delete implementation; grep shows `$tenantEm->remove(` appears exactly once in SharedEntityCopier.php)
    - `vendor/bin/phpunit --testsuite unit --filter SharedEntityCopierTest` exits 0 (no regression in existing copier tests)
    - `vendor/bin/phpunit --testsuite integration --filter SharedEntity` exits 0 (existing Phase 25/26 sync + resync integration tests still pass — delete-path refactor is behavior-preserving)
    - `vendor/bin/phpstan analyse src/Shared --level=9 --no-progress` exits 0
  </acceptance_criteria>
  <done>SharedEntityCopier exposes an idempotent, flag-guarded deleteRow() that the handler can call without a live entity; the sync delete path is unchanged (delegates to deleteRow); all existing copier tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add nullable bus + async dispatch branch to SharedEntitySyncSubscriber + subscriber unit test</name>
  <files>src/Subscriber/SharedEntitySyncSubscriber.php, tests/Unit/Subscriber/SharedEntitySyncSubscriberAsyncTest.php</files>
  <behavior>
    - testPostFlushDispatchesWhenAsyncEnabled (SHARE-03-a): subscriber constructed WITH a mock MessageBusInterface; buffer an insert via onFlush (or seed pendingChanges via a flush simulation); postFlush dispatches exactly one SharedEntityChangedMessage carrying the entity class + identifier + 'insert'; tenantProvider->findAll() is NEVER called (no sync fan-out)
    - testPostFlushUsesSyncFanOutWhenAsyncDisabled (SHARE-03-b): subscriber constructed with bus=null; postFlush invokes the sync fan-out (tenantProvider->findAll() IS called) and the bus is never touched
    - testDispatchClearsTenantContextToAvoidStamp (Pitfall 1, D-01 security invariant): when a tenant is active at postFlush time and async bus is present, TenantContext is cleared before dispatch and restored after (assert setTenant called with the previous tenant in the finally / clear() called before dispatch). NOTE: this unit test pins the stamp-clearing at the unit level; the integration-level proof that an ACTIVE-tenant-at-dispatch produces an all-tenant fan-out (no single-tenant stamp shortcut) lives in 27-03 Task 2 testWrongTenantIsolation (BLOCKER #3 resolution)
    - testDeleteDispatchUsesPreCapturedIds (D-04/D-05): a buffered delete dispatches a message whose identifier equals the pre-captured ids (not getIdentifierValues() which is [] for deletes)
  </behavior>
  <read_first>
    - src/Subscriber/SharedEntitySyncSubscriber.php (FULL FILE — current constructor lines 76-84 (6 args, positions 0-5), postFlush lines 138-189, switchToTenant lines 198-215, restoreTenantContext lines 282-295; the shared_db short-circuit lines 144-149; the `$changes = $this->pendingChanges; $this->pendingChanges = [];` line 151-152)
    - 27-PATTERNS.md "src/Subscriber/SharedEntitySyncSubscriber.php (modified)" section (lines 237-311): the exact constructor modification + postFlush async-branch insertion point + the Pitfall-1 clear-before-dispatch save/restore pattern
    - 27-RESEARCH.md Pattern 7 (lines 353-376): postFlush branch shape + identifier sourcing (delete=pre-captured ids; insert/update=getIdentifierValues() from $args->getObjectManager())
    - 27-RESEARCH.md Pitfall 1 (lines 409-419) + Pattern 4 stamp note (line 294): why tenant context MUST be cleared before dispatch (TenantSendingMiddleware stamps fan-to-all-tenants messages otherwise)
    - tests/Integration/Mailer/AsyncCanaryTest.php lines 231-330 (testAsyncDispatchInWorkerUsesTenantDsnNotLandlord — the project precedent that DOES exercise an active-tenant-at-dispatch → TenantSendingMiddleware stamp path at integration level; this is why 27-03 Task 2 can and does assert the all-tenant fan-out under an active dispatch tenant)
    - tests/Unit/Subscriber (existing subscriber unit tests, if any, for mock/fixture conventions) and tests/Integration/SharedEntity/Support/Entity (the #[Shared] test entity classes available for fixtures)
  </read_first>
  <action>
    Add a nullable `?MessageBusInterface $bus = null` as the 7th constructor argument of `SharedEntitySyncSubscriber` (D-07) — promoted `private readonly`, parameter name `$bus` (position 6), defaulting null so the subscriber stays constructible when Messenger is absent and async is false. Add the `use Symfony\Component\Messenger\MessageBusInterface;` and `use Tenancy\Bundle\Message\SharedEntityChangedMessage;` imports.
    In `postFlush()`, insert the async branch AFTER `$changes = $this->pendingChanges; $this->pendingChanges = [];` (line 152) and BEFORE the existing `$previousTenant = ...` sync setup (line 159): `if (null !== $this->bus) { ... return; }`. Inside the branch (per 27-PATTERNS.md lines 258-289 + RESEARCH Pattern 7): save `$previousTenant` (hasTenant() ? getTenant() : null); CRITICAL (Pitfall 1, D-01) — if `$previousTenant !== null`, call `$this->tenantContext->clear()` BEFORE dispatching so `TenantSendingMiddleware` does not stamp the fan-to-all-tenants message; in a `try { foreach ($changes as $change) { ... dispatch ... } } finally { if previousTenant !== null setTenant(previousTenant); }`. For each change, source the identifier: `'delete' === $type ? ($change['ids'] ?? []) : $args->getObjectManager()->getClassMetadata($entity::class)->getIdentifierValues($entity)`. Dispatch `new SharedEntityChangedMessage($entity::class, $ids, $type)` on `$this->bus`. NO `TenantStamp` is added (D-01). The sync path (current lines 159-188) stays untouched below the early return.
    Update the class-level PHPDoc to document the async branch: when a bus is injected (`tenancy.shared.async: true`), postFlush dispatches one message per changed entity and returns; otherwise the sync fan-out runs unchanged. Note D-03: the message only defers when the user routes it async in `framework.messenger.routing` — an unrouted message handles inline (still correct).
    Create `tests/Unit/Subscriber/SharedEntitySyncSubscriberAsyncTest.php` covering SHARE-03-a / SHARE-03-b plus the stamp-clearing and delete-id behaviors above, using a mock `MessageBusInterface` and a mock/stub `TenantProviderInterface` to assert findAll() is/ isn't called.
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse src/Subscriber tests/Unit/Subscriber --level=9 --no-progress && vendor/bin/phpunit --testsuite unit --filter SharedEntitySyncSubscriberAsyncTest</automated>
  </verify>
  <acceptance_criteria>
    - SharedEntitySyncSubscriber constructor has a 7th param `?MessageBusInterface $bus = null` (promoted readonly, parameter name `$bus`)
    - postFlush() async branch is guarded by `if (null !== $this->bus)`, dispatches `new SharedEntityChangedMessage(...)` per change, and `return`s before the sync fan-out
    - When a tenant is active and async is on, `TenantContext::clear()` is called before dispatch and the previous tenant restored in a finally (assert via the unit test — D-01 stamp-clearing invariant at unit level)
    - Delete-type dispatch uses `$change['ids']` (pre-captured), NOT `getIdentifierValues()`
    - `vendor/bin/phpunit --testsuite unit --filter SharedEntitySyncSubscriberAsyncTest` exits 0 (SHARE-03-a + SHARE-03-b green)
    - `vendor/bin/phpunit --testsuite integration --filter SharedEntity` exits 0 (sync path regression-free with bus=null)
    - `vendor/bin/phpstan analyse src/Subscriber --level=9 --no-progress` exits 0
  </acceptance_criteria>
  <done>postFlush dispatches messages when a bus is wired (SHARE-03-a) and runs the unchanged sync fan-out otherwise (SHARE-03-b); tenant context is cleared before dispatch to prevent stamp pollution (D-01, unit-level; integration-level all-tenant proof in 27-03 Task 2); delete dispatch uses pre-captured ids.</done>
</task>

<task type="auto">
  <name>Task 3: Build SharedEntityChangedMessageHandler + wire bus arg and handler registration in TenancyBundle</name>
  <files>src/MessageHandler/SharedEntityChangedMessageHandler.php, src/TenancyBundle.php</files>
  <read_first>
    - src/Subscriber/SharedEntitySyncSubscriber.php lines 154-295 (the verbatim source for the handler's fan-out loop: previousTenant save/restore, materialize tenants once via foreach (WR-04), switchToTenant() lines 198-215, restoreTenantContext() lines 282-295, structured error-log keys lines 255-260, per-tenant resetManager('tenant') recovery on failure lines 262-267)
    - 27-RESEARCH.md Handler Skeleton (lines 503-603) + Pattern 1 handler registration (lines 187-214): __invoke flow, re-fetch + vanished-row->delete + best-effort + throw-to-retry; explicit `messenger.message_handler` tag (NO #[AsMessageHandler]). CRITICAL: the skeleton's `$landlordEntity ?? new \stdClass()` (line 568) is an EXPLICIT PLACEHOLDER (the note at line 601 says so) — the OQ-1 deleteRow() resolution from Task 1 OVERRIDES it; do NOT construct `\stdClass`
    - 27-PATTERNS.md "SharedEntityChangedMessageHandler" section (lines 68-178): constructor dependency set, DI registration block, fan-out core pattern, switchToTenant/restoreTenantContext copy source, structured logging, aggregate-exception throw
    - 27-RESEARCH.md Pitfall 3 (lines 431-439) + Open Questions #3 (lines 697-700): call `$landlordEm->clear($class)` (or equivalent) BEFORE `find()` to avoid stale identity-map reads in long-running workers
    - 27-RESEARCH.md Security Domain (lines 769-789): validate `$message->entityClass` against `copier->findSharedClasses($landlordEm)` before processing; an unknown class is an UnrecoverableExceptionInterface (do NOT retry, do NOT silently skip)
    - src/TenancyBundle.php lines 273-312 (the interface_exists(EntityManagerInterface) shared-entity wiring block where the subscriber + copier are registered) and lines 280-290 (the subscriber definition whose `$bus` arg gets the bus)
    - src/Provider/TenantProviderInterface.php + src/Context/TenantContext.php (exact signatures for findAll() / hasTenant() / getTenant() / setTenant() / clear())
    - vendor/symfony/messenger/Exception/UnrecoverableExceptionInterface.php (the marker for the unknown-class non-retry path)
  </read_first>
  <action>
    Create `src/MessageHandler/SharedEntityChangedMessageHandler.php` in namespace `Tenancy\Bundle\MessageHandler` with `declare(strict_types=1)`. `final class SharedEntityChangedMessageHandler` with constructor dependencies matching the subscriber's sync-path set + landlord EM: `EntityManagerInterface $landlordEm`, `TenantProviderInterface $tenantProvider`, `SharedEntityCopierInterface $copier`, `TenantContext $tenantContext`, `ManagerRegistry $registry`, `LoggerInterface $logger` (all promoted readonly). Implement `public function __invoke(SharedEntityChangedMessage $message): void` per RESEARCH Handler Skeleton + 27-PATTERNS:
      1. Security gate (RESEARCH Security Domain): validate `$message->entityClass` is in `$this->copier->findSharedClasses($this->landlordEm)`; if not, throw an exception implementing `Symfony\Component\Messenger\Exception\UnrecoverableExceptionInterface` (so it is NOT retried) — log at error with entity_class/identifier.
      2. Stale-read mitigation (Pitfall 3): call `$this->landlordEm->clear($class)` BEFORE the re-fetch `find()`. This ordering (clear-then-find) is mandatory — a find() before clear() would return a stale identity-map row in a long-running worker.
      3. Re-fetch latest landlord state (D-05): `$landlordEntity = ('delete' !== $changeType) ? $this->landlordEm->find($class, $identifier) : null`.
      4. Vanished-row->delete (D-04): if `$landlordEntity === null && 'delete' !== $changeType`, log `tenancy.shared_entity_async_vanished_row` (with entity_class/identifier/original_type at warning, greppable per D-04) and set `$effectiveType = 'delete'`.
      5. Fan-out loop reusing the subscriber's mechanics: save `$previousTenant`; materialize tenants once via foreach (WR-04); `try { foreach tenants: $tenantEm = $this->switchToTenant($tenant); per-tenant try { if effectiveType==='delete' -> $this->copier->deleteRow($tenantEm, $class, $identifier); else -> $this->copier->applyRow($this->landlordEm, $tenantEm, $landlordEntity, $effectiveType, null); } catch (\Throwable) { log tenancy.shared_entity_async_fan_out_failed with tenant_slug/entity_class/identifier/error; $failures[] = slug; $this->registry->resetManager('tenant'); } } finally { $this->restoreTenantContext($previousTenant); }`. CRITICAL (OQ-1, OVERRIDES RESEARCH skeleton line 568): the upsert branch passes `$landlordEntity` which is GUARANTEED non-null here (step 4 collapsed every null to `effectiveType='delete'`, which routes to `deleteRow()` and never reaches `applyRow()`). NEVER construct `new \stdClass()` — the placeholder from the RESEARCH skeleton is resolved by the deleteRow()/applyRow() split.
      6. Throw-to-retry (D-02): after the loop, `if ([] !== $failures) throw new SharedEntityAsyncFanOutException(...)` with a message naming the failed tenant count + slugs; this is a plain `\RuntimeException` subclass so Messenger's retry_strategy engages.
    Copy `switchToTenant()` and `restoreTenantContext()` private methods VERBATIM from the subscriber (DECISION on OQ-2: duplicate the ~30 lines rather than extracting a shared service — keeps Plan 27-02 file-scoped, avoids touching the subscriber's proven CR-01/CR-02 internals; add a code comment noting the source-of-truth twin in SharedEntitySyncSubscriber so future edits stay in sync). Do NOT use `#[AsMessageHandler]` (RESEARCH Pattern 1 / Pitfall 4 — autoconfigure is not active for bundle services).
    In `TenancyBundle::loadExtension()`, inside the `if (interface_exists(\Doctrine\ORM\EntityManagerInterface::class))` shared-entity block (lines 273-312): add a nested `if (interface_exists(MessageBusInterface::class)) { ... }` that (a) sets the subscriber's bus argument to the default bus via the NAMED argument key (future-proof against arg-order changes — WARNING fix): `$builder->getDefinition('tenancy.shared_entity_sync_subscriber')->setArgument('$bus', new Reference('messenger.bus.default'));` — use the named `'$bus'` form, NOT positional `setArgument(6, ...)`; and (b) registers `$services->set('tenancy.shared_entity_changed_handler', SharedEntityChangedMessageHandler::class)->args([ service('doctrine.orm.landlord_entity_manager'), service('tenancy.provider'), service('tenancy.shared_entity_copier'), service('tenancy.context'), service('doctrine'), service('logger') ])->tag('messenger.message_handler', ['handles' => SharedEntityChangedMessage::class]);`. Add the `use Tenancy\Bundle\MessageHandler\SharedEntityChangedMessageHandler;` and `use Tenancy\Bundle\Message\SharedEntityChangedMessage;` imports (MessageBusInterface is already imported at line 17). Handler registration stays gated on `database.enabled` (RESEARCH Pitfall 5 — landlord EM only exists there).
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse src/MessageHandler src/TenancyBundle.php --level=9 --no-progress && vendor/bin/php-cs-fixer check src/MessageHandler src/TenancyBundle.php --diff && [ "$(grep -c 'new \\stdClass()' src/MessageHandler/SharedEntityChangedMessageHandler.php)" = "0" ] && grep -Pzoq '(?s)->clear\(.*?->find\(' src/MessageHandler/SharedEntityChangedMessageHandler.php && grep -q "setArgument('\$bus'" src/TenancyBundle.php && vendor/bin/phpunit --testsuite unit</automated>
  </verify>
  <acceptance_criteria>
    - src/MessageHandler/SharedEntityChangedMessageHandler.php contains `public function __invoke(SharedEntityChangedMessage $message): void` and does NOT carry a load-bearing `#[AsMessageHandler]` attribute for registration
    - KEY-LINK SAFETY (BLOCKER #2): `grep -c 'new \stdClass()' src/MessageHandler/SharedEntityChangedMessageHandler.php` returns 0 — the RESEARCH skeleton's `\stdClass` placeholder is overridden; `applyRow()` is NEVER called with a dummy/null entity (delete path uses `deleteRow()`, upsert path uses the live re-fetched non-null entity)
    - __invoke validates `$message->entityClass` against `findSharedClasses()` and throws an `UnrecoverableExceptionInterface` for unknown classes (security gate)
    - CLEAR-BEFORE-FIND ORDERING (WARNING fix): `grep -Pzoq '(?s)->clear\(.*?->find\(' src/MessageHandler/SharedEntityChangedMessageHandler.php` exits 0 — `$this->landlordEm->clear($class)` textually precedes the `$this->landlordEm->find($class, $identifier)` re-fetch in __invoke (stale-read mitigation, Pitfall 3, is structurally enforced — not reliant on a fresh-EM test passing either way)
    - __invoke converts a null find() result on insert/update into an `effectiveType = 'delete'` with a `tenancy.shared_entity_async_vanished_row` log (D-04)
    - delete (incl. vanished-row->delete) calls `copier->deleteRow(...)`; upsert calls `copier->applyRow(...)` with the live re-fetched entity (never null, never \stdClass)
    - after the per-tenant loop, `throw new SharedEntityAsyncFanOutException(...)` fires iff `$failures !== []` (D-02)
    - `restoreTenantContext()` runs in a `finally` so context is always restored after the loop
    - src/TenancyBundle.php registers `tenancy.shared_entity_changed_handler` with `->tag('messenger.message_handler', ['handles' => SharedEntityChangedMessage::class])` inside the `interface_exists(MessageBusInterface)` block
    - BUS WIRING (WARNING fix): `grep -q "setArgument('\$bus'" src/TenancyBundle.php` succeeds — the subscriber's bus is wired via the NAMED argument `setArgument('$bus', new Reference('messenger.bus.default'))`, NOT the fragile positional `setArgument(6, ...)` (future-proof against subscriber constructor arg-count changes)
    - `vendor/bin/phpstan analyse src/ --level=9 --no-progress` exits 0
    - `vendor/bin/php-cs-fixer check src/ --diff` reports no fixes needed for touched files
    - `vendor/bin/phpunit --testsuite unit` passes (no regressions)
  </acceptance_criteria>
  <done>The handler re-fetches latest state (clear-before-find), fans out to all tenants via copier applyRow/deleteRow (never a \stdClass dummy) with best-effort + throw-to-retry, validates the entity class, and is registered with an explicit messenger.message_handler tag; the subscriber receives the bus via the named `$bus` argument when Messenger is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| landlord HTTP request → worker (or inline bus) | The message crosses from the request thread (no tenant scope) to the fan-out loop that must set each tenant's context itself |
| worker tenant-switch loop | Each `switchToTenant()` is a tenant isolation boundary — wrong restore = cross-tenant write |
| message → handler (class name) | `entityClass` drives `landlordEm->find()`; an unexpected class must not be processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-LEAK | Information Disclosure | per-tenant fan-out loop in `__invoke()` | mitigate | Handler sets each tenant's context via the proven `switchToTenant()` (DBAL `close()` + `resetManager('tenant')`) and ALWAYS runs `restoreTenantContext()` in a `finally`. NO `TenantStamp` is honored (subscriber clears context before dispatch, T-27-02-STAMP) so the handler's own loop is authoritative. A failed tenant triggers `resetManager('tenant')` so the next tenant gets a clean EM — one tenant's failure cannot bleed into another. (V4 Access Control) |
| T-27-02-STAMP | Spoofing | `TenantSendingMiddleware` stamping a fan-to-all message (D-01 cross-tenant isolation invariant) | mitigate | The subscriber (Plan 27-02 Task 2) clears `TenantContext` before `bus->dispatch()` so no `TenantStamp` is attached; `TenantWorkerMiddleware` therefore boots no single-tenant context before the handler. Asserted at TWO levels: (1) unit — the subscriber test `testDispatchClearsTenantContextToAvoidStamp` proves clear()-before-dispatch with an active tenant; (2) integration — 27-03 Task 2 `testWrongTenantIsolation` boots a real kernel, sets an ACTIVE tenant on TenantContext BEFORE the flush/dispatch, and asserts ALL tenants (not just the would-be-stamped one) receive the change — the end-to-end proof that stamp-clearing holds the D-01 isolation guarantee. The project precedent for an active-tenant-at-dispatch integration test is the Mailer `AsyncCanaryTest::testAsyncDispatchInWorkerUsesTenantDsnNotLandlord` |
| T-27-02-CLASSINJ | Elevation of Privilege | unexpected `entityClass` in a message | mitigate | Handler validates `$message->entityClass` against `copier->findSharedClasses($landlordEm)` before any `find()`/fan-out; an unknown class throws `UnrecoverableExceptionInterface` (no retry, no silent skip) |
| T-27-02-STALE | Tampering | stale landlord identity-map read in a long-running worker | mitigate | Handler calls `$landlordEm->clear($class)` BEFORE `find()` (ordering structurally enforced by the Task 3 grep acceptance criterion) so the re-fetch hits the DB and reflects LATEST state (D-05); a deleted master converges to a tenant-side delete (D-04) rather than re-propagating stale data |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `vendor/bin/phpstan analyse --level=9 --no-progress` exits 0
- `vendor/bin/php-cs-fixer check --diff` reports no fixes needed
- `vendor/bin/phpunit --testsuite unit` passes (subscriber async branch SHARE-03-a/-b green)
- `vendor/bin/phpunit --testsuite integration --filter SharedEntity` passes (existing Phase 25/26 sync + resync coverage unaffected by the deleteRow refactor and the bus=null default)
- Handler behavior (re-fetch, vanished-row->delete, best-effort+throw, all-tenant fan-out, D-01 stamp-clearing isolation) is exercised end-to-end by Plan 27-03's canary (this plan ships the implementation; 27-03 ships the integration proof)
</verification>

<success_criteria>
- async dispatch branch present and gated on the injected bus (SHARE-03 acceptance "dispatches a SharedEntityChangedMessage instead of writing synchronously"; default sync preserved)
- handler re-fetches latest landlord state at handle time, clear-before-find (SHARE-03 acceptance "worker retrieves the current entity state ... tenants see the LATEST state")
- vanished landlord row propagates a tenant-side delete (D-04 convergence — no permanent orphan drift)
- best-effort attempt-all then throw `SharedEntityAsyncFanOutException` on any failure (D-02 retry contract)
- `deleteRow()` resolves the non-nullable-entity delete-path problem explicitly (OQ-1) — copier stays single source of truth; handler never constructs a `\stdClass` dummy (BLOCKER #2)
- explicit `messenger.message_handler` tag (NO autoconfigure); subscriber gets the bus via the named `$bus` argument only when Messenger present (D-07)
- PHPStan L9 + cs-fixer clean
</success_criteria>

<output>
Create `.planning/phases/27-async-shared-entities/27-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
