---
phase: 27-async-shared-entities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Message/SharedEntityChangedMessage.php
  - src/Exception/SharedEntityAsyncFanOutException.php
  - src/DependencyInjection/Compiler/SharedAsyncContractPass.php
  - src/TenancyBundle.php
  - tests/Unit/Message/SharedEntityChangedMessageTest.php
  - tests/Unit/DependencyInjection/Compiler/SharedAsyncContractPassTest.php
autonomous: true
requirements: [SHARE-03]
must_haves:
  truths:
    - "tenancy.shared.async config key exists and defaults to false (sync remains default — DEC-SHARE-01)"
    - "SharedEntityChangedMessage carries only class+identifier+change-type scalars, never the full entity"
    - "Compiling a container with tenancy.shared.async: true while symfony/messenger is absent throws a descriptive \\LogicException at build time"
    - "tenancy.shared.async parameter is always set (even when database.enabled is false) so the guard can short-circuit"
  artifacts:
    - path: "src/Message/SharedEntityChangedMessage.php"
      provides: "Lightweight async message value object (entityClass, identifier, changeType)"
      contains: "final class SharedEntityChangedMessage"
    - path: "src/Exception/SharedEntityAsyncFanOutException.php"
      provides: "Retryable aggregate exception thrown by the handler when any tenant failed (D-02)"
      contains: "extends \\RuntimeException"
    - path: "src/DependencyInjection/Compiler/SharedAsyncContractPass.php"
      provides: "Compile-time guard: async:true + no Messenger -> LogicException (D-06)"
      contains: "CompilerPassInterface"
  key_links:
    - from: "src/TenancyBundle.php"
      to: "tenancy.shared.async parameter"
      via: "loadExtension() unconditional parameter set"
      pattern: "tenancy\\.shared\\.async"
    - from: "src/TenancyBundle.php"
      to: "SharedAsyncContractPass"
      via: "build() addCompilerPass inside interface_exists(EntityManagerInterface) block"
      pattern: "new SharedAsyncContractPass"
---

<objective>
Build the Phase 27 contract layer: the `tenancy.shared.async` config knob (D-06), the `SharedEntityChangedMessage` value object (Claude's Discretion shape per RESEARCH Pattern 6), the retryable aggregate exception (D-02), and the `SharedAsyncContractPass` compile-time guard (D-06). These are the artifacts that Plan 27-02 (subscriber branch + handler) and Plan 27-03 (canary test) build against — define the contracts first so downstream tasks implement against fixed signatures.

Purpose: Establish the message contract, the async-mode opt-in flag, and the fail-loud guard so the rest of the phase is a thin branch over the proven Phase 25/26 sync machinery.
Output: New `Message`, `Exception`, and `Compiler` classes; modified `TenancyBundle::configure()` / `loadExtension()` / `build()`; Wave 0 unit tests for the message + guard.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-async-shared-entities/27-CONTEXT.md
@.planning/phases/27-async-shared-entities/27-RESEARCH.md
@.planning/phases/27-async-shared-entities/27-PATTERNS.md

<interfaces>
<!-- Contracts this plan creates — Plan 27-02 implements against these. Use directly, no codebase exploration needed. -->

NEW src/Message/SharedEntityChangedMessage.php:
  final class SharedEntityChangedMessage
  - public readonly string $entityClass        (class-string)
  - public readonly array $identifier           (array<string, mixed> — scalar PK values)
  - public readonly string $changeType          ('insert'|'update'|'delete')

NEW src/Exception/SharedEntityAsyncFanOutException.php:
  final class SharedEntityAsyncFanOutException extends \RuntimeException
  - no extra interface (RESEARCH Pattern 2: plain RuntimeException subclass is retryable via retry_strategy->isRetryable())

NEW src/DependencyInjection/Compiler/SharedAsyncContractPass.php:
  final class SharedAsyncContractPass implements CompilerPassInterface
  - const ASYNC_PARAM = 'tenancy.shared.async'
  - process(ContainerBuilder): void — 3-stage guard (hasParameter -> getParameter false -> interface_exists(MessageBusInterface))

EXISTING src/DependencyInjection/Compiler/MailerTransportContractPass.php (analog — exact template for the guard):
  - process(): early-return when interface absent, short-circuit on flag, throw \LogicException on misconfiguration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SharedEntityChangedMessage value object + SharedEntityAsyncFanOutException</name>
  <files>src/Message/SharedEntityChangedMessage.php, src/Exception/SharedEntityAsyncFanOutException.php</files>
  <read_first>
    - src/Messenger/TenantStamp.php (analog: pure readonly data carrier, declare(strict_types=1) + minimal namespace block — 27-PATTERNS.md lines 30-37)
    - 27-RESEARCH.md Pattern 6 (lines 327-351) + Code Examples "SharedEntityChangedMessage" (lines 465-501): the recommended scalar-only shape and PHPDoc
    - 27-RESEARCH.md Pattern 2 (lines 216-248): why the aggregate exception extends \RuntimeException directly (NOT HandlerFailedException)
    - src/Subscriber/SharedEntitySyncSubscriber.php lines 60-74 (pendingChanges identifier shape that the message's identifier array mirrors)
    - src/Shared/SharedEntityCopier.php line 97 (getIdentifierValues() scalar-array shape)
  </read_first>
  <action>
    Create `src/Message/SharedEntityChangedMessage.php` in namespace `Tenancy\Bundle\Message` with `declare(strict_types=1)`. A `final class SharedEntityChangedMessage` with a constructor of three promoted `public readonly` properties: `string $entityClass` (PHPDoc `@param class-string`), `array $identifier` (PHPDoc `@param array<string, mixed>` — scalar PK values, pre-captured in onFlush for deletes / from getIdentifierValues() for insert/update), and `string $changeType` (PHPDoc `@param 'insert'|'update'|'delete'`). Add a class-level PHPDoc stating the handler re-fetches LATEST landlord state at handle time (D-05) and that a vanished landlord row propagates a tenant-side delete (D-04); reference `27-CONTEXT.md §D-04, §D-05`. NO full entity object — only scalars (per acceptance: Messenger envelope-size discipline). Reuse the `getIdentifierValues()` scalar-array shape per Claude's Discretion (CONTEXT).
    Create `src/Exception/SharedEntityAsyncFanOutException.php` in namespace `Tenancy\Bundle\Exception` with `declare(strict_types=1)`: `final class SharedEntityAsyncFanOutException extends \RuntimeException` with no extra interface and no custom constructor. Class PHPDoc must state it is the D-02 aggregate exception, thrown by `SharedEntityChangedMessageHandler` after the per-tenant loop when any tenant failed, and that being a plain `\RuntimeException` subclass (NOT `UnrecoverableExceptionInterface`) is deliberate so Messenger's `retry_strategy->isRetryable()` engages (RESEARCH Pattern 2). Note explicitly it must NOT wrap or construct `HandlerFailedException` (anti-pattern from RESEARCH).
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse src/Message/SharedEntityChangedMessage.php src/Exception/SharedEntityAsyncFanOutException.php --level=9 --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - src/Message/SharedEntityChangedMessage.php contains `final class SharedEntityChangedMessage` with readonly promoted properties for entityClass (string), identifier (array), changeType (string)
    - src/Message/SharedEntityChangedMessage.php contains NO Doctrine/EntityManager import and no `object` entity property (scalar discipline)
    - src/Exception/SharedEntityAsyncFanOutException.php contains `final class SharedEntityAsyncFanOutException extends \RuntimeException` and does NOT reference HandlerFailedException
    - `php -r "require 'vendor/autoload.php'; \$m = new \Tenancy\Bundle\Message\SharedEntityChangedMessage('App\\Foo', ['id'=>1], 'insert'); \$r = unserialize(serialize(\$m)); echo \$r->entityClass === 'App\\Foo' && \$r->identifier === ['id'=>1] && \$r->changeType === 'insert' ? 'OK' : 'FAIL';"` prints `OK` (PHP-serializable round-trip of scalars)
    - `vendor/bin/phpstan analyse src/Message src/Exception --level=9 --no-progress` exits 0
  </acceptance_criteria>
  <done>Message value object and aggregate exception exist, are PHP-serializable, carry only scalars, and pass PHPStan L9.</done>
</task>

<task type="auto">
  <name>Task 2: Add SharedAsyncContractPass guard + wire tenancy.shared config node, parameter, and guard registration into TenancyBundle</name>
  <files>src/DependencyInjection/Compiler/SharedAsyncContractPass.php, src/TenancyBundle.php</files>
  <read_first>
    - src/DependencyInjection/Compiler/MailerTransportContractPass.php (FULL FILE — exact structural template: early-return when interface absent, hasParameter check, short-circuit on disabled, throw \LogicException on misconfiguration — lines 39-69)
    - 27-RESEARCH.md Pattern 5 (lines 298-325) + Compile-Time Guard Skeleton (lines 605-654): the 3-stage guard logic and where it registers
    - 27-PATTERNS.md "SharedAsyncContractPass" section (lines 182-233): 3-stage guard + build() registration alongside SharedEntityMutualExclusionPass
    - src/TenancyBundle.php lines 100-131 (configure(): mailer + filesystem array node pattern + the existing root-node validate() block)
    - src/TenancyBundle.php lines 155-185 (loadExtension(): mailer/filesystem config extraction -> parameters()->set() chain)
    - src/TenancyBundle.php lines 328-348 (build(): compiler-pass registration; SharedEntityMutualExclusionPass inside interface_exists(EntityManagerInterface) block — line 345-347)
    - src/TenancyBundle.php lines 17, 30 (existing `use Symfony\Component\Messenger\MessageBusInterface;` and `use ...SharedEntityMutualExclusionPass;` imports — add `SharedAsyncContractPass` import in the same Compiler group)
  </read_first>
  <action>
    Create `src/DependencyInjection/Compiler/SharedAsyncContractPass.php` in namespace `Tenancy\Bundle\DependencyInjection\Compiler` with `declare(strict_types=1)`, mirroring `MailerTransportContractPass` structurally. `final class SharedAsyncContractPass implements CompilerPassInterface` with `private const ASYNC_PARAM = 'tenancy.shared.async';`. `process(ContainerBuilder $container): void` performs the 3-stage guard (D-06): (1) `if (!$container->hasParameter(self::ASYNC_PARAM)) { return; }` — short-circuit when the shared stack is absent; (2) `if (!(bool) $container->getParameter(self::ASYNC_PARAM)) { return; }` — short-circuit when async disabled; (3) `if (!interface_exists(\Symfony\Component\Messenger\MessageBusInterface::class)) { throw new \LogicException(...); }` with a descriptive message instructing to `composer require symfony/messenger` or set `tenancy.shared.async: false`. CRITICAL ordering: the `interface_exists(MessageBusInterface)` throw MUST be the LAST stage (after both early-returns) so it only fires for the async:true + Messenger-absent misconfiguration. Add class PHPDoc explaining it mirrors `MailerTransportContractPass` and enforces D-06.
    In `TenancyBundle::configure()`: add a new `arrayNode('shared')->addDefaultsIfNotSet()->children()->booleanNode('async')->defaultFalse()->end()->end()->end()` AFTER the `filesystem` node (after line 121) and BEFORE the root-node `validate()` block (line 123). D-06: a plain `booleanNode()->defaultFalse()`, NOT the mailer tri-state scalar.
    In `TenancyBundle::loadExtension()`: extract `$sharedConfig = $config['shared'] ?? [];` and `$sharedAsync = is_scalar($sharedConfig['async'] ?? false) ? (bool) ($sharedConfig['async'] ?? false) : false;` near the other config extraction (after line 170), then add `->set('tenancy.shared.async', $sharedAsync)` to the `$container->parameters()->set(...)` chain (lines 172-185). CRITICAL (RESEARCH finding #5): set this parameter UNCONDITIONALLY — NOT inside the `database.enabled` block — so the guard can always find it and short-circuit when false/unset.
    In `TenancyBundle::build()`: add `$container->addCompilerPass(new SharedAsyncContractPass());` inside the existing `if (interface_exists(\Doctrine\ORM\EntityManagerInterface::class)) { ... }` block, alongside `new SharedEntityMutualExclusionPass()` (line 345-347). Add the `use Tenancy\Bundle\DependencyInjection\Compiler\SharedAsyncContractPass;` import in the Compiler import group.
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse src/DependencyInjection/Compiler/SharedAsyncContractPass.php src/TenancyBundle.php --level=9 --no-progress && vendor/bin/php-cs-fixer check src/DependencyInjection/Compiler/SharedAsyncContractPass.php src/TenancyBundle.php --diff</automated>
  </verify>
  <acceptance_criteria>
    - src/DependencyInjection/Compiler/SharedAsyncContractPass.php contains a 3-stage guard: `hasParameter` early return, `getParameter` false early return, `interface_exists(MessageBusInterface)` throw
    - The `throw new \LogicException` in SharedAsyncContractPass.php is the LAST statement of process() and is guarded by `if (!interface_exists(...MessageBusInterface...))` — verify with `grep -n 'interface_exists' src/DependencyInjection/Compiler/SharedAsyncContractPass.php` showing the MessageBusInterface check precedes the throw on the same branch (structural prerequisite for 27-01 Task 3's SHARE-03-i assertion)
    - `grep -v '^#' src/TenancyBundle.php | grep -c "tenancy.shared.async"` is >= 2 (parameter set + pass const reference path) AND the `->set('tenancy.shared.async', ...)` call is OUTSIDE the `if ($databaseConfig['enabled'] ?? false)` block
    - `grep -c "booleanNode('async')" src/TenancyBundle.php` returns 1 (plain boolean node, not tri-state scalar)
    - src/TenancyBundle.php build() registers `new SharedAsyncContractPass()` inside the `interface_exists(\Doctrine\ORM\EntityManagerInterface::class)` block
    - SharedAsyncContractPass class-level PHPDoc references D-06 and MailerTransportContractPass as analog
    - `vendor/bin/phpstan analyse src/ --level=9 --no-progress` exits 0
    - `vendor/bin/php-cs-fixer check src/ --diff` reports no fixes needed for the touched files
  </acceptance_criteria>
  <done>The `tenancy.shared.async` boolean config node exists, sets the parameter unconditionally, and the guard pass is registered in build(); PHPStan L9 + cs-fixer clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 unit tests — message scalar discipline + guard throws-when-Messenger-absent</name>
  <files>tests/Unit/Message/SharedEntityChangedMessageTest.php, tests/Unit/DependencyInjection/Compiler/SharedAsyncContractPassTest.php</files>
  <behavior>
    - SharedEntityChangedMessageTest::testCarriesOnlyScalars — construct with (class-string, ['id'=>1], 'insert'); assert each readonly property is the value passed; assert no property holds an object (SHARE-03-c)
    - SharedEntityChangedMessageTest::testSurvivesSerializeRoundTrip — `unserialize(serialize($msg))` reproduces all three properties equal (PHP-serializable scalar carrier)
    - SharedAsyncContractPassTest::testShortCircuitsWhenParameterAbsent — ContainerBuilder with NO `tenancy.shared.async` parameter -> process() does not throw
    - SharedAsyncContractPassTest::testShortCircuitsWhenAsyncDisabled — parameter set false -> process() does not throw (regardless of Messenger presence)
    - SharedAsyncContractPassTest::testGuardThrowsWhenMessengerAbsent — parameter true + Messenger interface absent -> process() throws \LogicException with a message naming symfony/messenger (SHARE-03-i). Skip-guard the test with markTestSkipped when interface_exists(MessageBusInterface) is true (cannot simulate absence when installed); the inverse positive-path test (parameter true + Messenger present -> no throw) runs when Messenger IS installed.
  </behavior>
  <read_first>
    - tests/Unit/DependencyInjection/Compiler/MailerTransportContractPassTest.php (FULL FILE — the ESTABLISHED PRECEDENT this test mirrors: setUp() markTestSkipped when optional dep absent; testNoOpWhen…InterfaceAbsent documents the early-return guard via a structural assertion; positive-path no-throw tests run in-environment. This is the project convention for unit-testing an optional-dependency compile-time guard — follow it exactly)
    - tests/Integration/Mailer/AsyncCanaryTest.php lines 74-104 (markTestSkipped guard pattern for optional-dep tests at class level)
    - 27-RESEARCH.md Validation Architecture -> Wave 0 Gaps (lines 754-763): exact test file list + which SHARE-03-* bullets each covers
    - 27-RESEARCH.md Phase Requirements -> Test Map (lines 735-746): SHARE-03-c, SHARE-03-i automated commands
    - src/DependencyInjection/Compiler/MailerTransportContractPass.php (the guard whose pattern SharedAsyncContractPass mirrors — to know what ContainerBuilder state triggers each branch)
    - phpunit.xml.dist (confirm unit testsuite includes tests/Unit)
  </read_first>
  <action>
    Create `tests/Unit/Message/SharedEntityChangedMessageTest.php` (namespace `Tenancy\Bundle\Tests\Unit\Message`, extends `PHPUnit\Framework\TestCase`) covering SHARE-03-c: construction + property access + a `serialize`/`unserialize` round-trip assertion proving the scalar carrier survives. Assert via reflection or `is_object()` on each property that none is an object.
    Create `tests/Unit/DependencyInjection/Compiler/SharedAsyncContractPassTest.php` (namespace `Tenancy\Bundle\Tests\Unit\DependencyInjection\Compiler`, extends TestCase) covering SHARE-03-i. Mirror the structure of `MailerTransportContractPassTest` (the established precedent for optional-dependency guard tests): instantiate a real `Symfony\Component\DependencyInjection\ContainerBuilder`, set/omit the `tenancy.shared.async` parameter, call `(new SharedAsyncContractPass())->process($container)`, and assert no-throw vs `\LogicException` per the behavior cases.
    For the throw-on-absent path (SHARE-03-i): because `symfony/messenger` IS installed in this repo, the runtime "Messenger absent" condition cannot be simulated — `markTestSkipped` when `interface_exists(MessageBusInterface::class)` is true, EXACTLY as `MailerTransportContractPassTest` does for `MailerInterface`. Add the positive-path test (`async=true` + Messenger present -> no exception) that runs in this environment. Document the skip reason inline citing the MailerTransportContractPassTest precedent. NOTE: the non-skippable proof that the throw lives in the guard at all is the grep-based structural acceptance criterion below (the `throw new \LogicException` inside the `interface_exists(MessageBusInterface)` block) — this is the deterministic SHARE-03-i guarantee that does NOT depend on markTestSkipped being a no-op.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite unit --filter 'SharedEntityChangedMessageTest|SharedAsyncContractPassTest' && grep -Pzoq '(?s)interface_exists\([^)]*MessageBusInterface.*?throw new \\\\LogicException' src/DependencyInjection/Compiler/SharedAsyncContractPass.php</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --testsuite unit --filter SharedEntityChangedMessageTest` exits 0
    - `vendor/bin/phpunit --testsuite unit --filter SharedAsyncContractPassTest` exits 0
    - STRUCTURAL SHARE-03-i PROOF (does NOT depend on markTestSkipped): `grep -Pzoq '(?s)interface_exists\([^)]*MessageBusInterface.*?throw new \\LogicException' src/DependencyInjection/Compiler/SharedAsyncContractPass.php` exits 0 — confirming the `throw new \LogicException` is present inside the `interface_exists(MessageBusInterface)` guard branch (the throw-on-absent path is structurally guaranteed even though the runtime negative path is skip-guarded when Messenger is installed)
    - SharedAsyncContractPassTest asserts: no-throw when parameter absent, no-throw when async=false, and (positive path) no-throw when async=true + Messenger present
    - SharedAsyncContractPassTest's testGuardThrowsWhenMessengerAbsent uses markTestSkipped when interface_exists(MessageBusInterface) is true and the inline comment cites MailerTransportContractPassTest as the project-established precedent for this optional-dependency guard convention
    - SharedEntityChangedMessageTest asserts no message property holds an object instance (scalar discipline, SHARE-03-c)
    - `vendor/bin/phpstan analyse tests/Unit/Message tests/Unit/DependencyInjection/Compiler/SharedAsyncContractPassTest.php --level=9 --no-progress` exits 0
  </acceptance_criteria>
  <done>Message + guard unit tests are green; scalar discipline (SHARE-03-c) is pinned; the guard's throw-on-absent path (SHARE-03-i) is proven structurally by a grep assertion (independent of markTestSkipped), with the runtime negative path skip-guarded per the MailerTransportContractPassTest precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container compile (build phase) | Misconfiguration (async:true + no Messenger) must be caught here, never at runtime |
| message contents (dispatch → dead-letter) | A dead-lettered message is operator-inspectable; it must reveal only class+PK, not entity data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-CONFIG | Denial of Service | async:true with symfony/messenger absent | mitigate | `SharedAsyncContractPass` throws a descriptive `\LogicException` at container build time (D-06) — the misconfiguration can never reach a running worker. The throw path is GUARANTEED structurally by the Task 3 grep assertion (`throw new \LogicException` inside the `interface_exists(MessageBusInterface)` branch), and additionally exercised at runtime in any CI matrix entry without symfony/messenger; when Messenger IS installed, the runtime negative path is skip-guarded per the MailerTransportContractPassTest precedent |
| T-27-01-DLQ | Information Disclosure | `SharedEntityChangedMessage` payload in a dead-letter queue | mitigate | Message carries ONLY `entityClass` (string) + `identifier` (scalar PK array) + `changeType` — never the entity object/field values. Dead-letter inspection reveals class name + PK only, not row data. Enforced by SharedEntityChangedMessageTest scalar-discipline assertion |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); nothing to verify |
</threat_model>

<verification>
- `vendor/bin/phpstan analyse --level=9 --no-progress` exits 0 (full src + touched tests)
- `vendor/bin/php-cs-fixer check --diff` reports no fixes needed
- `vendor/bin/phpunit --testsuite unit` passes (new message + guard tests green, no regressions)
- A container compiled with `tenancy.shared.async: true` and Messenger present compiles without error (positive path); the guard's throw path is asserted structurally (grep) at unit level and at runtime in Messenger-absent CI runs
</verification>

<success_criteria>
- `tenancy.shared.async` boolean config node exists (default false — SHARE-03 acceptance "async is opt-in; default remains sync")
- `tenancy.shared.async` parameter is set unconditionally (independent of database.enabled)
- `SharedEntityChangedMessage` carries class+identifier+change-type scalars only (SHARE-03 acceptance "NOT the full entity payload")
- `SharedAsyncContractPass` throws `\LogicException` at build time for async:true + no Messenger (SHARE-03 acceptance "MailerTransportContractPass-style compile-time guard"); the throw path is structurally proven by grep (SHARE-03-i) independent of skip-guarded runtime tests
- `SharedEntityAsyncFanOutException extends \RuntimeException` ready for Plan 27-02's handler (D-02)
- PHPStan L9 + cs-fixer clean; Wave 0 unit tests (SHARE-03-c, SHARE-03-i) green
</success_criteria>

<output>
Create `.planning/phases/27-async-shared-entities/27-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
