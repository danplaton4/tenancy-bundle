---
phase: 03-database-per-tenant-driver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Driver/TenantDriverInterface.php
  - src/Bootstrapper/DatabaseSwitchBootstrapper.php
  - tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php
autonomous: true
requirements:
  - ISOL-01

must_haves:
  truths:
    - "DatabaseSwitchBootstrapper delegates boot() to TenantConnection::switchTenant() with tenant connectionConfig"
    - "DatabaseSwitchBootstrapper delegates clear() to TenantConnection::reset()"
    - "TenantDriverInterface defines the boot/clear contract for isolation drivers"
  artifacts:
    - path: "src/Driver/TenantDriverInterface.php"
      provides: "Driver contract extending TenantBootstrapperInterface"
      contains: "interface TenantDriverInterface"
    - path: "src/Bootstrapper/DatabaseSwitchBootstrapper.php"
      provides: "Bootstrapper that switches DBAL connection on tenant boot"
      contains: "final class DatabaseSwitchBootstrapper"
      exports: ["boot", "clear"]
    - path: "tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php"
      provides: "Unit tests for boot/clear delegation"
      min_lines: 40
  key_links:
    - from: "src/Bootstrapper/DatabaseSwitchBootstrapper.php"
      to: "src/DBAL/TenantConnection.php"
      via: "constructor injection"
      pattern: "TenantConnection"
---

<objective>
Create the TenantDriverInterface contract and DatabaseSwitchBootstrapper that plugs into the existing BootstrapperChain to switch tenant database connections.

Purpose: Establishes the driver abstraction and the concrete bootstrapper that will call TenantConnection::switchTenant() — the core trigger for runtime connection switching.
Output: TenantDriverInterface, DatabaseSwitchBootstrapper (final class), and unit tests validating boot/clear delegation.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-database-per-tenant-driver/03-CONTEXT.md
@.planning/phases/03-database-per-tenant-driver/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/Bootstrapper/TenantBootstrapperInterface.php:
```php
interface TenantBootstrapperInterface
{
    public function boot(TenantInterface $tenant): void;
    public function clear(): void;
}
```

From src/TenantInterface.php:
```php
interface TenantInterface
{
    public function getSlug(): string;
    public function getDomain(): ?string;
    /** @return array<string, mixed> */
    public function getConnectionConfig(): array;
    public function getName(): string;
    public function isActive(): bool;
}
```

From src/Bootstrapper/BootstrapperChain.php:
```php
final class BootstrapperChain
{
    public function addBootstrapper(TenantBootstrapperInterface $bootstrapper): void;
    public function boot(TenantInterface $tenant): void;  // calls $bootstrapper->boot($tenant)
    public function clear(): void;  // calls $bootstrapper->clear() in reverse order
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TenantDriverInterface and DatabaseSwitchBootstrapper with unit tests</name>
  <files>src/Driver/TenantDriverInterface.php, src/Bootstrapper/DatabaseSwitchBootstrapper.php, tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php</files>
  <read_first>
    - src/Bootstrapper/TenantBootstrapperInterface.php
    - src/TenantInterface.php
    - src/Bootstrapper/BootstrapperChain.php
    - tests/Unit/Bootstrapper/BootstrapperChainTest.php
  </read_first>
  <behavior>
    - Test 1: boot() calls TenantConnection::switchTenant() with the tenant's connectionConfig array
    - Test 2: clear() calls TenantConnection::reset()
    - Test 3: DatabaseSwitchBootstrapper implements TenantBootstrapperInterface
    - Test 4: DatabaseSwitchBootstrapper implements TenantDriverInterface
  </behavior>
  <action>
1. Create `src/Driver/TenantDriverInterface.php`:
   - Namespace: `Tenancy\Bundle\Driver`
   - `interface TenantDriverInterface extends TenantBootstrapperInterface` — marker interface for isolation drivers (database-per-tenant, shared-db). No additional methods beyond TenantBootstrapperInterface.

2. Create `src/Bootstrapper/DatabaseSwitchBootstrapper.php`:
   - Namespace: `Tenancy\Bundle\Bootstrapper`
   - `final class DatabaseSwitchBootstrapper implements TenantDriverInterface`
   - Constructor: `__construct(private readonly TenantConnection $tenantConnection)` — type-hint against `Tenancy\Bundle\DBAL\TenantConnection` (the class from Plan 03-02; import it even though it doesn't exist yet — it will be created in Plan 02)
   - `boot(TenantInterface $tenant): void` — calls `$this->tenantConnection->switchTenant($tenant->getConnectionConfig())`
   - `clear(): void` — calls `$this->tenantConnection->reset()`

3. Create `tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php`:
   - Since TenantConnection is `final`, create a test double: use an anonymous class or a simple stub `FakeTenantConnection` that records calls. Alternatively, since TenantConnection doesn't exist yet, create a minimal mock object using PHPUnit's `createMock()` on a local interface or use `$this->createMock(TenantConnection::class)` — but TenantConnection is final. Instead: create a simple `TenantConnectionStub` inner class in the test that has `switchTenant(array $config)` and `reset()` methods and records calls. Pass it to DatabaseSwitchBootstrapper.
   - Actually, the simplest approach: since `TenantConnection` doesn't exist yet but will be `final`, create a minimal `TenantConnectionSpy` class in `tests/Unit/Bootstrapper/` that extends nothing but has the same `switchTenant(array $config): void` and `reset(): void` signatures. Then adjust — wait, DatabaseSwitchBootstrapper type-hints TenantConnection specifically.
   - Best approach: Use PHPUnit's `getMockBuilder(TenantConnection::class)->disableOriginalConstructor()->getMock()` — PHPUnit can mock final classes if `allowMockingUnknownTypes` is set, or use `#[MockClass]`. Since TenantConnection extends `Doctrine\DBAL\Connection` which has a complex constructor, the cleanest pattern is: create `TenantConnection` as a stub file first in this task (just the class skeleton with switchTenant/reset method signatures), OR accept that the test will use reflection-based mocking.
   - RECOMMENDED: Write the test assuming TenantConnection exists with `switchTenant(array $config): void` and `reset(): void`. Use PHPUnit `createMock()` with `disableOriginalConstructor()`. If TenantConnection is final, use `createTestProxy()` or the `@phpunit-extension` for final class mocking. The test file should have a clear TODO comment if mocking fails, but PHPUnit 11 supports mocking final classes via `allowMockingUnknownTypes`.
   - Test `testBootCallsSwitchTenantWithConnectionConfig`: create mock TenantConnection, expect `switchTenant` called once with `['dbname' => 'tenant_acme', 'host' => 'db.internal']`. Create a tenant stub implementing TenantInterface with `getConnectionConfig()` returning that array. Call `$bootstrapper->boot($tenant)`.
   - Test `testClearCallsReset`: expect `reset()` called once. Call `$bootstrapper->clear()`.
   - Test `testImplementsTenantBootstrapperInterface`: `assertInstanceOf(TenantBootstrapperInterface::class, $bootstrapper)`
   - Test `testImplementsTenantDriverInterface`: `assertInstanceOf(TenantDriverInterface::class, $bootstrapper)`
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && ./vendor/bin/phpunit tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php -x</automated>
  </verify>
  <acceptance_criteria>
    - src/Driver/TenantDriverInterface.php contains `interface TenantDriverInterface extends TenantBootstrapperInterface`
    - src/Bootstrapper/DatabaseSwitchBootstrapper.php contains `final class DatabaseSwitchBootstrapper implements TenantDriverInterface`
    - src/Bootstrapper/DatabaseSwitchBootstrapper.php contains `$this->tenantConnection->switchTenant($tenant->getConnectionConfig())`
    - src/Bootstrapper/DatabaseSwitchBootstrapper.php contains `$this->tenantConnection->reset()`
    - tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php contains `testBootCallsSwitchTenant`
    - tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php contains `testClearCallsReset`
    - ./vendor/bin/phpunit tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php exits 0
  </acceptance_criteria>
  <done>DatabaseSwitchBootstrapper delegates boot/clear to TenantConnection methods, verified by passing unit tests</done>
</task>

</tasks>

<verification>
- `./vendor/bin/phpunit tests/Unit/Bootstrapper/DatabaseSwitchBootstrapperTest.php -x` passes
- `grep -r "TenantDriverInterface" src/` shows interface and implementation
- `grep "final class DatabaseSwitchBootstrapper" src/Bootstrapper/DatabaseSwitchBootstrapper.php` confirms final class
</verification>

<success_criteria>
- TenantDriverInterface extends TenantBootstrapperInterface
- DatabaseSwitchBootstrapper is final, implements TenantDriverInterface
- boot() delegates to switchTenant() with connectionConfig, clear() delegates to reset()
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-per-tenant-driver/03-01-SUMMARY.md`
</output>
