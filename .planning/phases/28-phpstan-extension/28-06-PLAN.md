---
phase: 28-phpstan-extension
plan: 06
type: execute
wave: 2
depends_on: ["28-05"]
autonomous: true
gap_closure: true
requirements: [DX-03]
files_modified:
  - extension-doctrine.neon
  - phpstan-extension-dogfood.neon
  - phpstan-extension-dogfood-nodoctrine.neon
  - .github/workflows/ci.yml
user_setup: []

must_haves:
  truths:
    # Closes VERIFICATION truth #10 (WR-01) — the resolver must actually be injected
    - "When phpstan/phpstan-doctrine is installed, ObjectMetadataResolver is injected into TenantIdDriftRule (the D-02 metadata path is reachable, not dead code) — via a conditionally-included neon fragment referencing @PHPStan\\Type\\Doctrine\\ObjectMetadataResolver"
    - "The base extension.neon stays resolver-less so the extension loads cleanly when phpstan-doctrine is ABSENT (no @service reference crash at config-merge time)"
    - "The dogfood proves the wired+fixed metadata path runs over src/ with phpstan-doctrine present and produces ZERO false positives on the bundle's own #[TenantAware] entities"
    # Closes WR-05 — optional-dependency guards are actually tested
    - "CI runs the shipped rules (PHPStan dogfood + tests/Unit/PHPStan) WITHOUT Doctrine installed, proving the class_exists/interface_exists guards degrade gracefully (the rules load and stay clean with no Doctrine)"
    - "All CI lanes stay green; bundle level-9 self-analysis + full suite unaffected"
  artifacts:
    - path: "extension-doctrine.neon"
      provides: "Conditionally-included fragment that wires objectMetadataResolver into TenantIdDriftRule when phpstan-doctrine is present"
      contains: "ObjectMetadataResolver"
    - path: "phpstan-extension-dogfood.neon"
      provides: "Dogfood that includes the doctrine fragment so the WIRED metadata path is exercised in CI over src/"
      contains: "extension-doctrine.neon"
    - path: "phpstan-extension-dogfood-nodoctrine.neon"
      provides: "Base-only dogfood (resolver-less extension.neon) for the no-Doctrine CI lane to prove graceful degradation"
      contains: "extension.neon"
    - path: ".github/workflows/ci.yml"
      provides: "no-doctrine job extended to run the base dogfood + tests/Unit/PHPStan without Doctrine (WR-05)"
      contains: "tests/Unit/PHPStan"
  key_links:
    - from: "extension-doctrine.neon"
      to: "src/PHPStan/Rule/TenantIdDriftRule.php (objectMetadataResolver constructor arg)"
      via: "arguments: objectMetadataResolver: @PHPStan\\Type\\Doctrine\\ObjectMetadataResolver"
      pattern: "objectMetadataResolver"
    - from: "phpstan-extension-dogfood.neon"
      to: "extension-doctrine.neon"
      via: "includes (so the wired path runs in the dogfood lane that has phpstan-doctrine)"
      pattern: "extension-doctrine"
    - from: ".github/workflows/ci.yml"
      to: "tests/Unit/PHPStan + phpstan-extension-dogfood-nodoctrine.neon"
      via: "no-doctrine job steps after composer remove doctrine/* (and phpstan-doctrine)"
      pattern: "no-doctrine"
---

<objective>
Close Gap 2 (WR-01, BLOCKER) from 28-VERIFICATION.md and fold in WR-05. This plan depends on 28-05: the CR-01 FieldMapping fix MUST land first, because wiring the resolver before the fix would immediately expose consumers to the false-positive storm (REVIEW.md WR-01: "Until CR-01 is fixed, wiring this would expose the FieldMapping bug — fix CR-01 first, then wire").

Gap 2 (WR-01): `TenantIdDriftRule` is registered in `extension.neon` with no `arguments`, and PHPStan's Nette DI container cannot autowire a `?object`-typed parameter to the concrete `ObjectMetadataResolver` service — so `$this->objectMetadataResolver` is always null even with phpstan-doctrine installed, and the advertised "XML/YAML-mapped entity analysis" (composer.json suggest) silently does nothing. The fix is to inject `@PHPStan\Type\Doctrine\ObjectMetadataResolver`. Because that `@service` reference is resolved at config-merge time and would crash when phpstan-doctrine is absent, the wiring MUST live in a conditionally-included neon fragment — the base `extension.neon` stays resolver-less for the degrade-to-reflection path (D-02).

WR-05 (folded): CI never exercises the optional-dependency guards — the `no-doctrine` job runs only a hand-listed set of unit dirs and never runs PHPStan, the dogfood, or `tests/Unit/PHPStan`. A regression that hard-references a Doctrine class at class-load time would pass all current CI. Extend the `no-doctrine` job to run a base-only dogfood + `tests/Unit/PHPStan` without Doctrine.

Purpose: WR-01 makes the D-02 "present" path live (truth #10) so the soft-integration the user locked actually functions; WR-05 locks in the optional-dependency contract that is the bundle's defining identity (Doctrine guarded-optional). Together with 28-05 they make the metadata path correct, wired, AND tested.
Output: a conditionally-included doctrine wiring fragment + a dogfood that runs the wired path + a base-only dogfood + a no-doctrine CI lane proving graceful degradation.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-phpstan-extension/28-CONTEXT.md
@.planning/phases/28-phpstan-extension/28-VERIFICATION.md
@.planning/phases/28-phpstan-extension/28-REVIEW.md
@.planning/phases/28-phpstan-extension/28-05-PLAN.md

<interfaces>
<!-- Contracts extracted from the codebase + vendor. Do NOT re-explore. -->

Current base extension.neon (package root) — TenantIdDriftRule service has NO arguments block:
  services:
    - { class: Tenancy\Bundle\PHPStan\Rule\MutualExclusionRule, tags: [phpstan.rules.rule] }
    - { class: Tenancy\Bundle\PHPStan\Rule\TenantIdDriftRule, tags: [phpstan.rules.rule] }   # no arguments (WR-01)
    - { class: Tenancy\Bundle\PHPStan\Rule\SharedEntityLeakRule, arguments: { checkSharedEntityLeaks: %tenancy.checkSharedEntityLeaks%, reflectionProvider: @reflectionProvider }, tags: [phpstan.rules.rule] }
  KEEP this base file resolver-less — it loads when phpstan-doctrine is absent.

TenantIdDriftRule constructor (src/PHPStan/Rule/TenantIdDriftRule.php):
  __construct(private readonly ?object $objectMetadataResolver = null)
  The DI must pass objectMetadataResolver: @PHPStan\Type\Doctrine\ObjectMetadataResolver to populate it.

phpstan-doctrine registers the resolver as a service (vendor/phpstan/phpstan-doctrine/extension.neon line 119 — CONFIRMED):
  - { class: PHPStan\Type\Doctrine\ObjectMetadataResolver, arguments: { objectManagerLoader: %doctrine.objectManagerLoader%, tmpDir: %tmpDir% } }
  Other phpstan-doctrine rules reference it as @PHPStan\Type\Doctrine\ObjectMetadataResolver (line 157).
  phpstan-doctrine auto-loads via extension-installer GeneratedConfig (CONFIRMED present in vendor), so when a consumer
  installs phpstan-doctrine, its extension.neon (incl. the resolver service) is merged BEFORE our fragment.
  KEY CONSTRAINT: a base-extension.neon @PHPStan\Type\Doctrine\ObjectMetadataResolver reference would fail config-merge
  when phpstan-doctrine is ABSENT — hence a SEPARATE fragment included only where the resolver service exists.

DUPLICATE-REGISTRATION RISK: if both extension.neon (resolver-less TenantIdDriftRule) and extension-doctrine.neon
  (resolver-wired TenantIdDriftRule) are loaded, the same rule may register twice and fire twice. The executor must
  resolve this so EXACTLY ONE TenantIdDriftRule rule is active in the wired dogfood (e.g. the fragment registers the
  wired rule under a distinct service key while the base stays resolver-less for the non-doctrine path; or the dogfood
  composes base + fragment such that PHPStan dedupes — verify with the dogfood run and the acceptance grep below).

Current dogfood (phpstan-extension-dogfood.neon):
  includes: [extension.neon]
  parameters: { level: 9, paths: [src], treatPhpDocTypesAsCertain: false, ignoreErrors:[{identifier: trait.unused, reportUnmatched: false}], parallel:{maximumNumberOfProcesses:1} }
  Runs in the CI `phpstan` job (Doctrine + phpstan-doctrine PRESENT). Currently exercises the resolver-LESS extension.neon
  (reflection path only). After this plan, including extension-doctrine.neon proves the WIRED metadata path over src/.

Current CI no-doctrine job (.github/workflows/ci.yml lines 95-115):
  - composer remove --dev doctrine/orm doctrine/dbal doctrine/doctrine-bundle doctrine/migrations --no-interaction
  - vendor/bin/phpunit <hand-listed unit dirs>   # does NOT include tests/Unit/PHPStan; does NOT run phpstan/dogfood
  NOTE: phpstan-doctrine has doctrine/orm as a CONFLICT (<2.5) + require-dev (^2.16), NOT a hard require. The cleanest
  no-doctrine proof also removes phpstan/phpstan-doctrine so the resolver SERVICE disappears, then runs a BASE-ONLY dogfood
  (extension.neon, resolver-less) + tests/Unit/PHPStan. The 28-05 metadata tests self-skip via their class_exists
  ObjectMetadataResolver guard, so tests/Unit/PHPStan stays green with phpstan-doctrine removed.

extension-installer GeneratedConfig (vendor/phpstan/extension-installer/src/GeneratedConfig.php) does NOT include the root
  package's own extension.neon during the bundle's self-analysis (Pitfall 4 correctly avoided — confirmed by REVIEW.md).
  So adding extension-doctrine.neon does not auto-leak into phpstan.neon.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ObjectMetadataResolver via a conditionally-included neon fragment + run the wired path in the dogfood</name>
  <files>extension-doctrine.neon, phpstan-extension-dogfood.neon</files>
  <read_first>
    - extension.neon (the base file — KEEP resolver-less; this task ADDS a separate fragment, does not add a resolver ref to the base)
    - src/PHPStan/Rule/TenantIdDriftRule.php (constructor ?object $objectMetadataResolver = null — the arg the fragment injects; metadata path fixed in 28-05)
    - vendor/phpstan/phpstan-doctrine/extension.neon (lines 119-122 resolver service registration; line 157 @PHPStan\Type\Doctrine\ObjectMetadataResolver reference style)
    - phpstan-extension-dogfood.neon (current dogfood — add the doctrine fragment include)
    - .planning/phases/28-phpstan-extension/28-REVIEW.md (WR-01 fix block lines 156-171 — "must live in a conditionally-included fragment")
    - .planning/phases/28-phpstan-extension/28-RESEARCH.md (Pattern 5 + Pitfall 4 — extension.neon cannot conditionally inject; keep base separate)
    - ./CLAUDE.md (Doctrine OPTIONAL — degrade gracefully; PHPStan level 9)
  </read_first>
  <action>
    Create `extension-doctrine.neon` at the package root: a small fragment whose only job is to register `Tenancy\Bundle\PHPStan\Rule\TenantIdDriftRule` with an `arguments` block injecting `objectMetadataResolver: @PHPStan\Type\Doctrine\ObjectMetadataResolver`, tagged `phpstan.rules.rule`. This fragment is included only in contexts where phpstan-doctrine is present (its resolver service must exist at config-merge time). Add a header comment stating: (a) this fragment is the D-02 "phpstan-doctrine present" wiring for WR-01; (b) it MUST NOT be included unless phpstan/phpstan-doctrine is installed (the @service reference fails config-merge otherwise); (c) the base extension.neon deliberately registers TenantIdDriftRule WITHOUT the resolver so the degrade-to-reflection path works when phpstan-doctrine is absent; (d) consumers who installed phpstan-doctrine and want the metadata path add an includes line pointing at this fragment (documented for Phase 29 DOC-20).

    Resolve the duplicate-registration risk: if including both the base extension.neon (resolver-less TenantIdDriftRule) and extension-doctrine.neon (resolver-wired TenantIdDriftRule) causes the rule to register/fire twice, ensure EXACTLY ONE TenantIdDriftRule rule is active in the wired dogfood. Verify the actual PHPStan behavior with the dogfood run; if duplication occurs, register the wired rule so it supersedes the base one (e.g. the dogfood includes the fragment and the fragment is the sole TenantIdDriftRule registration for that run), confirmed by the acceptance check that the dogfood reports no duplicate-service / double-fire.

    Update `phpstan-extension-dogfood.neon` to `includes:` BOTH `extension.neon` and `extension-doctrine.neon` (the CI `phpstan` job has phpstan-doctrine present, so the resolver service exists). This makes the dogfood exercise the WIRED + CR-01-fixed metadata path over `src/` — proving truth #10 end-to-end: the bundle's own #[TenantAware] entities produce ZERO false positives through the resolver path. Keep level: 9, paths: [src], and the existing trait.unused ignore.

    Do NOT add an @PHPStan\Type\Doctrine\ObjectMetadataResolver reference to the base extension.neon (it would crash the no-doctrine path). No changes to phpstan.neon (Pitfall 4).
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon --memory-limit=512M && vendor/bin/phpstan analyse</automated>
  </verify>
  <acceptance_criteria>
    - vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon --memory-limit=512M exits 0 (the wired metadata path runs over src/ with phpstan-doctrine present and produces NO false positives — the WR-01+CR-01 end-to-end proof; would have failed loudly before 28-05's fix).
    - extension-doctrine.neon exists and injects the resolver: grep -c 'objectMetadataResolver' extension-doctrine.neon >= 1 AND grep -c 'ObjectMetadataResolver' extension-doctrine.neon >= 1.
    - phpstan-extension-dogfood.neon includes the fragment: grep -c 'extension-doctrine' phpstan-extension-dogfood.neon >= 1.
    - The base extension.neon is NOT given an @PHPStan\Type\Doctrine\ObjectMetadataResolver reference: grep -c 'ObjectMetadataResolver' extension.neon is 0 (degrade path preserved).
    - vendor/bin/phpstan analyse (bundle self-analysis) exits 0 — extension-doctrine.neon does NOT leak into phpstan.neon.
    - Exactly one tenancy.tenantIdDrift rule is active in the wired dogfood (no duplicate-registration error / no double-firing).
  </acceptance_criteria>
  <done>ObjectMetadataResolver is injected into TenantIdDriftRule via a conditionally-included fragment; the dogfood proves the wired+fixed metadata path runs clean over the bundle's own src/; the base extension.neon stays resolver-less so the no-doctrine path still loads.</done>
</task>

<task type="auto">
  <name>Task 2: WR-05 — no-doctrine CI lane runs a base dogfood + tests/Unit/PHPStan to prove graceful degradation</name>
  <files>phpstan-extension-dogfood-nodoctrine.neon, .github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the no-doctrine job lines 95-115; the phpstan job lines 59-75 for the dogfood step pattern to mirror)
    - .planning/phases/28-phpstan-extension/28-REVIEW.md (WR-05 fix block lines 271-275 — add dogfood-without-Doctrine + phpunit tests/Unit/PHPStan after composer remove)
    - src/PHPStan/Rule/TenantIdDriftRule.php (the interface_exists(EntityManagerInterface) guard at the top of processNode — the guard WR-05 must prove)
    - src/PHPStan/Rule/SharedEntityLeakRule.php (same interface_exists guard)
    - extension.neon (the BASE resolver-less file the no-doctrine dogfood must target — NOT extension-doctrine.neon, whose @service reference is gone with phpstan-doctrine)
    - phpstan-extension-dogfood.neon (the with-doctrine dogfood — do NOT reuse it in the no-doctrine lane; it includes extension-doctrine.neon)
  </read_first>
  <action>
    Create `phpstan-extension-dogfood-nodoctrine.neon` at the package root: a base-only dogfood that includes ONLY `extension.neon` (resolver-less — NO extension-doctrine.neon), with level: 9, paths: [src], the same trait.unused ignore, and parallel maximumNumberOfProcesses: 1. Header comment: "No-Doctrine dogfood — proves the shipped rules LOAD and run clean when Doctrine + phpstan-doctrine are absent (WR-05). Includes only the base extension.neon; the doctrine wiring fragment is intentionally excluded because @PHPStan\Type\Doctrine\ObjectMetadataResolver does not exist without phpstan-doctrine."

    Extend the existing `no-doctrine` job in `.github/workflows/ci.yml` (do NOT touch other jobs). In the remove step, ALSO remove phpstan/phpstan-doctrine so its resolver service disappears: `composer remove --dev doctrine/orm doctrine/dbal doctrine/doctrine-bundle doctrine/migrations phpstan/phpstan-doctrine --no-interaction` (keep phpstan/phpstan itself for the dogfood; keep phpstan/extension-installer). After removal, add two steps:
      1. "Run PHPStan rule unit tests without Doctrine" — `vendor/bin/phpunit tests/Unit/PHPStan` (the 28-05 metadata tests self-skip via their class_exists ObjectMetadataResolver guard; the reflection-path + MappedSuperclass tests still assert behavior). Add the hand-listed `tests/Unit/PHPStan` to the existing phpunit step's directory list OR as a separate step.
      2. "PHPStan dogfood without Doctrine — prove optional-dep guards" — `vendor/bin/phpstan analyse -c phpstan-extension-dogfood-nodoctrine.neon --memory-limit=512M` and assert it exits clean.
    Add explanatory comments mirroring the existing job's "self-skips via class_exists() guard" style so a future maintainer understands the lane proves the degrade-to-reflection contract: with Doctrine + phpstan-doctrine absent, (i) the PHPStan rule classes load without fatal, and (ii) the shipped rules over src/ exit clean.

    Keep the no-doctrine job on the same setup-php / composer-install scaffolding it already uses. Do NOT change the with-doctrine `phpstan` job. Validate the YAML stays well-formed.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); s=yaml.dump(d['jobs']['no-doctrine']); assert 'tests/Unit/PHPStan' in s, 'no-doctrine must run tests/Unit/PHPStan'; assert 'phpstan-extension-dogfood-nodoctrine' in s, 'no-doctrine must run the base dogfood'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - phpstan-extension-dogfood-nodoctrine.neon exists and includes ONLY the base extension.neon: grep -c 'extension-doctrine' phpstan-extension-dogfood-nodoctrine.neon is 0 AND grep -Ec 'extension\.neon' phpstan-extension-dogfood-nodoctrine.neon >= 1.
    - The no-doctrine job's composer remove step also removes phpstan/phpstan-doctrine: grep on ci.yml shows the remove line contains 'phpstan-doctrine'.
    - The no-doctrine job runs tests/Unit/PHPStan: the python yaml check above passes ('tests/Unit/PHPStan' present in the no-doctrine job).
    - The no-doctrine job runs the base dogfood: the python yaml check confirms 'phpstan-extension-dogfood-nodoctrine' present.
    - .github/workflows/ci.yml is valid YAML (python yaml.safe_load succeeds) and only the no-doctrine job changed (the with-doctrine phpstan job at lines 59-75 is untouched).
  </acceptance_criteria>
  <done>The no-doctrine CI lane proves the optional-dependency guards: with Doctrine + phpstan-doctrine removed, the rule classes load, tests/Unit/PHPStan passes (metadata tests self-skip), and the base dogfood exits clean over src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer install path → extension wiring | A consumer who follows composer.json `suggest` installs phpstan-doctrine; the wired metadata path then activates |
| optional Doctrine dependency → rule class load | The shipped rules must load and run when Doctrine is absent (guarded-optional identity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-deadpath | (rule-health / Elevation via leak) | WR-01: the advertised metadata path is dead code (resolver never injected), so the consumer-facing "XML/YAML-mapped entity analysis" silently does nothing and the leak guard the user paid for is absent | mitigate | Task 1 injects the resolver via a conditional fragment; the dogfood proves the wired path actually runs and is correct (depends on 28-05's CR-01 fix so wiring exposes correct behavior, not the false-positive storm) |
| T-28-06-mergecrash | Denial of Service (consumer CI) | A base-file @ObjectMetadataResolver reference would crash config-merge for every consumer WITHOUT phpstan-doctrine, breaking their PHPStan entirely | mitigate | Wiring lives ONLY in the conditionally-included extension-doctrine.neon; base extension.neon stays resolver-less; no-doctrine dogfood (base-only) proves the absent-phpstan-doctrine path loads |
| T-28-06-loadtime-regression | Tampering (latent) | A future hard Doctrine class reference at rule class-load time would silently pass CI and break consumers without Doctrine | mitigate | WR-05: the no-doctrine lane runs the rules' tests + a base dogfood with Doctrine + phpstan-doctrine removed, failing loudly on any load-time hard reference |

Rationale: this plan's security value is making the leak-detection guard ACTUALLY function for consumers who opted into full coverage (WR-01) while never breaking the guarded-optional contract (WR-05). Sequencing after 28-05 ensures wiring exposes a correct rule, not a false-positive generator.
</threat_model>

<verification>
- vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon --memory-limit=512M exits 0 (wired metadata path clean over src/).
- vendor/bin/phpstan analyse -c phpstan-extension-dogfood-nodoctrine.neon --memory-limit=512M exits 0 in an environment with Doctrine present (sanity: base dogfood still loads); the true no-doctrine assertion runs in CI.
- vendor/bin/phpstan analyse exits 0 (bundle self-analysis; no neon leak into phpstan.neon).
- vendor/bin/phpunit (full suite) exits 0 — no regression.
- .github/workflows/ci.yml is valid YAML; only the no-doctrine job changed.
</verification>

<success_criteria>
- VERIFICATION truth #10 (WR-01) closed: ObjectMetadataResolver is injected when phpstan-doctrine is installed (metadata path reachable), proven by the wired dogfood; base extension.neon stays resolver-less for the degrade path.
- WR-05 closed: a no-doctrine CI lane runs tests/Unit/PHPStan + a base dogfood with Doctrine + phpstan-doctrine removed, proving graceful degradation.
- Bundle level-9 self-analysis + full suite + both dogfoods green; no neon leak into phpstan.neon (Pitfall 4 intact).
</success_criteria>

<output>
Create `.planning/phases/28-phpstan-extension/28-06-SUMMARY.md` when done. Record: how the duplicate-registration risk was resolved (distinct service key / fragment-only registration / dogfood composition) and the verified single-fire behavior; the exact includes structure of the three dogfood configs; whether phpstan/phpstan-doctrine was removed in the no-doctrine job and how tests/Unit/PHPStan stayed green (metadata-test self-skip); confirmation that the with-doctrine phpstan job was untouched and phpstan.neon never includes any extension neon.
</output>
