---
phase: 28-phpstan-extension
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
autonomous: true
requirements: [DX-03]
files_modified:
  - .github/workflows/ci.yml
  - phpstan-extension-dogfood.neon
user_setup: []

must_haves:
  truths:
    - "All three rule test suites pass together (full vendor/bin/phpunit green)"
    - "The bundle's own level-9 phpstan analyse is green after all of src/PHPStan/ landed"
    - "php-cs-fixer @Symfony is clean across all new PHPStan rule + test + fixture files"
    - "CI runs the PHPStan rule tests (covered by the existing phpunit job over tests/Unit) and a dogfood analysis of the shipped rules against the bundle's own #[Shared]/#[TenantAware] usages"
    - "The extension-installer zero-config auto-load path is documented as a manual-only verification (cannot be reproduced inside the bundle's own RuleTestCase harness)"
  artifacts:
    - path: "phpstan-extension-dogfood.neon"
      provides: "Separate dogfood config: includes the shipped extension.neon and analyses bundle src so the rules run on the bundle's own attribute usages — kept OUT of the level-9 self-analysis phpstan.neon"
      contains: "extension.neon"
    - path: ".github/workflows/ci.yml"
      provides: "CI step running the dogfood analysis (phpstan analyse -c phpstan-extension-dogfood.neon)"
      contains: "phpstan-extension-dogfood.neon"
  key_links:
    - from: "phpstan-extension-dogfood.neon"
      to: "extension.neon"
      via: "includes: array"
      pattern: "extension.neon"
    - from: ".github/workflows/ci.yml"
      to: "phpstan-extension-dogfood.neon"
      via: "CI runs phpstan with the dogfood config"
      pattern: "phpstan-extension-dogfood"
---

<objective>
Close Phase 28 with the integration gate: prove all three rules pass together, the bundle's own level-9 self-analysis is still green, and code style is clean. Then exercise Claude's discretion (per CONTEXT.md) to DOGFOOD the shipped rules on the bundle's own `src` via a SEPARATE config (`phpstan-extension-dogfood.neon`) that `includes:` the shipped `extension.neon` — kept entirely out of the level-9 `phpstan.neon` per the research's naming-collision pitfall. Wire a CI step for the dogfood run and document the extension-installer zero-config auto-load as a manual-only verification.

Purpose: A single green gate proving DX-03 end-to-end (all 5 acceptance criteria), plus standing CI protection that the shipped rules stay loadable and the bundle's own attribute usages stay clean.
Output: `phpstan-extension-dogfood.neon`, a CI dogfood step, and a documented manual-verification note in the SUMMARY.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-phpstan-extension/28-VALIDATION.md
@.planning/phases/28-phpstan-extension/28-01-SUMMARY.md
@.planning/phases/28-phpstan-extension/28-02-SUMMARY.md
@.planning/phases/28-phpstan-extension/28-03-SUMMARY.md

<interfaces>
<!-- Contracts from prior plans + the codebase. -->

extension.neon (Plan 01, package root): parametersSchema + parameters (tenancy.checkSharedEntityLeaks: true) + 3 rule services tagged phpstan.rules.rule + the AttributeHierarchyHelper. MUST NOT be included by phpstan.neon (level-9 self-analysis). The dogfood neon includes it instead.

phpstan.neon (bundle level-9 self-analysis — UNCHANGED): level 9, paths: [src], NO include of extension.neon.

.github/workflows/ci.yml: has a `phpstan:` job (~line 59) running `vendor/bin/phpstan analyse` (~line 72), and a phpunit job. The existing phpunit job over tests/Unit already covers tests/Unit/PHPStan (no phpunit.xml.dist change needed).

Bundle's own #[Shared]/#[TenantAware] usages live on the demo/test entities; the bundle src itself carries the marker attribute DEFINITIONS, not co-present misuse — so the dogfood run should be GREEN (it proves the rules load + run clean on legitimate usage, NOT that they fire).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phpstan-extension-dogfood.neon and wire a CI dogfood step</name>
  <files>phpstan-extension-dogfood.neon, .github/workflows/ci.yml</files>
  <read_first>
    - phpstan.neon (the level-9 self-analysis config — the dogfood neon must NOT be merged into it)
    - extension.neon (Plan 01 — the shipped config the dogfood neon includes)
    - .github/workflows/ci.yml (the existing phpstan job ~line 59 + phpunit job — mirror its setup/steps for the new dogfood step)
    - .planning/phases/28-phpstan-extension/28-RESEARCH.md (§ Pitfall 4 extension.neon naming collision — keep configs separate)
    - .planning/phases/28-phpstan-extension/28-CONTEXT.md (§ Claude's Discretion — dogfooding is the planner/executor's call)
  </read_first>
  <action>
    Create `phpstan-extension-dogfood.neon` at package root: an `includes:` array referencing `extension.neon`, plus `parameters:` with `level: 9` (or a lower level if the shipped rules require it — match what keeps it green), `paths:` targeting the bundle's own attribute-using sources (e.g. `src`, and the demo/test entities that legitimately carry `#[Shared]`/`#[TenantAware]`), and any `ignoreErrors`/`excludePaths` needed so the run is green on legitimate usage. This run proves the shipped rules LOAD and RUN clean on the bundle's own non-misusing code — it is NOT expected to fire. Keep this file entirely separate from `phpstan.neon`; do NOT add any `include` of `extension.neon` to `phpstan.neon`.

    Add a CI step to `.github/workflows/ci.yml` (in the existing phpstan job, after the level-9 self-analysis step, OR as a clearly-named adjacent step) running `vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon`. Ensure `phpstan/extension-installer` + `phpstan/phpstan-doctrine` are present in the CI install (they are in require-dev as of Plan 01, so the existing `composer install` covers them). Name the step clearly (e.g. "PHPStan extension dogfood — run shipped rules on bundle sources").
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon && grep -c 'phpstan-extension-dogfood.neon' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon` exits 0 (shipped rules load + run green on bundle sources).
    - `phpstan-extension-dogfood.neon` contains an `includes:` entry for `extension.neon`.
    - `phpstan.neon` still does NOT include extension.neon — `grep -c 'extension.neon' phpstan.neon` is 0.
    - `.github/workflows/ci.yml` references `phpstan-extension-dogfood.neon` (grep count >= 1).
  </acceptance_criteria>
  <done>Dogfood config loads the shipped rules against the bundle's own sources and is green in CI; the level-9 self-analysis config stays separate.</done>
</task>

<task type="auto">
  <name>Task 2: Phase integration gate — full suite + level-9 + cs-fixer green, and document the manual extension-installer verification</name>
  <files>.planning/phases/28-phpstan-extension/28-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/28-phpstan-extension/28-VALIDATION.md (§ Per-Task Verification Map — all 11 acceptance rows must be claimed; § Manual-Only Verifications — the two manual items to document)
    - .planning/phases/28-phpstan-extension/28-01-SUMMARY.md, 28-02-SUMMARY.md, 28-03-SUMMARY.md (what each prior plan delivered + recorded gotchas)
  </read_first>
  <action>
    Run the full phase gate: `vendor/bin/phpunit` (all suites, including the three new RuleTestCase files), `vendor/bin/phpstan analyse` (level-9 self-analysis), `vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon` (dogfood), and `vendor/bin/php-cs-fixer check --diff` (style). All MUST be green. If cs-fixer reports fixable drift on the new files, run `vendor/bin/php-cs-fixer fix` and re-verify (mind the project-memory cs-fixer quirks: no same-namespace use statements, optional-before-required param deprecation — fix at the source, do not re-introduce stripped imports).

    Author `28-04-SUMMARY.md` recording: the final pass/fail of each gate command; a mapping of each DX-03 acceptance criterion (AC1-AC5) to the test that proves it; and the two MANUAL-ONLY verifications from VALIDATION.md that this phase intentionally does NOT automate — (1) extension-installer zero-config auto-load in a real downstream consumer project, (2) the phpstan-doctrine "present" ObjectMetadataResolver path against XML/YAML-mapped entities — with the exact manual steps to run each (so /gsd:verify-work and Phase 29 docs can reference them). Note that the reflection fallback is the CI-tested primary path for Rule 3.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit && vendor/bin/phpstan analyse && vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon && vendor/bin/php-cs-fixer check --diff</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit` exits 0 (full suite green, all three rule tests included).
    - `vendor/bin/phpstan analyse` exits 0 (level-9 self-analysis).
    - `vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon` exits 0.
    - `vendor/bin/php-cs-fixer check --diff` exits 0 (clean).
    - 28-04-SUMMARY.md maps AC1-AC5 to proving tests and lists the 2 manual-only verifications with steps.
  </acceptance_criteria>
  <done>Whole phase gate green; DX-03 acceptance-to-test mapping + manual-only verifications documented for the verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → shipped extension | CI loads + runs the shipped rules (dogfood) — analysis-time only, no runtime/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-neon-dogfood | Tampering | Dogfood neon merged into the level-9 phpstan.neon would run consumer rules on bundle src with wrong context (Pitfall 4) | mitigate | Dogfood lives in a SEPARATE phpstan-extension-dogfood.neon; phpstan.neon left unchanged (grep gate asserts no extension.neon include in it) |
| T-28-regress | (rule-health) | A future change silently breaks rule loading or makes the bundle's own attribute usage trip the rules | mitigate | CI dogfood step + the three RuleTestCase suites run on every push; full-suite + level-9 gate is the phase exit criterion |
| T-28-manual-gap | Tampering | extension-installer zero-config + phpstan-doctrine present-path are NOT auto-tested (cannot be inside the bundle harness) | accept | Documented as manual-only verifications with exact steps; reflection fallback is the CI-tested primary path; deferred consumer-project proof noted for Phase 29 docs |

Rationale: this plan is verification + CI wiring, analysis-time only. The accepted manual-only gap (extension-installer / phpstan-doctrine present-path) is inherent to testing a consumer-facing extension from inside its own package and is fully documented.
</threat_model>

<verification>
- `vendor/bin/phpunit` exits 0 (full suite).
- `vendor/bin/phpstan analyse` exits 0 (level-9).
- `vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon` exits 0.
- `vendor/bin/php-cs-fixer check --diff` exits 0.
</verification>

<success_criteria>
- All five DX-03 acceptance criteria proven green together and mapped to tests in the SUMMARY.
- Bundle's own level-9 self-analysis unaffected (DX-03 non-regression).
- Standing CI dogfood protection for the shipped rules.
- Manual-only verifications (extension-installer auto-load, phpstan-doctrine present-path) documented with steps for /gsd:verify-work and Phase 29.
</success_criteria>

<output>
Create `.planning/phases/28-phpstan-extension/28-04-SUMMARY.md` when done. Include the AC1-AC5 → test mapping table and the two manual-only verification procedures.
</output>
