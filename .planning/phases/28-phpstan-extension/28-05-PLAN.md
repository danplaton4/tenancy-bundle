---
phase: 28-phpstan-extension
plan: 05
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
requirements: [DX-03]
files_modified:
  - src/PHPStan/Rule/TenantIdDriftRule.php
  - tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php
  - tests/Unit/PHPStan/Rule/Fixtures/TenantAwareConcreteChild.php
user_setup: []

must_haves:
  truths:
    # Closes VERIFICATION truth #9 (CR-01) — the metadata path must produce CORRECT results on ORM 3.x
    - "TenantIdDriftRule::checkViaMetadata() reads Doctrine ORM 3.x FieldMapping correctly (FieldMapping is an object, not an array) — a valid #[TenantAware] entity with a non-nullable string tenant_id produces ZERO errors via the metadata path"
    - "The metadata path is exercised in CI: a RuleTestCase test constructs TenantIdDriftRule WITH a real PHPStan\\Type\\Doctrine\\ObjectMetadataResolver and asserts a valid mapped entity is silent AND a missing-tenant_id entity fires"
    # Closes VERIFICATION truth #11 (WR-02) — MappedSuperclass/abstract bases must not false-fire
    - "A #[TenantAware] #[ORM\\MappedSuperclass] base class with no own tenant_id is SILENT (no false positive); a concrete instantiable subclass lacking tenant_id still FIRES"
    # WR-04 folded — false-negative in the security direction
    - "A positionally-declared #[ORM\\Column('tenant_id', 'integer')] / nullable positional column is detected (positional type/nullable args are read, not silently passed)"
    - "Bundle's own vendor/bin/phpstan analyse (level 9) still passes; full vendor/bin/phpunit stays green"
  artifacts:
    - path: "src/PHPStan/Rule/TenantIdDriftRule.php"
      provides: "Rule 3 with ORM-3.x-correct metadata path (property access on FieldMapping), MappedSuperclass/abstract skip, positional-arg reading"
      contains: "MappedSuperclass"
    - path: "tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php"
      provides: "Metadata-path coverage via real ObjectMetadataResolver + corrected MappedSuperclass test (parent silent, concrete child fires)"
      contains: "ObjectMetadataResolver"
    - path: "tests/Unit/PHPStan/Rule/Fixtures/TenantAwareConcreteChild.php"
      provides: "Concrete instantiable #[ORM\\Entity] subclass of TenantAwareParent with NO tenant_id — proves the child still fires while the MappedSuperclass parent is silent"
      contains: "TenantAwareParent"
  key_links:
    - from: "src/PHPStan/Rule/TenantIdDriftRule.php"
      to: "Doctrine\\ORM\\Mapping\\FieldMapping (ORM 3.x)"
      via: "property access (columnName/nullable/type) or ArrayAccess-aware guard on $metadata->fieldMappings"
      pattern: "columnName"
    - from: "src/PHPStan/Rule/TenantIdDriftRule.php"
      to: "Doctrine\\ORM\\Mapping\\MappedSuperclass + ReflectionClass::isAbstract()"
      via: "skip non-instantiable bases before evaluating the column"
      pattern: "isAbstract|MappedSuperclass"
    - from: "tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php"
      to: "PHPStan\\Type\\Doctrine\\ObjectMetadataResolver"
      via: "getRule() variant constructs the rule WITH a real resolver to exercise checkViaMetadata()"
      pattern: "ObjectMetadataResolver"
---

<objective>
Close Gap 1 (CR-01, BLOCKER) and Gap 3 (WR-02, WARNING) from 28-VERIFICATION.md, and fold in WR-04 — all of which live in `src/PHPStan/Rule/TenantIdDriftRule.php` and its test. Consolidating every change to these two files into one plan avoids the file-conflict serialization that would otherwise force Gaps 1 and 3 into separate waves.

Gap 1 (CR-01): `checkViaMetadata()` is broken on Doctrine ORM 3.x — `FieldMapping` entries are objects implementing `ArrayAccess`, not arrays, so the `if (!is_array($mapping)) continue;` guard skips every mapping and fires a false "no tenant_id column" on every valid `#[TenantAware]` entity. Fix via direct `$metadata->fieldMappings` property access (`$fm->columnName` / `$fm->nullable` / `$fm->type`), which also resolves IN-02 (the brittle `(array) $metadata` cast). Add a RuleTestCase test that constructs the rule WITH a real `ObjectMetadataResolver` so the metadata path is exercised in CI (its zero coverage is why CR-01 shipped).

Gap 3 (WR-02): the rule false-fires on `#[TenantAware]` `#[ORM\MappedSuperclass]` / abstract bases that legitimately defer `tenant_id` to concrete subclasses. Skip abstract + MappedSuperclass classes (when no tenant_id is in their own hierarchy) and correct `testFiresOnInheritedTenantAware()` so the MappedSuperclass parent is SILENT while a concrete child still fires.

WR-04 (folded): `evaluateFinding()` / `checkViaReflection()` read only NAMED `nullable`/`type` args on `#[ORM\Column]`; positional args silently pass. Read positional args too so a positionally-declared drift isn't missed.

Purpose: the D-02 metadata path is the consumer-facing feature the bundle advertises in `composer.json` suggest; shipping it broken (false-positive storm) trains consumers to suppress the rule, defeating its leak-detection value (security-relevant). WR-04 closes the one false-negative direction.
Output: ORM-3.x-correct Rule 3 + metadata-path test coverage + MappedSuperclass exemption + corrected hierarchy test + positional-arg reading.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-phpstan-extension/28-CONTEXT.md
@.planning/phases/28-phpstan-extension/28-VERIFICATION.md
@.planning/phases/28-phpstan-extension/28-REVIEW.md
@.planning/phases/28-phpstan-extension/28-02-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase + vendor. Do NOT re-explore. -->

Current rule (src/PHPStan/Rule/TenantIdDriftRule.php):
  __construct(private readonly ?object $objectMetadataResolver = null)  — ?object so the class loads when phpstan-doctrine is absent (D-02).
  processNode(InClassNode): array — guards interface_exists(Doctrine\ORM\EntityManagerInterface), then hasTenantAwareInHierarchy(); if resolver !== null && class_exists(PHPStan\Type\Doctrine\ObjectMetadataResolver) → checkViaMetadata($metadata, $className); else checkViaReflection().
  checkViaMetadata(object $metadata, string $className) — CURRENTLY BROKEN: $raw = (array) $metadata; $fieldMappings = $raw['fieldMappings'] ?? []; foreach { if (!is_array($mapping)) continue; ... } — every ORM 3.x FieldMapping is skipped (BUG CR-01).
  checkViaReflection(ClassReflection, string) — walks #[ORM\Column] across hierarchy; resolves colName = $args['name'] ?? $args[0] ?? camelCase->snake_case; reads $args['nullable'] ?? false and $args['type'] (NAMED ONLY — BUG WR-04).
  evaluateFinding(?array{nullable?:bool,type?:string|null}, string): list<IdentifierRuleError> — null→"no column mapped to tenant_id"; nullable→nullable error; non-string type→type error. STRING_TYPES = ['string','ascii_string','guid','uuid'] (case-insensitive). Identifier: tenancy.tenantIdDrift.

Doctrine ORM 3.x FieldMapping (vendor/doctrine/orm/src/Mapping/FieldMapping.php — CONFIRMED):
  final class FieldMapping implements ArrayAccess {
    public function __construct(public string $type, public string $fieldName, public string $columnName) {}
    public bool|null $nullable = null;  // public promoted/declared properties
    // also: length, id, unique, options, etc. — all PUBLIC.
  }
  ClassMetadata::$fieldMappings is array<string, FieldMapping> keyed by PROPERTY name. Column name is $fm->columnName.
  Both property access ($fm->columnName) AND ArrayAccess ($fm['columnName']) work on ORM 3.x; only property access avoids the (array) cast (IN-02).

ObjectMetadataResolver (vendor/phpstan/phpstan-doctrine/src/Type/Doctrine/ObjectMetadataResolver.php — CONFIRMED):
  __construct(?string $objectManagerLoader, string $tmpDir)
  getClassMetadata(string $className): ?ClassMetadata<T>
  KEY FACT for the test: with $objectManagerLoader = null, getClassMetadata() falls back to a standalone
  PHPStan\Doctrine\Mapping\ClassMetadataFactory($tmpDir) and STILL resolves attribute-mapped (#[ORM\Entity]) classes.
  So `new ObjectMetadataResolver(null, sys_get_temp_dir())` is a fully working resolver for #[ORM\Entity] fixtures in CI —
  no objectManagerLoader/EntityManager needed. Returns null for #[ORM\MappedSuperclass] / transient classes.

Existing fixtures (tests/Unit/PHPStan/Rule/Fixtures/):
  TenantAwareParent.php — #[TenantAware] #[ORM\MappedSuperclass], id column only, NO tenant_id (the WR-02 base).
  TenantIdMissingChild.php — #[ORM\Entity] extends TenantAwareParent, $name column, NO tenant_id (inherits #[TenantAware]).
  TenantIdValidClean.php — #[TenantAware] #[ORM\Entity] with non-nullable string tenant_id (the metadata-path "silent" fixture).
  TenantIdMissingViolating.php — #[TenantAware] #[ORM\Entity], NO tenant_id (the metadata-path "fires" fixture).

Current test (tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php):
  getRule() returns `new TenantIdDriftRule()` (NO resolver) — checkViaMetadata has ZERO coverage.
  getAdditionalConfigFiles() returns [__DIR__.'/../../../../extension.neon'].
  testFiresOnInheritedTenantAware() CURRENTLY asserts TWO errors: TenantAwareParent (line 10) AND TenantIdMissingChild (line 9).
  The TenantAwareParent assertion CODIFIES THE WR-02 BUG and MUST be removed.

WR-02 fix shape (from REVIEW.md): after hasTenantAwareInHierarchy() and BEFORE checking the column, in processNode():
  $nativeReflection = $classReflection->getNativeReflection();
  if ($nativeReflection->isAbstract() || [] !== $nativeReflection->getAttributes(\Doctrine\ORM\Mapping\MappedSuperclass::class)) { return []; }
  (Optionally also #[ORM\Embeddable].) This must run for BOTH the metadata and reflection paths, so place it in processNode before the path branch.

WR-04 fix shape (from REVIEW.md): ORM\Column constructor positional order has type at index 1 and nullable at index 6.
  $nullableRaw = $args['nullable'] ?? $args[6] ?? false; $typeRaw = $args['type'] ?? $args[1] ?? null;
  Apply in checkViaReflection where $found is built (the named-only read at lines ~183-184).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01 (ORM 3.x metadata path) + WR-04 (positional args) in TenantIdDriftRule</name>
  <files>src/PHPStan/Rule/TenantIdDriftRule.php</files>
  <read_first>
    - src/PHPStan/Rule/TenantIdDriftRule.php (the file being fixed — checkViaMetadata ~110-139, checkViaReflection ~155-199, evaluateFinding ~208-249)
    - .planning/phases/28-phpstan-extension/28-VERIFICATION.md (gap #1 `missing:` array — the prescribed CR-01 fix; Anti-Patterns rows 117-118, 124-126, 183-184)
    - .planning/phases/28-phpstan-extension/28-REVIEW.md (CR-01 fix block lines 109-133; WR-04 fix block lines 241-250; IN-02 lines 296-308)
    - vendor/doctrine/orm/src/Mapping/FieldMapping.php (the ORM 3.x FieldMapping object shape — public $columnName/$nullable/$type)
    - ./CLAUDE.md (Doctrine is OPTIONAL — guard with class_exists/interface_exists; PHPStan level 9; @Symfony cs-fixer — NO same-namespace use statements)
  </read_first>
  <behavior>
    - Given an ObjectMetadataResolver returning real ORM 3.x ClassMetadata for a valid #[TenantAware] entity (non-nullable string tenant_id) → checkViaMetadata returns [] (NO false positive). This is the exact CR-01 regression.
    - Given metadata for a #[TenantAware] entity with NO tenant_id column → checkViaMetadata returns one tenancy.tenantIdDrift "no column mapped to tenant_id" error.
    - Given metadata for a tenant_id column that is nullable → nullable error; non-string type → type error (parity with the reflection path).
    - Given a reflection-path #[ORM\Column('tenant_id', 'integer')] (positional type at index 1) → non-string-type error fires (WR-04: positional type read).
    - Given a reflection-path #[ORM\Column('tenant_id', 'string', 63, null, null, false, true)] (positional nullable=true at index 6) → nullable error fires (WR-04: positional nullable read).
  </behavior>
  <action>
    In `checkViaMetadata()`: replace the broken `(array) $metadata` cast + `is_array($mapping)` loop with direct property access on the public `fieldMappings` collection (resolves CR-01 AND IN-02). Read the field mappings from `$metadata->fieldMappings` (public `array<string, FieldMapping>` on ORM 3.x; guard with `property_exists`/`isset` for cross-version safety per REVIEW.md). For each `$fm`, read the column name via `$fm->columnName` (ORM 3.x FieldMapping property). When the column name equals `tenant_id`, build the `$found` array reading `$fm->nullable` (cast to bool) and `$fm->type` (string|null), then `break`. Pass `$found` to the existing `evaluateFinding()`. Because `$metadata` is typed `object` (no hard ClassMetadata import — D-02 / CLAUDE.md optional-dep rule), access the dynamic properties in a way that stays PHPStan level-9 clean WITHOUT introducing `@phpstan-ignore-line` (the prior attempt at ignore comments produced unmatched-ignore errors per 28-02-SUMMARY deviation #2). Prefer narrowing via `isset($metadata->fieldMappings)` / iterating an `iterable`, and read each `$fm`'s columnName/nullable/type via the ArrayAccess-aware accessor (`$fm instanceof \ArrayAccess ? $fm['columnName'] : $fm->columnName`) OR pure property access guarded by `property_exists` — whichever keeps level 9 green. Do NOT reintroduce the `is_array()`-only guard.

    In `checkViaReflection()` where `$found` is built (the named-only `$args['nullable'] ?? false` / `$args['type']` read): also fall back to positional indices when named keys are absent — `nullable` at positional index 6, `type` at positional index 1 — mirroring the existing `name` resolution (`$args['name'] ?? $args[0]`). Keep the `is_string()` guard on the resolved type. This is WR-04.

    Keep the `tenancy.tenantIdDrift` identifier, the STRING_TYPES list, and all three evaluateFinding branches unchanged. Do NOT touch processNode's branch logic in this task (WR-02 skip is Task 2). No same-namespace use statements; @Symfony cs-fixer compliant; strict_types intact.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpstan analyse && vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpstan analyse` exits 0 (level-9 self-analysis green; no new errors, no `@phpstan-ignore` suppressions introduced).
    - `grep -c 'is_array($mapping)' src/PHPStan/Rule/TenantIdDriftRule.php` is 0 (the broken ORM-3.x guard is gone).
    - `grep -c '(array) $metadata' src/PHPStan/Rule/TenantIdDriftRule.php` is 0 (IN-02 cast removed).
    - `grep -E 'columnName' src/PHPStan/Rule/TenantIdDriftRule.php` matches (metadata path reads FieldMapping columnName).
    - checkViaReflection reads positional fallbacks: file contains both `$args[6]` (nullable) and `$args[1]` (type) — `grep -c '\$args\[6\]' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1 AND `grep -c '\$args\[1\]' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1.
    - The 5 existing TenantIdDriftRuleTest tests still pass (reflection-path regression intact) — note testFiresOnInheritedTenantAware is corrected in Task 2.
  </acceptance_criteria>
  <done>checkViaMetadata reads ORM 3.x FieldMapping objects correctly (no false "missing tenant_id" on valid entities); positional nullable/type args are read; phpstan L9 green; no ignore comments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 — skip MappedSuperclass/abstract bases + correct the hierarchy test</name>
  <files>src/PHPStan/Rule/TenantIdDriftRule.php, tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php, tests/Unit/PHPStan/Rule/Fixtures/TenantAwareConcreteChild.php</files>
  <read_first>
    - src/PHPStan/Rule/TenantIdDriftRule.php (processNode ~49-83 — where the skip guard goes, after hasTenantAwareInHierarchy, before the path branch)
    - tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php (testFiresOnInheritedTenantAware lines ~104-126 — codifies the WR-02 bug, MUST be corrected)
    - tests/Unit/PHPStan/Rule/Fixtures/TenantAwareParent.php (#[TenantAware] #[ORM\MappedSuperclass] base — must become SILENT)
    - tests/Unit/PHPStan/Rule/Fixtures/TenantIdMissingChild.php (#[ORM\Entity] concrete child, inherits #[TenantAware], no tenant_id — must still FIRE; reports at line 9)
    - .planning/phases/28-phpstan-extension/28-REVIEW.md (WR-02 fix block lines 188-198)
    - .planning/phases/28-phpstan-extension/28-CONTEXT.md (D-04 — hierarchy awareness; the AbstractTenant split is exactly this pattern)
  </read_first>
  <behavior>
    - Given TenantAwareParent (#[TenantAware] #[ORM\MappedSuperclass], no tenant_id) → SILENT (skipped: non-instantiable base defers tenant_id to subclasses). This is the WR-02 fix.
    - Given TenantIdMissingChild (#[ORM\Entity] concrete, inherits #[TenantAware], no tenant_id) → FIRES one tenancy.tenantIdDrift "no column" error (concrete entity must resolve tenant_id).
    - Given an abstract #[TenantAware] class with no tenant_id → SILENT.
    - Given TenantIdValidClean (#[TenantAware] #[ORM\Entity], valid tenant_id) → still SILENT (no regression).
  </behavior>
  <action>
    In `processNode()`, after the `hasTenantAwareInHierarchy()` early-return and BEFORE the metadata/reflection branch, add a skip guard for non-instantiable bases: obtain `$nativeReflection = $classReflection->getNativeReflection()` and return `[]` when the class is abstract (`$nativeReflection->isAbstract()`) OR carries `#[\Doctrine\ORM\Mapping\MappedSuperclass]` (`[] !== $nativeReflection->getAttributes(\Doctrine\ORM\Mapping\MappedSuperclass::class)`). Optionally also skip `#[\Doctrine\ORM\Mapping\Embeddable]`. This runs for both code paths (it precedes the branch), so a MappedSuperclass parent is silent regardless of whether the resolver is wired. Document inline that only concrete, instantiable entities are required to resolve a tenant_id (concrete subclasses declare their own column — the bundle's own AbstractTenant split is this pattern). Keep phpstan L9 clean (the getNativeReflection() return is a ClassReflection-native type — getAttributes()/isAbstract() are available; mirror how hasTenantAwareInHierarchy already calls getNativeReflection()->getAttributes(TenantAware::class)).

    Create `tests/Unit/PHPStan/Rule/Fixtures/TenantAwareConcreteChild.php`: namespace `Tenancy\Bundle\Tests\Unit\PHPStan\Rule\Fixtures`, `declare(strict_types=1)`, `use Doctrine\ORM\Mapping as ORM;`. A `#[ORM\Entity]` concrete class extending `TenantAwareParent` (inherits #[TenantAware]) with one non-tenant column (e.g. `#[ORM\Column(type: 'string')] private string $title;`) and NO tenant_id — mirrors TenantIdMissingChild but is the dedicated WR-02 "concrete child fires" fixture. (Reuse TenantIdMissingChild directly if it already serves; this fixture documents the intent explicitly.)

    Rewrite `testFiresOnInheritedTenantAware()` in TenantIdDriftRuleTest.php: REMOVE the assertion that TenantAwareParent fires (it codifies the bug). Assert the analysis of [TenantAwareParent.php, TenantIdMissingChild.php] produces EXACTLY ONE error — on TenantIdMissingChild (the concrete child, at its actual line, currently 9) — and NO error on TenantAwareParent. Rename the test if clearer (e.g. `testMappedSuperclassParentSilentConcreteChildFires`) but keep it a real RuleTestCase assertion. Add a focused test `testSilentOnMappedSuperclassBase()` analysing TenantAwareParent.php alone and asserting ZERO errors. Confirm actual fixture line numbers by running the test (the executor adjusts to the real line, as prior tasks did).
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage && vendor/bin/phpstan analyse</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` exits 0.
    - src/PHPStan/Rule/TenantIdDriftRule.php contains the MappedSuperclass/abstract skip: `grep -E 'isAbstract|MappedSuperclass' src/PHPStan/Rule/TenantIdDriftRule.php` matches BOTH `isAbstract` and `MappedSuperclass`.
    - TenantIdDriftRuleTest.php no longer asserts TenantAwareParent fires: `grep -c "TenantAwareParent::class.' is #\[TenantAware\] but has no column" tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php` is 0.
    - A test asserting the MappedSuperclass base is silent exists AND a test asserting the concrete child fires exists (the corrected hierarchy test asserts exactly one error, on the concrete child).
    - `vendor/bin/phpstan analyse` exits 0.
  </acceptance_criteria>
  <done>MappedSuperclass/abstract #[TenantAware] bases are silent; concrete subclasses lacking tenant_id still fire; the test that codified the WR-02 false positive is corrected to parent-silent/child-fires.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Metadata-path CI coverage — resolver-injected RuleTestCase test (closes CR-01's "invisible to CI")</name>
  <files>tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php</files>
  <read_first>
    - src/PHPStan/Rule/TenantIdDriftRule.php (the metadata path fixed in Task 1 — checkViaMetadata, constructor ?object resolver)
    - tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php (current getRule() with NO resolver; getAdditionalConfigFiles path)
    - vendor/phpstan/phpstan-doctrine/src/Type/Doctrine/ObjectMetadataResolver.php (constructor `(?string $objectManagerLoader, string $tmpDir)`; getClassMetadata falls back to standalone ClassMetadataFactory($tmpDir) when objectManagerLoader is null — works for #[ORM\Entity] fixtures)
    - tests/Unit/PHPStan/Rule/Fixtures/TenantIdValidClean.php (the valid mapped entity — metadata path must be SILENT on it)
    - tests/Unit/PHPStan/Rule/Fixtures/TenantIdMissingViolating.php (missing tenant_id — metadata path must FIRE on it)
    - .planning/phases/28-phpstan-extension/28-VERIFICATION.md (gap #1 `missing:` item 3 — "Add a RuleTestCase test that constructs TenantIdDriftRule WITH a real ObjectMetadataResolver and a mapped entity fixture")
  </read_first>
  <behavior>
    - With the rule constructed WITH `new \PHPStan\Type\Doctrine\ObjectMetadataResolver(null, sys_get_temp_dir())`: analysing TenantIdValidClean.php → ZERO errors (this is the exact CR-01 case that previously false-fired on EVERY valid entity).
    - With the same resolver-injected rule: analysing TenantIdMissingViolating.php → one tenancy.tenantIdDrift "no column mapped to tenant_id" error (proves the metadata path produces CORRECT results, not just silence).
    - The existing reflection-path tests (rule built with NO resolver) continue to pass unchanged.
  </behavior>
  <action>
    Add metadata-path coverage to TenantIdDriftRuleTest.php WITHOUT breaking the existing reflection-path tests. Because `getRule()` is shared by all `analyse()` calls in a RuleTestCase, make the resolver injectable per-test: add a private nullable property (e.g. `?object $resolver = null`) and have `getRule()` return `new TenantIdDriftRule($this->resolver)` (null → reflection path, as today). In each metadata test, set `$this->resolver = new \PHPStan\Type\Doctrine\ObjectMetadataResolver(null, sys_get_temp_dir());` before calling `analyse()`. Guard each metadata test with `if (!class_exists(\PHPStan\Type\Doctrine\ObjectMetadataResolver::class)) { self::markTestSkipped('phpstan-doctrine not installed'); }` so the no-doctrine / no-phpstan-doctrine lane self-skips (mirrors the bundle's class_exists skip-guard convention) — phpstan-doctrine IS in require-dev so the test RUNS in the standard CI lane.

    Add two tests:
      - `testMetadataPathSilentOnValidEntity()` — resolver injected; analyse TenantIdValidClean.php; assert ZERO errors. THIS is the CR-01 regression guard.
      - `testMetadataPathFiresOnMissingTenantId()` — resolver injected; analyse TenantIdMissingViolating.php; assert one tenancy.tenantIdDrift "no column mapped to tenant_id" error (use the same message + actual line as the reflection test asserts, currently line 10). This proves the metadata path produces correct POSITIVE results, not just silence (a path that always returns [] would also pass a silence-only test).

    Update the getRule() docblock to note the resolver is now per-test injectable (was hardcoded null). Keep getAdditionalConfigFiles() and all existing tests intact.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage && vendor/bin/phpstan analyse</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` exits 0 (metadata tests RUN, not skipped, because phpstan-doctrine is in require-dev).
    - TenantIdDriftRuleTest.php references the resolver: `grep -c 'ObjectMetadataResolver' tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php` >= 1.
    - A metadata test asserts ZERO errors on the valid entity (the CR-01 regression guard) AND a metadata test asserts a FIRING error on the missing-tenant_id entity (proves correct positive result).
    - Each metadata test carries a `class_exists(...ObjectMetadataResolver...)` skip-guard so the no-phpstan-doctrine lane self-skips.
    - `vendor/bin/phpstan analyse` exits 0.
  </acceptance_criteria>
  <done>The previously-zero-coverage metadata path is exercised in CI: a resolver-injected test proves a valid entity is silent (CR-01 closed) and a missing-tenant_id entity fires (correct positive). CR-01 can no longer ship invisibly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer entity mappings → static analysis | Rule 3 reads Doctrine ClassMetadata / #[ORM\Column] attributes at analysis time only — no runtime, no untrusted input |
| advertised phpstan-doctrine integration → consumer CI | The metadata path runs in real consumer projects that follow the composer.json `suggest` (install phpstan/phpstan-doctrine) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-fp-metadata | (rule-health / Elevation via leak) | CR-01: broken metadata path fires a false "missing tenant_id" on EVERY valid #[TenantAware] entity, training consumers to disable the rule and losing the leak guard | mitigate | Task 1 fixes FieldMapping property access for ORM 3.x; Task 3 adds a resolver-injected regression test so the false-positive storm can never ship invisibly again |
| T-28-05-fp-mappedsuper | (rule-health) | WR-02: false positive on #[TenantAware] MappedSuperclass/abstract bases that legitimately defer tenant_id, training suppression | mitigate | Task 2 skips non-instantiable bases; corrected test asserts parent-silent/child-fires so the exemption is locked in CI |
| T-28-05-fn-positional | Tampering (false negative, security direction) | WR-04: a positionally-declared nullable/non-string tenant_id (#[ORM\Column('tenant_id','integer')] or nullable at index 6) is a real cross-tenant leak risk silently passed by the named-only read | mitigate | Task 1 reads positional type (index 1) and nullable (index 6) fallbacks; this closes the one false-negative direction in an otherwise precision-first rule |
| T-28-05-metadata-degrade | (availability of guard) | When phpstan-doctrine is absent the metadata path is unreachable; rule degrades to reflection (D-02) | accept | Reflection fallback is the primary CI-tested path and covers attribute-mapped entities; metadata path is the documented "full coverage" upgrade — degrade-to-reflection is the locked D-02 design |

Rationale: all three fixes target the false-positive direction except WR-04 (the lone false negative). A precision-first leak-detection rule only protects if it stays enabled — false positives that train suppression are the dominant residual risk, and the new CI coverage makes the metadata-path correctness provable rather than assumed.
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` exits 0 (reflection-path + metadata-path + MappedSuperclass tests all green).
- `vendor/bin/phpunit` (full suite) exits 0 — no regression across the 757-test suite.
- `vendor/bin/phpstan analyse` exits 0 (level-9 self-analysis green; no new @phpstan-ignore suppressions).
- `vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon --memory-limit=512M` exits 0 (dogfood stays green; the reflection path runs over src/ which has no phpstan-doctrine resolver wired yet — that wiring is Plan 28-06).
- `vendor/bin/php-cs-fixer check --diff` clean for modified/new files.
</verification>

<success_criteria>
- VERIFICATION truth #9 (CR-01) closed: the metadata path produces CORRECT results on ORM 3.x AND is exercised by a resolver-injected test.
- VERIFICATION truth #11 (WR-02) closed: the MappedSuperclass parent is silent; the concrete child fires; the bug-codifying test is corrected.
- WR-04 closed: positional nullable/type args on #[ORM\Column] are read (no false negative).
- IN-02 resolved: the brittle `(array) $metadata` cast is replaced by property access.
- Bundle level-9 self-analysis + full suite + dogfood all green.
</success_criteria>

<output>
Create `.planning/phases/28-phpstan-extension/28-05-SUMMARY.md` when done. Record: the exact metadata-access shape used to stay level-9 clean (property access vs ArrayAccess accessor) and whether any narrowing helper was needed; the actual fixture line numbers asserted in the corrected hierarchy test; whether TenantAwareConcreteChild was created or TenantIdMissingChild reused; the metadata-test skip-guard form; confirmation that no `@phpstan-ignore` comments were introduced.
</output>
