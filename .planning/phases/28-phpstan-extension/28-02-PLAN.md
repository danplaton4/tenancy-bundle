---
phase: 28-phpstan-extension
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
autonomous: true
requirements: [DX-03]
files_modified:
  - src/PHPStan/Rule/TenantIdDriftRule.php
  - tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php
  - tests/Unit/PHPStan/Rule/data/tenant-id-drift-violating.php
  - tests/Unit/PHPStan/Rule/data/tenant-id-drift-clean.php
user_setup: []

must_haves:
  truths:
    - "Rule 3 fires when a #[TenantAware] entity has NO column mapped to tenant_id"
    - "Rule 3 fires when the tenant_id column is mapped nullable"
    - "Rule 3 fires when the tenant_id column is mapped to a non-string type (e.g. integer)"
    - "Rule 3 stays silent on a valid #[TenantAware] entity (non-nullable string tenant_id)"
    - "Rule 3 uses the reflection #[ORM\\Column] fallback as the primary tested path (degrades cleanly when phpstan-doctrine ObjectMetadataResolver is absent)"
    - "Rule 3 is hierarchy-aware (a #[TenantAware] marker on a parent / MappedSuperclass is detected)"
    - "Bundle's own vendor/bin/phpstan analyse (level 9) still passes"
  artifacts:
    - path: "src/PHPStan/Rule/TenantIdDriftRule.php"
      provides: "Rule 3 — tenant_id config-drift InClassNode rule (reflection fallback primary + optional ObjectMetadataResolver path)"
      contains: "tenancy.tenantIdDrift"
    - path: "tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php"
      provides: "RuleTestCase coverage: missing / nullable / non-string / clean"
      contains: "RuleTestCase"
    - path: "tests/Unit/PHPStan/Rule/data/tenant-id-drift-violating.php"
      provides: "Fixtures: missing tenant_id, nullable tenant_id, integer tenant_id (+ inherited #[TenantAware])"
      contains: "TenantAware"
    - path: "tests/Unit/PHPStan/Rule/data/tenant-id-drift-clean.php"
      provides: "Fixture: valid non-nullable string tenant_id"
      contains: "tenant_id"
  key_links:
    - from: "src/PHPStan/Rule/TenantIdDriftRule.php"
      to: "src/Filter/TenantAwareFilter.php"
      via: "validates the literal hardcoded column name tenant_id"
      pattern: "tenant_id"
    - from: "src/PHPStan/Rule/TenantIdDriftRule.php"
      to: "Tenancy\\Bundle\\PHPStan\\Rule\\Helper\\AttributeHierarchyHelper"
      via: "constructor-injected helper for the #[TenantAware] hierarchy walk"
      pattern: "hasAttributeInHierarchy"
---

<objective>
Replace the Plan 01 skeleton of `TenantIdDriftRule` with the full Rule 3 implementation: detect `#[TenantAware]` entities whose `tenant_id` column is missing, nullable, or non-string (D-04). The reflection `#[ORM\Column]` scan is the PRIMARY, fully-tested path; the `phpstan/phpstan-doctrine` `ObjectMetadataResolver` path is guarded (`class_exists` / nullable injection) and degrades cleanly when absent. Add `RuleTestCase` coverage with violating + clean Doctrine-entity fixtures.

Purpose: Rule 3 catches the `tenant_id` configuration drift that would silently break the runtime `TenantAwareFilter` (which compares `"%s.tenant_id = '%s'"` against a quoted string slug) — an `int`/nullable/missing column is a latent cross-tenant leak (DX-03-AC3).
Output: Working Rule 3 + test (4 cases) + fixtures.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-phpstan-extension/28-CONTEXT.md
@.planning/phases/28-phpstan-extension/28-RESEARCH.md
@.planning/phases/28-phpstan-extension/28-PATTERNS.md
@.planning/phases/28-phpstan-extension/28-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + the codebase. Do not re-explore. -->

From Plan 01 (already created):
  src/PHPStan/Rule/TenantIdDriftRule.php — SKELETON. Constructor:
    __construct(private readonly AttributeHierarchyHelper $helper, private readonly ?object $objectMetadataResolver = null)
    getNodeType(): string => InClassNode::class ; processNode(): array currently returns [] with a TODO marker.
  src/PHPStan/Rule/Helper/AttributeHierarchyHelper.php — public hasAttributeInHierarchy(\ReflectionClass, string): bool.
  extension.neon — already registers TenantIdDriftRule (helper arg) tagged phpstan.rules.rule.

The hardcoded scope column (src/Filter/TenantAwareFilter.php line 47): "%s.tenant_id = '%s'" compared against addslashes($tenant->getSlug()) — a QUOTED STRING. Rule 3 validates the literal COLUMN name `tenant_id` (NOT the property name) and that it is a non-nullable string. NOT configurable (D-04).

Doctrine attribute for reflection fallback: Doctrine\ORM\Mapping\Column — args read via ReflectionAttribute::getArguments():
  'name' (explicit column name; absent => Doctrine derives from property name), 'nullable' (bool, default false), 'type' (string, default 'string').

Optional phpstan-doctrine path: PHPStan\Type\Doctrine\ObjectMetadataResolver (guard with class_exists; injected only when present).
String Doctrine types to accept: 'string', 'ascii_string', 'guid' (and Doctrine\DBAL\Types\Types::STRING / ::ASCII_STRING / ::GUID constants).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TenantIdDriftRule reflection fallback + ObjectMetadataResolver guarded path</name>
  <files>src/PHPStan/Rule/TenantIdDriftRule.php</files>
  <read_first>
    - src/PHPStan/Rule/TenantIdDriftRule.php (the Plan 01 skeleton being replaced)
    - src/PHPStan/Rule/Helper/AttributeHierarchyHelper.php (the hierarchy walk it delegates to)
    - src/Filter/TenantAwareFilter.php (lines 24-51 — the hardcoded tenant_id quoted-string comparison Rule 3 mirrors)
    - .planning/phases/28-phpstan-extension/28-PATTERNS.md (§ TenantIdDriftRule — reflection fallback pattern, optional-dep guard, hardcoded column)
    - .planning/phases/28-phpstan-extension/28-RESEARCH.md (§ Example 2 reflection fallback, § Pattern 5 ObjectMetadataResolver, § Pitfall 6 column-vs-property name)
    - src/DependencyInjection/Compiler/SharedEntityMutualExclusionPass.php (lines 43-46 — interface_exists Doctrine guard idiom to mirror)
  </read_first>
  <behavior>
    - Given a #[TenantAware] class with NO #[ORM\Column] mapping to tenant_id → fire tenancy.tenantIdDrift ("missing").
    - Given a #[TenantAware] class with tenant_id mapped nullable: true → fire ("nullable").
    - Given a #[TenantAware] class with tenant_id mapped type: 'integer' (or any non-string) → fire ("non-string type").
    - Given a #[TenantAware] class with a non-nullable string tenant_id → no error.
    - Given a class WITHOUT #[TenantAware] in its hierarchy → no error (return early).
    - Given Doctrine ORM not installed (interface_exists false) → return [] (optional-dep guard).
  </behavior>
  <action>
    In `processNode()`: get `$classReflection = $node->getClassReflection()` and `$nativeReflection = $classReflection->getNativeReflection()`. Return `[]` early if `!interface_exists(\Doctrine\ORM\EntityManagerInterface::class)` (optional-dep guard, mirroring SharedEntityMutualExclusionPass line 44). Return `[]` early unless `$this->helper->hasAttributeInHierarchy($nativeReflection, TenantAware::class)`.

    Resolve the `tenant_id` column mapping. PRIMARY (reflection fallback): walk `$nativeReflection->getProperties()` (and across the hierarchy — also scan parent properties so a tenant_id mapped on a MappedSuperclass is found), read each property's `Doctrine\ORM\Mapping\Column` attributes via `getAttributes(...)->getArguments()`; resolve the column name as `$args['name'] ?? $args[0] ?? <camelCase-to-snake_case of property name>` (per Pitfall 6 — a `$tenantId` property with no explicit name maps to `tenant_id`); when the column name equals `tenant_id`, capture `nullable` (default false) and `type` (default 'string'). OPTIONAL (guarded): if `$this->objectMetadataResolver !== null` AND `class_exists(\PHPStan\Type\Doctrine\ObjectMetadataResolver::class)`, prefer real `ClassMetadata` from it (read fieldMappings keyed by column for nullable/type); on null metadata, fall through to the reflection path. Do NOT hard-import ObjectMetadataResolver (keep `?object` type on the property; reference the FQCN only inside the class_exists guard).

    Emit a single `RuleErrorBuilder::message(...)->identifier('tenancy.tenantIdDrift')->build()` for the FIRST failing condition, with a message that names the class FQCN AND the specific drift kind: missing tenant_id column / tenant_id is nullable / tenant_id maps to non-string type <type>. Accept string types: 'string', 'ascii_string', 'guid' (compare case-insensitively; include the Types::* constant string values). No length assertion (D-04 — explicitly do NOT check VARCHAR length). Annotate `@implements Rule<InClassNode>` and `@return list<\PHPStan\Rules\IdentifierRuleError>`. No fenced same-namespace use statements; @Symfony cs-fixer compliant.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpstan analyse</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpstan analyse` exits 0 (level-9 self-analysis green with the new logic).
    - `grep -c "tenancy.tenantIdDrift" src/PHPStan/Rule/TenantIdDriftRule.php` is >= 1.
    - File contains the optional-dep guard `interface_exists` AND the `class_exists(\PHPStan\Type\Doctrine\ObjectMetadataResolver::class)` guard.
    - File contains NO length/VARCHAR assertion (D-04) — `grep -ci 'varchar\|strlen\|length' src/PHPStan/Rule/TenantIdDriftRule.php` is 0.
    - No `use PHPStan\Type\Doctrine\ObjectMetadataResolver;` import (only referenced inside class_exists guard).
  </acceptance_criteria>
  <done>Rule 3 implements missing/nullable/non-string detection via reflection fallback + guarded ObjectMetadataResolver path; carries tenancy.tenantIdDrift; phpstan L9 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TenantIdDriftRuleTest + violating/clean fixtures (missing, nullable, non-string, valid)</name>
  <files>tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php, tests/Unit/PHPStan/Rule/data/tenant-id-drift-violating.php, tests/Unit/PHPStan/Rule/data/tenant-id-drift-clean.php</files>
  <read_first>
    - src/PHPStan/Rule/TenantIdDriftRule.php (implemented in Task 1 — the message strings + identifier the test asserts)
    - tests/Unit/PHPStan/Rule/MutualExclusionRuleTest.php (Plan 01 — the RuleTestCase header, getRule, getAdditionalConfigFiles relative path to copy)
    - .planning/phases/28-phpstan-extension/28-PATTERNS.md (§ TenantIdDriftRuleTest — 4 required cases; § No Analog Found — use RESEARCH Example 2 column patterns for fixtures)
    - .planning/phases/28-phpstan-extension/28-RESEARCH.md (§ Example 2, § Pitfall 6)
    - src/Attribute/TenantAware.php (the marker fixtures carry)
  </read_first>
  <behavior>
    - testFiresWhenTenantIdMissing: a #[TenantAware] entity with id+name columns but NO tenant_id → one tenancy.tenantIdDrift error.
    - testFiresWhenTenantIdNullable: tenant_id mapped #[ORM\Column(nullable: true)] → one error.
    - testFiresWhenTenantIdNonString: tenant_id mapped #[ORM\Column(type: 'integer')] → one error.
    - testNoViolationForValidTenantId: tenant_id mapped #[ORM\Column(type: 'string')] non-nullable → zero errors.
    - (optional) testFiresOnInheritedTenantAware: #[TenantAware] on a parent, missing tenant_id → fires.
  </behavior>
  <action>
    Create `tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php` (namespace `Tenancy\Bundle\Tests\Unit\PHPStan\Rule`, `final`, extends `PHPStan\Testing\RuleTestCase`, `@extends RuleTestCase<TenantIdDriftRule>`). `getRule()` returns `new TenantIdDriftRule(new AttributeHierarchyHelper())` (no resolver — exercises the reflection fallback PRIMARY path, which is what CI tests). Copy `getAdditionalConfigFiles()` (returns the extension.neon path) from MutualExclusionRuleTest. Add the test methods above, each calling `$this->analyse([fixture], [[message, line]])` with the real fixture line numbers.

    Create `tests/Unit/PHPStan/Rule/data/tenant-id-drift-violating.php` (namespace `Fixtures\PHPStan`, `declare(strict_types=1)`; import `Tenancy\Bundle\Attribute\TenantAware` and `Doctrine\ORM\Mapping as ORM`): three `#[TenantAware]` `#[ORM\Entity]` classes — (a) no tenant_id column, (b) `#[ORM\Column(name: 'tenant_id', nullable: true)]`, (c) `#[ORM\Column(name: 'tenant_id', type: 'integer')]`. Each with at least an id property so it is a plausible entity. Create `tests/Unit/PHPStan/Rule/data/tenant-id-drift-clean.php`: a `#[TenantAware]` `#[ORM\Entity]` class with `#[ORM\Column(name: 'tenant_id', type: 'string')]` (non-nullable). Fixtures need doctrine/orm classes — they are present in require-dev, so the attributes resolve at analysis time.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` exits 0.
    - Test class has >= 4 test methods covering missing / nullable / non-string / valid.
    - `grep -c "tenant_id" tests/Unit/PHPStan/Rule/data/tenant-id-drift-violating.php` is >= 2 (nullable + integer fixtures).
    - clean fixture has a non-nullable string tenant_id column.
  </acceptance_criteria>
  <done>Rule 3 proven for missing/nullable/non-string drift and silent on valid tenant_id, via the reflection fallback path that CI runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer entity source → static analysis | Rule 3 reads #[ORM\Column] attribute args (or ClassMetadata) at analysis time only — no runtime, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-drift | Tampering (→ cross-tenant leak) | Rule 3 false-negative on a misconfigured tenant_id (missing/nullable/int) that silently breaks TenantAwareFilter scoping | mitigate | Reflection fallback covers attribute-mapped entities (the common case) with 3 violation fixtures + a valid fixture; non-string check beyond the acceptance-named conditions catches the int/association latent leak |
| T-28-drift-xml | Tampering | Rule 3 reflection fallback blind to XML/YAML-mapped #[TenantAware] entities | accept | Documented limitation; ObjectMetadataResolver path (phpstan-doctrine) covers XML/YAML when installed — degrade-gracefully per D-02; runtime TenantAwareFilter still scopes correctly regardless |

Rationale: Rule 3 is analysis-time only with no runtime surface. Its security value is catching a tenant_id misconfiguration before it ships as a scoping bug. The XML/YAML blindness of the fallback is an accepted, documented gap covered by the optional phpstan-doctrine path.
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` exits 0.
- `vendor/bin/phpstan analyse` exits 0 (level-9 self-analysis green).
- `vendor/bin/php-cs-fixer check --diff` clean for new files.
</verification>

<success_criteria>
- DX-03-AC3 (missing OR nullable OR non-string tenant_id) proven by TenantIdDriftRuleTest's four cases.
- Reflection fallback is the CI-tested primary path; ObjectMetadataResolver path guarded + degrades (D-02).
- tenancy.tenantIdDrift identifier + clear drift-kind message (DX-03-AC5).
</success_criteria>

<output>
Create `.planning/phases/28-phpstan-extension/28-02-SUMMARY.md` when done. Record: actual fixture line numbers; the exact list of string types accepted; whether the camelCase→snake_case property-name fallback was needed for any fixture; confirmation no length assertion exists.
</output>
