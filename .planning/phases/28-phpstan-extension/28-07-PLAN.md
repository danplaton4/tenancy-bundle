---
phase: 28-phpstan-extension
plan: 07
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
requirements: [DX-03-AC3]
files_modified:
  - tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNonStringViolating.php
  - tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNullableViolating.php
  - tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php
  - src/PHPStan/Rule/TenantIdDriftRule.php
user_setup: []

must_haves:
  truths:
    # WR-04 BLOCKER — the sole gaps_found entry. Behavior must be demonstrable in CI, not "code exists".
    - "A positionally-declared #[ORM\\Column('tenant_id', 'integer')] entity (positional type at index 1) fires exactly one tenancy.tenantIdDrift non-string-type error — proven by a passing RuleTestCase method, not code presence"
    - "A positionally-declared #[ORM\\Column('tenant_id', 'string', 63, null, null, false, true)] entity (positional nullable=true at index 6) fires exactly one tenancy.tenantIdDrift nullable error — proven by a passing RuleTestCase method"
    - "ANTI-REVERT PROOF: reverting TenantIdDriftRule.php lines 224-225 back to the named-only form ($args['nullable'] ?? false; $args['type'] ?? null) WOULD now break CI — the two new tests exercise the $args[1] (type) and $args[6] (nullable) positional fallbacks, so a silent revert is no longer invisible"
    # W1 WARNING (forward-compat, folded in) — closes the E_USER_DEPRECATED notice + ORM-4.0 unsafety
    - "checkViaMetadata() no longer uses ArrayAccess offset accessors in the instanceof \\ArrayAccess branch (no $fm['columnName'] / $fm['nullable'] / $fm['type'] offset reads remain there); it reads $fm->columnName / $fm->nullable / $fm->type via property access after an inner object-shape @var narrowing, emitting zero E_USER_DEPRECATED notices and staying ORM-4.0-safe"
    - "Behavior is unchanged after the W1 refactor: testMetadataPathSilentOnValidEntity() and testMetadataPathFiresOnMissingTenantId() both still pass (the regression guard)"
    # W2 WARNING (cosmetic, folded in)
    - "The TenantIdDriftRule.php line ~152 comment no longer claims plain arrays 'satisfy \\ArrayAccess check' — it correctly states ORM 2.x plain-array entries are handled by the separate is_array() branch and are NOT matched by the instanceof \\ArrayAccess check"
    - "Bundle level-9 self-analysis stays [OK]; full vendor/bin/phpunit stays green; both dogfoods stay [OK]; cs-fixer stays clean"
  artifacts:
    - path: "tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNonStringViolating.php"
      provides: "#[TenantAware] #[ORM\\Entity] fixture with #[ORM\\Column('tenant_id', 'integer')] — positional name at index 0, positional type 'integer' at index 1; exercises the $args[1] fallback (WR-04 type direction)"
      contains: "TenantAware"
    - path: "tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNullableViolating.php"
      provides: "#[TenantAware] #[ORM\\Entity] fixture with #[ORM\\Column('tenant_id', 'string', 63, null, null, false, true)] — positional nullable=true at index 6; exercises the $args[6] fallback (WR-04 nullable direction)"
      contains: "TenantAware"
    - path: "tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php"
      provides: "Two new positional-arg test methods asserting each positional fixture fires exactly the expected tenancy.tenantIdDrift error on the correct line — total tests 9 → 11"
      contains: "Positional"
    - path: "src/PHPStan/Rule/TenantIdDriftRule.php"
      provides: "W1: property-access read of FieldMapping (->columnName/->nullable/->type) inside the instanceof \\ArrayAccess branch; W2: corrected line-152 comment. WR-04 code at lines 224-225 untouched."
      contains: "columnName"
  key_links:
    - from: "tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNonStringViolating.php"
      to: "TenantIdDriftRule::checkViaReflection() $args[1] positional type fallback"
      via: "RuleTestCase analyse() asserting one tenancy.tenantIdDrift non-string-type error"
      pattern: "tenant_id', 'integer'"
    - from: "tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNullableViolating.php"
      to: "TenantIdDriftRule::checkViaReflection() $args[6] positional nullable fallback"
      via: "RuleTestCase analyse() asserting one tenancy.tenantIdDrift nullable error"
      pattern: "false, true"
    - from: "src/PHPStan/Rule/TenantIdDriftRule.php checkViaMetadata() instanceof \\ArrayAccess branch"
      to: "Doctrine\\ORM\\Mapping\\FieldMapping public properties (ORM 3.x), ORM-4.0-safe"
      via: "property access $fm->columnName/->nullable/->type after inner object{columnName: string, nullable: bool|null, type: string} @var narrowing"
      pattern: "\\$fm->columnName"
---

<objective>
Close the sole remaining BLOCKER from 28-VERIFICATION.md (status: gaps_found, score 8/9) and fold in the two non-blocking WARNINGs the user elected to fix in this same gap-closure pass. Plans 28-01..28-06 already shipped and are verified; this plan touches ONLY the WR-04 test coverage, the W1 read-path refactor, and the W2 comment. It does NOT replan the phase and does NOT revisit already-verified work.

Three items, in order of priority:

1. WR-04 (BLOCKER — the only `gaps_found` entry): the positional `#[ORM\Column]` code fix is ALREADY present and confirmed correct at `TenantIdDriftRule.php` lines 224-225 (`$nullableRaw = $args['nullable'] ?? $args[6] ?? false;` and `$typeRaw = $args['type'] ?? $args[1] ?? null;`). The gap is purely MISSING TEST COVERAGE: no positional fixtures exist and no test exercises the `$args[1]` / `$args[6]` fallbacks, so a silent revert of those two lines is invisible to CI. This is the lone false-negative direction in an otherwise precision-first security rule — a positionally-declared non-string or nullable `tenant_id` on a `#[TenantAware]` entity would ship undetected and become a cross-tenant data leak at runtime. We add two fixtures and two test methods so the fallback paths are exercised and a revert breaks CI.

2. W1 (WARNING — forward-compat, folded in): inside `checkViaMetadata()`'s `if ($fm instanceof \ArrayAccess)` branch (lines ~153-160), the ArrayAccess offset accessors (`$fm['columnName']` / `$fm['nullable']` / `$fm['type']`) fire `E_USER_DEPRECATED` on every ORM 3.x FieldMapping read (Doctrine `FieldMapping::offsetGet()` calls `Deprecation::trigger()`) and stop working when ORM 4.0 drops ArrayAccess from FieldMapping. Replace them with property access (`$fm->columnName` / `$fm->nullable` / `$fm->type`) after an inner object-shape `@var` narrowing — eliminates the notices and is ORM-4.0-safe. Behavior must stay identical; the existing metadata-path tests are the regression guard.

3. W2 (WARNING — cosmetic, folded in): the comment at line ~152 wrongly states ORM 2.x plain arrays "satisfy \ArrayAccess check" — plain PHP arrays do NOT implement `\ArrayAccess`. Reword to state ORM 2.x plain-array entries are handled by the separate `is_array()` branch and are NOT matched by the `instanceof \ArrayAccess` check.

Purpose: WR-04 is security-relevant (false negative on a tenant-scope column) and the only thing blocking phase completion; W1/W2 are quality fixes the user chose to bundle in rather than carry as debt.
Output: two new positional fixtures, two new test methods (9 → 11 tests), a deprecation-free ORM-4.0-safe metadata read path, and a corrected comment — bundle L9 + full suite + both dogfoods + cs-fixer all green.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-phpstan-extension/28-CONTEXT.md
@.planning/phases/28-phpstan-extension/28-VERIFICATION.md
@.planning/phases/28-phpstan-extension/28-05-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase + vendor. Do NOT re-explore. -->

WR-04 code under test (src/PHPStan/Rule/TenantIdDriftRule.php — checkViaReflection, lines 220-231 — ALREADY CORRECT, DO NOT EDIT):
  When the resolved column name equals 'tenant_id':
    $nullableRaw = $args['nullable'] ?? $args[6] ?? false;   // positional nullable at index 6
    $typeRaw     = $args['type']     ?? $args[1] ?? null;     // positional type at index 1
    $found = ['nullable' => (bool) $nullableRaw, 'type' => is_string($typeRaw) ? $typeRaw : null];
  $args is $attr->getArguments() — keyed by NAME for named args, by 0-based INDEX for positional args.
  Column name resolution (line 211): $colName = $args['name'] ?? $args[0] ?? null; — positional name is index 0.

Doctrine\ORM\Mapping\Column constructor positional order (vendor/doctrine/orm/src/Mapping/Column.php — CONFIRMED):
  0=name, 1=type, 2=length, 3=precision, 4=scale, 5=unique, 6=nullable, 7=insertable, 8=updatable, ...
  So #[ORM\Column('tenant_id', 'integer')]                          → $args[0]='tenant_id', $args[1]='integer'      (type via $args[1]).
     #[ORM\Column('tenant_id', 'string', 63, null, null, false, true)] → $args[0]='tenant_id', $args[6]=true (nullable via $args[6]);
                                                                          positions 2..5 are length=63,precision=null,scale=null,unique=false.

evaluateFinding() output strings (src/PHPStan/Rule/TenantIdDriftRule.php — the EXACT messages to assert):
  non-string type → "Class %s is #[TenantAware] but its tenant_id column maps to non-string type \"%s\". The TenantAwareFilter compares tenant_id as a quoted string slug; accepted types: string, ascii_string, guid, uuid."
    (for the integer fixture, %s type = "integer")
  nullable        → "Class %s is #[TenantAware] but its tenant_id column is nullable. The tenant_id column must be non-nullable to prevent cross-tenant data leaks."
  identifier on all branches: tenancy.tenantIdDrift

Named-arg fixture analogs to MIRROR (tests/Unit/PHPStan/Rule/Fixtures/):
  TenantIdNonStringViolating.php — #[TenantAware] (line 10) / #[ORM\Entity] (line 11) / class (line 12); $id int; tenant_id via #[ORM\Column(name: 'tenant_id', type: 'integer')] → fires non-string at line 10.
  TenantIdNullableViolating.php  — same header layout; tenant_id via #[ORM\Column(name: 'tenant_id', nullable: true)] on ?string → fires nullable at line 10.
  Both use: declare(strict_types=1); namespace Tenancy\Bundle\Tests\Unit\PHPStan\Rule\Fixtures; use Doctrine\ORM\Mapping as ORM; use Tenancy\Bundle\Attribute\TenantAware;
  IMPORTANT: the RuleTestCase error line for InClassNode is the #[TenantAware] attribute line. In the named-arg fixtures that is line 10 (no docblock). If the new positional fixtures keep the identical header layout (no docblock, #[TenantAware] on line 10), the expected error line is 10. The executor MUST confirm the actual line from PHPUnit output and use it (prior plans adjusted line numbers empirically — see 28-05-SUMMARY "Corrected Hierarchy Test").

Existing test class (tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php — 9 tests, all green):
  Per-test injectable resolver: private ?object $resolver = null; getRule() returns new TenantIdDriftRule($this->resolver). null → reflection PRIMARY path (what the new positional tests use — NO resolver).
  getAdditionalConfigFiles() returns [__DIR__.'/../../../../extension.neon'].
  testFiresWhenTenantIdNonString() (lines 82-94) and testFiresWhenTenantIdNullable() (lines 65-77) are the named-arg analogs to mirror — each calls $this->analyse([fixture], [[message, line]]).
  Use ::class on the new fixture FQCNs in the expected message (e.g. TenantIdPositionalNonStringViolating::class) — mirrors how the existing tests build the message with TenantIdNonStringViolating::class.

W1 target (src/PHPStan/Rule/TenantIdDriftRule.php — checkViaMetadata, lines 150-174):
  Line 152 (the W2 comment): "// ORM 2.x: plain array entries also satisfy \ArrayAccess check via the is_array() branch below." — FACTUALLY WRONG.
  Lines 153-163, the instanceof branch (W1 target):
    if ($fm instanceof \ArrayAccess) {
        $colName = $fm['columnName'] ?? null;
        if ('tenant_id' === $colName) {
            $nullableRaw = $fm['nullable'] ?? false;
            $typeRaw     = $fm['type']     ?? null;
            ... $found = [...]; break;
        }
    } elseif (is_array($fm)) { ... }   // ORM 2.x plain-array branch — LEAVE UNCHANGED
  Lines 145-146 already narrow the OUTER iterable: /** @var object{fieldMappings: iterable<object>} $meta */ $meta = $metadata;
    so each $fm is typed `object`. After `$fm instanceof \ArrayAccess`, PHPStan narrows $fm to \ArrayAccess (no public props known) — a bare $fm->columnName is a level-9 error.
  W1 FIX SHAPE (from VERIFICATION "W1 Forward-Compatibility Detail", line 128): add an INNER narrowing INSIDE the instanceof branch
    /** @var object{columnName: string, nullable: bool|null, type: string} $fm */
    then read $fm->columnName / $fm->nullable / $fm->type. FieldMapping declares these as public properties on ORM 3.x.

Doctrine\ORM\Mapping\FieldMapping (vendor/doctrine/orm/src/Mapping/FieldMapping.php — CONFIRMED, 3.6.3 installed):
  final class FieldMapping implements ArrayAccess { public string $type; public string $fieldName; public string $columnName; public bool|null $nullable = null; ... }
  All three (columnName: string, nullable: bool|null, type: string) are PUBLIC declared properties — property access is the ORM-4.0-safe read; ArrayAccess offsetGet fires Deprecation::trigger() on ORM 3.x.

CLAUDE.md constraints: Doctrine is OPTIONAL (the rule already guards interface_exists at processNode; the metadata path is guarded by class_exists(ObjectMetadataResolver) — do NOT add hard imports); strict_types in every file; PHPStan level 9; cs-fixer @Symfony (NO same-namespace use statements; do not import classes from the file's own namespace).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-04 — add positional #[ORM\Column] fixtures + tests (close the sole gaps_found BLOCKER)</name>
  <files>tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNonStringViolating.php, tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNullableViolating.php, tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php</files>
  <read_first>
    - tests/Unit/PHPStan/Rule/Fixtures/TenantIdNonStringViolating.php (named-arg non-string analog — mirror header layout: declare/namespace/use, #[TenantAware] on line 10, #[ORM\Entity] line 11, class line 12, $id, then the tenant_id column)
    - tests/Unit/PHPStan/Rule/Fixtures/TenantIdNullableViolating.php (named-arg nullable analog — same layout)
    - tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php (existing 9 tests; mirror testFiresWhenTenantIdNonString lines 82-94 and testFiresWhenTenantIdNullable lines 65-77 — same analyse([fixture],[[message,line]]) shape, same ::class message construction)
    - src/PHPStan/Rule/TenantIdDriftRule.php (checkViaReflection lines 200-243 — confirm the $args[6] / $args[1] fallbacks the new tests must exercise; evaluateFinding lines 252-293 for the exact assertion strings; DO NOT EDIT this file in this task)
    - .planning/phases/28-phpstan-extension/28-VERIFICATION.md (gaps.missing array — the verbatim fixture/test requirements; "W1 Forward-Compatibility Detail" is Task 2, not here)
    - ./CLAUDE.md (strict_types; @Symfony cs-fixer — no same-namespace use statements; tests under tests/Unit, no container/DB)
  </read_first>
  <behavior>
    - Given TenantIdPositionalNonStringViolating with #[ORM\Column('tenant_id', 'integer')] (positional name index 0, positional type 'integer' index 1) → checkViaReflection reads type via $args[1] → evaluateFinding fires exactly ONE tenancy.tenantIdDrift "maps to non-string type \"integer\"" error on the #[TenantAware] line. This is the WR-04 type direction.
    - Given TenantIdPositionalNullableViolating with #[ORM\Column('tenant_id', 'string', 63, null, null, false, true)] (positional nullable=true index 6) → checkViaReflection reads nullable via $args[6] → evaluateFinding fires exactly ONE tenancy.tenantIdDrift "tenant_id column is nullable" error on the #[TenantAware] line. This is the WR-04 nullable direction.
    - ANTI-REVERT: if lines 224-225 were reverted to $args['nullable'] ?? false / $args['type'] ?? null, BOTH new tests would FAIL (the positional integer would resolve type=null → silent; the positional nullable would resolve nullable=false → silent), making the false-negative regression visible in CI.
    - The existing 9 tests continue to pass unchanged (no edits to their fixtures or code).
  </behavior>
  <action>
    Create `tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNonStringViolating.php`: `declare(strict_types=1)`, namespace `Tenancy\Bundle\Tests\Unit\PHPStan\Rule\Fixtures`, `use Doctrine\ORM\Mapping as ORM;`, `use Tenancy\Bundle\Attribute\TenantAware;`. Mirror TenantIdNonStringViolating's exact header layout (NO docblock above the attributes, so #[TenantAware] lands on line 10) so the expected error line matches the analog. Class is `#[TenantAware]` `#[ORM\Entity]` named `TenantIdPositionalNonStringViolating` with an `#[ORM\Id] #[ORM\GeneratedValue] #[ORM\Column] private int $id;` and a tenant_id property carrying `#[ORM\Column('tenant_id', 'integer')]` — POSITIONAL name at index 0 ('tenant_id'), POSITIONAL type at index 1 ('integer'), NO named args. Type the property `private int $tenantId;`. Add a one-line comment noting the ORM\Column positional signature for maintainers: name=0, type=1, length=2, precision=3, scale=4, unique=5, nullable=6 — and that this fixture exercises the $args[1] (type) WR-04 fallback.

    Create `tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNullableViolating.php`: same file scaffolding and header layout. Class `#[TenantAware]` `#[ORM\Entity]` named `TenantIdPositionalNullableViolating` with the same `$id` property and a tenant_id property carrying `#[ORM\Column('tenant_id', 'string', 63, null, null, false, true)]` — POSITIONAL name='tenant_id' (0), type='string' (1), length=63 (2), precision=null (3), scale=null (4), unique=false (5), nullable=true (6). Type the property `private ?string $tenantId;`. Add the same positional-signature comment, noting this fixture exercises the $args[6] (nullable) WR-04 fallback. (Type is a valid string type here — the ONLY violation is positional nullable=true, isolating the $args[6] path.)

    In `tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php`: add the two new fixture FQCNs to the `use` import block (alongside the existing TenantIdNonStringViolating / TenantIdNullableViolating imports, alphabetical order to satisfy cs-fixer ordered_imports). Add two test methods mirroring the named-arg analogs:
      - `testFiresWhenTenantIdPositionalNonString()` — analyse [Fixtures/TenantIdPositionalNonStringViolating.php] expecting one error: the exact non-string-type message from evaluateFinding with `TenantIdPositionalNonStringViolating::class` and type "integer", on the #[TenantAware] line. Docblock: "WR-04: positional #[ORM\Column('tenant_id', 'integer')] — type read via the $args[1] positional fallback. Anti-revert guard for TenantIdDriftRule lines 224-225."
      - `testFiresWhenTenantIdPositionalNullable()` — analyse [Fixtures/TenantIdPositionalNullableViolating.php] expecting one error: the exact nullable message with `TenantIdPositionalNullableViolating::class`, on the #[TenantAware] line. Docblock: "WR-04: positional nullable=true at index 6 — read via the $args[6] positional fallback. Anti-revert guard for lines 224-225."
    Confirm the actual error line empirically by running the test (mirror prior-plan practice — the line is the #[TenantAware] attribute line, expected 10 if the layout matches the analogs; adjust to the real line from PHPUnit output if it differs). Do NOT modify any existing test, fixture, or src/ file in this task.
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` exits 0 with 11 tests (was 9; +2 positional). Confirm count: the PHPUnit summary line reports `OK (11 tests, N assertions)`.
    - Both fixtures exist and carry POSITIONAL args (no `name:` / `type:` / `nullable:` named keys on the tenant_id column): `grep -c "'tenant_id', 'integer'" tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNonStringViolating.php` >= 1 AND `grep -c "'tenant_id', 'string', 63, null, null, false, true" tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositionalNullableViolating.php` >= 1.
    - Neither positional fixture uses named column args: `grep -cE "name:|type:|nullable:" tests/Unit/PHPStan/Rule/Fixtures/TenantIdPositional*Violating.php` is 0.
    - The two test methods exist and target the positional fixtures: `grep -cE 'testFiresWhenTenantIdPositionalNonString|testFiresWhenTenantIdPositionalNullable' tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php` is 2.
    - Each test asserts the `tenancy.tenantIdDrift` direction it covers: the non-string test asserts the message containing `non-string type "integer"`; the nullable test asserts the message containing `tenant_id column is nullable` (use `grep` on the test file or read it to confirm both message fragments are present).
    - ANTI-REVERT PROOF documented: each new test's docblock names lines 224-225 and the `$args[1]`/`$args[6]` fallback it guards. (Behavioral proof is implicit — the positional fixtures cannot fire under the named-only form.)
  </acceptance_criteria>
  <done>Two positional #[ORM\Column] fixtures and two test methods exist; the suite is 11/11; the WR-04 $args[1] (type) and $args[6] (nullable) fallbacks are exercised so a revert of lines 224-225 now breaks CI. No src/ or existing-test edits in this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: W1 + W2 — property-access FieldMapping read (ORM-4.0-safe) + corrected comment in checkViaMetadata()</name>
  <files>src/PHPStan/Rule/TenantIdDriftRule.php</files>
  <read_first>
    - src/PHPStan/Rule/TenantIdDriftRule.php (checkViaMetadata lines 133-178 — the instanceof \ArrayAccess branch lines 153-163 is the W1 target; the line-152 comment is the W2 target; the outer @var narrowing is at lines 145-146; DO NOT touch checkViaReflection lines 194-243 or its WR-04 fallbacks at 224-225)
    - vendor/doctrine/orm/src/Mapping/FieldMapping.php (confirm public properties: string $columnName, string $type, bool|null $nullable — property access is the ORM-4.0-safe read; offsetGet fires Deprecation::trigger())
    - .planning/phases/28-phpstan-extension/28-VERIFICATION.md ("W1 Forward-Compatibility Detail" line 128 — the prescribed inner @var object{columnName: string, nullable: bool|null, type: string} narrowing + property access; W2 finding lines 75-81 / Anti-Patterns row at line 146)
    - .planning/phases/28-phpstan-extension/28-05-SUMMARY.md ("Metadata-Access Shape" + "No @phpstan-ignore Comments Introduced" — the existing object-shape @var pattern this fix extends; DO NOT introduce any @phpstan-ignore)
    - tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php (testMetadataPathSilentOnValidEntity lines 189-212 + testMetadataPathFiresOnMissingTenantId lines 226-253 — the behavior-identical regression guard; these MUST stay green after the refactor, and they self-skip when phpstan-doctrine is absent)
    - ./CLAUDE.md (Doctrine OPTIONAL — no hard ClassMetadata/FieldMapping import; PHPStan level 9; @Symfony cs-fixer)
  </read_first>
  <behavior>
    - W1: With phpstan-doctrine installed, analysing a valid #[TenantAware] entity (TenantIdValidClean) via the metadata path produces ZERO errors AND triggers ZERO E_USER_DEPRECATED notices (no FieldMapping::offsetGet calls). testMetadataPathSilentOnValidEntity() stays green.
    - W1: analysing a #[TenantAware] entity with no tenant_id via the metadata path still fires exactly one tenancy.tenantIdDrift "no column mapped to tenant_id" error. testMetadataPathFiresOnMissingTenantId() stays green.
    - W1: a nullable / non-string tenant_id read through the metadata path still fires the matching error (property read is value-identical to the prior offset read).
    - W2: the corrected comment accurately describes that ORM 2.x plain arrays go through the separate is_array() branch and are NOT matched by instanceof \ArrayAccess — no behavioral change.
    - The ORM 2.x `elseif (is_array($fm))` plain-array branch is untouched; checkViaReflection (incl. the WR-04 $args[1]/$args[6] fallbacks) is untouched.
  </behavior>
  <action>
    In `checkViaMetadata()`, inside the `if ($fm instanceof \ArrayAccess) { ... }` branch (lines ~153-163): replace the three ArrayAccess OFFSET reads with PROPERTY access. Immediately after entering the instanceof branch and BEFORE the first read, add an inner narrowing docblock `/** @var object{columnName: string, nullable: bool|null, type: string} $fm */` (this is the exact shape from VERIFICATION "W1 Forward-Compatibility Detail" — PHPStan 2.1.x accepts it to read the three public FieldMapping properties at level 9). Then read `$fm->columnName` for the column-name compare (replacing `$fm['columnName'] ?? null`), and inside the `'tenant_id' === $colName` block read `$fm->nullable` (replacing `$fm['nullable'] ?? false`) and `$fm->type` (replacing `$fm['type'] ?? null`). Keep the existing `$found` shape build identical: `nullable` cast to `(bool)`, `type` guarded with `is_string()` to match evaluateFinding's `{nullable?: bool, type?: string|null}` contract, then `break`. Because the inner @var narrows `nullable` to `bool|null`, the existing `(bool)` cast already normalizes a null to false — keep it. No `??` fallbacks are needed on the property reads since the @var declares the properties present; if cs-fixer/PHPStan prefers a null-safe read on `nullable` (declared `bool|null`), `(bool) ($fm->nullable ?? false)` is acceptable and stays level-9 clean.

    Do NOT add ANY `@phpstan-ignore` comment — the inner object-shape `@var` is the level-9 mechanism (mirrors the outer `object{fieldMappings: iterable<object>}` narrowing already in the file per 28-05-SUMMARY). Do NOT modify the outer `@var` at lines 145-146. Do NOT touch the `elseif (is_array($fm))` ORM 2.x plain-array branch — it stays exactly as-is. Do NOT touch `checkViaReflection()` or its WR-04 positional fallbacks at lines 224-225.

    W2 (same file, the comment at line ~152, directly above the instanceof branch): reword from the false "ORM 2.x: plain array entries also satisfy \ArrayAccess check via the is_array() branch below." to a correct statement — that ORM 3.x FieldMapping objects (which implement \ArrayAccess) are read here via property access, while ORM 2.x plain-array entries do NOT implement \ArrayAccess and are handled by the separate `is_array()` branch below (they are NOT matched by this `instanceof \ArrayAccess` check). Keep the comment concise and accurate; remove the word "satisfy" in the false sense.

    Preserve strict_types, the `tenancy.tenantIdDrift` identifier, STRING_TYPES, all evaluateFinding branches, and @Symfony cs-fixer compliance (no same-namespace use statements).
  </action>
  <verify>
    <automated>cd "/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy" && vendor/bin/phpstan analyse --memory-limit=1G && vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpstan analyse --memory-limit=1G` exits 0 ([OK] No errors) — the inner `@var object{columnName: string, nullable: bool|null, type: string}` narrowing stays L9-clean.
    - NO offset reads remain in the instanceof branch: `grep -cE "\\\$fm\['(columnName|nullable|type)'\]" src/PHPStan/Rule/TenantIdDriftRule.php` is 0 (the ORM 2.x is_array branch uses `$fm['...']` on a PLAIN ARRAY which is allowed and expected — confirm those remaining matches are ONLY inside the `elseif (is_array($fm))` block by reading the file; the count above must drop by exactly the three offset reads removed from the instanceof branch).
    - Property reads are present in the instanceof branch: `grep -c '\$fm->columnName' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1 AND `grep -c '\$fm->nullable' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1 AND `grep -c '\$fm->type' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1.
    - The inner narrowing exists: `grep -c 'columnName: string, nullable: bool|null, type: string' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1.
    - Zero suppressions introduced: `grep -c '@phpstan-ignore' src/PHPStan/Rule/TenantIdDriftRule.php` is 0.
    - W2 comment corrected: `grep -c 'satisfy .ArrayAccess check' src/PHPStan/Rule/TenantIdDriftRule.php` is 0 (the false phrasing is gone); the file still references the `is_array()` ORM 2.x branch in a comment near line 152.
    - The ORM 2.x branch is intact: `grep -c 'elseif (is_array($fm))' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1; the WR-04 fallbacks are intact: `grep -c '\$args\[6\]' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1 AND `grep -c '\$args\[1\]' src/PHPStan/Rule/TenantIdDriftRule.php` >= 1.
    - Behavior unchanged: `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` exits 0 — testMetadataPathSilentOnValidEntity and testMetadataPathFiresOnMissingTenantId both pass (the regression guard).
  </acceptance_criteria>
  <done>checkViaMetadata reads FieldMapping via property access ($fm->columnName/->nullable/->type) after an inner object-shape @var narrowing — zero deprecation notices, ORM-4.0-safe, level-9 clean, zero @phpstan-ignore; the ORM 2.x is_array branch and the WR-04 reflection fallbacks are untouched; the line-152 comment is factually corrected; metadata-path tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer entity mappings → static analysis | TenantIdDriftRule reads #[ORM\Column] attribute args / Doctrine ClassMetadata at analysis time only — no runtime, no untrusted input, no network or filesystem write |
| advertised phpstan-doctrine integration → consumer CI | The metadata path runs in real consumer projects following composer.json `suggest` (install phpstan/phpstan-doctrine); a deprecation-spamming or ORM-4.0-broken read degrades the consumer experience |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-fn-positional | Tampering (false negative — the security direction) | WR-04: a positionally-declared non-string or nullable tenant_id (`#[ORM\Column('tenant_id','integer')]` or nullable=true at index 6) on a #[TenantAware] entity is the ONE false-negative direction in this precision-first rule. Undetected, the entity ships with a tenant_id that cannot reliably scope queries → cross-tenant data leak at runtime. | mitigate | Task 1 adds two fixtures + two tests that exercise the $args[1] (type) and $args[6] (nullable) positional fallbacks, making the false-negative path OBSERVABLE in CI. A revert of the WR-04 fix (lines 224-225) now breaks the suite at static-analysis/CI time rather than surfacing as a production leak. This is the exact gap the plan closes. |
| T-28-07-orm4-regression | (rule-health / forward-compat) | W1: ArrayAccess offset reads on FieldMapping fire E_USER_DEPRECATED per field on ORM 3.x and STOP WORKING on ORM 4.0 — when ArrayAccess is removed, the offset reads would resolve to null and re-introduce the CR-01 false-positive storm (every valid entity flagged "no tenant_id"), training consumers to disable the leak guard. | mitigate | Task 2 switches to property access ($fm->columnName/->nullable/->type) after an inner object-shape @var narrowing — no deprecation notices, ORM-4.0-safe; the existing metadata-path tests (silent-on-valid / fires-on-missing) are the behavior-identical regression guard. |
| T-28-07-w2-maintainer | (rule-health / maintainability) | W2: the misleading line-152 comment ("plain arrays satisfy \ArrayAccess check") could lead a future maintainer to misroute ORM 2.x handling and reintroduce a metadata-read bug. | accept→mitigate | Cosmetic; Task 2 corrects it as a low-cost fold-in. No behavioral surface. |

No new external input, attack surface, or dependency is introduced — Task 1 adds test fixtures (analysis-time only, never executed against a DB) and two test methods; Task 2 is a read-path refactor over the same Doctrine metadata already consumed. After this plan the sole HIGH-severity threat (T-28-07-fn-positional, false negative on a tenant-scope column) is mitigated and observable in CI; no new HIGH threats remain open.
</threat_model>

<verification>
Run from the bundle root `/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy`:

- `vendor/bin/phpunit tests/Unit/PHPStan/Rule/TenantIdDriftRuleTest.php --no-coverage` → 11/11 tests pass (was 9; +2 positional WR-04 tests). PHPUnit summary reports `OK (11 tests, N assertions)`.
- `vendor/bin/phpunit` (full suite) → green (no regression across the suite).
- `vendor/bin/phpstan analyse --memory-limit=1G` → [OK] No errors (the W1 inner `@var object{columnName: string, nullable: bool|null, type: string}` narrowing stays level-9 clean; zero `@phpstan-ignore` introduced).
- `vendor/bin/phpstan analyse -c phpstan-extension-dogfood.neon --memory-limit=1G` → [OK] No errors (wired/metadata path dogfood over src/).
- `vendor/bin/phpstan analyse -c phpstan-extension-dogfood-nodoctrine.neon --memory-limit=1G` → [OK] No errors (base/no-doctrine path dogfood).
- `vendor/bin/php-cs-fixer check --diff` → clean (no diff) for the two new fixtures, the test file, and the rule.
- Behavioral WR-04 proof: the positional fixtures fire — confirm via the PHPUnit output / RuleTestCase expected-error lines for `testFiresWhenTenantIdPositionalNonString` (non-string "integer") and `testFiresWhenTenantIdPositionalNullable` (nullable), proving the `$args[1]` / `$args[6]` paths are exercised. A quick anti-revert sanity check (optional, not committed): temporarily reverting lines 224-225 to the named-only form makes exactly these two tests fail.
</verification>

<success_criteria>
- WR-04 (sole gaps_found BLOCKER, VERIFICATION truth #12) closed: positional non-string (index 1) and positional nullable (index 6) #[ORM\Column] declarations each fire exactly one tenancy.tenantIdDrift error, proven by two passing RuleTestCase methods — the false-negative path is now observable and a revert of lines 224-225 breaks CI.
- W1 closed: checkViaMetadata() reads FieldMapping via property access after an inner object-shape @var narrowing — zero E_USER_DEPRECATED notices, ORM-4.0-safe, level-9 clean, zero @phpstan-ignore; metadata-path tests stay green (behavior identical).
- W2 closed: the line-152 comment correctly describes the ORM 2.x is_array() branch and no longer claims plain arrays satisfy the \ArrayAccess check.
- Suite is 11/11; full suite green; bundle level-9 self-analysis + both dogfoods + cs-fixer all green.
- No already-verified work touched: checkViaReflection's WR-04 code (lines 224-225), the ORM 2.x is_array branch, processNode's MappedSuperclass/abstract skip, the extension/dogfood neon configs, and the existing 9 tests are unchanged except for the two added test methods + their imports.
</success_criteria>

<output>
Create `.planning/phases/28-phpstan-extension/28-07-SUMMARY.md` when done. Record: the actual error line asserted for each positional fixture (and whether it matched the expected line 10 or was adjusted empirically); confirmation the suite is 11/11 and the full suite + both dogfoods + cs-fixer are green; the exact W1 property-access shape committed (the inner `@var object{columnName: string, nullable: bool|null, type: string}` + `$fm->columnName/->nullable/->type` reads) and confirmation zero `@phpstan-ignore` were introduced; the corrected W2 comment text; explicit confirmation that lines 224-225, the ORM 2.x is_array branch, and the existing 9 tests were not altered (beyond the two new test methods + imports).
</output>
