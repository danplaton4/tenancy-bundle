---
phase: 32-maintenance-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Maintenance/TenantMaintenanceConfigTrait.php
  - src/TenantInterface.php
  - src/Entity/AbstractTenant.php
  - src/Event/TenantMaintenanceEnabled.php
  - src/Event/TenantMaintenanceDisabled.php
  - src/DependencyInjection/Compiler/MaintenanceModeContractPass.php
  - tests/Unit/Entity/TenantMaintenanceConfigTraitTest.php
  - tests/Unit/DependencyInjection/Compiler/MaintenanceModeContractPassTest.php
autonomous: true
requirements: [MAINT-05, MAINT-08]

must_haves:
  truths:
    - "Any TenantInterface implementation exposes isInMaintenance(): bool"
    - "A tenant not put into maintenance reports isInMaintenance() === false (BC-break mitigated by trait default)"
    - "MaintenanceModeContractPass throws LogicException at compile time if the maintenance listener is wired at kernel.request priority >= 20 (Success Criterion 3)"
    - "MaintenanceModeContractPass is a no-op when tenancy.maintenance.enabled is false or the parameter is absent"
    - "TenantMaintenanceEnabled and TenantMaintenanceDisabled events each carry the tenant that changed state"
  artifacts:
    - path: "src/Maintenance/TenantMaintenanceConfigTrait.php"
      provides: "bool $inMaintenance column + isInMaintenance()/setInMaintenance() default trait"
      contains: "private bool $inMaintenance = false"
    - path: "src/TenantInterface.php"
      provides: "isInMaintenance(): bool contract method"
      contains: "public function isInMaintenance(): bool"
    - path: "src/Entity/AbstractTenant.php"
      provides: "inlined $inMaintenance boolean column + accessors"
      contains: "private bool $inMaintenance = false"
    - path: "src/Event/TenantMaintenanceEnabled.php"
      provides: "readonly-constructor event dispatched on real enable"
      contains: "public readonly TenantInterface $tenant"
    - path: "src/Event/TenantMaintenanceDisabled.php"
      provides: "readonly-constructor event dispatched on real disable"
      contains: "public readonly TenantInterface $tenant"
    - path: "src/DependencyInjection/Compiler/MaintenanceModeContractPass.php"
      provides: "compile-time priority < 20 guard for the maintenance listener"
      contains: "throw new \\LogicException"
  key_links:
    - from: "src/Entity/AbstractTenant.php"
      to: "src/TenantInterface.php"
      via: "implements isInMaintenance()"
      pattern: "isInMaintenance"
    - from: "src/DependencyInjection/Compiler/MaintenanceModeContractPass.php"
      to: "src/EventListener/TenantContextOrchestrator.php"
      via: "compares against TenantContextOrchestrator::PRIORITY"
      pattern: "TenantContextOrchestrator::PRIORITY"
---

<objective>
Build the maintenance-mode foundation: the persisted boolean state (trait + interface method + AbstractTenant column), the two toggle events, and the compile-time listener-priority guard. Everything downstream (listener, commands, wiring) depends on these contracts existing.

Purpose: Establish the sole BC-break surface (`TenantInterface::isInMaintenance(): bool`, D-05) with its `false`-default mitigation, and lock the priority-16 invariant at container-compile time (Success Criterion 3) so the listener can never ship at a broken priority.
Output: 6 source artifacts + 2 unit test files, all passing under `vendor/bin/phpunit --testsuite unit`.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-maintenance-mode/32-CONTEXT.md
@.planning/phases/32-maintenance-mode/32-RESEARCH.md
@.planning/phases/32-maintenance-mode/32-PATTERNS.md

<interfaces>
<!-- Contracts this plan mirrors or produces. Executor uses these directly; no exploration needed. -->

TenantInterface current surface (src/TenantInterface.php:1-25) — add isInMaintenance(): bool after isActive():
```php
interface TenantInterface
{
    public function getSlug(): string;
    public function getDomain(): ?string;
    public function getConnectionConfig(): array;
    public function getName(): string;
    public function isActive(): bool;
    public function getMailerDsn(): ?string;
    public function getMailerFrom(): ?string;
    public function getMailerReplyTo(): ?string;
    // ADD: public function isInMaintenance(): bool;
}
```

Existing $isActive column pattern in AbstractTenant (src/Entity/AbstractTenant.php:38-39, 107-115) — self-return setters:
```php
#[ORM\Column(type: 'boolean')]
private bool $isActive = true;
public function isActive(): bool { return $this->isActive; }
public function setIsActive(bool $isActive): self { $this->isActive = $isActive; return $this; }
```

TenantContextOrchestrator priority constant (src/EventListener/TenantContextOrchestrator.php:22-23) — the ceiling the pass enforces:
```php
public const PRIORITY = 20;
```

Event analog (src/Event/TenantResolved.php) — readonly-constructor:
```php
final class TenantResolved {
    public function __construct(
        public readonly TenantInterface $tenant,
        public readonly ?Request $request,
        public readonly string $resolvedBy,
    ) {}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Maintenance state — trait, interface method, AbstractTenant column</name>
  <files>src/Maintenance/TenantMaintenanceConfigTrait.php, src/TenantInterface.php, src/Entity/AbstractTenant.php, tests/Unit/Entity/TenantMaintenanceConfigTraitTest.php</files>
  <read_first>
    - src/Filesystem/TenantFilesystemConfigTrait.php (PRIMARY analog: docblock "Do NOT combine with AbstractTenant" warning at lines 36-39; property+getter+setter shape; `static` return)
    - src/Mailer/TenantMailerConfigTrait.php (secondary analog: same trait shape)
    - src/TenantInterface.php (the interface being modified; current 8-method surface)
    - src/Entity/AbstractTenant.php (the entity being modified; $isActive column lines 38-39 + isActive()/setIsActive() lines 107-115 use `self` return)
    - .planning/phases/32-maintenance-mode/32-PATTERNS.md § "TenantMaintenanceConfigTrait" and § "AbstractTenant" (deviations: bool non-nullable; trait uses `static`, AbstractTenant uses `self`)
  </read_first>
  <behavior>
    - A class using TenantMaintenanceConfigTrait reports isInMaintenance() === false by default (never set)
    - setInMaintenance(true) then isInMaintenance() === true; setInMaintenance() returns the same instance (fluent, static)
    - AbstractTenant (which inlines the column) reports isInMaintenance() === false by default and true after setInMaintenance(true)
  </behavior>
  <action>
    Create `src/Maintenance/TenantMaintenanceConfigTrait.php` in namespace `Tenancy\Bundle\Maintenance` with `declare(strict_types=1)`. Mirror `TenantFilesystemConfigTrait` exactly EXCEPT the property is non-nullable: `#[ORM\Column(type: 'boolean')] private bool $inMaintenance = false;`, getter `isInMaintenance(): bool` (is-prefix, not get-), fluent setter `setInMaintenance(bool $inMaintenance): static`. Reproduce the docblock warning adapted to this column: "Do NOT use with AbstractTenant, which already inlines `$inMaintenance` — combining both causes a Doctrine duplicate column mapping error" (MAINT-05 / RESEARCH Pitfall 6). Import `Doctrine\ORM\Mapping as ORM` — the attribute is only honored when doctrine/orm is installed; the trait works as plain PHP storage otherwise (state this in the docblock, matching the filesystem trait).

    Modify `src/TenantInterface.php`: add EXACTLY ONE method `public function isInMaintenance(): bool;` immediately after `isActive(): bool;`. This is the sole BC break of v0.5 (D-05) — add no other methods.

    Modify `src/Entity/AbstractTenant.php`: inline the column after the `$filesystemConfig` property (per PATTERNS.md, after line ~59): `#[ORM\Column(type: 'boolean')] private bool $inMaintenance = false;` with a comment referencing Phase 32/MAINT-05 and noting custom entities can `use TenantMaintenanceConfigTrait` instead. Add `public function isInMaintenance(): bool` returning the property and `public function setInMaintenance(bool $inMaintenance): self` (self, matching AbstractTenant's own `setIsActive` convention — NOT `static`). Do NOT `use` the trait in AbstractTenant (would duplicate the column).
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/Entity/TenantMaintenanceConfigTraitTest.php</automated>
  </verify>
  <acceptance_criteria>
    - src/Maintenance/TenantMaintenanceConfigTrait.php contains `private bool $inMaintenance = false` and `public function isInMaintenance(): bool` and `setInMaintenance(bool $inMaintenance): static`
    - src/Maintenance/TenantMaintenanceConfigTrait.php docblock contains the string "Do NOT use with" (AbstractTenant duplicate-column warning)
    - src/TenantInterface.php contains `public function isInMaintenance(): bool;` and NO other new method
    - src/Entity/AbstractTenant.php contains `private bool $inMaintenance = false` and `public function isInMaintenance(): bool` and `setInMaintenance(bool $inMaintenance): self`
    - `vendor/bin/phpunit tests/Unit/Entity/TenantMaintenanceConfigTraitTest.php` exits 0
  </acceptance_criteria>
  <done>Trait, interface method, and AbstractTenant column exist; default is false; setter is fluent; trait test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Toggle events — TenantMaintenanceEnabled / TenantMaintenanceDisabled</name>
  <files>src/Event/TenantMaintenanceEnabled.php, src/Event/TenantMaintenanceDisabled.php</files>
  <read_first>
    - src/Event/TenantResolved.php (readonly-constructor analog, full file)
    - src/Event/TenantBootstrapped.php (closer shape: single-entity-arg events)
    - .planning/phases/32-maintenance-mode/32-PATTERNS.md § "TenantMaintenanceEnabled / TenantMaintenanceDisabled" (deviation: ONLY `public readonly TenantInterface $tenant`)
  </read_first>
  <action>
    Create `src/Event/TenantMaintenanceEnabled.php` and `src/Event/TenantMaintenanceDisabled.php` in namespace `Tenancy\Bundle\Event`, `declare(strict_types=1)`, each a `final class` with a single readonly constructor promotion `public readonly TenantInterface $tenant` (import `Tenancy\Bundle\TenantInterface`). No `$request`, no `$resolvedBy` — mirror `TenantBootstrapped`'s single-entity shape. These carry only the tenant whose state changed (MAINT-08). They will be dispatched by the enable/disable commands in plan 32-03 only on a real bool transition (D-08).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite unit 2>&1 | tail -5; php -r "require 'vendor/autoload.php'; new ReflectionClass('Tenancy\\Bundle\\Event\\TenantMaintenanceEnabled'); new ReflectionClass('Tenancy\\Bundle\\Event\\TenantMaintenanceDisabled'); echo 'OK';"</automated>
  </verify>
  <acceptance_criteria>
    - src/Event/TenantMaintenanceEnabled.php contains `final class TenantMaintenanceEnabled` and `public readonly TenantInterface $tenant`
    - src/Event/TenantMaintenanceDisabled.php contains `final class TenantMaintenanceDisabled` and `public readonly TenantInterface $tenant`
    - Neither event class declares a constructor arg other than `$tenant`
    - Both classes are autoloadable (ReflectionClass instantiation prints OK)
  </acceptance_criteria>
  <done>Both event classes exist, final, single readonly $tenant arg, autoloadable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MaintenanceModeContractPass — compile-time priority < 20 guard (Success Criterion 3)</name>
  <files>src/DependencyInjection/Compiler/MaintenanceModeContractPass.php, tests/Unit/DependencyInjection/Compiler/MaintenanceModeContractPassTest.php</files>
  <read_first>
    - src/DependencyInjection/Compiler/FilesystemContractPass.php (analog A: early-return-when-disabled at lines 67-72 using hasParameter/getParameter)
    - src/DependencyInjection/Compiler/MailerTransportContractPass.php (analog B: hasParameter/hasDefinition/`throw new \LogicException` idiom)
    - tests/Unit/DependencyInjection/Compiler/MailerTransportContractPassTest.php (test harness pattern: raw ContainerBuilder, setParameter, setDefinition, expectException)
    - src/EventListener/TenantContextOrchestrator.php (the PRIORITY = 20 constant the pass compares against, lines 22-23)
    - .planning/phases/32-maintenance-mode/32-PATTERNS.md § "MaintenanceModeContractPass" (concrete process() logic: read `kernel.event_listener` tag priority)
    - .planning/phases/32-maintenance-mode/32-RESEARCH.md § "ContractPass Implementation Pattern" (tag inspection is the compile-time-correct approach)
  </read_first>
  <behavior>
    - Pass is a no-op when parameter `tenancy.maintenance.enabled` is absent or false
    - When enabled and listener service `tenancy.maintenance.listener` is absent → throws \LogicException
    - When enabled and the listener's kernel.event_listener tag for kernel.request has priority 20 → throws \LogicException whose message mentions the priority
    - When enabled and the listener's tag priority is 16 (< 20) → no exception, process() completes
    - When enabled and the tag priority is any value >= TenantContextOrchestrator::PRIORITY (e.g. 25) → throws
  </behavior>
  <action>
    Create `src/DependencyInjection/Compiler/MaintenanceModeContractPass.php` in namespace `Tenancy\Bundle\DependencyInjection\Compiler` implementing `CompilerPassInterface`, `declare(strict_types=1)`. In `process(ContainerBuilder $container)`: (1) early-return if `!$container->hasParameter('tenancy.maintenance.enabled') || !$container->getParameter('tenancy.maintenance.enabled')` (mirrors FilesystemContractPass:70); (2) if enabled, `throw new \LogicException` when `!$container->hasDefinition('tenancy.maintenance.listener')`; (3) read `$container->findDefinition('tenancy.maintenance.listener')->getTag('kernel.event_listener')`, and for each tag whose `event` equals `\Symfony\Component\HttpKernel\KernelEvents::REQUEST`, compute `$priority = (int) ($tag['priority'] ?? 0)` and `throw new \LogicException` if `$priority >= \Tenancy\Bundle\EventListener\TenantContextOrchestrator::PRIORITY`. The message MUST name `TenantContextOrchestrator::PRIORITY`, the observed priority, and instruct to use priority 16 or lower. Use `sprintf` per the mailer analog. This pass has NO optional-library dependency so it registers unconditionally in plan 32-04.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/DependencyInjection/Compiler/MaintenanceModeContractPassTest.php</automated>
  </verify>
  <acceptance_criteria>
    - src/DependencyInjection/Compiler/MaintenanceModeContractPass.php contains `throw new \LogicException` and references `TenantContextOrchestrator::PRIORITY`
    - Test asserts: no-op when param absent/false; LogicException when listener def missing; LogicException at priority 20 and 25; no exception at priority 16
    - `vendor/bin/phpunit tests/Unit/DependencyInjection/Compiler/MaintenanceModeContractPassTest.php` exits 0 with >= 4 passing test methods
  </acceptance_criteria>
  <done>Contract pass throws LogicException for kernel.request priority >= 20, passes at 16, no-ops when disabled; unit test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container compile ↔ runtime listener wiring | A misconfigured priority would silently disable maintenance (empty TenantContext); the contract pass is the compile-time gate that prevents it |
| custom tenant entity ↔ TenantInterface | Third-party entities must gain isInMaintenance(); the trait `false` default and AbstractTenant inline column keep the BC break survivable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | listener priority misconfig (≥20 → empty context, maintenance silently bypassed) | mitigate | MaintenanceModeContractPass throws \LogicException at compile time when kernel.request priority >= TenantContextOrchestrator::PRIORITY (Task 3) |
| T-32-02 | Information Disclosure | cross-tenant maintenance flag leak | mitigate | State is a per-row boolean column on the tenant entity (row-level isolation); no static/shared property (Task 1) |
| T-32-03 | Denial of Service | BC break bricks apps with custom TenantInterface impls | mitigate | trait provides `isInMaintenance(): bool { return false; }` default; AbstractTenant inlines column with false default (Task 1); UPGRADE note deferred to Phase 34 |
| T-32-SC | Tampering | npm/pip/composer installs | accept | This phase introduces zero new composer dependencies (RESEARCH § "No New Dependencies"); no install task; no legitimacy gate required |
</threat_model>

<verification>
- `vendor/bin/phpunit --testsuite unit` green (all new unit tests + no regression)
- `vendor/bin/phpstan analyse --memory-limit=512M` clean at level 9 for the new files
- `vendor/bin/php-cs-fixer check --diff` clean (@Symfony ruleset)
- Doctrine-optional: the trait and interface addition compile with no doctrine/orm present (verified end-to-end in plan 32-04 via ZeroConfigKernelBootTest)
</verification>

<success_criteria>
- TenantInterface gains exactly `isInMaintenance(): bool` (D-05, MAINT-05)
- TenantMaintenanceConfigTrait supplies a `false`-default boolean column + fluent `static` setter with the AbstractTenant-conflict warning
- AbstractTenant inlines the same column with `self`-return accessors
- TenantMaintenanceEnabled / TenantMaintenanceDisabled events exist (MAINT-08 substrate)
- MaintenanceModeContractPass fails compilation at priority >= 20 and passes at 16 (Success Criterion 3)
</success_criteria>

<output>
Create `.planning/phases/32-maintenance-mode/32-01-SUMMARY.md` when done. Record the final class names/namespaces, the exact TenantInterface method signature added, and confirm the contract-pass service ID it looks for (`tenancy.maintenance.listener`) so plans 32-02/03/04 use the identical ID.
</output>
