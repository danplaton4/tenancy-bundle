---
phase: 32-maintenance-mode
plan: 04
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-03]
files_modified:
  - src/TenancyBundle.php
  - config/services.php
  - tests/Integration/ListenerPriorityTest.php
  - tests/Integration/ZeroConfigKernelBootTest.php
autonomous: true
requirements: [MAINT-01, MAINT-02, MAINT-03, MAINT-04, MAINT-06, MAINT-07, MAINT-09]

must_haves:
  truths:
    - "The maintenance config node exists under tenancy.maintenance with enabled(false), template(null), retry_after(3600), allow_ips([]), allow_routes([]), allow_paths([])"
    - "Parameters tenancy.maintenance.{enabled,retry_after,template,allow_ips,allow_routes,allow_paths} are set in loadExtension()"
    - "TenantMaintenanceModeListener is registered (autoconfigure=true) only when tenancy.maintenance.enabled is true, at kernel.request priority 16, wired with the six config args + nullable Twig"
    - "The three commands are registered with console.command tags; enable/disable get arg 0 rewired to doctrine.orm.landlord_entity_manager when database.enabled is true"
    - "MaintenanceModeContractPass is registered unconditionally in TenancyBundle::build()"
    - "With tenancy.maintenance.enabled true + listener at 16, the container compiles and the listener fires after TenantContextOrchestrator (priority 20)"
    - "The bundle still compiles and boots with no doctrine/orm present (maintenance feature degrades safely)"
  artifacts:
    - path: "src/TenancyBundle.php"
      provides: "maintenance config node + parameter wiring + conditional listener registration + landlord-EM rewire + contract-pass registration"
      contains: "arrayNode('maintenance')"
    - path: "config/services.php"
      provides: "console.command registrations for the three maintenance commands (Doctrine-guarded)"
      contains: "tenancy.command.maintenance"
    - path: "tests/Integration/ListenerPriorityTest.php"
      provides: "assertion that the maintenance listener is at priority 16, after the orchestrator @20"
      contains: "TenantMaintenanceModeListener"
    - path: "tests/Integration/ZeroConfigKernelBootTest.php"
      provides: "assertion that the bundle boots with maintenance present but no doctrine/orm"
      contains: "maintenance"
  key_links:
    - from: "src/TenancyBundle.php"
      to: "src/EventListener/TenantMaintenanceModeListener.php"
      via: "conditional service registration when tenancy.maintenance.enabled"
      pattern: "TenantMaintenanceModeListener::class"
    - from: "src/TenancyBundle.php"
      to: "src/DependencyInjection/Compiler/MaintenanceModeContractPass.php"
      via: "addCompilerPass in build()"
      pattern: "new MaintenanceModeContractPass"
    - from: "config/services.php"
      to: "src/Command/TenantMaintenanceEnableCommand.php"
      via: "console.command tagged service, arg 0 = landlord EM"
      pattern: "tenancy.command.maintenance.enable"
---

<objective>
Wire everything together: the `maintenance` config node + parameter wiring in `TenancyBundle`, conditional listener registration at priority 16, unconditional `MaintenanceModeContractPass` registration, the three command service definitions in `config/services.php` with the landlord-EM rewire, and the two integration tests that prove the priority-16 wiring and the no-Doctrine compile safety.

Purpose: Turn the isolated classes from plans 01-03 into a working, compile-verified feature. This plan proves Success Criterion 3 end-to-end (listener actually registered at 16, orchestrator at 20) and the Doctrine-optional invariant.
Output: 2 modified source/config files + 2 extended integration tests, full suite green under `vendor/bin/phpunit`.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-maintenance-mode/32-CONTEXT.md
@.planning/phases/32-maintenance-mode/32-RESEARCH.md
@.planning/phases/32-maintenance-mode/32-PATTERNS.md
@.planning/phases/32-maintenance-mode/32-01-SUMMARY.md
@.planning/phases/32-maintenance-mode/32-02-SUMMARY.md
@.planning/phases/32-maintenance-mode/32-03-SUMMARY.md

<interfaces>
<!-- Anchor lines in the files being modified (confirmed via grep). Use exactly. -->

TenancyBundle.php anchors:
- configure() nodes: `->arrayNode('origin')` @78, `->arrayNode('filesystem')` @119, `->arrayNode('shared')` @128 — add `->arrayNode('maintenance')` alongside these under the same children() block
- filesystem param derivation @175-176; parameter ->set() chain @198-202 (`->set('tenancy.filesystem.enabled', ...)` ... `->set('tenancy.shared.async', ...)`)
- landlord rewire precedent @252: `->setArgument(0, new Reference('doctrine.orm.landlord_entity_manager'));`
- build() compiler passes @396-412: existing `$container->addCompilerPass(new FilesystemContractPass());` inside interface_exists guards; unconditional passes at 396-399
- use-statements block @24-32 (compiler passes) and @34 (event listeners)

config/services.php anchors:
- `if (interface_exists(Doctrine\ORM\EntityManagerInterface::class)) {` @60 and @103 — commands go inside a Doctrine-guarded block
- provider wiring @61-67 uses `service('doctrine.orm.default_entity_manager')`, `service('cache.app')`, `param('tenancy.tenant_entity_class')`
- console.command tag precedent @127-130 / @136 / @147: `->tag('console.command')`

Maintenance config node to add (from PATTERNS.md § TenancyBundle):
```
->arrayNode('maintenance')
->addDefaultsIfNotSet()
->children()
->booleanNode('enabled')->defaultFalse()->end()
->scalarNode('template')->defaultNull()->end()
->integerNode('retry_after')->defaultValue(3600)->min(1)->end()
->arrayNode('allow_ips')->scalarPrototype()->end()->defaultValue([])->end()
->arrayNode('allow_routes')->scalarPrototype()->end()->defaultValue([])->end()
->arrayNode('allow_paths')->scalarPrototype()->end()->defaultValue([])->end()
->end()
->end()
```

Listener constructor arg order: see 32-02-SUMMARY.md (authoritative). Command constructor arg order + landlord-EM index: see 32-03-SUMMARY.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config node + parameter wiring + service registration + contract-pass in TenancyBundle & services.php</name>
  <files>src/TenancyBundle.php, config/services.php</files>
  <read_first>
    - src/TenancyBundle.php (configure() nodes @78/119/128; param derivation @175-176 + ->set chain @198-202; landlord rewire @252; build() @396-412; use-statements @24-34)
    - config/services.php (Doctrine-guarded blocks @60,103; provider wiring @61-67; console.command tags @127-147)
    - .planning/phases/32-maintenance-mode/32-PATTERNS.md § "src/TenancyBundle.php" (full maintenance node, param wiring with is_scalar/is_int/is_array guards, listener conditional registration, build() addition, use statements) and § "config/services.php" (command registration + landlord-EM rewire block)
    - .planning/phases/32-maintenance-mode/32-01-SUMMARY.md, 32-02-SUMMARY.md, 32-03-SUMMARY.md (final service IDs + constructor arg orders — authoritative over the plan's guesses)
    - src/Provider/DoctrineTenantProvider.php (confirms default_entity_manager arg 0 → rewired to landlord under database.enabled, the same pattern commands follow)
  </read_first>
  <action>
    In `src/TenancyBundle.php` `configure()`: add the `->arrayNode('maintenance')->addDefaultsIfNotSet()->children()...` node (exact node body in the interfaces block above) alongside the `filesystem`/`shared` nodes. Add use statements: `use Tenancy\Bundle\DependencyInjection\Compiler\MaintenanceModeContractPass;` and `use Tenancy\Bundle\EventListener\TenantMaintenanceModeListener;`.

    In `loadExtension()`: derive maintenance values from `$config['maintenance'] ?? []` with the same `is_scalar`/`is_int`/`is_array` type guards used for filesystem (PATTERNS.md § param wiring): `$maintenanceEnabled`, `$maintenanceRetryAfter` (int, default 3600), `$maintenanceTemplate` (?string), `$maintenanceAllowIps`/`AllowRoutes`/`AllowPaths` (arrays). Append to the parameter `->set()` chain (after `tenancy.shared.async` @202): `tenancy.maintenance.enabled`, `tenancy.maintenance.retry_after`, `tenancy.maintenance.template`, `tenancy.maintenance.allow_ips`, `tenancy.maintenance.allow_routes`, `tenancy.maintenance.allow_paths`.

    Register the listener conditionally in `loadExtension()`: `if ($maintenanceEnabled) { $services->set('tenancy.maintenance.listener', TenantMaintenanceModeListener::class)->autoconfigure(true)->args([...]); }` wiring the args in the EXACT order from 32-02-SUMMARY.md — `service('tenancy.context')`, `param('tenancy.maintenance.retry_after')`, `param('tenancy.maintenance.template')`, `param('tenancy.maintenance.allow_ips')`, `param('tenancy.maintenance.allow_routes')`, `param('tenancy.maintenance.allow_paths')`, `service('twig')->nullOnInvalid()`. `autoconfigure(true)` converts the `#[AsEventListener]` attribute to the `kernel.event_listener` tag the contract pass inspects.

    In `build()`: add `$container->addCompilerPass(new MaintenanceModeContractPass());` UNCONDITIONALLY (no interface_exists guard — no optional-library dep), alongside the unconditional passes @396-399.

    In the `database.enabled: true` block near @252 (Doctrine present): rewire the enable/disable command definitions' arg 0 to the landlord EM — `$builder->getDefinition('tenancy.command.maintenance.enable')->setArgument(0, new Reference('doctrine.orm.landlord_entity_manager'));` and the same for `.disable`, each guarded by `interface_exists(\Doctrine\ORM\EntityManagerInterface::class)`.

    In `config/services.php`, inside a `if (interface_exists(Doctrine\ORM\EntityManagerInterface::class))` block (commands require Doctrine): register `tenancy.command.maintenance.status` (arg `service('tenancy.provider')->nullOnInvalid()`), `tenancy.command.maintenance.enable` and `.disable` (args in the 32-03-SUMMARY order: arg 0 `service('doctrine.orm.default_entity_manager')` [rewired to landlord above], `param('tenancy.tenant_entity_class')`, `service('cache.app')`, `service('event_dispatcher')`), each `->tag('console.command')` using the exact command class names from 32-03-SUMMARY.md.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Integration/ContainerCompilationTest.php tests/Integration/AutoconfigurationTest.php 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/TenancyBundle.php contains `arrayNode('maintenance')`, `->set('tenancy.maintenance.enabled'`, `TenantMaintenanceModeListener::class`, and `new MaintenanceModeContractPass()`
    - src/TenancyBundle.php rewires `tenancy.command.maintenance.enable`/`.disable` arg 0 to `doctrine.orm.landlord_entity_manager` under database.enabled
    - config/services.php contains `tenancy.command.maintenance.status`, `.enable`, `.disable` each `->tag('console.command')`
    - `vendor/bin/phpunit tests/Integration/ContainerCompilationTest.php tests/Integration/AutoconfigurationTest.php` exits 0
  </acceptance_criteria>
  <done>Config node, parameters, conditional listener, three commands, landlord-EM rewire, and unconditional contract-pass are all wired; container compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests — priority 16 (Success Criterion 3) + no-Doctrine boot</name>
  <files>tests/Integration/ListenerPriorityTest.php, tests/Integration/ZeroConfigKernelBootTest.php</files>
  <read_first>
    - tests/Integration/ListenerPriorityTest.php (the existing orchestrator-@20 assertion; getListeners/getListenerPriority pattern lines 26-73; TestKernel usage; note: TestKernel must enable tenancy.maintenance.enabled=true for the listener to register)
    - tests/Integration/TestKernel.php (how config is set for the integration kernel — enable maintenance there or in a test-local kernel config so the listener registers at 16)
    - tests/Integration/ZeroConfigKernelBootTest.php (no-Doctrine kernel @170; testContainerCompilesAndKernelBoots @81; RemoveTenancyProviderPass @273 — the no-ORM lane)
    - .planning/phases/32-maintenance-mode/32-VALIDATION.md § "Per-Task Verification Map" (the two integration rows: priority-16 + no-Doctrine)
    - .planning/phases/32-maintenance-mode/32-02-SUMMARY.md (listener class name + PRIORITY const)
  </read_first>
  <action>
    Extend `tests/Integration/ListenerPriorityTest.php`: ensure the test kernel has `tenancy.maintenance.enabled: true` (add to TestKernel config or the test's kernel config so the listener service registers). Add a test method `testMaintenanceListenerRegisteredAtPriority16AfterOrchestrator()` that walks `$dispatcher->getListeners(KernelEvents::REQUEST)`, finds the `TenantMaintenanceModeListener` instance, asserts its `getListenerPriority(...)` equals `TenantMaintenanceModeListener::PRIORITY` (16), and asserts 16 < `TenantContextOrchestrator::PRIORITY` (20) so it fires AFTER the orchestrator (Success Criterion 3). Also assert `TenantMaintenanceModeListener::PRIORITY === 16` directly (mirror the existing `testPriorityConstantMatchesRegisteredPriority`).

    Extend `tests/Integration/ZeroConfigKernelBootTest.php`: add a test asserting the bundle still compiles and boots when doctrine/orm is absent AND maintenance config is present (e.g. add maintenance config to the ZeroConfigTestKernel or a sibling kernel with `tenancy.maintenance.enabled: false` default — the listener/commands are Doctrine-guarded so absence must not break compilation). Assert the container compiles and the console application still exits 0 for a benign command (mirror `testConsoleApplicationVersionCommandExitsZero` @132). If enabling maintenance under no-Doctrine is required, assert it degrades safely (commands not registered when EntityManagerInterface absent) rather than throwing.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Integration/ListenerPriorityTest.php tests/Integration/ZeroConfigKernelBootTest.php</automated>
  </verify>
  <acceptance_criteria>
    - tests/Integration/ListenerPriorityTest.php contains `TenantMaintenanceModeListener` and asserts its registered priority === 16 and that 16 < TenantContextOrchestrator::PRIORITY
    - tests/Integration/ZeroConfigKernelBootTest.php contains a maintenance-related assertion proving no-Doctrine boot is unaffected
    - `vendor/bin/phpunit tests/Integration/ListenerPriorityTest.php tests/Integration/ZeroConfigKernelBootTest.php` exits 0
    - Full suite: `vendor/bin/phpunit` exits 0
  </acceptance_criteria>
  <done>Integration proves the listener is registered at priority 16 (after orchestrator @20 — Success Criterion 3) and the bundle boots without doctrine/orm; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container compile ↔ runtime | The maintenance listener's registered priority must be < 20 or maintenance silently no-ops; compile-time pass + integration test both guard it |
| optional Doctrine ↔ bundle compile | The bundle must compile with no doctrine/orm; commands are Doctrine-guarded, listener needs no ORM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | listener registered at wrong priority (silently disables maintenance) | mitigate | MaintenanceModeContractPass registered unconditionally in build() (Task 1); ListenerPriorityTest asserts priority 16 at runtime (Task 2) — Success Criterion 3 end-to-end |
| T-32-14 | Denial of Service | bundle fails to compile without doctrine/orm (breaks Doctrine-optional invariant) | mitigate | Commands guarded by interface_exists(EntityManagerInterface); listener needs no ORM; ZeroConfigKernelBootTest asserts safe boot (Task 2) |
| T-32-15 | Elevation of Privilege | enable/disable command writes to the wrong EM (tenant EM instead of landlord) | mitigate | arg 0 rewired to doctrine.orm.landlord_entity_manager under database.enabled (Task 1), matching the provider rewire @252 |
| T-32-05 | Denial of Service | landlord routes 503'd | mitigate | Listener !hasTenant() early return (plan 32-02); this plan wires it at the correct priority so TenantContext is populated |
| T-32-SC | Tampering | composer installs | accept | Zero new dependencies in this phase (RESEARCH § "No New Dependencies") |
</threat_model>

<verification>
- `vendor/bin/phpunit` FULL suite green (all unit + integration, including the new priority-16 and no-Doctrine assertions)
- `vendor/bin/phpstan analyse --memory-limit=512M` clean at level 9
- `vendor/bin/php-cs-fixer check --diff` clean (@Symfony ruleset)
- Manual grep sanity (informational, not the gate): `grep -n "arrayNode('maintenance')" src/TenancyBundle.php` returns a line; `grep -c "tenancy.command.maintenance" config/services.php` >= 3
- Worktree note (project memory): if run in a worktree without vendor/, the orchestrator MUST re-run the full suite in the main checkout; purge stale `$(sys_get_temp_dir)/tenancy_*` caches if fake MappingException failures appear
</verification>

<success_criteria>
- Maintenance config node + parameters exist (D-06, feature-flag per RESEARCH Open Q1)
- Listener registered at priority 16 only when enabled; contract pass registered unconditionally (Success Criterion 3, MAINT-03/04/06/07)
- Three commands registered + landlord-EM rewired (MAINT-01/02/09)
- Bundle compiles with no doctrine/orm (Doctrine-optional invariant)
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/32-maintenance-mode/32-04-SUMMARY.md` when done. Record the final config-node shape, the six parameter names, the listener service ID + registered priority, the three command service IDs, and confirm full-suite + PHPStan L9 + cs-fixer are green. Note for Phase 34 that `tenancy.maintenance.allow_paths` is the config key where `/_tenancy` health routes get exempted.
</output>
