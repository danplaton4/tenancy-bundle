---
phase: 24-filesystem-bootstrapper
plan: 07
type: execute
wave: 3
depends_on: [24-03, 24-05, 24-06]
files_modified:
  - src/Bootstrapper/FilesystemBootstrapper.php
  - src/DependencyInjection/Compiler/FilesystemContractPass.php
  - src/TenancyBundle.php
  - config/services.php
  - tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php
  - tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php
autonomous: true
requirements: [BOOT-03]
tags: [filesystem, bootstrapper, compiler-pass, di-wiring]

must_haves:
  truths:
    - "FilesystemBootstrapper implements TenantBootstrapperInterface with a no-op boot() (decorators read live state per Plan 24-05/06) and a clear() that flushes LruFilesystemCache — mirrors MailerBootstrapper exactly."
    - "FilesystemBootstrapper is registered with tag attribute `priority: -30` so boot order is DatabaseSwitch (-0) → Doctrine (-10) → Mailer (-20) → Filesystem (-30) and clear() runs in reverse (DEC-FILE-PRIORITY)."
    - "FilesystemContractPass walks `findTaggedServiceIds('tenancy.scoped')` and, for each tagged service, registers a decorator Definition via `setDecoratedService()`. Strategy `prefix` → FilesystemPrefixingDecorator; strategy `per_tenant_adapter` → TenantAwareFilesystemDecorator. The pass implements the three guards from DEC-FILE-COMPILE-PASS (bundle-installed + allow_per_tenant_adapter + valid-strategy)."
    - "TenancyBundle.configure() gains a `tenancy.filesystem` node: enabled (bool, default false), allow_per_tenant_adapter (bool, default true), prefix_template (string, default 'tenant_{slug}/'), cache_size (int, default 32)."
    - "TenancyBundle.loadExtension() registers FilesystemBootstrapper, LruFilesystemCache, AdapterDsnParser, TenantContextClearedListener — all conditional on `interface_exists(\\League\\Flysystem\\FilesystemOperator::class)`. Setting `tenancy.filesystem.enabled: true` with the bundle absent is a compile-time error (Contract Pass guard 1)."
    - "TenancyBundle.build() registers FilesystemContractPass conditional on `interface_exists(\\League\\Flysystem\\FilesystemOperator::class)` — symmetric with the MailerTransportContractPass registration."
    - "Integration test container: a tagged storage with strategy=prefix becomes an instance of FilesystemPrefixingDecorator; a tagged storage with strategy=per_tenant_adapter becomes an instance of TenantAwareFilesystemDecorator; an untagged storage is unchanged (instance of \\League\\Flysystem\\Filesystem)."
  artifacts:
    - path: "src/Bootstrapper/FilesystemBootstrapper.php"
      provides: "TenantBootstrapperInterface implementation — no-op boot + LRU clear"
      contains: "final class FilesystemBootstrapper"
    - path: "src/DependencyInjection/Compiler/FilesystemContractPass.php"
      provides: "Compile-time guard + tag-driven decoration injection"
      contains: "final class FilesystemContractPass"
    - path: "src/TenancyBundle.php"
      provides: "tenancy.filesystem config node + conditional DI registration + FilesystemContractPass registration in build()"
      contains: "tenancy.filesystem"
    - path: "config/services.php"
      provides: "Service definitions for FilesystemBootstrapper, LruFilesystemCache, AdapterDsnParser, TenantContextClearedListener — guarded by interface_exists block"
      contains: "FilesystemBootstrapper"
  key_links:
    - from: "src/TenancyBundle.php"
      to: "src/DependencyInjection/Compiler/FilesystemContractPass.php"
      via: "build() registers via container->addCompilerPass(new FilesystemContractPass())"
      pattern: "new FilesystemContractPass"
    - from: "src/DependencyInjection/Compiler/FilesystemContractPass.php"
      to: "tagged storage definitions (user-applied tenancy.scoped tag)"
      via: "findTaggedServiceIds('tenancy.scoped') → setDecoratedService($id)"
      pattern: "findTaggedServiceIds.*tenancy\\.scoped"
---

<objective>
Ship the wiring layer that ties together everything from Waves 1+2: the `FilesystemBootstrapper` (lifecycle participant), the `FilesystemContractPass` (tag-driven decoration injection + 3 compile-time guards), and the bundle-level config node + DI registration. This is the plan that makes the bundle's filesystem story usable — until now everything was loose plumbing.

Per DEC-FILE-PRIORITY: bootstrapper priority is `-30`. Per DEC-FILE-COMPILE-PASS: three guards (bundle installed, allow_per_tenant_adapter respected, valid strategy attribute). Per DEC-FILE-MULTI: tag-driven opt-in (`tenancy.scoped`) with strategy + prefix_template attributes.

Output: 2 new production files (bootstrapper + compiler pass) + 2 modified production files (TenancyBundle + config/services.php) + 2 unit test files flipped from incomplete to passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md
@.planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md
@CLAUDE.md
@src/Bootstrapper/MailerBootstrapper.php
@src/Bootstrapper/TenantBootstrapperInterface.php
@src/DependencyInjection/Compiler/MailerTransportContractPass.php
@src/DependencyInjection/Compiler/BootstrapperChainPass.php
@src/TenancyBundle.php
@config/services.php
</context>

<interfaces>
Existing `MailerBootstrapper` (precedent — `src/Bootstrapper/MailerBootstrapper.php`):

```
final class MailerBootstrapper implements TenantBootstrapperInterface {
    public function __construct(private readonly ?LruTransportCache $cache = null);
    public function boot(TenantInterface $tenant): void;  // no-op
    public function clear(): void;                        // $this->cache?->clear();
}
```

Symfony DI primitives used in FilesystemContractPass (RESEARCH.md §Pattern 3 + §Code Examples §Symfony service decoration):

```
$decorator = new Definition($decoratorClass);
$decorator->setDecoratedService($id, null, /* priority */ 0);
$decorator->setArguments([
    new Reference('.inner'),         // the original tagged storage service
    new Reference('tenancy.context'),
    // strategy-specific args follow
]);
$container->setDefinition($id . '.tenant_scoped', $decorator);
```

Flysystem-bundle service IDs (RESEARCH.md §Pitfall 1): user-supplied storage name (e.g. `users.storage`), NOT `flysystem.filesystem.users.storage`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FilesystemBootstrapper + flip its stub test</name>
  <files>src/Bootstrapper/FilesystemBootstrapper.php, tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php</files>
  <read_first>
    - src/Bootstrapper/MailerBootstrapper.php (line-by-line precedent — copy the file shape; substitute LruFilesystemCache for LruTransportCache)
    - src/Bootstrapper/TenantBootstrapperInterface.php (contract: boot(TenantInterface $tenant): void; clear(): void)
    - src/Filesystem/LruFilesystemCache.php (Plan 24-03 — clear() is the integration point)
    - tests/Unit/Bootstrapper/MailerBootstrapperTest.php (test precedent)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-PRIORITY (priority -30 rationale)
    - tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php (Plan 24-00 stub — replace body)
  </read_first>
  <behavior>
    - `boot($tenant)` is a no-op — no exception, no state change, no side effects.
    - `clear()` calls `$this-&gt;cache?-&gt;clear()` exactly once.
    - With cache argument null (the default), `clear()` is a safe no-op.
    - Implements `TenantBootstrapperInterface`.
    - Final class.
  </behavior>
  <action>
    `src/Bootstrapper/FilesystemBootstrapper.php`: copy `src/Bootstrapper/MailerBootstrapper.php` end-to-end. Substitutions: namespace stays `Tenancy\Bundle\Bootstrapper`; import `use Tenancy\Bundle\Filesystem\LruFilesystemCache;` instead of the Mailer one; class name `FilesystemBootstrapper`; constructor `private readonly ?LruFilesystemCache $cache = null`; docblock cites .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-PRIORITY (priority -30, boot order rationale) and Plan 24-05/06 (decorators read live state — boot() is no-op).
    Replace `tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php` stub body. Cover all 5 behaviours above with simple assertions; use a real `LruFilesystemCache(maxSize: 2)` and seed it with a stub `FilesystemOperator` to verify clear() empties it.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php &amp;&amp; vendor/bin/phpstan analyse src/Bootstrapper/FilesystemBootstrapper.php tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php --level 9 &amp;&amp; vendor/bin/php-cs-fixer check --diff src/Bootstrapper/FilesystemBootstrapper.php tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php</automated>
  </verify>
  <done>FilesystemBootstrapper.php is a structural twin of MailerBootstrapper.php; test green; PHPStan + cs-fixer clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FilesystemContractPass with 3 guards + tag→decorator rewrite</name>
  <files>src/DependencyInjection/Compiler/FilesystemContractPass.php, tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php</files>
  <read_first>
    - src/DependencyInjection/Compiler/MailerTransportContractPass.php (precedent — copy the parameter-access + LogicException-shape pattern)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-COMPILE-PASS (lock: 3 guards — bundle-installed, allow_per_tenant_adapter, valid strategy)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-MULTI (tag attributes: strategy + prefix_template)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Pattern 3 + §Code Examples §Symfony service decoration via compiler pass (the canonical setDecoratedService pattern — copy this verbatim, do not invent)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Pitfall 1 (storage service IDs are bare names, not flysystem.filesystem.* derived)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Pitfall 6 (autowiring through the decorator is preserved by setDecoratedService — verify in Plan 24-08 integration tests)
    - tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php (Plan 24-00 stub — replace body)
  </read_first>
  <behavior>
    Guard 1 (BOOT-03 compile guard 1 from REQUIREMENTS.md): if `tenancy.filesystem.enabled === true` AND `interface_exists(\League\Flysystem\FilesystemOperator::class) === false` → throw `\LogicException` with a clear message including the install hint `composer require league/flysystem-bundle`. If `tenancy.filesystem.enabled === false` (default), pass returns early (no-op).
    Guard 2: for each tag entry with `strategy: per_tenant_adapter`, if `tenancy.filesystem.allow_per_tenant_adapter === false` → throw `\LogicException` naming the offending service ID and pointing at the config flag.
    Guard 3: for each tag entry, `strategy` must be `'prefix'` or `'per_tenant_adapter'` (default `'prefix'` if absent). Anything else → throw `\LogicException`.
    Tag walking: `findTaggedServiceIds('tenancy.scoped')` returns `id =&gt; [tags...]`. For each `(id, tags)`:
    - Resolve effective strategy from tag attribute (default 'prefix').
    - Resolve effective prefix_template from tag attribute (default `'tenant_{slug}/'`).
    - Construct a decorator `Definition`:
      - For prefix: `new Definition(FilesystemPrefixingDecorator::class)-&gt;setArguments([new Reference('.inner'), new Reference('tenancy.context'), $prefixTemplate])`.
      - For per_tenant_adapter: `new Definition(TenantAwareFilesystemDecorator::class)-&gt;setArguments([new Reference('.inner'), new Reference('tenancy.context'), new Reference('tenancy.filesystem.lru_cache'), new Reference('tenancy.filesystem.adapter_dsn_parser')])`.
    - `$decorator-&gt;setDecoratedService($id);`
    - `$container-&gt;setDefinition($id . '.tenant_scoped', $decorator);`
    Behavioural tests:
    - Test guard 1 (enabled + bundle absent): can't be exercised because PHPUnit runs WITH league/flysystem-bundle installed via require-dev. SKIP this test with `markTestSkipped('league/flysystem-bundle present in require-dev — cannot exercise the bundle-absent guard. Integration test in Plan 24-08 covers it via a stub kernel.')` UNLESS a programmatic way to simulate absence exists (e.g. using `class_alias` to hide the class — likely not robust; skip).
    - Test guard 2: build a fresh ContainerBuilder, set parameters `tenancy.filesystem.enabled = true`, `tenancy.filesystem.allow_per_tenant_adapter = false`; add a Definition tagged `tenancy.scoped` with attribute `strategy: per_tenant_adapter`; invoke `$pass-&gt;process($container)`; expect `\LogicException` with a message containing the service ID.
    - Test guard 3: same setup, tag attribute `strategy: nope`; expect `\LogicException`.
    - Tag walking — prefix path: container has a tagged storage; after `process()`, container has the decorator Definition under `<id>.tenant_scoped` and its class is `FilesystemPrefixingDecorator::class`.
    - Tag walking — per_tenant_adapter path: same setup with `allow_per_tenant_adapter = true`; class is `TenantAwareFilesystemDecorator::class`.
    - Untagged storage: container has a Definition tagged `flysystem.storage` but NOT `tenancy.scoped`; after `process()`, no decorator is registered for it.
    - Empty container (no tagged services): pass returns cleanly without exception.
    - Disabled (default): `tenancy.filesystem.enabled = false` (or unset); even if `tenancy.scoped` tags are present, the pass returns early WITHOUT decorating them. This is the "feature gated by config flag" semantic.
    - Effective prefix_template default: when the tag entry omits `prefix_template`, the decorator Definition's 3rd argument is exactly the string `'tenant_{slug}/'`.
  </behavior>
  <action>
    `src/DependencyInjection/Compiler/FilesystemContractPass.php`: `<?php declare(strict_types=1); namespace Tenancy\Bundle\DependencyInjection\Compiler;` with imports `use Symfony\Component\DependencyInjection\Compiler\CompilerPassInterface;`, `use Symfony\Component\DependencyInjection\ContainerBuilder;`, `use Symfony\Component\DependencyInjection\Definition;`, `use Symfony\Component\DependencyInjection\Reference;`. Class-level docblock cites .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-COMPILE-PASS + §DEC-FILE-MULTI and RESEARCH.md §Pattern 3.
    `final class FilesystemContractPass implements CompilerPassInterface` carrying:
    - Constants: `private const TAG = 'tenancy.scoped';`, `private const ENABLED_PARAM = 'tenancy.filesystem.enabled';`, `private const ALLOW_PER_TENANT_PARAM = 'tenancy.filesystem.allow_per_tenant_adapter';`, `private const DEFAULT_PREFIX_TEMPLATE = 'tenant_{slug}/';`, `private const PREFIX_DECORATOR = \Tenancy\Bundle\Filesystem\FilesystemPrefixingDecorator::class;`, `private const PER_TENANT_DECORATOR = \Tenancy\Bundle\Filesystem\TenantAwareFilesystemDecorator::class;`.
    - `public function process(ContainerBuilder $container): void` — implements the behavioural logic above.
    - Guard 1 logic: early-return when `!$container-&gt;hasParameter(self::ENABLED_PARAM) || !$container-&gt;getParameter(self::ENABLED_PARAM)`. Then `if (!interface_exists(\League\Flysystem\FilesystemOperator::class))` throw with install hint.
    - Tag walk: foreach loop over `findTaggedServiceIds(self::TAG)`. For each tag entry: validate strategy (guard 3), apply allow_per_tenant guard (guard 2), build decorator Definition per strategy, `setDecoratedService($id)`, register under `$id.'.tenant_scoped'`.
    Replace `tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php` stub. Each behavioural bullet from above becomes one test method. Build the ContainerBuilder fresh per test; assert via `$container-&gt;getDefinition($id.'.tenant_scoped')-&gt;getClass()`.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php &amp;&amp; vendor/bin/phpstan analyse src/DependencyInjection/Compiler/FilesystemContractPass.php tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php --level 9 &amp;&amp; vendor/bin/php-cs-fixer check --diff src/DependencyInjection/Compiler/FilesystemContractPass.php tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php</automated>
  </verify>
  <done>FilesystemContractPass enforces 3 guards and rewrites tagged services into decorator Definitions; test class is fully green; PHPStan + cs-fixer clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire TenancyBundle config node + DI services + FilesystemContractPass registration</name>
  <files>src/TenancyBundle.php, config/services.php</files>
  <read_first>
    - src/TenancyBundle.php (the file being modified — note the existing mailer config node at lines 89-101, the existing parameter setting at lines 149-150, the existing MailerTransportContractPass conditional registration at lines 250-252)
    - config/services.php (the file being modified — find the Mailer service block and use it as the placement model for the Filesystem service block)
    - src/DependencyInjection/Compiler/FilesystemContractPass.php (just created in Task 2 — TenancyBundle.build() registers it)
    - src/Bootstrapper/FilesystemBootstrapper.php (just created in Task 1 — config/services.php registers it with tag `tenancy.bootstrapper` priority `-30`)
    - src/Filesystem/LruFilesystemCache.php (Plan 24-03 — registered with constructor arg from `tenancy.filesystem.cache_size`)
    - src/Filesystem/AdapterDsnParser.php (Plan 24-04 — zero-arg service)
    - src/Filesystem/TenantContextClearedListener.php (Plan 24-03 — registered as event subscriber)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-PRIORITY + §DEC-FILE-COMPILE-PASS + §DEC-FILE-MULTI (the config keys: enabled, allow_per_tenant_adapter, prefix_template, cache_size)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Open Questions Q4 (Profiler subsection deferred — DO NOT add it in this plan; defer per recommendation)
  </read_first>
  <action>
    `src/TenancyBundle.php` — three modifications:
    1. In `configure()` (immediately after the `mailer` arrayNode at line 89-101), add a `filesystem` arrayNode with default-empty semantics: `booleanNode('enabled')-&gt;defaultFalse()-&gt;end()`, `booleanNode('allow_per_tenant_adapter')-&gt;defaultTrue()-&gt;end()`, `scalarNode('prefix_template')-&gt;defaultValue('tenant_{slug}/')-&gt;end()`, `integerNode('cache_size')-&gt;defaultValue(32)-&gt;min(1)-&gt;end()`. Maintain the existing fluent-call style.
    2. In `loadExtension()` (immediately after the mailer parameter block at lines 135-150), pull `$filesystemConfig = $config['filesystem'] ?? [];` set four parameters: `tenancy.filesystem.enabled`, `tenancy.filesystem.allow_per_tenant_adapter`, `tenancy.filesystem.prefix_template`, `tenancy.filesystem.cache_size`. Apply the same `is_scalar(…) ? (string|int|bool) : default` cleanup pattern the mailer block uses.
    3. In `build()` (line 250-252), add a second conditional block: `if (interface_exists(\League\Flysystem\FilesystemOperator::class)) { $container-&gt;addCompilerPass(new FilesystemContractPass()); }`. Add `use Tenancy\Bundle\DependencyInjection\Compiler\FilesystemContractPass;` at the top of the file.
    `config/services.php` — add a single `if (interface_exists(\League\Flysystem\FilesystemOperator::class))` block. Inside it, register:
    - `tenancy.filesystem.lru_cache` = `LruFilesystemCache::class`, ctor arg `%tenancy.filesystem.cache_size%`. Public: false. (Match the LruTransportCache registration pattern in the file.)
    - `tenancy.filesystem.adapter_dsn_parser` = `AdapterDsnParser::class`, zero-arg.
    - `tenancy.filesystem.bootstrapper` = `FilesystemBootstrapper::class`, ctor arg ref `tenancy.filesystem.lru_cache`, tagged `tenancy.bootstrapper` with attribute `priority: -30`.
    - `tenancy.filesystem.context_cleared_listener` = `TenantContextClearedListener::class`, ctor arg ref `tenancy.filesystem.lru_cache`, tagged `kernel.event_subscriber`.
    Service IDs use the `tenancy.filesystem.*` prefix to match the existing `tenancy.mailer.*` naming convention. NONE of these services need to be `public`; they're consumed by tag and compiler-pass machinery only.
    Anti-pattern guards: NO same-namespace `use` statements in `src/TenancyBundle.php` for `Tenancy\Bundle\…` classes — match the existing imports policy in the file (the existing `use Tenancy\Bundle\…` lines are fine because TenancyBundle lives in `Tenancy\Bundle` root namespace, not in the subnamespaces being imported — these are NOT same-namespace).
    `tenancy.filesystem.enabled: false` (the default) means the contract pass returns early and the four new services are still registered but inert (no `tenancy.scoped` tags to walk, no decoration, no observable effect). This preserves the zero-config story for v0.3 users upgrading to v0.4 — they don't see any behaviour change until they opt in.
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse src/TenancyBundle.php config/services.php --level 9 &amp;&amp; vendor/bin/php-cs-fixer check --diff src/TenancyBundle.php config/services.php &amp;&amp; vendor/bin/phpunit --testsuite unit 2&gt;&amp;1 | tail -20 | grep -E "(OK|Tests:|FAILURES|ERRORS)" &amp;&amp; php -r "require 'vendor/autoload.php'; \$k = new \\Tenancy\\Bundle\\Tests\\Integration\\Filesystem\\FilesystemTestKernel('test', true); \$k-&gt;boot(); \$c = \$k-&gt;getContainer(); echo \$c-&gt;hasParameter('tenancy.filesystem.enabled') ? 'param:ok' : 'FAIL'; \$k-&gt;shutdown();" | grep -q "param:ok"</automated>
  </verify>
  <done>configure() adds tenancy.filesystem node with 4 keys; loadExtension() sets the 4 parameters; config/services.php registers 4 conditional services; build() registers FilesystemContractPass conditional on the interface; full unit suite green; PHPStan + cs-fixer clean; FilesystemTestKernel boots and exposes tenancy.filesystem.enabled parameter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle config (YAML) → ContainerBuilder | Validated by Symfony Config component; the planner's filesystem config node defines the schema. |
| user-applied `tenancy.scoped` tag → FilesystemContractPass | The pass validates `strategy` attribute against an allow-list; invalid → compile-time LogicException. |
| Compile-time guards | Three guards from DEC-FILE-COMPILE-PASS prevent misconfigured runtime states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07-01 | Tampering | Misconfigured `strategy` attribute slipping through to runtime | mitigate | FilesystemContractPass guard 3 is the regression gate; behavioural test pins it. |
| T-24-07-02 | Information Disclosure | A user with shared-hosting deployment accidentally enabling per_tenant_adapter mode | mitigate | `tenancy.filesystem.allow_per_tenant_adapter: false` admin escape hatch; guard 2 enforces. |
| T-24-07-03 | Denial of Service | `interface_exists` guard absent → fatal on prod install without league/flysystem-bundle | mitigate | Both loadExtension and build() guard via interface_exists; the only fatal path is the explicit guard 1 LogicException, which is INTENDED behaviour when `tenancy.filesystem.enabled: true` + bundle absent. |
| T-24-07-04 | Spoofing | Untagged storages bypass scoping → developer expectation mismatch (think they're scoped, get landlord access) | accept | Tag-based opt-in is documented in DEC-FILE-MULTI rationale. Plan 24-09 (docs) covers the user-facing surface. Untagged-bypass test in Plan 24-08 pins the contract. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php` green.
- `vendor/bin/phpunit --testsuite unit` full suite green.
- PHPStan level 9 clean across all 4 modified files.
- cs-fixer clean.
- FilesystemTestKernel boots; container exposes `tenancy.filesystem.enabled` parameter; the 4 new service IDs (`tenancy.filesystem.bootstrapper`, `.lru_cache`, `.adapter_dsn_parser`, `.context_cleared_listener`) are present.
- Build-time: a tagged storage with `strategy: prefix` is wrapped at compile time (the Definition under `<id>.tenant_scoped` exists with `FilesystemPrefixingDecorator::class`).
</verification>

<success_criteria>
- FilesystemBootstrapper is a structural twin of MailerBootstrapper.
- FilesystemContractPass enforces all 3 guards from DEC-FILE-COMPILE-PASS and rewrites tagged services.
- TenancyBundle: new `tenancy.filesystem` config node + parameter wiring + conditional CompilerPass registration.
- config/services.php: 4 new services under `if (interface_exists(\League\Flysystem\FilesystemOperator::class))`.
- Bundle continues to load when league/flysystem-bundle is uninstalled (interface_exists discipline preserved).
- All previous tests still green; Plan 24-08 can now run its integration suite.
</success_criteria>

<output>
Create `.planning/phases/24-filesystem-bootstrapper/24-07-SUMMARY.md` when done.
</output>
