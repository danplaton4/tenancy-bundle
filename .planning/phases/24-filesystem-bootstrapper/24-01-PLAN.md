---
phase: 24-filesystem-bootstrapper
plan: 01
type: execute
wave: 1
depends_on: [24-00]
files_modified:
  - src/Filesystem/TenantFilesystemConfigTrait.php
  - src/Entity/AbstractTenant.php
  - tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php
autonomous: true
requirements: [BOOT-03]
tags: [filesystem, entity, trait]

must_haves:
  truths:
    - "Users with custom tenant entities can `use TenantFilesystemConfigTrait;` to inherit the filesystemConfig nullable JSON column and its getFilesystemConfig() / setFilesystemConfig() accessors — zero BC break (TenantInterface gains NO new abstract method per DEC-FILE-CONFIG)."
    - "AbstractTenant carries a filesystemConfig column inlined alongside mailerDsn — symmetric with Phase 20 precedent — so the shipped concrete Tenant entity works out of the box."
    - "Trait return shape matches DEC-FILE-CONFIG exactly: ?array{prefix?: string, adapter_dsn?: string, services?: array<string>}. The trait does not validate the array shape; validation lives in Plan 24-06 (TenantAwareFilesystemDecorator) and Plan 24-07 (FilesystemContractPass)."
  artifacts:
    - path: "src/Filesystem/TenantFilesystemConfigTrait.php"
      provides: "Optional trait exposing getFilesystemConfig() / setFilesystemConfig() for custom tenant entities"
      contains: "trait TenantFilesystemConfigTrait"
    - path: "src/Entity/AbstractTenant.php"
      provides: "Inline filesystemConfig nullable JSON column + accessor pair (mirrors mailerDsn placement)"
      contains: "filesystemConfig"
    - path: "tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php"
      provides: "Verifies trait default impl returns null, accepts ?array, and round-trips set/get"
      contains: "TenantFilesystemConfigTraitTest"
  key_links:
    - from: "src/Entity/AbstractTenant.php"
      to: "filesystemConfig column"
      via: "#[ORM\\Column(type: 'json', nullable: true)] private ?array $filesystemConfig = null"
      pattern: "filesystemConfig"
    - from: "src/Filesystem/TenantFilesystemConfigTrait.php"
      to: "AbstractTenant.filesystemConfig inline column"
      via: "structural equivalence — UPGRADE 0.3 → 0.4 documents either path"
      pattern: "filesystemConfig"
---

<objective>
Ship the tenant-side entity surface for Phase 24: a `TenantFilesystemConfigTrait` users can mix into custom tenant entities, and an inline `filesystemConfig` column on `AbstractTenant` so the bundled `Tenant` entity works out of the box. Per DEC-FILE-CONFIG (locked), `TenantInterface` does NOT gain a new abstract method — adoption is OPTIONAL via the trait.

Purpose: every per_tenant_adapter-mode write (Plan 24-06) and every prefix-override use case needs a place to store the tenant's filesystem configuration. This plan creates that storage. Plan 24-07's `FilesystemContractPass` will eventually validate the array shape; this plan only ships storage + accessors.

Output: 1 new trait file + 1 modified entity file + 1 test file flipped from incomplete to passing. After this plan, `Tenant.setFilesystemConfig(['prefix' =&gt; 'custom_acme/'])` persists and round-trips via Doctrine in the integration fixture.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md
@.planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md
@CLAUDE.md
@src/Mailer/TenantMailerConfigTrait.php
@src/Entity/AbstractTenant.php
</context>

<interfaces>
Existing trait precedent — `src/Mailer/TenantMailerConfigTrait.php`:

```
namespace Tenancy\Bundle\Mailer;

use Doctrine\ORM\Mapping as ORM;

trait TenantMailerConfigTrait
{
    #[ORM\Column(type: 'string', length: 255, nullable: true)]
    private ?string $mailerDsn = null;
    // … getters / setters
    public function getMailerDsn(): ?string;
    public function setMailerDsn(?string $mailerDsn): static;
}
```

Existing inline-on-AbstractTenant pattern (lines 41-51 + 111-145 of `src/Entity/AbstractTenant.php`) — symmetric column + accessor placement above the createdAt/updatedAt lifecycle pair.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TenantFilesystemConfigTrait + flip its stub test to GREEN</name>
  <files>src/Filesystem/TenantFilesystemConfigTrait.php, tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php</files>
  <read_first>
    - src/Mailer/TenantMailerConfigTrait.php (line-by-line precedent — copy the shape, substitute `filesystemConfig: ?array` for the three string columns)
    - tests/Unit/Mailer/TenantMailerConfigTraitTest.php (precedent — copy the assertion shape)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-CONFIG (return shape)
    - tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php (the stub from Plan 24-00 — replace its body)
    - CLAUDE.md (Doctrine deps optional — class-level docblock notes the #[ORM\Column] attribute is honored only when Doctrine is installed)
  </read_first>
  <behavior>
    - Test 1: A fresh anonymous class that `use`s the trait returns `null` from `getFilesystemConfig()`.
    - Test 2: After `setFilesystemConfig(['prefix' =&gt; 'tenant_acme/'])`, `getFilesystemConfig()` returns `['prefix' =&gt; 'tenant_acme/']`.
    - Test 3: After `setFilesystemConfig(['adapter_dsn' =&gt; 's3:///bucket?region=eu-central-1', 'services' =&gt; ['users.storage']])`, the array round-trips identically.
    - Test 4: `setFilesystemConfig(null)` resets the value to `null`.
    - Test 5: `setFilesystemConfig(['unknown_key' =&gt; 'x'])` does NOT throw — array shape validation lives in downstream plans, not in the trait.
    - Test 6: Reflection assertion — the trait's single property is named `filesystemConfig` and is private + nullable array.
  </behavior>
  <action>
    Create `src/Filesystem/TenantFilesystemConfigTrait.php` under namespace `Tenancy\Bundle\Filesystem`. `use Doctrine\ORM\Mapping as ORM;` (single import — Mailer trait precedent). Trait body: one private property `?array $filesystemConfig = null` annotated `#[ORM\Column(type: 'json', nullable: true)]` and PHPDoc `@var array{prefix?: string, adapter_dsn?: string, services?: array<string>}|null`. Two methods: `public function getFilesystemConfig(): ?array` (returns the property), `public function setFilesystemConfig(?array $filesystemConfig): static` (assigns and `return $this`). Class-level docblock cites .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-CONFIG, notes Doctrine attribute is honored only when ORM is installed, and points users at UPGRADE.md §0.3→0.4 (Plan 24-09 owns the UPGRADE.md edit).
    Replace the stub in `tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php` with the six tests from `<behavior>`. The test class itself extends `\PHPUnit\Framework\TestCase`. To exercise the trait, use an anonymous class declared inline: `$t = new class { use \Tenancy\Bundle\Filesystem\TenantFilesystemConfigTrait; };` (the FQCN-on-use form sidesteps the @Symfony no_unused_imports cs-fixer rule for same-namespace tests).
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php &amp;&amp; vendor/bin/phpstan analyse src/Filesystem/TenantFilesystemConfigTrait.php tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php --level 9 &amp;&amp; vendor/bin/php-cs-fixer check --diff src/Filesystem/TenantFilesystemConfigTrait.php tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php</automated>
  </verify>
  <done>Trait file ships with one nullable-array property + 2 accessors; 6-assertion test file is fully green; PHPStan level 9 clean; cs-fixer clean.</done>
</task>

<task type="auto">
  <name>Task 2: Inline the filesystemConfig column onto AbstractTenant</name>
  <files>src/Entity/AbstractTenant.php</files>
  <read_first>
    - src/Entity/AbstractTenant.php (the file being modified — note the existing mailerDsn/mailerFrom/mailerReplyTo inline placement at lines 41-51 with their accessor pair at lines 111-145, including the comment block explaining the alternative trait path)
    - src/Filesystem/TenantFilesystemConfigTrait.php (created in Task 1 — accessor signatures MUST match exactly so swapping inline ↔ trait is observably a no-op)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-CONFIG (placement decision: inline on AbstractTenant for the shipped Tenant; trait for custom entities)
  </read_first>
  <action>
    Add a single private property `?array $filesystemConfig = null` annotated `#[ORM\Column(type: 'json', nullable: true)]` immediately AFTER the `$mailerReplyTo` declaration (line 51) and BEFORE the `$createdAt` declaration (line 53). PHPDoc above the property: `@var array{prefix?: string, adapter_dsn?: string, services?: array<string>}|null` plus a comment block — mirror the existing "Mailer config (Phase 20 / BOOT-04)" comment — that reads "Filesystem config (Phase 24 / BOOT-03). Users with a custom Tenant entity can equivalently `use \Tenancy\Bundle\Filesystem\TenantFilesystemConfigTrait;` instead of inlining this column. See UPGRADE.md §0.3→0.4 and .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-CONFIG."
    Add the accessor pair AFTER the existing `setMailerReplyTo()` (around line 145), BEFORE the existing `setConnectionConfig()` (around line 147): `public function getFilesystemConfig(): ?array { return $this-&gt;filesystemConfig; }` and `public function setFilesystemConfig(?array $filesystemConfig): self { $this-&gt;filesystemConfig = $filesystemConfig; return $this; }`. Use `self` return type (not `static`) — matches the surrounding `setMailerReplyTo(): self` pattern on this concrete-but-abstract class.
    NO same-namespace `use` statements (php-cs-fixer @Symfony `no_unused_imports` would strip them). The existing `use Doctrine\ORM\Mapping as ORM;` import is already present and reused.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/Entity tests/Integration/Entity 2&gt;/dev/null; vendor/bin/phpstan analyse src/Entity/AbstractTenant.php --level 9 &amp;&amp; vendor/bin/php-cs-fixer check --diff src/Entity/AbstractTenant.php &amp;&amp; php -r "require 'vendor/autoload.php'; \$rc = new ReflectionClass(\\Tenancy\\Bundle\\Entity\\AbstractTenant::class); \$p = \$rc-&gt;getProperty('filesystemConfig'); echo \$p-&gt;isPrivate() ? 'private:ok' : 'FAIL';" | grep -q "private:ok"</automated>
  </verify>
  <done>AbstractTenant carries an inline filesystemConfig nullable JSON column + accessor pair structurally equivalent to the mailerDsn/mailerFrom/mailerReplyTo trio; reflection confirms the property is private; PHPStan and cs-fixer clean; existing Tenant-related tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant entity → landlord DB | filesystemConfig is admin-supplied (via seeding / admin UI), not user-supplied. Trusted boundary identical to mailerDsn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Information Disclosure | filesystemConfig.adapter_dsn may carry S3 credentials | accept | Storage at rest is the application's responsibility (matches mailerDsn precedent). DSN sanitization on error paths is Plan 24-06's concern, not Plan 24-01's. |
| T-24-01-02 | Tampering | A malformed array reaching the column passes through unvalidated | mitigate | Validation lives at consumption (Plan 24-06 + Plan 24-07). The trait/column intentionally does NOT validate per DEC-FILE-CONFIG. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php` passes 6 tests.
- `vendor/bin/phpunit` full suite remains green.
- `vendor/bin/phpstan analyse --level 9` clean.
- `vendor/bin/php-cs-fixer check --diff` clean.
- Reflection assertion: `AbstractTenant::class` has a private nullable-array property named `filesystemConfig` whose accessors exist with the exact signatures `getFilesystemConfig(): ?array` / `setFilesystemConfig(?array): self`.
- Trait + AbstractTenant produce structurally identical accessor signatures so a user can swap inline ↔ trait without breaking calling code.
</verification>

<success_criteria>
- New `src/Filesystem/TenantFilesystemConfigTrait.php` exists with one nullable-array property + 2 accessors.
- AbstractTenant carries an inline `filesystemConfig` column with matching accessors.
- TenantFilesystemConfigTraitTest is fully green (6 tests).
- TenantInterface is UNCHANGED (zero BC break — DEC-FILE-CONFIG honored).
- Full test suite green; PHPStan level 9 clean; cs-fixer clean.
</success_criteria>

<output>
Create `.planning/phases/24-filesystem-bootstrapper/24-01-SUMMARY.md` when done.
</output>
