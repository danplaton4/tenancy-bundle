---
phase: 24-filesystem-bootstrapper
plan: 09
type: execute
wave: 5
depends_on: [24-08]
files_modified:
  - examples/saas/src/Controller/TenantUploadController.php
  - examples/saas/templates/upload/index.html.twig
  - examples/saas/config/packages/flysystem.yaml
  - examples/saas/config/services.yaml
  - examples/saas/composer.json
  - docs/user-guide/filesystem-bootstrapper.md
  - mkdocs.yml
  - UPGRADE.md
autonomous: true
requirements: [BOOT-03]
tags: [filesystem, demo, docs]

must_haves:
  truths:
    - "examples/saas/ gains a minimal per-tenant upload page — one controller action (GET form + POST handler) + one Twig template — exercising the bundle's prefix-mode filesystem bootstrapper end-to-end. Per RESEARCH.md Q5 recommendation: small enough to fit, valuable enough to justify (live-stack verification step, not a feature)."
    - "docs/user-guide/filesystem-bootstrapper.md is created as a SEED — covers prefix mode, per-tenant-adapter mode, the `tenancy.scoped` tag attributes, the `MissingFilesystemConfigException` no-retry contract, and the path-traversal trust boundary. Phase 29 (DOC-20) will polish/expand this page; this plan ensures it exists with the load-bearing details."
    - "mkdocs.yml lists the new page under the User Guide nav."
    - "UPGRADE.md gains a `0.3 → 0.4` section explicitly noting: BOOT-03 has ZERO BC break (TenantInterface unchanged per DEC-FILE-CONFIG); users adopting per-tenant filesystem isolation add the `tenancy.scoped` tag to their existing Flysystem storage definitions; an optional `filesystemConfig` migration recipe for per-tenant-adapter mode."
    - "examples/saas/ composer.json gains league/flysystem-bundle + league/flysystem-memory to require (the demo is the live-stack verification step — the deps MUST be present at the demo level, not just at the bundle's require-dev)."
  artifacts:
    - path: "examples/saas/src/Controller/TenantUploadController.php"
      provides: "GET /uploads (form) + POST /uploads (handler) — writes via $usersStorage = bundle-decorated FilesystemOperator"
      contains: "class TenantUploadController"
    - path: "examples/saas/templates/upload/index.html.twig"
      provides: "Minimal upload form + list of uploaded files for the active tenant"
      contains: "form action=\"/uploads\""
    - path: "examples/saas/config/packages/flysystem.yaml"
      provides: "users.storage configured with the local adapter under %kernel.project_dir%/var/storage/users"
      contains: "users.storage"
    - path: "examples/saas/config/services.yaml"
      provides: "Tag users.storage with tenancy.scoped (strategy: prefix); enable tenancy.filesystem.enabled"
      contains: "tenancy.scoped"
    - path: "docs/user-guide/filesystem-bootstrapper.md"
      provides: "Seed user-guide page covering prefix mode, per-tenant-adapter mode, the tag, the exception, the trust boundary"
      contains: "FilesystemBootstrapper"
    - path: "UPGRADE.md"
      provides: "0.3 → 0.4 section explaining adoption path for BOOT-03 (no BC break)"
      contains: "0.3 → 0.4"
  key_links:
    - from: "examples/saas/src/Controller/TenantUploadController.php"
      to: "users.storage (bundle-decorated)"
      via: "constructor autowiring of FilesystemOperator $usersStorage"
      pattern: "FilesystemOperator"
    - from: "examples/saas/config/services.yaml"
      to: "users.storage"
      via: "tags: - { name: tenancy.scoped, strategy: prefix }"
      pattern: "tenancy.scoped"
---

<objective>
Close Phase 24 with the developer-facing surface: a minimal live-exercise upload page in `examples/saas/`, a seed user-guide page, and the UPGRADE 0.3 → 0.4 note.

Per RESEARCH.md Open Question Q5 (recommendation adopted): a thin demo upload page is the live-stack verification step that catches integration bugs the in-process tests miss (Phase 21 lesson — see project_phase21 memory in MEMORY.md). One controller + one template; not feature work, risk mitigation.

Per RESEARCH.md Open Question Q4: the Profiler "Filesystem" subsection is DEFERRED to Phase 29 (docs refresh polish). This plan does not touch the Profiler templates.

Output: 1 new controller + 1 new template + 2 demo-side config edits + 1 demo composer.json edit + 1 new docs page + mkdocs.yml nav entry + UPGRADE.md section.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md
@.planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md
@CLAUDE.md
@examples/saas/composer.json
@examples/saas/config/services.yaml
@docs/user-guide/mailer-bootstrapper.md
@mkdocs.yml
@UPGRADE.md
@src/Filesystem/FilesystemPrefixingDecorator.php
@src/Exception/MissingFilesystemConfigException.php
</context>

<interfaces>
Phase 20 precedent for `examples/saas/` integration — read `examples/saas/src/Controller/DemoMailerController.php` (`/_demo/send-test-mail` endpoint pattern used by Plan 23-04's smoke.sh) for the controller shape.

Phase 22 docs precedent — read `docs/user-guide/mailer-bootstrapper.md` for the page shape (headings: Overview / Quick Start / Modes / Configuration / Exception Handling / Path Traversal / FAQ).

Existing UPGRADE.md format — single H2 section per minor-version-pair (`## 0.2 → 0.3` already present from Phase 20).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement TenantUploadController + Twig template + examples/saas/ config wiring</name>
  <files>examples/saas/src/Controller/TenantUploadController.php, examples/saas/templates/upload/index.html.twig, examples/saas/config/packages/flysystem.yaml, examples/saas/config/services.yaml, examples/saas/composer.json</files>
  <read_first>
    - examples/saas/src/Controller/DemoMailerController.php (precedent — copy the controller shape; matches the smoke.sh contract Plan 23-04 ships)
    - examples/saas/composer.json (the file being modified — note the existing tenancy-bundle path repository entry; add league/flysystem-bundle + league/flysystem-memory)
    - examples/saas/config/services.yaml (the file being modified — note the existing service definitions; add the users.storage tag)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-MODE (this demo uses prefix mode — DEFAULT — per the v0.3 install-funnel-friendliness rationale)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Open Questions Q5 (recommendation: include MINIMAL upload page — one controller + one template)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Code Examples §"Bundle config — Tagging a storage for tenancy scoping" (the canonical YAML shape — copy)
    - MEMORY.md project_phase21_install_regression.md (live-stack verification lesson — this demo is the verification step)
  </read_first>
  <action>
    `examples/saas/src/Controller/TenantUploadController.php`: namespace `App\Controller`, `final class TenantUploadController extends AbstractController` with `public function __construct(private readonly \League\Flysystem\FilesystemOperator $usersStorage)`. Two routes:
    - `#[Route('/uploads', name: 'tenant_upload_index', methods: ['GET'])] public function index(): Response` — calls `$this-&gt;usersStorage-&gt;listContents('')` (returns tenant-relative paths thanks to Q1 implementation in Plan 24-05); renders `upload/index.html.twig` with the files list.
    - `#[Route('/uploads', name: 'tenant_upload_create', methods: ['POST'])] public function create(Request $request): Response` — read the uploaded file from `$request-&gt;files-&gt;get('upload')` (sanity-check it's an `UploadedFile`); write via `$this-&gt;usersStorage-&gt;writeStream($filename, $resource)` where `$filename = $file-&gt;getClientOriginalName();` (with `\basename()` sanitisation — application-level concern per Pitfall 5, but reasonable demo hygiene); redirect to the index route.
    - Constructor parameter binding via `services.yaml` autowiring — `FilesystemOperator $usersStorage` is autowired by `league/flysystem-bundle`'s `registerAliasForArgument` machinery (RESEARCH.md §Summary: bundle calls `registerAliasForArgument` so `$defaultStorage` / `$usersStorage` resolves by camelCased variable name to the matching storage).
    `examples/saas/templates/upload/index.html.twig`: extends `'base.html.twig'`. One H1 (`{{ tenant.name }} uploads` — `tenant` exposed by the existing demo base template). A `<ul>` listing files. A `<form action="{{ path('tenant_upload_create') }}" method="post" enctype="multipart/form-data">` with one `<input type="file" name="upload">` and one submit button. ~25 lines total.
    `examples/saas/config/packages/flysystem.yaml`: storages.users.storage uses `local: directory: '%kernel.project_dir%/var/storage/users'` (per the canonical RESEARCH.md §Code Examples shape).
    `examples/saas/config/services.yaml`: extend the existing services block. Add `users.storage: tags: - { name: tenancy.scoped, strategy: prefix, prefix_template: 'tenant_{slug}/' }`. Also under the `tenancy:` config root (the existing config block — find it and extend), set `filesystem: { enabled: true }`.
    `examples/saas/composer.json`: add `league/flysystem-bundle: ^3.7` and `league/flysystem-memory: ^3.31` to `require` (NOT require-dev — the demo IS the live-stack verification step; the deps must be present at demo composer install time). Add `composer update` to the demo's lock file. Sort packages.
    Anti-pattern guards:
    - Twig template renders the files list UNCONDITIONALLY — does NOT wrap in `{% if tenant_state == 'resolved' %}` (Phase 23 INT-01 lesson re: nested-branch rendering — kept defensive even for non-Profiler templates).
    - Controller does NOT manually prepend the tenant slug — the bundle decorator handles it transparently. (If the controller did, prefixes would double-up.)
    - `basename($filename)` sanitisation is a demo-level cosmetic guard; document the path-traversal trust boundary in the user guide (Task 2).
  </action>
  <verify>
    <automated>cd examples/saas &amp;&amp; composer validate --no-check-publish &amp;&amp; jq -e '.require["league/flysystem-bundle"] and .require["league/flysystem-memory"]' composer.json &amp;&amp; grep -q "tenancy.scoped" config/services.yaml &amp;&amp; grep -q "users.storage" config/packages/flysystem.yaml &amp;&amp; grep -q "filesystem:" config/services.yaml &amp;&amp; test -f src/Controller/TenantUploadController.php &amp;&amp; test -f templates/upload/index.html.twig</automated>
  </verify>
  <done>Demo gains a /uploads page exercising the bundle's prefix-mode decorator; composer.json declares the deps; services.yaml tags users.storage with tenancy.scoped and enables tenancy.filesystem; composer validate clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/user-guide/filesystem-bootstrapper.md seed page + mkdocs.yml nav entry</name>
  <files>docs/user-guide/filesystem-bootstrapper.md, mkdocs.yml</files>
  <read_first>
    - docs/user-guide/mailer-bootstrapper.md (precedent — copy the page structure: Overview / Quick Start / Modes / Configuration / Exception Handling / FAQ)
    - mkdocs.yml (the file being modified — find the User Guide nav section; insert filesystem-bootstrapper.md immediately after mailer-bootstrapper.md to preserve alphabetical-by-feature ordering)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §All locked decisions (the page references DEC-FILE-MODE, DEC-FILE-MULTI, DEC-FILE-CONFIG, DEC-FILE-EXCEPTION by ID)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Pitfall 5 (path-traversal trust boundary — needs a prominent docs section)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Common Pitfalls (each pitfall → one FAQ entry)
  </read_first>
  <action>
    `docs/user-guide/filesystem-bootstrapper.md`: structure with H1 `# Filesystem Bootstrapper (BOOT-03)` and the following H2 sections:
    1. **Overview** — what the bootstrapper does (per-tenant scoping of Flysystem operations); when to use it (multi-tenant SaaS with user uploads / exports / artifacts).
    2. **Installation** — `composer require league/flysystem-bundle` (note: optional dep; the bundle gracefully no-ops if absent — the install command suggestion is sourced from Plan 24-07's compile-time error message).
    3. **Quick Start (prefix mode)** — copy the YAML shape from `examples/saas/config/packages/flysystem.yaml` + `examples/saas/config/services.yaml`. Show: one storage, one tag with `strategy: prefix`. Reference DEC-FILE-MODE.
    4. **Per-tenant-adapter mode** — when to use (regulated deploys, per-tenant buckets). Show config: `strategy: per_tenant_adapter` on the tag + the `Tenant::setFilesystemConfig(['adapter_dsn' =&gt; 's3:///bucket?region=…'])` recipe. Document the supported DSN schemes from Plan 24-04: `local://`, `memory://`, `s3://`. Mention `addScheme()` extension point. Reference DEC-FILE-MODE + DEC-FILE-CONFIG.
    5. **Configuration reference** — table of `tenancy.filesystem.*` keys (enabled, allow_per_tenant_adapter, prefix_template, cache_size) with defaults + meaning.
    6. **Exception handling** — `MissingFilesystemConfigException extends \LogicException` semantics; Messenger no-retry contract; UnsupportedAdapterDsnSchemeException. Reference DEC-FILE-EXCEPTION.
    7. **Trust boundary: path traversal** — prominent warning section (matches RESEARCH.md Pitfall 5 + the security disclosure pointer). Application MUST sanitise user-supplied paths BEFORE calling `$storage-&gt;write($path)`. The bundle treats path arguments as TRUSTED. Example sanitisation snippet: `basename($filename)` for upload paths, `realpath()` checks for explicit-path reads.
    8. **FAQ** — one bullet per Pitfall 1-7 from RESEARCH.md, with the short-answer remediation.
    Page length: ~250 lines. Forward-reference Phase 29 (DOC-20) for the polished version.
    `mkdocs.yml`: add `- Filesystem bootstrapper: user-guide/filesystem-bootstrapper.md` immediately after the mailer-bootstrapper entry in the User Guide nav section.
  </action>
  <verify>
    <automated>test -f docs/user-guide/filesystem-bootstrapper.md &amp;&amp; grep -q "filesystem-bootstrapper.md" mkdocs.yml &amp;&amp; grep -q "DEC-FILE-MODE\|prefix mode" docs/user-guide/filesystem-bootstrapper.md &amp;&amp; grep -q "MissingFilesystemConfigException" docs/user-guide/filesystem-bootstrapper.md &amp;&amp; grep -q "path traversal\|Path Traversal\|trust boundary" docs/user-guide/filesystem-bootstrapper.md &amp;&amp; (command -v mkdocs &gt;/dev/null &amp;&amp; mkdocs build --strict 2&gt;&amp;1 | grep -qE "(ERROR|WARNING.*filesystem-bootstrapper)" &amp;&amp; exit 1 || true)</automated>
  </verify>
  <done>Docs page exists with all 8 H2 sections; mkdocs.yml lists it under User Guide; mkdocs --strict build does not error on the new page; the page mentions path-traversal trust boundary, MissingFilesystemConfigException, and the supported DSN schemes.</done>
</task>

<task type="auto">
  <name>Task 3: Append 0.3 → 0.4 section to UPGRADE.md</name>
  <files>UPGRADE.md</files>
  <read_first>
    - UPGRADE.md (the file being modified — find the existing `## 0.2 → 0.3` section; insert `## 0.3 → 0.4` ABOVE it, since UPGRADE.md is newest-first per the project's convention shown by the 0.2→0.3 section position)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-CONFIG (BC break analysis: ZERO break — TenantInterface unchanged)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Don't Hand-Roll (the migration recipes users need)
  </read_first>
  <action>
    Insert a new H2 section `## 0.3 → 0.4` at the top of the version-pair sections. Content:
    - **Breaking changes:** _None._ TenantInterface is unchanged (DEC-FILE-CONFIG locked).
    - **New: Filesystem Bootstrapper (BOOT-03).** Brief paragraph linking to `user-guide/filesystem-bootstrapper.md`. Adoption path:
      1. `composer require league/flysystem-bundle league/flysystem-memory` (memory adapter optional; only needed for in-memory tests).
      2. Tag the storage(s) you want tenant-scoped with `tenancy.scoped` (strategy: `prefix` (default) or `per_tenant_adapter`).
      3. Set `tenancy.filesystem.enabled: true` in your bundle config.
      4. (Optional, for per_tenant_adapter mode) `use \Tenancy\Bundle\Filesystem\TenantFilesystemConfigTrait;` on your custom Tenant entity OR rely on the inline `filesystemConfig` column on AbstractTenant — then add a Doctrine migration adding the `filesystem_config` JSON column to your tenancy_tenants table. (Show the migration snippet inline — 6-line SQL: `ALTER TABLE tenancy_tenants ADD COLUMN filesystem_config JSON DEFAULT NULL;`.)
    - **No action required** if you don't want per-tenant filesystem scoping — the bundle continues to behave as in v0.3 when `tenancy.filesystem.enabled: false` (the default).
    - Forward-reference the Phase 29 polish: "This section will be expanded with troubleshooting tips and additional adoption notes in the v0.4 docs refresh (Phase 29 / DOC-20)."
    Anti-pattern guard: do not claim a `tenancy:install --with-filesystem` flag exists — that is a Phase 25+ candidate, not in scope for Phase 24.
  </action>
  <verify>
    <automated>grep -A 20 "^## 0.3 → 0.4" UPGRADE.md | grep -q "Filesystem Bootstrapper" &amp;&amp; grep -A 30 "^## 0.3 → 0.4" UPGRADE.md | grep -q "tenancy.scoped" &amp;&amp; grep -A 40 "^## 0.3 → 0.4" UPGRADE.md | grep -q "TenantFilesystemConfigTrait\|filesystem_config" &amp;&amp; (grep -c "^## 0\." UPGRADE.md | xargs test 2 -le) &amp;&amp; head -5 UPGRADE.md | grep -q "Tenancy Bundle Upgrade Guide\|UPGRADE"</automated>
  </verify>
  <done>UPGRADE.md gains a `## 0.3 → 0.4` section above the existing `## 0.2 → 0.3` section; section documents the no-BC-break adoption path; mentions the inline migration SQL snippet and the trait alternative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-uploaded file → demo controller | Application MUST sanitise. Demo applies `basename()` as a cosmetic safeguard; docs section spells out the full trust boundary. |
| Demo composer.json deps → demo container | league/flysystem-bundle is now a hard demo dep (no optional path for the live-stack verification). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09-01 | Tampering | Path-traversal in demo upload (user supplies `../../../etc/passwd` as filename) | mitigate | Demo applies `basename($file-&gt;getClientOriginalName())` before write; the path-traversal trust boundary is the application's responsibility per docs (RESEARCH.md Pitfall 5). |
| T-24-09-02 | Information Disclosure | Demo upload exposes tenant slug in the file's local-disk path | accept | Q2 — inherent property of prefix mode. Documented prominently in the user guide. |
| T-24-09-SC | Tampering | Demo composer install of league/flysystem-bundle ^3.7 + league/flysystem-memory ^3.31 | mitigate | Same packages already mitigated in Plan 24-00; thephpleague org, MIT, verified VERIFIED. |
</threat_model>

<verification>
- `cd examples/saas &amp;&amp; composer validate` clean.
- `examples/saas/composer.json` declares league/flysystem-bundle + league/flysystem-memory in require.
- `examples/saas/config/services.yaml` tags `users.storage` with `tenancy.scoped` + sets `tenancy.filesystem.enabled: true`.
- Demo upload page renders + the underlying Flysystem write reaches `var/storage/users/tenant_<slug>/<file>` (verifiable by hand or by demo smoke test extension — out of scope for this plan, but the path layout is documented in the user guide).
- `docs/user-guide/filesystem-bootstrapper.md` exists with all 8 sections; mkdocs.yml nav entry present; `mkdocs build --strict` (if mkdocs is installed in CI) does not warn or error.
- `UPGRADE.md` gains `## 0.3 → 0.4` section above the existing 0.2→0.3 section; documents the no-BC-break adoption path.
- Full PHPUnit suite still green; no regression in unit/integration tests from this plan's changes (the plan touches demo + docs only — production source code is unchanged).
</verification>

<success_criteria>
- Demo upload page exercises the bundle's prefix-mode decorator end-to-end.
- docs/user-guide/filesystem-bootstrapper.md is the seed page covering modes, config, exceptions, trust boundary.
- UPGRADE 0.3 → 0.4 documents the zero-BC-break adoption path.
- mkdocs --strict builds cleanly.
- Phase 24 closes with both in-process verification (Plan 24-08) AND live-exercise verification path (this plan's demo) in place.
</success_criteria>

<output>
Create `.planning/phases/24-filesystem-bootstrapper/24-09-SUMMARY.md` when done.
</output>
