---
phase: 24-filesystem-bootstrapper
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - composer.json
  - tests/Unit/Filesystem/FilesystemPrefixingDecoratorTest.php
  - tests/Unit/Filesystem/TenantAwareFilesystemDecoratorTest.php
  - tests/Unit/Filesystem/LruFilesystemCacheTest.php
  - tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php
  - tests/Unit/Filesystem/TenantContextClearedListenerTest.php
  - tests/Unit/Filesystem/AdapterDsnParserTest.php
  - tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php
  - tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php
  - tests/Unit/Exception/MissingFilesystemConfigExceptionTest.php
  - tests/Integration/Filesystem/FilesystemTestKernel.php
  - tests/Integration/Filesystem/MakeFilesystemServicesPublicPass.php
  - tests/Integration/Filesystem/FilesystemBootstrapperIntegrationTest.php
  - tests/Integration/Filesystem/LongRunningWorkerFilesystemSimulationTest.php
  - tests/Integration/Support/StubTenantFilesystemExtension.php
autonomous: true
requirements: [BOOT-03]
tags: [filesystem, scaffolding, testing]

must_haves:
  truths:
    - "league/flysystem-bundle and league/flysystem-memory are loadable from require-dev so Wave 1+ test files can reference League\\Flysystem\\FilesystemOperator and League\\Flysystem\\InMemory\\InMemoryFilesystemAdapter without fatal errors."
    - "Every Wave 1-4 implementation task has a target test file that already exists and runs as markTestIncomplete() (RED → not-yet-implemented), not a missing-file error."
    - "FilesystemTestKernel boots with FrameworkBundle + DoctrineBundle + FlysystemBundle + TenancyBundle and registers two memory storages (users.storage tagged tenancy.scoped; public.storage untagged) per RESEARCH.md §Wave 0 Gaps."
    - "StubTenantFilesystemExtension trait can be mixed onto test stub tenant entities so integration tests can set/get filesystemConfig before the production trait lands."
  artifacts:
    - path: "composer.json"
      provides: "league/flysystem-bundle ^3.7 + league/flysystem-memory ^3.31 in require-dev AND suggest blocks (NOT require — DEC-FILE-BUNDLE locks optional dep policy)"
      contains: "league/flysystem-bundle"
    - path: "tests/Unit/Filesystem/FilesystemPrefixingDecoratorTest.php"
      provides: "Stub PHPUnit class for Plan 24-05 (prefix-mode decorator)"
      contains: "class FilesystemPrefixingDecoratorTest"
    - path: "tests/Unit/Filesystem/TenantAwareFilesystemDecoratorTest.php"
      provides: "Stub for Plan 24-06 (per-tenant-adapter decorator)"
      contains: "class TenantAwareFilesystemDecoratorTest"
    - path: "tests/Unit/Filesystem/LruFilesystemCacheTest.php"
      provides: "Stub for Plan 24-03 (LRU cache + clear())"
      contains: "class LruFilesystemCacheTest"
    - path: "tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php"
      provides: "Stub for Plan 24-01 (trait default impl)"
      contains: "class TenantFilesystemConfigTraitTest"
    - path: "tests/Unit/Filesystem/TenantContextClearedListenerTest.php"
      provides: "Stub for Plan 24-03 (listener flushes LRU on TenantContextCleared)"
      contains: "class TenantContextClearedListenerTest"
    - path: "tests/Unit/Filesystem/AdapterDsnParserTest.php"
      provides: "Stub for Plan 24-04 (DSN parser, 3 schemes + unsupported-scheme exception)"
      contains: "class AdapterDsnParserTest"
    - path: "tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php"
      provides: "Stub for Plan 24-07 (bootstrapper no-op boot + cache clear)"
      contains: "class FilesystemBootstrapperTest"
    - path: "tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php"
      provides: "Stub for Plan 24-07 (3 compile-time guards)"
      contains: "class FilesystemContractPassTest"
    - path: "tests/Unit/Exception/MissingFilesystemConfigExceptionTest.php"
      provides: "Stub for Plan 24-02 (LogicException ancestry pinning)"
      contains: "class MissingFilesystemConfigExceptionTest"
    - path: "tests/Integration/Filesystem/FilesystemTestKernel.php"
      provides: "Kernel for integration suite — FrameworkBundle + DoctrineBundle + FlysystemBundle + TenancyBundle, two memory storages (one tagged tenancy.scoped, one untagged)"
      contains: "class FilesystemTestKernel"
    - path: "tests/Integration/Filesystem/MakeFilesystemServicesPublicPass.php"
      provides: "Compiler pass exposing private storage services as public for test container::get() assertions (mirrors MakeMailerServicesPublicPass)"
      contains: "class MakeFilesystemServicesPublicPass"
    - path: "tests/Integration/Filesystem/FilesystemBootstrapperIntegrationTest.php"
      provides: "Stub for Plan 24-08 (5 BOOT-03 scenarios)"
      contains: "class FilesystemBootstrapperIntegrationTest"
    - path: "tests/Integration/Filesystem/LongRunningWorkerFilesystemSimulationTest.php"
      provides: "Stub for Plan 24-08 (100-tenant LRU simulation mirroring Phase 20)"
      contains: "class LongRunningWorkerFilesystemSimulationTest"
    - path: "tests/Integration/Support/StubTenantFilesystemExtension.php"
      provides: "Mixin trait that adds filesystemConfig nullable JSON column for test stub tenants — mirrors StubTenantMailerExtension"
      contains: "trait StubTenantFilesystemExtension"
  key_links:
    - from: "composer.json"
      to: "league/flysystem-bundle + league/flysystem-memory"
      via: "require-dev block + suggest block (NOT require)"
      pattern: "league/flysystem-bundle"
    - from: "tests/Integration/Filesystem/FilesystemTestKernel.php"
      to: "League\\Flysystem\\FilesystemOperator"
      via: "test container registers two memory storages via flysystem-bundle config"
      pattern: "users\\.storage"
---

<objective>
Wave 0 scaffolding for Phase 24 Filesystem Bootstrapper. Adds league/flysystem-bundle + league/flysystem-memory as optional deps (require-dev + suggest, NOT require per DEC-FILE-BUNDLE). Creates stub PHPUnit test classes (markTestIncomplete) for every Wave 1-4 component so subsequent plans convert RED-incomplete to GREEN rather than touching missing-file errors. Creates the FilesystemTestKernel + MakeFilesystemServicesPublicPass integration scaffolding and the StubTenantFilesystemExtension trait that test stub entities mix in.

Purpose: per the Nyquist Rule, every Wave 1+ task's `<verify><automated>` command must point at a test file that already exists. Phase 20 followed the same pattern (Plan 20-00). RESEARCH.md §Wave 0 Gaps and Pitfall 3 require an EXPLICIT `composer require --dev league/flysystem-memory` because the package is NOT transitively pulled by league/flysystem 3.34.0 — CONTEXT.md's DEC-FILE-TEST-ADAPTER claim is wrong on this point and this plan corrects it.

Output: composer.json edits + 14 test scaffolding files. After this plan, `vendor/bin/phpunit tests/Unit/Filesystem tests/Integration/Filesystem tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php tests/Unit/Exception/MissingFilesystemConfigExceptionTest.php` reports every test as incomplete (zero failures, zero errors). The bundle continues to load when league/flysystem-bundle is uninstalled (interface_exists guard discipline preserved — no production code added yet).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md
@.planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md
@CLAUDE.md
@composer.json
@tests/Integration/Mailer/MailerTestKernel.php
@tests/Integration/Mailer/MakeMailerServicesPublicPass.php
@tests/Integration/Support/StubTenantMailerExtension.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Flysystem deps to composer.json (require-dev + suggest)</name>
  <files>composer.json</files>
  <read_first>
    - composer.json (the file being modified — read existing require-dev + suggest layout)
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-BUNDLE (lock: require-dev + suggest, NOT require)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Standard Stack §Pitfall 3 (league/flysystem-memory NOT transitive — explicit install required)
  </read_first>
  <action>
    Add two entries to the `require-dev` block, sorted alphabetically (composer.json `config.sort-packages: true`): `league/flysystem-bundle` constraint `^3.7` and `league/flysystem-memory` constraint `^3.31`. Add matching entries in the `suggest` block: `league/flysystem-bundle` value "Required for per-tenant Filesystem bootstrapper (Phase 24 / BOOT-03) (^3.7)"; `league/flysystem-memory` value "In-memory Flysystem adapter — used by the bundle integration tests AND the memory:// DSN scheme of AdapterDsnParser (^3.31)". Do NOT add either package to `require`. After editing, run `composer update --no-scripts --no-interaction league/flysystem-bundle league/flysystem-memory` so `composer.lock` resolves the new deps. Verify the bundle still loads in `--no-dev` mode by running `composer install --no-dev --no-scripts --no-interaction --dry-run` — the command must succeed without Flysystem in the resolved set.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite unit --filter "MailerBootstrapperTest|BootstrapperChainTest" &amp;&amp; jq -e '.["require-dev"]["league/flysystem-bundle"] and .["require-dev"]["league/flysystem-memory"] and (.require["league/flysystem-bundle"] // null) == null and .suggest["league/flysystem-bundle"]' composer.json &gt;/dev/null</automated>
  </verify>
  <done>composer.json contains league/flysystem-bundle ^3.7 + league/flysystem-memory ^3.31 in require-dev and suggest; neither is in require; existing unit suite still green; composer.lock updated and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Create 9 stub unit test classes (markTestIncomplete)</name>
  <files>tests/Unit/Filesystem/FilesystemPrefixingDecoratorTest.php, tests/Unit/Filesystem/TenantAwareFilesystemDecoratorTest.php, tests/Unit/Filesystem/LruFilesystemCacheTest.php, tests/Unit/Filesystem/TenantFilesystemConfigTraitTest.php, tests/Unit/Filesystem/TenantContextClearedListenerTest.php, tests/Unit/Filesystem/AdapterDsnParserTest.php, tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php, tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php, tests/Unit/Exception/MissingFilesystemConfigExceptionTest.php</files>
  <read_first>
    - tests/Unit/Mailer/MailerBootstrapperTest.php (precedent — show the stub shape used in Phase 20)
    - tests/Unit/Mailer/TenantAwareTransportsDecoratorTest.php (precedent — Mailer-side stub class doc)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Phase Requirements → Test Map (the per-test-file behaviours that Wave 1-4 plans will implement)
    - CLAUDE.md (PHP 8.2+ strict_types, PSR-4 autoload `Tenancy\\Bundle\\Tests\\` → `tests/`)
  </read_first>
  <action>
    Create each test file under PSR-4 namespace `Tenancy\Bundle\Tests\Unit\Filesystem\` / `Tenancy\Bundle\Tests\Unit\Bootstrapper\` / `Tenancy\Bundle\Tests\Unit\DependencyInjection\Compiler\` / `Tenancy\Bundle\Tests\Unit\Exception\` matching the file path. Each class is a `final class …Test extends \PHPUnit\Framework\TestCase` with `declare(strict_types=1);`. Single method `public function testStub(): void` body `$this->markTestIncomplete('Stub — implemented in Plan 24-XX. See .planning/phases/24-filesystem-bootstrapper/24-VALIDATION.md.');` Reference the correct downstream plan ID per the Test Map:
    - FilesystemPrefixingDecoratorTest → Plan 24-05
    - TenantAwareFilesystemDecoratorTest → Plan 24-06
    - LruFilesystemCacheTest → Plan 24-03
    - TenantFilesystemConfigTraitTest → Plan 24-01
    - TenantContextClearedListenerTest → Plan 24-03
    - AdapterDsnParserTest → Plan 24-04
    - FilesystemBootstrapperTest → Plan 24-07
    - FilesystemContractPassTest → Plan 24-07
    - MissingFilesystemConfigExceptionTest → Plan 24-02
    Class-level docblock above each MUST cite the canonical decision: include the line "Per .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-XX". No production-class imports yet — these are stubs.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Unit/Filesystem tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php tests/Unit/Exception/MissingFilesystemConfigExceptionTest.php 2&gt;&amp;1 | grep -E "(OK|Incomplete: 9)" &amp;&amp; vendor/bin/phpunit tests/Unit/Filesystem tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php tests/Unit/Exception/MissingFilesystemConfigExceptionTest.php 2&gt;&amp;1 | grep -v "Failures\\|Errors" &gt;/dev/null</automated>
  </verify>
  <done>9 stub unit test files exist; PHPUnit reports exactly 9 incomplete tests, 0 failures, 0 errors when running the union of `tests/Unit/Filesystem` + the three single-file paths.</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test scaffolding (kernel + compiler pass + 2 stub integration tests + stub tenant trait)</name>
  <files>tests/Integration/Filesystem/FilesystemTestKernel.php, tests/Integration/Filesystem/MakeFilesystemServicesPublicPass.php, tests/Integration/Filesystem/FilesystemBootstrapperIntegrationTest.php, tests/Integration/Filesystem/LongRunningWorkerFilesystemSimulationTest.php, tests/Integration/Support/StubTenantFilesystemExtension.php</files>
  <read_first>
    - tests/Integration/Mailer/MailerTestKernel.php (precedent — full kernel scaffold with bundles array + registerContainerConfiguration callback)
    - tests/Integration/Mailer/MakeMailerServicesPublicPass.php (precedent — public-aliasing compiler pass)
    - tests/Integration/Support/StubTenantMailerExtension.php (precedent — trait shape)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Recommended Project Structure and §Wave 0 Gaps (FilesystemTestKernel must register two memory storages; one tagged tenancy.scoped, one untagged)
    - .planning/phases/24-filesystem-bootstrapper/24-RESEARCH.md §Code Examples §"Bundle config — Tagging a storage for tenancy scoping" and §"In-memory adapter for tests" — copy the YAML shape
    - .planning/phases/24-filesystem-bootstrapper/24-CONTEXT.md §DEC-FILE-MULTI (tag attributes: strategy, prefix_template)
  </read_first>
  <action>
    `FilesystemTestKernel`: `final class FilesystemTestKernel extends \Symfony\Component\HttpKernel\Kernel` under namespace `Tenancy\Bundle\Tests\Integration\Filesystem`. `registerBundles()` returns `FrameworkBundle`, `DoctrineBundle` (guarded by class_exists), `\League\FlysystemBundle\FlysystemBundle` (guarded by class_exists), `TenancyBundle`. `registerContainerConfiguration(LoaderInterface $loader)` loads inline config: `framework.test: true`, `framework.http_method_override: false`, `doctrine.dbal.connections.default.url: 'sqlite:///:memory:'`, `doctrine.orm.mappings: { TenancyBundle: { is_bundle: false, type: attribute, dir: %kernel.project_dir%/src/Entity, prefix: 'Tenancy\\Bundle\\Entity', alias: TenancyBundle } }`, `flysystem.storages: { users.storage: { adapter: memory }, public.storage: { adapter: memory } }`. Then via `$loader-&gt;load(static function (ContainerBuilder $c): void { … })` tag `users.storage` with `tenancy.scoped` (attributes `strategy: prefix`, `prefix_template: tenant_{slug}/`); leave `public.storage` untagged. Register `MakeFilesystemServicesPublicPass` via `$c-&gt;addCompilerPass(...)`. Override `getCacheDir()` / `getLogDir()` under `sys_get_temp_dir().'/tenancy_fs_kernel/'.$this-&gt;environment` (mirror MailerTestKernel). Constructor accepts the bundle list trick if needed for test variants.
    `MakeFilesystemServicesPublicPass`: `final class … implements CompilerPassInterface`. `process(ContainerBuilder $container)` calls `$container-&gt;getDefinition('users.storage')-&gt;setPublic(true);` and same for `public.storage`. Wraps each in `try/catch (ServiceNotFoundException)` so the pass is no-op when Flysystem bundle is absent.
    Integration stub tests: each is a `final class … extends \Symfony\Component\HttpKernel\Test\KernelTestCase` (no, KernelTestCase is `Symfony\Bundle\FrameworkBundle\Test\KernelTestCase`) with `protected static function getKernelClass(): string { return FilesystemTestKernel::class; }` and one `public function testStub(): void { $this-&gt;markTestIncomplete('Stub — implemented in Plan 24-08.'); }`.
    `StubTenantFilesystemExtension` trait: namespace `Tenancy\Bundle\Tests\Integration\Support`. Trait body declares `#[\Doctrine\ORM\Mapping\Column(type: 'json', nullable: true)] private ?array $filesystemConfig = null;` (guard the attribute with `class_exists(\Doctrine\ORM\Mapping\Column::class)` at runtime if Doctrine is absent? No — match StubTenantMailerExtension precedent which keeps Doctrine attributes unconditional since require-dev pulls Doctrine). Public `getFilesystemConfig(): ?array` and `setFilesystemConfig(?array $config): static` accessors.
    Anti-pattern guard: no same-namespace `use` statements in any of the new files; reference Tenancy classes by FQCN or rely on PSR-4 autoloading.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Integration/Filesystem 2&gt;&amp;1 | grep -E "(OK|Incomplete: 2)" &amp;&amp; php -r "require 'vendor/autoload.php'; \$k = new \\Tenancy\\Bundle\\Tests\\Integration\\Filesystem\\FilesystemTestKernel('test', true); \$k-&gt;boot(); \$c = \$k-&gt;getContainer(); echo \$c-&gt;has('users.storage') ? 'has:users.storage' : 'missing'; echo PHP_EOL; echo \$c-&gt;has('public.storage') ? 'has:public.storage' : 'missing'; \$k-&gt;shutdown();" | grep -E "has:users.storage" &amp;&amp; grep -q "filesystemConfig" tests/Integration/Support/StubTenantFilesystemExtension.php</automated>
  </verify>
  <done>FilesystemTestKernel boots without exception; both `users.storage` and `public.storage` services are present in the container; `users.storage` carries the `tenancy.scoped` DI tag; `MakeFilesystemServicesPublicPass` is registered; 2 stub integration tests report incomplete; StubTenantFilesystemExtension trait exposes the filesystemConfig accessor pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer install → require-dev | The Flysystem bundle is a new optional dep; consumed code paths are gated by `interface_exists`. Production installs must not fail when the bundle is absent. |
| Test fixtures | StubTenantFilesystemExtension mixed into test-only stubs; never reaches production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-00-SC | Tampering | composer require-dev installs of league/flysystem-bundle ^3.7 + league/flysystem-memory ^3.31 | mitigate | Both packages are owned by `thephpleague` and verified VERIFIED in RESEARCH.md §Package Legitimacy Audit (50M+/mo downloads, 12+ year history). No `[ASSUMED]`/`[SUS]` packages — no blocking-human checkpoint required per audit. |
| T-24-00-01 | Information Disclosure | FilesystemTestKernel cache/log dirs in sys_get_temp_dir() | accept | Test-only paths, isolated per environment, cleaned up by Phase 20 precedent without issue. |
</threat_model>

<verification>
- Composer: `jq -e '.["require-dev"]["league/flysystem-bundle"] and .["require-dev"]["league/flysystem-memory"]' composer.json` exits 0.
- Composer: `composer validate --no-check-publish --no-interaction` exits 0.
- PHPUnit: `vendor/bin/phpunit tests/Unit/Filesystem tests/Integration/Filesystem tests/Unit/Bootstrapper/FilesystemBootstrapperTest.php tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php tests/Unit/Exception/MissingFilesystemConfigExceptionTest.php` reports 11 incomplete tests, 0 failures, 0 errors.
- Existing suites: `vendor/bin/phpunit` full run remains green (no regression from Phase 23 baseline).
- Production load: bundle loads cleanly with `composer install --no-dev` — `interface_exists(\League\Flysystem\FilesystemOperator::class)` returns false and no production code references Flysystem yet.
- cs-fixer: `vendor/bin/php-cs-fixer check --diff` clean over `tests/Unit/Filesystem`, `tests/Integration/Filesystem`, `tests/Integration/Support/StubTenantFilesystemExtension.php`.
- PHPStan: `vendor/bin/phpstan analyse tests/Unit/Filesystem tests/Integration/Filesystem tests/Integration/Support/StubTenantFilesystemExtension.php --level 9` clean.
</verification>

<success_criteria>
- Composer deps added under require-dev + suggest (not require).
- 11 stub test files exist and report as incomplete (9 unit + 2 integration).
- FilesystemTestKernel boots and registers users.storage (tagged tenancy.scoped) + public.storage (untagged).
- MakeFilesystemServicesPublicPass exposes private storage services for test container::get() assertions.
- StubTenantFilesystemExtension trait provides filesystemConfig accessor pair.
- No regression in existing test suite.
- Production-grade installs (without league/flysystem-bundle) continue to boot.
</success_criteria>

<output>
Create `.planning/phases/24-filesystem-bootstrapper/24-00-SUMMARY.md` when done. Section structure mirrors `.planning/phases/20-mailer-bootstrapper/20-00-SUMMARY.md`.
</output>
