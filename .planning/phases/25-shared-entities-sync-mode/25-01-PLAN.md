---
phase: 25-shared-entities-sync-mode
plan: 01
type: execute
wave: 2
depends_on: ["25-00"]
files_modified:
  - src/Attribute/Shared.php
  - src/Exception/SharedEntityWriteInTenantContextException.php
autonomous: true
requirements: [SHARE-01]

must_haves:
  truths:
    - "#[Shared] is a bare class-target marker attribute with no constructor params (mirrors #[TenantAware])"
    - "SharedEntityWriteInTenantContextException extends \\LogicException and carries an actionable message naming the entity class and tenant slug"
  artifacts:
    - path: "src/Attribute/Shared.php"
      provides: "The #[Shared] marker attribute (D-06)"
      contains: "#[\\Attribute(\\Attribute::TARGET_CLASS)]"
    - path: "src/Exception/SharedEntityWriteInTenantContextException.php"
      provides: "The write-protection exception (D-02)"
      contains: "extends \\LogicException"
  key_links:
    - from: "src/Exception/SharedEntityWriteInTenantContextException.php"
      to: "forEntity static factory"
      via: "public static function forEntity(string $entityClass, string $tenantSlug): self"
      pattern: "public static function forEntity"
---

<objective>
Deliver the two leaf primitives every other Phase 25 component depends on: the `#[Shared]` marker attribute (D-06) and `SharedEntityWriteInTenantContextException` (D-02). Both are exact-analog clones of existing bundle files, so this plan is small and unblocks the compiler pass (25-02) and both subscribers (25-03) running in parallel afterward.

Purpose: define the contracts (attribute FQCN + exception factory signature) that downstream tasks build against — interface-first ordering, no scavenger hunt.
Output: `src/Attribute/Shared.php`, `src/Exception/SharedEntityWriteInTenantContextException.php`.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-shared-entities-sync-mode/25-CONTEXT.md
@.planning/phases/25-shared-entities-sync-mode/25-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: #[Shared] marker attribute</name>
  <files>src/Attribute/Shared.php</files>
  <read_first>
    - src/Attribute/TenantAware.php (the EXACT structural analog — copy the shape verbatim, change the name + docblock)
    - tests/Unit/Attribute/SharedTest.php (from Plan 25-00 — the RED test this turns GREEN: testSharedAttributeIsClassTarget asserts flags === \Attribute::TARGET_CLASS)
  </read_first>
  <behavior>
    - testSharedAttributeIsClassTarget: \ReflectionClass(Shared::class)->getAttributes(\Attribute::class)[0]->newInstance()->flags === \Attribute::TARGET_CLASS
    - testSharedAttributeCanBeInstantiated: new Shared() is instanceof Shared (zero constructor params)
  </behavior>
  <action>
    Create `Tenancy\Bundle\Attribute\Shared` mirroring `src/Attribute/TenantAware.php` exactly: `declare(strict_types=1)`, `#[\Attribute(\Attribute::TARGET_CLASS)]`, `final class Shared` with an empty body and NO constructor (D-06 bare marker). Replace the docblock to describe the `#[Shared]` contract: marks a Doctrine entity as a landlord-side master record synced read-only into each active tenant's EM via SharedEntitySyncSubscriber; MUST NOT be combined with `#[TenantAware]` (enforced by SharedEntityMutualExclusionPass at container build). Do not add any properties or methods.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter testSharedAttributeIsClassTarget</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/Attribute/Shared.php` contains `#[\Attribute(\Attribute::TARGET_CLASS)]` and `final class Shared` with no constructor.
    - Behavior assertion: `vendor/bin/phpunit --filter testSharedAttributeIsClassTarget` exits 0.
    - Behavior assertion: `vendor/bin/phpunit --filter testSharedAttributeCanBeInstantiated` exits 0.
  </acceptance_criteria>
  <done>SharedTest is GREEN; `#[Shared]` is a zero-param TARGET_CLASS marker in namespace Tenancy\Bundle\Attribute.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SharedEntityWriteInTenantContextException</name>
  <files>src/Exception/SharedEntityWriteInTenantContextException.php</files>
  <read_first>
    - src/Exception/MissingFilesystemConfigException.php (EXACT analog — final class extends \LogicException + single static factory using sprintf + new self; the docblock explaining why \LogicException not \RuntimeException, WR-01 no-retry invariant)
    - .planning/phases/25-shared-entities-sync-mode/25-PATTERNS.md (Pattern 2 — forEntity(string $entityClass, string $tenantSlug): self signature)
  </read_first>
  <behavior>
    - SharedEntityWriteInTenantContextException::forEntity('App\\Plan', 'acme') returns a self whose getMessage() contains 'App\\Plan' and 'acme'
    - The class is an instanceof \LogicException (NOT \RuntimeException) — Messenger will not retry
  </behavior>
  <action>
    Create `Tenancy\Bundle\Exception\SharedEntityWriteInTenantContextException` as `final class ... extends \LogicException` mirroring `MissingFilesystemConfigException`. Single factory `public static function forEntity(string $entityClass, string $tenantSlug): self` returning `new self(sprintf(...))`. The message MUST name the entity class and tenant slug and state the remedy: shared entities are read-only on the tenant side — write to the landlord EM instead. Prefix the message with `tenancy:` for searchability (bundle convention). Add a docblock explaining the `\LogicException` (not `\RuntimeException`) choice per the WR-01 no-retry invariant, same as the analog. Do NOT add a constructor — only the static factory.
  </action>
  <verify>
    <automated>php -l src/Exception/SharedEntityWriteInTenantContextException.php && php -r "require 'vendor/autoload.php'; \$e = \Tenancy\Bundle\Exception\SharedEntityWriteInTenantContextException::forEntity('App\\\\Plan','acme'); exit(\$e instanceof \LogicException && str_contains(\$e->getMessage(),'App\\\\Plan') && str_contains(\$e->getMessage(),'acme') ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file contains `final class SharedEntityWriteInTenantContextException extends \LogicException` and `public static function forEntity(string $entityClass, string $tenantSlug): self`.
    - Behavior assertion: the inline `php -r` check exits 0 (instanceof \LogicException AND message contains both the class and slug).
    - CLI: `php -l` exits 0.
  </acceptance_criteria>
  <done>The exception extends \LogicException, exposes forEntity(class, slug), and produces an actionable message; ready for the write-protection listener (25-03) to throw it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant EM flush → DB | The exception thrown here is the mechanism that blocks tenant-context writes to `#[Shared]` entities (consumed by 25-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | tenant write to `#[Shared]` corrupting landlord master | mitigate (foundation here) | SharedEntityWriteInTenantContextException (extends \LogicException, D-02) is created in this plan; the listener that throws it lands in 25-03 |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (RESEARCH § Package Legitimacy Audit empty) |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter testSharedAttributeIsClassTarget` and `--filter testSharedAttributeCanBeInstantiated` GREEN.
- The exception instanceof + message check passes.
- `vendor/bin/phpunit --testsuite unit` shows no new failures introduced by these two files.
</verification>

<success_criteria>
- `#[Shared]` is a bare TARGET_CLASS marker (no params) — D-06 satisfied.
- `SharedEntityWriteInTenantContextException extends \LogicException` with forEntity(class, slug) factory — D-02 foundation satisfied.
- Both SharedTest methods GREEN.
</success_criteria>

<output>
Create `.planning/phases/25-shared-entities-sync-mode/25-01-SUMMARY.md` when done. Record the exact FQCNs (`Tenancy\Bundle\Attribute\Shared`, `Tenancy\Bundle\Exception\SharedEntityWriteInTenantContextException`) and the forEntity signature so 25-02 and 25-03 reference them without re-deriving.
</output>
