---
phase: 25-shared-entities-sync-mode
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Integration/SharedEntity/Support/Entity/TestPlan.php
  - tests/Integration/SharedEntity/Support/Entity/TestPlanCategory.php
  - tests/Integration/SharedEntity/Support/Entity/TestPlanWithAssociation.php
  - tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php
  - tests/Integration/SharedEntity/Support/StubMultiTenantProvider.php
  - tests/Integration/SharedEntity/Support/MakeSharedEntityServicesPublicPass.php
  - tests/Integration/SharedEntity/SharedEntitySyncIntegrationTest.php
  - tests/Integration/SharedEntity/SharedEntityNoDatabaseKernelTest.php
  - tests/Unit/Attribute/SharedTest.php
  - tests/Unit/DependencyInjection/Compiler/SharedEntityMutualExclusionPassTest.php
autonomous: true
requirements: [SHARE-01]

must_haves:
  truths:
    - "Every SHARE-01-a..m behavior has a NEW test file that exists and is discoverable by phpunit --filter"
    - "An integration kernel with a landlord EM + two tenant EMs (file-based SQLite) boots and exposes the subscriber, write-protection listener, and both EMs as public services"
    - "A stub TenantProvider returns exactly two test tenants for the fan-out loop (no real DB lookup)"
  artifacts:
    - path: "tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php"
      provides: "Landlord + 2-tenant SQLite kernel for fan-out + write-protection integration tests"
      contains: "tenancy_shared_test_landlord.db"
    - path: "tests/Integration/SharedEntity/Support/Entity/TestPlan.php"
      provides: "#[Shared] entity with scalar fields only (SHARE-01-c..k)"
      contains: "Tenancy\\Bundle\\Attribute\\Shared"
    - path: "tests/Integration/SharedEntity/Support/Entity/TestPlanWithAssociation.php"
      provides: "#[Shared] entity with a ManyToOne association (SHARE-01-m cascade boundary)"
      contains: "ManyToOne"
    - path: "tests/Unit/Attribute/SharedTest.php"
      provides: "SHARE-01-a attribute target-class assertions"
      contains: "testSharedAttributeIsClassTarget"
    - path: "tests/Unit/DependencyInjection/Compiler/SharedEntityMutualExclusionPassTest.php"
      provides: "SHARE-01-l mutual-exclusion guard assertions"
      contains: "testMutualExclusionGuardThrows"
  key_links:
    - from: "SharedEntitySyncTestKernel"
      to: "tenancy (database.enabled: true)"
      via: "loadFromExtension('tenancy', ['database' => ['enabled' => true]])"
      pattern: "database.*enabled.*true"
    - from: "StubMultiTenantProvider"
      to: "tenancy.provider service definition"
      via: "MakeSharedEntityServicesPublicPass setClass override"
      pattern: "tenancy\\.provider"
---

<objective>
Create the Wave 0 test infrastructure for Phase 25 so every later wave has automated sampling continuity (Nyquist Dimension 8). This plan builds the 7 NEW test files / kernel called for in 25-VALIDATION.md § Wave 0 Requirements: a landlord + 2-tenant SQLite integration kernel, the `#[Shared]` test entities (scalar-only + association), a 2-tenant stub provider, a public-services compiler pass, plus the unit + integration test files with their behavior methods.

These tests are RED on creation (production code lands in Waves 2-3). Test method bodies assert the SHARE-01-a..m behaviors with the exact `--filter` names from 25-VALIDATION.md so the planner's downstream `<verify>` commands resolve.

Purpose: no 3 consecutive implementation tasks may run without an automated verify; the test harness must exist first.
Output: 2 unit test files, 2 integration test files, 1 multi-EM kernel, 3 entities, 1 stub provider, 1 public-services pass.
</objective>

<execution_context>
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danplaton/dev/packages/[claude-code] symfony-multitenancy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-shared-entities-sync-mode/25-CONTEXT.md
@.planning/phases/25-shared-entities-sync-mode/25-RESEARCH.md
@.planning/phases/25-shared-entities-sync-mode/25-PATTERNS.md
@.planning/phases/25-shared-entities-sync-mode/25-VALIDATION.md

<interfaces>
TenantInterface (src/TenantInterface.php) — getSlug(): string; getDomain(): ?string; getConnectionConfig(): array; getName(): string; isActive(): bool; getMailerDsn(): ?string; getMailerFrom(): ?string; getMailerReplyTo(): ?string

TenantProviderInterface (src/Provider/TenantProviderInterface.php) — findBySlug(string $slug): TenantInterface; findAll(): TenantInterface[]

TenantContext (src/Context/TenantContext.php) — setTenant(TenantInterface): void; getTenant(): ?TenantInterface; hasTenant(): bool; clear(): void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-EM integration kernel + stub provider + public-services pass + #[Shared] test entities</name>
  <files>tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php, tests/Integration/SharedEntity/Support/StubMultiTenantProvider.php, tests/Integration/SharedEntity/Support/MakeSharedEntityServicesPublicPass.php, tests/Integration/SharedEntity/Support/Entity/TestPlan.php, tests/Integration/SharedEntity/Support/Entity/TestPlanCategory.php, tests/Integration/SharedEntity/Support/Entity/TestPlanWithAssociation.php</files>
  <read_first>
    - tests/Integration/Support/DoctrineTestKernel.php (the kernel to clone — landlord+tenant dual-EM SQLite shape, getCacheDir/getLogDir pattern, enable_native_lazy_objects guard)
    - tests/Integration/DBAL/DatabasePerTenantMiddlewareIntegrationTest.php (setUpBeforeClass schema-creation + resetManager('tenant') pattern this kernel must support)
    - tests/Integration/Support/Entity/TestProduct.php (entity analog: #[ORM\Entity], #[ORM\Id]+GeneratedValue int PK, #[ORM\Column] scalars)
    - tests/Integration/Support/NullTenantProvider.php (provider stub analog — implements TenantProviderInterface)
    - tests/Integration/Support/ReplaceTenancyProviderPass.php (compiler-pass-that-overrides-tenancy.provider analog)
    - tests/Integration/Support/MakeDatabaseServicesPublicPass.php (public-services pass analog)
    - src/Attribute/TenantAware.php (the marker-attribute shape #[Shared] mirrors — entities reference Tenancy\Bundle\Attribute\Shared)
  </read_first>
  <action>
    Create namespace `Tenancy\Bundle\Tests\Integration\SharedEntity\Support`.

    SharedEntitySyncTestKernel: clone DoctrineTestKernel exactly but use THREE SQLite files — `tenancy_shared_test_landlord.db` (landlord connection, default_connection), and the `tenant` connection placeholder `tenancy_shared_test_placeholder.db`. The kernel's tenant EM mapping `dir` must point at this plan's `Support/Entity` dir with prefix `Tenancy\Bundle\Tests\Integration\SharedEntity\Support\Entity`. Landlord EM maps BOTH `src/Entity` (Tenancy\Bundle\Entity) AND this Support/Entity dir so the landlord can persist `TestPlan`. Keep `tenancy.database.enabled: true`. In build(), register MakeSharedEntityServicesPublicPass + a provider-replacement that swaps `tenancy.provider` to StubMultiTenantProvider. Mirror DoctrineTestKernel's getCacheDir/getLogDir (md5(static::class)) and the enable_native_lazy_objects version guard verbatim. The two per-tenant DB file paths (`tenancy_shared_test_tenant_a.db`, `tenancy_shared_test_tenant_b.db`) are created by the integration test's setUpBeforeClass via SchemaTool — the kernel only declares the placeholder tenant connection; the stub tenants carry their own path via getConnectionConfig().

    StubMultiTenantProvider: implements TenantProviderInterface. findAll() returns exactly TWO TenantInterface instances (slugs `tenant_a`, `tenant_b`), each a `Tenancy\Bundle\Entity\Tenant` constructed with slug + name and `->setConnectionConfig(['path' => sys_get_temp_dir().'/tenancy_shared_test_tenant_<slug>.db'])` so resetManager('tenant') + TenantDriverMiddleware route to the right file. findBySlug() returns the matching tenant or throws TenantNotFoundException. Expose the two tenants via a public static factory so the integration test reuses the identical path strings.

    MakeSharedEntityServicesPublicPass: implements CompilerPassInterface. Make these IDs public so the integration test can fetch them: `tenancy.shared_entity_sync_subscriber`, `tenancy.shared_entity_write_protection`, `tenancy.context`, `doctrine.orm.landlord_entity_manager`, `doctrine`. Guard each with hasDefinition()/hasAlias() before setPublic(true) — these services do not exist until Wave 3 wiring lands, so the pass must tolerate their absence (mirror ReplaceTenancyProviderPass's hasDefinition guard).

    TestPlan: a `#[Tenancy\Bundle\Attribute\Shared]` Doctrine entity. `#[ORM\Entity]`, `#[ORM\Table(name: 'test_plans')]`. Integer `#[ORM\Id]`+`#[ORM\GeneratedValue]` PK, plus scalar columns `name` (string) and `priceCents` (integer). Public getters/setters for the update test. NOTE: this references `Shared` which lands in Plan 25-01 — until then the class will reference an undefined attribute. That is expected for a RED Wave 0; add a top-of-file comment `// Shared attribute lands in Plan 25-01`.

    TestPlanCategory: a plain (NON-shared) entity — `#[ORM\Entity]`, int PK, `label` string column. The association target for TestPlanWithAssociation.

    TestPlanWithAssociation: a `#[Shared]` entity with int PK, a scalar `title` column, AND a `#[ORM\ManyToOne(targetEntity: TestPlanCategory::class)]` association named `category`. Used by SHARE-01-m to prove association fields are NOT fanned out (only scalar fields copied).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter SharedEntitySyncTestKernel 2>&1 | grep -qv "Class .* not found"; php -l "tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php" && php -l "tests/Integration/SharedEntity/Support/Entity/TestPlanWithAssociation.php"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php` contains the string `tenancy_shared_test_landlord.db` and `'database' => ['enabled' => true]`.
    - Source assertion: `tests/Integration/SharedEntity/Support/StubMultiTenantProvider.php` `implements TenantProviderInterface` and its `findAll()` returns an array of size 2.
    - Source assertion: `tests/Integration/SharedEntity/Support/Entity/TestPlanWithAssociation.php` contains both `#[\Tenancy\Bundle\Attribute\Shared]` (or `use ...Shared; #[Shared]`) and `ManyToOne`.
    - CLI: `php -l` exits 0 for all 6 created files.
  </acceptance_criteria>
  <done>The 3-DB kernel, 2-tenant stub provider, public-services pass, and 3 entities exist and lint clean; entities reference the `Shared` attribute that Plan 25-01 will create.</done>
</task>

<task type="auto">
  <name>Task 2: Unit + integration test files with SHARE-01-a..m behavior methods (RED scaffolds)</name>
  <files>tests/Unit/Attribute/SharedTest.php, tests/Unit/DependencyInjection/Compiler/SharedEntityMutualExclusionPassTest.php, tests/Integration/SharedEntity/SharedEntitySyncIntegrationTest.php, tests/Integration/SharedEntity/SharedEntityNoDatabaseKernelTest.php</files>
  <read_first>
    - tests/Unit/Attribute/TenantAwareTest.php (exact analog for SharedTest — testAttributeHasTargetClassFlag + testAttributeCanBeInstantiated)
    - tests/Unit/DependencyInjection/Compiler/FilesystemContractPassTest.php (exact analog for the mutual-exclusion pass test — ContainerBuilder + Definition->addTag + (new Pass())->process + expectException(\LogicException))
    - tests/Integration/DBAL/DatabasePerTenantMiddlewareIntegrationTest.php (integration harness: setUpBeforeClass unlink+SchemaTool::createSchema per tenant file, resetManager('tenant'), tearDownAfterClass cleanup)
    - tests/Integration/SharedEntity/Support/SharedEntitySyncTestKernel.php (from Task 1 — the kernel these tests boot)
    - tests/Integration/SharedEntity/Support/StubMultiTenantProvider.php (from Task 1 — the 2-tenant source of truth)
    - tests/Integration/Support/SharedDbTestKernel.php (analog for the shared_db no-op kernel used by SharedEntityNoDatabaseKernelTest)
  </read_first>
  <action>
    SharedTest (namespace Tenancy\Bundle\Tests\Unit\Attribute): clone TenantAwareTest. Method `testSharedAttributeIsClassTarget()` reflects `Tenancy\Bundle\Attribute\Shared`, asserts `#[\Attribute]` present and `flags === \Attribute::TARGET_CLASS`. Method `testSharedAttributeCanBeInstantiated()` asserts `new Shared()` is instanceof Shared. Covers SHARE-01-a.

    SharedEntityMutualExclusionPassTest (namespace Tenancy\Bundle\Tests\Unit\DependencyInjection\Compiler): clone FilesystemContractPassTest's container-builder shape. Method `testMutualExclusionGuardThrows()`: register a service Definition whose class carries BOTH `#[Shared]` and `#[TenantAware]`, tagged `tenancy.shared_entity`, then `expectException(\LogicException::class)` + `expectExceptionMessage` for the class name, and call `(new SharedEntityMutualExclusionPass())->process($container)`. Method `testNoExceptionWhenOnlySharedPresent()`: a class with only `#[Shared]` tagged `tenancy.shared_entity` → no exception. Method `testUntaggedClassIsIgnored()`: a both-attributed class with NO tag → no exception (proves the pass only inspects tagged services). Define the test fixture classes (BothAttributesEntity, OnlySharedEntity) as nested or same-file final classes carrying the attributes. Covers SHARE-01-l. NOTE: references `SharedEntityMutualExclusionPass` (Plan 25-02) + `Shared` (Plan 25-01) — RED until those land; add the `// production class lands in Plan 25-02` comment.

    SharedEntitySyncIntegrationTest (namespace Tenancy\Bundle\Tests\Integration\SharedEntity): boots SharedEntitySyncTestKernel in setUpBeforeClass, pre-creates landlord + both tenant SQLite schemas via SchemaTool (mirror DatabasePerTenant test exactly), tearDownAfterClass unlinks all 3 DB files. Methods, each fetching public services from the container:
      - `testSubscriberWiredToLandlordEm()` (SHARE-01-b, T-25-04): assert `tenancy.shared_entity_sync_subscriber` service exists and its definition carries `doctrine.event_listener` tags with `connection: landlord` for onFlush + postFlush.
      - `testInsertFansOutToAllTenants()` (SHARE-01-c): persist+flush a TestPlan on landlord EM; assert each tenant EM (resetManager per tenant + setTenant) has a row with the same id+fields.
      - `testUpdateFansOutToAllTenants()` (SHARE-01-d): mutate+flush; assert tenant copies reflect the new value.
      - `testDeleteFansOutToAllTenants()` (SHARE-01-e): remove+flush landlord row; assert tenant copies are gone.
      - `testTenantSidePersistThrows()` (SHARE-01-f, T-25-01): setTenant, persist a NEW TestPlan on tenant EM, expectException(SharedEntityWriteInTenantContextException) on flush.
      - `testTenantSideUpdateThrows()` (SHARE-01-g, T-25-01): setTenant, dirty a managed TestPlan copy, expectException on flush.
      - `testTenantSideDeleteThrows()` (SHARE-01-h, T-25-01): setTenant, remove a managed copy, expectException on flush.
      - `testSyncWriteBypassesWriteProtection()` (SHARE-01-i, T-25-04): the fan-out in testInsertFansOutToAllTenants implicitly proves the subscriber's own tenant flush is NOT blocked; add an explicit assertion that the insert fan-out completed without throwing.
      - `testPerTenantFailureIsLogged()` (SHARE-01-k): inject a test PSR-3 logger (or fetch the test logger), corrupt one tenant (e.g. drop its table / point at unwritable path) so its fan-out throws, flush a landlord #[Shared] insert, assert the OTHER tenant still got the row AND the logger recorded a warning containing tenant slug + entity class + identifier.
      - `testAssociationsNotSynced()` (SHARE-01-m, T-25-02): persist a TestPlanWithAssociation on landlord with a category set; assert the tenant copy has the scalar `title` but the `category` association is NULL/not loaded (scalar-only copy).
      Use `vendor/bin/phpunit --filter <method>` discoverability — method names must match 25-VALIDATION.md exactly.

    SharedEntityNoDatabaseKernelTest (namespace Tenancy\Bundle\Tests\Integration\SharedEntity): boots a `shared_db` driver kernel (clone SharedDbTestKernel shape — single `default` EM, `driver: shared_db`). Method `testNoOpUnderSharedDb()` (SHARE-01-j): persist+flush a #[Shared] entity; assert NO fan-out attempt occurs (e.g. provider.findAll() is never called / subscriber short-circuits — assert via a spy provider or that no exception/log fires and the single shared row exists exactly once).
  </action>
  <verify>
    <automated>php -l tests/Unit/Attribute/SharedTest.php && php -l tests/Unit/DependencyInjection/Compiler/SharedEntityMutualExclusionPassTest.php && php -l tests/Integration/SharedEntity/SharedEntitySyncIntegrationTest.php && php -l tests/Integration/SharedEntity/SharedEntityNoDatabaseKernelTest.php</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `vendor/bin/phpunit --list-tests --filter testSharedAttributeIsClassTarget` lists the test (method resolvable).
    - Source assertion: `SharedEntitySyncIntegrationTest.php` defines all of: testSubscriberWiredToLandlordEm, testInsertFansOutToAllTenants, testUpdateFansOutToAllTenants, testDeleteFansOutToAllTenants, testTenantSidePersistThrows, testTenantSideUpdateThrows, testTenantSideDeleteThrows, testSyncWriteBypassesWriteProtection, testPerTenantFailureIsLogged, testAssociationsNotSynced.
    - Source assertion: `SharedEntityNoDatabaseKernelTest.php` defines `testNoOpUnderSharedDb` and references `driver` `shared_db`.
    - Source assertion: `SharedEntityMutualExclusionPassTest.php` defines `testMutualExclusionGuardThrows` with `expectException(\LogicException::class)`.
    - CLI: `php -l` exits 0 for all 4 files.
  </acceptance_criteria>
  <done>All 12 SHARE-01-a..m behaviors have a named test method in a file that lints clean and is discoverable by phpunit --filter; tests are RED pending production code from Waves 1-3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant EM flush → DB | Tenant-context writes to a `#[Shared]` entity must be rejected before reaching the DB |
| landlord EM flush → tenant DBs | Fan-out crosses into per-tenant databases; only scalar fields may cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | tenant-side write of `#[Shared]` entity | mitigate (verified here) | SharedEntitySyncIntegrationTest::testTenantSidePersistThrows/Update/Delete assert the exception — these RED tests pin the D-02 contract |
| T-25-02 | Information Disclosure | association copy in fan-out | mitigate (verified here) | testAssociationsNotSynced pins the cascade-depth-1 boundary (DEC-SHARE-02) |
| T-25-04 | Tampering | subscriber fires on wrong EM | mitigate (verified here) | testSubscriberWiredToLandlordEm asserts `connection: landlord` tags |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (RESEARCH § Package Legitimacy Audit empty) |
</threat_model>

<verification>
- `php -l` clean for all 10 new files.
- `vendor/bin/phpunit --list-tests` resolves the new test methods (RED is expected — production classes land in later waves).
- No existing test regresses: `vendor/bin/phpunit --testsuite unit` still passes for everything NOT referencing the new production classes (the new RED tests will error on missing classes — that is the expected Wave 0 state, documented in the SUMMARY).
</verification>

<success_criteria>
- 7+ NEW test files / kernel exist per 25-VALIDATION.md § Wave 0 Requirements.
- Every SHARE-01-a..m behavior maps to a discoverable named test method.
- The integration kernel boots a landlord + 2-tenant SQLite topology with database.enabled: true.
- All new files lint clean (`php -l`).
</success_criteria>

<output>
Create `.planning/phases/25-shared-entities-sync-mode/25-00-SUMMARY.md` when done. Record: the RED test inventory (which behaviors are pending which production class), the exact 3 DB file paths used by the kernel, and the `tenancy.shared_entity` tag name the mutual-exclusion test expects.
</output>
