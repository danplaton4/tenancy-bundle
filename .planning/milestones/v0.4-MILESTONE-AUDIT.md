---
milestone: v0.4
audited: 2026-06-19T00:00:00Z
reaudit_of: 2026-06-18
status: tech_debt
scores:
  requirements: 6/6
  phases: 7/7
  integration: 7/7
  flows: 6/6
gaps:
  requirements: []
  integration: []
  flows: []
closed_since_prior_audit:  # Phase 30 (pre-tag closure) — verified 11/11
  - id: "W-01"
    was: "SharedEntitySyncSubscriber + SharedEntityWriteProtectionListener type-hinted concrete final SharedEntityCopier"
    now: "Both type-hint SharedEntityCopierInterface; mock copier injectable (seam test added)"
  - id: "W-02"
    was: "SharedEntityChangedMessageHandler duplicated switchToTenant()/restoreTenantContext() byte-for-byte from the subscriber"
    now: "Logic extracted to single TenantEmSwitcher; both consumers call $this->switcher->switchTo()/restore()"
  - id: "W-03"
    was: "Asymmetric tenant-switch mechanism (resync setTenant()+boot() vs subscriber/handler close()+resetManager()) undocumented"
    now: "Asymmetry documented bidirectionally on TenantEmSwitcher + SharedEntityResyncCommand docblocks; behavior unchanged"
  - id: "WR-06/WR-07"
    was: "docs/roadmap.md framed shipped v0.4 features as 'Next/not started' + mischaracterized the 3 PHPStan rules"
    now: "v0.4 under Shipped; 3 real rule IDs named; stale v0.3/Phase-22 framing removed"
  - id: "WR-03/D-10"
    was: "docs-lint.sh D-15 awk whitelist cross-file state leak (no FNR==1 reset)"
    now: "FNR==1 { in_whitelist=0 } reset added; docs-lint exits 0"
tech_debt:  # Residual — non-blocking, non-code-closeable, accept-and-track
  - phase: 26-tenancy-shared-resync-command
    items:
      - "HUMAN-UAT pending: interactive TTY confirm prompt (SHARE-02-c) not manually verified (CI/real-world gated)"
      - "Nyquist: VALIDATION.md present but nyquist_compliant=false / wave_0_complete=false — /gsd:validate-phase 26"
  - phase: 28-phpstan-extension
    items:
      - "HUMAN-UAT pending: extension-installer zero-config auto-load in a real consumer project not manually verified"
      - "Nyquist: VALIDATION.md present but nyquist_compliant=false / wave_0_complete=false — /gsd:validate-phase 28"
  - phase: 27-async-shared-entities
    items:
      - "Code review (advisory, deferred): unvalidated changeType; stale-context-on-failure; dead findAll() loop (the W-02 'tenant-switch twin without drift test' item is now MOOT — twin eliminated by Phase 30)"
  - phase: 29-docs-refresh
    items:
      - "Code review (deferred, info): tenancy:run shell-interpretation caveat; slug-validation 'clear error' overstated; addslashes characterization in sql-filter.md"
      - "mkdocs build --strict not runnable locally (mkdocs not installed) — CI-deferred; docs-lint.sh is the local green proxy"
      - "Nyquist: VALIDATION.md present but nyquist_compliant=false / wave_0_complete=false — /gsd:validate-phase 29"
  - phase: 24-filesystem-bootstrapper
    items:
      - "Nyquist: no VALIDATION.md — /gsd:validate-phase 24"
  - phase: 30-v0-4-pre-tag-closure
    items:
      - "Nyquist: no VALIDATION.md (pure refactor + docs closure; VERIFICATION 11/11, full suite green) — discovery-only flag"
nyquist:
  compliant_phases: [25, 27]
  partial_phases: [26, 28, 29]
  missing_phases: [24, 30]
  overall: partial
---

# v0.4 Storage & Shared Entities — Milestone Audit (re-audit, post-Phase 30)

**Status:** `tech_debt` — all requirements satisfied, cross-phase integration verified, **0 blockers**. The actionable code-level debt that triggered the first audit's closure phase is now **CLOSED**. The residual debt is non-code-closeable (manual UAT + Nyquist discovery + advisory notes) and is recommended for accept-and-tag.

**Audited:** 2026-06-19 (re-audit of the 2026-06-18 audit that produced status `tech_debt` and spawned Phase 30)

## What changed since the first audit

The 2026-06-18 audit found 0 blockers and 6/6 requirements satisfied but flagged ~14 tech-debt items, of which the *closeable code-level* ones were folded into **Phase 30 (pre-tag closure)**. Phase 30 is now complete and verified **11/11 must-haves** (`30-VERIFICATION.md`, status `passed`). It closed:

| ID | Item | Status |
|----|------|--------|
| **W-01** | Concrete `SharedEntityCopier` type-hint in subscriber + write-protection listener | ✅ Closed — now `SharedEntityCopierInterface`; mock-copier seam test added |
| **W-02** | Duplicated `switchToTenant()`/`restoreTenantContext()` across subscriber + async handler | ✅ Closed — extracted to single `TenantEmSwitcher`; both consumers delegate |
| **W-03** | Undocumented asymmetric tenant-switch mechanisms | ✅ Closed — documented bidirectionally; behavior unchanged |
| **WR-06/WR-07** | `docs/roadmap.md` framed shipped v0.4 as "Next"; mischaracterized PHPStan rules | ✅ Closed — v0.4 under Shipped; 3 real rule IDs named |
| **WR-03/D-10** | `docs-lint.sh` awk cross-file state leak | ✅ Closed — `FNR==1` reset added |

## Definition of Done

v0.4 = per-tenant storage + shared-entity propagation + static-analysis safety net + docs. Six requirements across phases 24–29, plus an audit-driven pre-tag closure phase (30). All six requirements are checked off in `REQUIREMENTS.md` and corroborated by a phase VERIFICATION.md + SUMMARY frontmatter.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | REQUIREMENTS.md | VERIFICATION | SUMMARY frontmatter | Final |
|--------|-------|-----------------|--------------|---------------------|-------|
| BOOT-03 | 24 — Filesystem Bootstrapper | `[x]` Complete | passed | listed (24-05/07/08/09) | **satisfied** |
| SHARE-01 | 25 — Shared Entities (sync) | `[x]` Complete | passed | listed (25-00..03) | **satisfied** |
| SHARE-02 | 26 — `tenancy:shared:resync` | `[x]` Complete | human_needed* | listed (26-01..04) | **satisfied** |
| SHARE-03 | 27 — Async Shared Entities | `[x]` Complete | passed | listed (27-01/03) | **satisfied** |
| DX-03 | 28 — PHPStan Extension | `[x]` Complete | human_needed* | listed (28-01..07) | **satisfied** |
| DOC-20 | 29 — Docs Refresh | `[x]` Complete | passed | listed (29-02) | **satisfied** |

*`human_needed` = all automated must-haves passed; one manual-only UAT item remains (tracked as tech debt). No requirement is `unsatisfied` or `orphaned` → **FAIL gate not triggered**.

Phase 30 tracks audit IDs (W-/WR-/D-), not REQUIREMENTS.md REQ-IDs — known/expected for audit-driven tech-debt phases; no orphan.

**Score: 6/6 requirements satisfied.**

## Phase Verification Summary

| Phase | VERIFICATION | Notes |
|-------|--------------|-------|
| 24 | passed | — |
| 25 | passed | — |
| 26 | human_needed | TTY confirm prompt pending manual check (26-HUMAN-UAT.md) |
| 27 | passed | — |
| 28 | human_needed | extension-installer auto-load pending manual check (28-HUMAN-UAT.md); all code-review blockers closed by 28-05/06/07 |
| 29 | passed | 4 in-scope code-review warnings fixed; 3 deferred |
| 30 | **passed** | 11/11 must-haves verified; W-01/W-02/W-03 + docs drift closed |

## Cross-Phase Integration (re-verified post-Phase 30)

Integration checker (live-source trace against `src/`): **0 blockers, 0 missing connections, 0 orphaned exports, 0 DI/constructor arg mismatches.**

- The Phase 30 refactor is correctly wired: `tenancy.shared.em_switcher` (`TenantEmSwitcher`) is registered in `TenancyBundle.php:297-302`, aliased to `TenantEmSwitcherInterface`, and injected into BOTH the sync subscriber (arg 7) and the async handler (arg 7). Constructor arg counts match DI definitions in all four consumers.
- All three shared-entity write paths converge on the single `tenancy.shared_entity_copier` (`SharedEntityCopier::applyRow()`). The sync subscriber + async handler now route tenant-switching through the single `TenantEmSwitcher`; the resync command intentionally keeps the full `setTenant()`+`bootstrapperChain->boot()` path (documented W-03 asymmetry).
- One canonical `Tenancy\Bundle\Attribute\Shared` referenced by subscriber, resync, async handler, and all PHPStan rules.
- Optional deps correctly guarded: Flysystem (`FilesystemContractPass` / `interface_exists`), Messenger (`SharedAsyncContractPass` 3-stage guard), phpstan-doctrine (`?object` resolver hint, never hard-imported).

### Integration warnings — all CLEARED
W-01, W-02, W-03 from the prior audit are confirmed **closed** by live-source trace (see "What changed" table). No new integration warnings introduced by the refactor.

## End-to-End Flows

| Flow | REQ | Status |
|------|-----|--------|
| Write `#[Shared]` on landlord → read-only copy in tenant EMs (sync) | SHARE-01 | ✓ complete |
| Flip `tenancy.shared.async: true` → same fan-out via Messenger worker | SHARE-03 | ✓ complete |
| `tenancy:shared:resync` backfills a tenant (idempotent, continue-on-failure) | SHARE-02 | ✓ complete |
| PHPStan extension catches mutualExclusion / sharedEntityLeak / tenantIdDrift | DX-03 | ✓ complete |
| Filesystem bootstrapper prefix / per-tenant-adapter isolation | BOOT-03 | ✓ complete |
| Docs match wired behavior (3 rule IDs, command flags, `tenancy.shared.async`) | DOC-20 | ✓ complete |

## Live Gate Verification (main checkout, fresh — caches purged)

Independently re-run today, confirming Phase 30's VERIFICATION claims:

| Gate | Result |
|------|--------|
| `vendor/bin/phpunit` (full) | **770 tests / 3242 assertions / 2 skipped / 0 failures** |
| `vendor/bin/phpstan analyse --memory-limit=512M` | **[OK] No errors** (level 9) |
| `vendor/bin/php-cs-fixer check --diff` | **0 diffs** |
| `bash scripts/docs-lint.sh` | **OK** |

## Nyquist Validation Coverage (discovery only)

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 24 | missing | — | `/gsd:validate-phase 24` |
| 25 | exists | true | — |
| 26 | exists | false | `/gsd:validate-phase 26` |
| 27 | exists | true | — |
| 28 | exists | false | `/gsd:validate-phase 28` |
| 29 | exists | false | `/gsd:validate-phase 29` |
| 30 | missing | — | discovery-only (pure refactor + docs; VERIFICATION 11/11, suite green) |

Overall: **partial** (2 compliant, 3 partial, 2 missing). Discovery only — never auto-run.

## Residual Tech Debt (non-blocking, accept-and-track)

Down from ~14 items to a residual set that is **all non-code-closeable**. None of these were closure-worthy code work — the first audit already folded every closeable code/docs item into Phase 30, and Phase 30 closed them all.

- **Manual UAT pending (2):** Phase 26 TTY confirm prompt; Phase 28 extension-installer auto-load. Both verifiable only by a human in a real environment (effectively CI/real-world gated).
- **Nyquist (5 discovery flags):** phases 24/26/28/29/30 lack a compliant VALIDATION.md. Discovery-only; the live suite is green regardless.
- **Phase 27 advisory code review (deferred):** unvalidated changeType, stale-context-on-failure, dead `findAll()` loop. (The "tenant-switch twin without drift test" item is now **moot** — Phase 30 eliminated the twin.)
- **Phase 29 info items (3) + docs gate:** `tenancy:run` shell caveat, slug-validation wording, `addslashes` characterization; `mkdocs build --strict` CI-deferred (mkdocs not installable locally — `docs-lint.sh` is the local green proxy).

## Verdict

**No critical blockers. 6/6 requirements satisfied, 7/7 phases verified, cross-phase wiring re-confirmed clean, all live gates green.** The closure phase (Phase 30) did exactly what the first audit scoped it to do — every actionable integration warning and docs-drift item is closed. The residual `tech_debt` is irreducible without a human (manual UAT) or is discovery-only/advisory; **a further cleanup phase is NOT warranted.**

**Recommended path: proceed to `/gsd:complete-milestone v0.4`**, accepting the residual debt into the backlog. Optionally run `/gsd:validate-phase 24|26|28|29|30` first if you want Nyquist-compliant VALIDATION.md coverage before tagging, and manually exercise the two HUMAN-UAT items (26 TTY confirm, 28 extension-installer auto-load) in a real consumer project.
