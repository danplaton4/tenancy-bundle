services:
  php:
    build:
      context: .
      args:
        PHP_VERSION: ${PHP_VERSION:-8.2}
    volumes:
      - .:/app
      - composer-cache:/root/.composer
    working_dir: /app

volumes:
  composer-cache:
